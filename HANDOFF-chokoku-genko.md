# 引継ぎメモ — 彫刻原稿（chokoku-genko.html）

最終更新: 2026-09-21　**chokoku-genko.html = v35.1**／**index_b.html = v1.9.441**／**tekkyo.html = v3.5**

新しいセッションを始めたら、まずこのファイルを読んでください。
（アプリ全体の古い資料は `HANDOFF.md`。バージョン記述が v1.9.073 のまま古いので注意）

---

## いまの状態

- 作業ブランチは `claude/chokoku-genko-v3-7-9xj06c`。`main` と同じ内容
  （`claude/init-project-setup-smotz-00s2gp` はマージ後に消えています）
- 未コミットの変更なし。**すべて GitHub に反映ずみ**
- GitHub Pages に配信ずみ（`.deploy-trigger` 更新まで完了）

## 作業の進め方（毎回この手順）

1. `chokoku-genko.html` を直す
2. `var APP_VER` と `<title>` のバージョンを上げる
3. 中の `<script>` を取り出して `node --check` で構文確認
4. コミット → `git fetch origin main && git rebase origin/main`
5. `git push -u origin <branch> --force-with-lease`
   と `git push origin <branch>:main` の両方
6. `mcp__github__create_or_update_file` で `.deploy-trigger` を更新（Pages の再生成用）
7. `git fetch origin main && git merge --ff-only origin/main && git push -u origin <branch>`

`mcp__github__*` はよく切断されるので、使う直前に ToolSearch で取り直すこと。

## テスト

Playwright のテスト一式がコンテナ内の作業用ディレクトリにありました（t51〜t82、824項目）。
**コンテナが破棄されると消えます。** 新しいセッションでは作り直しになります。

やり方：`python3 -m http.server 8899` でリポジトリを配信し、
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome` を `playwright-core` から起動して
`http://127.0.0.1:8899/chokoku-genko.html` を開く。
SharePoint は `ctx.route('https://graph.microsoft.com/**')` で差し替える。

## 最近直したこと（v3.0〜v3.7）

| 版 | 内容 |
|----|------|
| v3.0 | 作字画面：ダイヤルで拡大・縮小、中ボタンで移動、右クリックメニュー |
| v3.1 | 確認資料に作字が反映されない不具合。実寸mm・横倍率・上下の合わせ方もゴムシートに統一 |
| v3.2 | 確認資料の水色の破線を、ガイドのチェックが入っているときだけ出す |
| v3.3 | 右クリックメニューが作字画面の裏に回っていた（z-index 70→90）／囲んで選ぶ |
| v3.4 | フォントの記憶が消える件。`navigator.storage.persist()` と書き込み失敗の通知 |
| v3.5 | 確認資料に直しが出ない／拓本取込を「SP取込」に／テンプレートの〔追加〕 |
| v3.6 | テンプレートで足したガイドラインが拓本解析に出ない（`centersPx` の数が合っていなかった） |
| v3.7 | 囲んだ枠を残し、四隅で拡大・縮小。画面拡大のボタンは削除 |
| v3.8 | 「構成」を 1画面に。校正文字（左）と拓本（右）を同じ縮尺で並べる |
| v3.9 | 「構成」に拓本側の線（頭／脚・センター・文字の長さ）。選んだガイドラインの分だけ出す |
| v4.0 | 「構成」で線をつかんで動かす。ゴムシートの「文字の位置」をガイドラインごとに |
| v4.1 | v4.0 のつまみが小さすぎて掴めなかった。枠の外の余白へ。ガイドラインの位置も直せる |
| v4.2 | つかんだ状態が動かすと外れる不具合（作り直した SVG を測っていた） |
| v4.3 | 拓本を重ねるを「構成」へ。ゴムシートは出力の下見と文字位置だけに |
| v4.4 | 拓本タブを削除。「構成」→「構成」。押した先で左メニューが追随 |
| v6.3 | 保存先を `Folder_Pass/工事仕様/彫刻資料` に。無ければ作る（`spEnsurePath`）。メニュー名を「テンプレート」に |
| v6.4 | **枠**（石の彫刻できる面）を追加。校正に出す。文字と拓本は枠に入れる（拓本は枠外も可） |
| v6.5 | 文字の既定を **20mm**。枠の選択をガイドラインの中へ。枠もテンプレートに入る。左メニューを枠ごとにまとめる |
| v6.6〜v7.2 | 枠の名前を枠の外へ（`FRTOP` の余白）。枠の上端は固定にし、**文字の始まり `top` は拓本の頭に合わせる**（`framesFitRub`）。枠テンプレートを「テンプレート」の中へ |
| v7.3〜v7.5 | **並び替えを `S.glSeq` で持つ**。それまでは `centers[].x` を動かしていたが、行は同じ x に置くのがふつうなので何も動かなかった |
| v7.6〜v7.7 | 確認資料の出る条件を見直し。既定は校正と同じ配置、［位置調整］でずらせる、［元に戻す］、枠名も出す |
| v7.8〜v7.9 | 枠テンプレートに中のガイドラインも入れる。**まとめて入力**（改行ごとに同じガイドラインの文字グループ①②③へ）＋［数字を漢数字に］ |
| v8.0 | 確認資料の位置調整が利かなかった（拓本の x を `demoX` から出していて文字と一緒に動いていた）→ `demoXBase` |
| v8.1 | 案件が「読み込み中」で止まる。`spGetBlob` の大きさ確認が厳しすぎた（下の「気をつけるところ」） |
| v8.2 | 作字に **［画（丸）を足す］**（`ovalContour`）。Shift でまん丸。何もない所から字を起こすときに使う |
| v8.3 | ［画（四角）を足す］をやめた（角度が付けられず使い道が無い）。左メニューの「文字編集」と行の一覧のあいだを 1行分あけた |
| v8.4 | **枠が上下に動くようになった**。拓本の頭を基準に `f.y` を逆算（それまでは枠が固定で、拓本と合わせようがなかった）。枠カードに「上端 mm」を戻し、校正のつまみは上下でも動く |
| v8.5 | トリミングを確認資料から**拓本解析**へ移した（`cropMode`）。180°反転すると切れる所が上下逆になる不具合も直した |
| v8.6 | トリミングを**校正にも**効かせた。枠の「上端」は石の面の位置なので、直すと**中の文字もいっしょに動く**（下げは不変）に戻した |
| v8.7 | 枠の「上端 mm」の欄を**廃止**。上端は下げから計算で出す（`framesSyncY`）。校正のつまみは左右だけに戻した |
| v8.8 | トリミングで切った分が**場所を取ったまま**だったのを直した（枠の上や横に空きができていた）。校正に**拓本の左右のつまみ**（緑のふうせん・`S.rubDx`）|
| v8.9 | 確認資料の拓本の置き場所を、**校正とまったく同じ決め方**にした（それまでセンターラインに合わせる別の式だった）|
| v9.0 | 拓本を左右にずらしたあと［拓本を重ねる］を押すと、拓本のガイド線と文字がずれた。`bothRubX` をずらす前に覚えていたため |
| v9.1 | **拓本解析の作り直し**。直す操作はぜんぶ拓本の画面の帯へ、パネルは「② 修正結果」の一覧だけに。実測長の欄は脚の線の下へ |
| v9.2 | 拓本の画面から**文字まわりの線**（センターライン・文字グループの上下）を消した。「② 修正結果」→「② 補正結果」|
| v9.3 | 左メニューの［行追加］に**テンプレートから追加**を、その下に［**枠追加**］（新規・コピー・テンプレートから）を足した。「テンプレート」→「テンプレート管理」|
| v9.4 | **左メニューが唯一の選び場所**に。枠の札・拓本の札を押せるようにし、メインメニューの「拓本解析」「文字編集」「枠」を消した |
| v9.5 | 拓本解析の ① に「入れる枠」のえらびを足した（ガイドラインの「入れる枠」と同じ形）|
| v9.6 | 枠のサブメニューから［拓本をこの枠に入れる］を外した（拓本解析の「入れる枠」と左メニューのドラッグで足りる）|
| v9.7 | ガイドラインのサブメニューを **行の構成 → まとめて入力 → その字の中身** の順に。グループ数の表示・⣿ のつまみ・［いまの内容を出す］を消し、「入れる枠」「行幅 mm」を 1行に |
| v9.8 | **センターラインを左右に動かせるようにした**（行の構成の「左右 mm」と、校正の丸いつまみ `cenx:`）。それまでどこにも動かす手が無かった |
| v9.9 | v9.8 の手動の左右移動をやめ、**同じガイドラインのセンターラインはすきま無く並べ直す**（`packCentersInGl`）。行の左右は枠の［1行目の位置］のふうせんで |
| v10.0 | センターラインを足すと**ガイドラインの幅を分け合う**ようにした（足しても広がらない＝枠からはみ出さない）。読み込みのときも並べ直す |
| v10.1 | **自動整列をやめ、ふうせんを戻した**。拓本の写真に目で合わせるので、左右は手で決める。文字がまだ無い線にも線とふうせんを出す |
| v10.2 | センターラインの左右の**決め方を仕様どおりに**。1-1 はふうせん無し、2本目からは 拓本があれば拓本まかせ・無ければふうせん |
| v10.3 | 拓本の上のセンターラインの線とつまみを、**文字の側と同じ上下**に（線は文字ぶんの長さ、つまみはその すぐ上）|
| v10.4 | 行の構成の「左右 mm」の欄を廃止。センターラインの左右は**ふうせんと拓本だけ**で決める |
| v10.5 | **文字グループの札の中で文字を打てる**ように（`.gtxt`）。下の「文字」欄と［数字を漢数字に］は廃止。連動の既定を「入」に |
| v10.6 | ［画（丸）を足す］が **1つ引くごとに解除**されていた（2つめが引けず「効かない」ように見えた）。道具は入れたままにした |
| v10.7 | ［画（丸）を足す］は **押した所をクリックすると、そこに丸ができる**。斜めドラッグでしか出なかったのを直した |
| v10.8 | 作字を **モード（続く）と 操作（1回だけ）** に分けた。モードの札・モードごとのポインタ・消すモードの赤い × |
| v10.9 | 上の帯に **［試し切り］** を追加。刃の効きを見るためのかたちを 1枚だけ出す（原稿の中身には触らない） |
| v11.0 | ［SVG］の出力を **赤・黒の色分けのまま** にした（`outSVG("allc")`）。ぜんぶ黒だった |
| v11.1 | 文字グループの札の中で**字が打てなかった**のを直した（行のクリックが作り直していた）。［試し切り］は帯から**設定パネル**へ移した |
| v11.2 | 作字に **自動の名前（月1・月2…）** を付けた。同じ字を何個でも共有フォルダーに残せる。前は同じ字・同じフォントだと上書きされて消えていた |
| v11.3 | 備考メモを組み直した。**その他**の欄、**訂正マーク**（納骨リストと違うところ）、**縦書き 2行**。文面の欄・置き場所の欄は廃止 |
| v11.4 | 縦書きで **かっこ・棒を 90度倒す**ようにした。申し送りは**切れない縁の内側に収める**ようにして、はみ出しの空振り警告をなくした |
| v11.5 | v11.4 の「縁の内側に収める」で申し送りが**動かせなくなった**のを直した。紙の中なら自由に置ける。縦書きへの切り替えは**下端をそのまま**にする |
| v11.6 | 申し送りの**はみ出し警告を廃止**した。見れば分かるので出さない |
| v11.7 | フォント・作字を整理。**共有フォルダーの作字の一覧**（見本つき）を出し、そこから**編集・削除**できるようにした |
| v11.8 | 作字の一覧を**別ウインドウ**に移した（`#glibWin`）。しぼりこみ欄つき。パネルは［作字の一覧をひらく］だけ |
| v11.9 | **見出しを帯**にして、ほかの字と見分けがつくようにした（全メニュー）。作字の一覧と作字の画面を**つかんで動かせる**ようにした |
| v12.0 | 校正の［寸法］に、**枠の上から最初の字まで**と**最後の字から枠の下まで**を足した。右上の「ホイールで拡大縮小」の案内は削除 |
| v12.1 | 連動を**小さいトグル**にして、横・連動・縦を 1行にした。文字グループの札の**2つめ以降の欄に入れなかった**のを直した |
| v12.2 | 札の**太わく（選んでいる印）と、下の編集がずれていた**のを直した |
| v12.3 | 文字1つぶんの右クリックメニューを並べ替え・改名。上に**決定・取消**を付け、**つかんで動かせる**ようにした |
| v12.4 | 右クリックメニューから「縦横そろえる」「既定の説明」「細さのしきい値」「グループぜんぶ調べる」を削除。**この字を赤に**は太さで自動選ぶのをやめ、**単純な赤/黒トグル**にした |
| v12.5 | ［作字一覧を表示］が、文字メニューごと消えていたのを直した。**同じ窓の中**に一覧を出すようにした |
| v12.6 | 寸法が**センターラインを足すと出なくなる**のを直した。列をまたいでもブロック間を測る |
| v12.7 | 文字メニューの候補一覧を**常に表示**に。**異体字も同じ一覧**へ。絵は半分、共有名・フォント名は出さない。［パスで編集］に改名 |
| v12.8 | 作字に**［新規登録］**（上書きせず別の名前で）を追加。校正で**拓本の場所を留める**ようにした（ガイドラインを動かすと拓本まで動いていた） |
| v12.9 | 校正で**ガイドラインの置き場所も留める**ようにした。センターラインを動かしても、親の文字・ほかのガイドライン・拓本は動かない |
| v13.0 | **拓本を重ねたままでも、拓本のセンターラインを動かせる**ようにした（合わせ先の 1本だけは動かせない） |
| v13.1 | 重ねているときの**合わせ先の線も動かせる**ようにした。拓本は動かず、**文字のほうが動く** |
| v13.2 | SVG は**下見を見てから保存**するようにした。出力に**実測した文字全長のチェック線**（青）を入れた |
| v13.3 | チェック線の字を**線の下**へ（上だと校正文字にかぶる）。SVG の下見に**ホイールの拡大縮小とドラッグ移動**を付けた |
| v13.4 | **校正のロック**。お客様に出した校正の**中身**を読み取り専用にする（位置合わせは通す）。解除は［設定］メニュー |
| v13.5 | **案件ファイルを履歴つきに**。1 つの .json に版をならべて持つ。ロックした版は書き換わらない。開くとき版を選べる |
| v13.6 | メインメニューに**［ファイル管理］**を新設（履歴が見られる）。「ロックを解除」を**［新規バージョンで制作］**に改名 |
| v13.7 | ファイル管理を 履歴／エクスポート／インポート の 3 つに整理。**ステータスは後から直せる**。上のバーに［🔒 ロック］ |
| v13.8 | ステータスを**選択式**にして履歴の行でそのまま選べるように。「案件」の語を外し、ラベルを**［✎ 修正（新Verへ）］**に |
| v13.9 | 履歴の行を**たたんだ**（見出しを押すと開く）。開き具合は `VOPEN` に覚える |
| v14.0 | 上のバーに**いまの版**を常に出す（ロック中でなくても）。「新Ver 準備中」を履歴にも出す |
| v14.1 | **作字のロック**。ロック中は共有フォルダーの作字を書き換えない。同じ名前で形がちがうときは**見くらべて選ぶ** |
| v14.2 | 見くらべ窓から［やめる］を外し、**元のフォントに戻す**を足して 3 択にした |
| v14.3 | 見くらべ窓の 3 択を作り直した。**案件から共有を書き換えることはしない**（共有に合わせるか、別名で足すか、やめるか） |
| v14.4 | 赤・黒・SVG の出力を**ゴムシートのバー**へ移した。「✥ 文字の位置」をやめて**いつでも動かせる** |
| v14.5 | ロックから**備考メモを外した**。ゴムシートで備考メモを右クリック → 縦横の切替。確認資料で**校正文字ぜんぶ**を動かせる |
| v14.6 | **トリミングをロックの外**へ。ロック中でも拓本の出す範囲を決められる |
| v14.7 | 確認資料に**［▣ トリミング］**を付けた。「位置調整」と「元に戻す」は廃止 |
| v14.8 | メニューの「ファイル管理」を**「ファイル」**に |
| v15.0 | 確認資料の移動を**枠ごと／行ごと**に。行の入っていない**空の枠は内容の左**へ（新しい枠ほど左） |
| v15.1 | 校正を**枠エリア（x<0）と枠外エリア（x≧0）**に分けた。枠の場所は動かず、行を入れると**文字が枠へ移る** |
| v15.2 | 校正にも**［列を白地に］**。覚えていた拓本の場所が文字に食い込んでいたら、右へ逃がす |
| v15.3 | 枠エリアと枠外エリアの**あいだにすき間**。枠があるときも**確認資料に拓本を出す** |
| v15.4 | **文字の入っていない行にも校正の場所を取る**（重ならない）。自動の文字の大きさに**30mm の上限** |
| v15.5 | 行を足すときは**いまある行ぜんぶの左**へ、外側どうし **6mm** あける（`ADD_GAP`） |
| v15.6 | 拓本があると、足したセンターラインが**画素から計算し直されて重なる**のを直した |
| v15.7 | **校正で、足した行がいつも先頭に置かれて重なる**のを直した（`placeRun`） |
| v15.8 | 足す行は**かならず左**（紙からはみ出しても右へ回さない） |
| v15.9 | **並べ替えが校正に効かない**のを直した（`glSeqMove` で `S.bothOff` を捨てる） |
| v16.0 | ゴムシートで **Shift＋ドラッグ** して行をまとめて選び、**まとめて動かせる** |
| v16.1 | 囲んで選ぶのに **Shift はいらない**。Shift＋クリックで**足し引き** |
| v16.2 | 確認資料の**枠名が切れない**ように。JPEG 保存のときに**ロックするか聞く** |
| v16.3 | テンプレートに**入れるものをレ点で選ぶ**（ゴムシート／枠／校正文字）。組み合わせで 4 とおり |
| v16.4 | テンプレートと枠テンプレートを**つながった時点で読む**（開く前にそろえておく） |
| v16.5 | **組み込みの枠**（`BUILTIN_FRT`）。共有フォルダーにつながっていなくても使える |
| v16.6 | 組み込みの枠を**実際に使う 5 つ**に入れ替え。［置換］は外して［追加］だけ |
| v16.7 | 組み込みの枠の名前を短く。**寄せはぜんぶ右** |
| v16.8 | 組み込みの枠を、左メニューの**［枠追加］**からも入れられるように |
| v16.9 | **右クリックの振り分け**。字を選んでから右クリック＝1 文字の調整、選んでいなければ**文字グループのまとめ調整**（`#grpMenu`） |
| v17.0 | **拓本に重ねている間の、文字の打ち直し欄**（`#bothEd`）。校正の画面のすみで、見くらべながら字を直せる |
| v17.1 | 文字グループの右クリック（`#grpMenu`）にも、**文字の打ち直し欄**を足した |
| v17.2 | `#grpMenu` の**字間 ±mm の欄をやめた**（効かないため）。代わりに**［拓本を重ねる］**を足した |
| v17.3 | 1文字の移動を**十字パッド**に（数を打つ欄はやめた）。作字が読めていないときは、候補を開いたときに**読み直す** |
| v17.4 | **後ろの送り**も矢印（− 0 ＋）だけに。きざみは十字パッドと共通 |
| v18.0 | **お客様からの資料**。工事フォルダの画像を登録し、校正を見ながら別の窓で読める。案件ファイルには置き場所だけ |
| v18.1 | パスで編集：**Esc で閉じない**／［輪郭を消す］モードをやめ **［点をまとめて選択］**を追加／説明文を全部消す／［作字登録］［1つ戻す］を上のバーへ |
| v18.2 | 作字登録を目立たせた／［点をまとめて選択］で**一辺をワンクリック**／右クリックのメニューは**使えるものだけ**出す |
| v18.3 | ［点をまとめて選択］を**［輪郭を選択］**に改名／右クリックに［この輪郭の点をぜんぶ選ぶ］［たて・よこにそろえる］／モードを変えても選択を残す |
| v19.0 | **はじめの画面**。保存された原稿が無いとき、［新規］／［追加彫刻］を選んで、要るものを順に登録できる |
| v19.1 | はじめの画面：拓本も**工事フォルダから選べる**／一覧はその場に出て**窓は閉じない**／行は押しても増えず**入れかえ**／行のテンプレートに**下見**を付けた |
| v19.2 | 資料は**大きな写真を縮めてから置く**（4MB の壁）／同じ名前は**（2）**を付ける／下見を縦 1.5 倍にし、**足したセンターライン**も出るよう作り直した |
| v19.3 | 資料に**スクリーンショットの貼り付け**と、**文字で届いた内容のまとめて登録**（資料に入れる／行に入れる）を追加 |
| v19.4 | 資料の写真を、拓本と同じように**校正に重ねられる**（`S.docOv`）。位置・大きさ・回転・濃さは手で合わせる |
| v19.5 | はじめの画面：工事フォルダの一覧を**小さな絵（サムネ）**で並べる／［行をえらぶ］に**拓本を縮小表示** |
| v19.6 | サムネの問い合わせが 400 になるのを直した（`$expand=thumbnails(select=…)` は通らない）。だめなら**サムネ無しで取り直す** |
| v19.7 | 拓本の読み込み中に**回る印と「読み込み中」**を出し、**二度押しを受けない**ようにした／行の下見を**倍の大きさ**（180×330） |
| v19.8 | はじめの画面の［はじめる］は、**拓本があれば拓本補正へ**移る |
| v19.9 | **実測長が効かなくなるバグ**を直した（古い `S.rub` を掴んでいた）／拓本の上の**右クリックメニュー**（回転補正・トリミング・完了） |
| v20.0 | はじめの画面で、テンプレートを**読めていないのか無いのか**が分かるように。読み込み中の表示と**［🔄 読み込む／読み直す］** |
| v20.1 | 校正に重ねる資料を**データ URL**で描くようにした（blob: は SVG で出ないことがある）／**読めないときに知らせる** |
| v20.26 | **校正ロック中でも、重ねた資料はつかんで動かせる**ようにした（校正の画面にしか出ないため。校正文字・拓本は止めたまま） |
| v20.25 | 資料を出している間は 上のボタンが **［✕ 資料を閉じる］** に変わり、ワンクリックで窓も重ねもまとめて閉じる |
| v20.24 | 上の［📄 資料］を押すと **えらび**（閲覧／重ねて表示 ＋ どの資料）を出すようにした |
| v20.23 | 資料の窓の見出しに **［重ねる］** を足した（左の［資料］の「重」と同じ。押すとやめる） |
| v20.22 | 資料が読めないときに **名前でさがし直して置き場所を直す**（`docFetch`／`docRefindId`）／登録したときの**ドライブから読む**／読めなかったときの案内を**理由ごと**に |
| v20.21 | 資料の窓に **⟳ 読み直し** と **［見え方］**（そのまま／くっきり／白黒つよめ／反転）を足した |
| v20.20 | 資料の窓の**写真を左クリックでつかんで動かせる**ようにした（スクロールではなく置き場所で動かす。窓に収まっているときも動く） |
| v20.19 | 中空のつまみを押したときに**「紐づいているため動かせません。右クリックで解除すると動かせます」**とお知らせを出す |
| v20.18 | 足しただけのセンターラインは**画面に出さない**（文字が入った時点で出る）／足した線の**行幅は 0 から** |
| v20.17 | センターラインを足す場所を **その行の中（いちばん右の線と行幅の右はしのまん中）** に／「ここへ文字グループをドラッグ」を**押して足せる**ボタンに |
| v20.16 | まとめ調整の「コピーした文字グループです…」の案内を削除／**画面の線の名前は 1本目だけ**（足した線には出さない） |
| v20.15 | 鎖の印を **75% に縮小・青**に／**つなぎ直すと、センターラインの左右の位置と行の長さもコピー元に戻す** |
| v20.14 | 借りものの行で**紐づけを外した線が、拓本でつかんでも動かなかった**のを修正／まとめ調整の［この文字グループの紐づけを解除］は外した（鎖の印と右クリックでできるため） |
| v20.13 | 校正でガイドラインを押しても**行の構成が切り替わらない**のを修正（`renderCols()` を呼んでいなかった）／**コピーの行の x を拓本の画素から計算し直さない**ようにした（外した線が元の親につられて動いていた） |
| v20.12 | 紐付けの札を **字なしの鎖の印**に（つながった鎖＝一緒に動く／切れた鎖＝別々） |
| v20.11 | 名前を **`2　センターライン1`**（画面の線の上は **`2`** だけ）／**［紐付中／紐解除］の切り替えボタン**をセンターラインと文字グループに（もう一度押すと戻る）／ロックの判定を**つまみごと**に／**`migrate()` が `linkTo` を消していたのを修正** |
| v20.10 | 中空のつまみの右クリックは、**押したつまみの文字グループが相手**。えらばせない（センターラインのつまみも、乗っている文字グループを相手にする） |
| v20.9 | 中空のつまみの右クリックを、**文字グループ 1つずつの解除**に（行まるごとではない）。押したつまみの文字グループが先頭に出る |
| v20.8 | 拓本の上で**親（1本目）の線を動かすと、あとから足した線も一緒に動く**／**コピーの行のつまみは 中空（○□）で動かせない**。右クリック →［紐づけを解除］で 動かせる ●■ になる |
| v20.7 | **テンプレートを足したとき、拓本側の線が文字側とまったく違う所に出ていたのを直した**（シート上のずらし量が拓本にも乗っていた）。解析はんいの まん中に、行どうしの離れ方はそのままで置く／`tplAdd` の末尾で毎回 例外になっていた（MG0・SW0 が未定義）のも修正 |
| v20.6 | **コピーの位置決めライン（拓本の画素）を、元とまったく同じ場所**にした。コピーしたときにシートのずらし量ぶん動かして持たせていたのが原因。古い案件も読み込み時に直る |
| v20.5 | **拓本と重ねている間は、コピーの行の線もつかめる**ようにした（線が出るのは元の行の位置。動かすと元の画素を動かす） |
| v20.4 | センターラインは**コピー元の行で決める**（コピーの行では線の欄を出さない）／**左右の位置も行幅と同じく共通**。どちらから動かしても結ばれた線が同じ量だけ動く／× は結ばれた分ごと消す |
| v20.3 | （v20.4 で作り直し）センターラインを足すと結ばれた行にも足す／［紐づけを解除］を**いちばん上**・［詳細］を**いちばん下**へ／解除したのにメニューに「○ のコピー」が残るのを直した |
| v20.2 | 拓本に重ねている間の**打ち直し欄をやめた**（v17.0）／資料の窓は**ホイールで拡大縮小・つかんで移動**。［＋］［−］［収める］［別ウインドウ］はやめた |

## 気をつけるところ

- **1回だけつなぐ手あては、`S.rub` を外に閉じ込めてはいけない**（v19.9 で直した）。
  実測長の欄（`#rLen`）は `#rubwrap` の中に 1つだけあり、`bindRub()` が
  `le0._bound` で 1回だけつないでいる。その中で外側の `var r = S.rub` を
  使っていたため、**`migrate()` が `S.rub` を作り直すと古い入れものに書いてしまい**、
  「実測長を打っても効かない」「線を動かすと数値が戻る」という形で表に出ていた。
  `migrate()` は枠を足す・テンプレートを入れる・案件を開く、どれでも走る。
  handler の中では **その場で `S.rub` を読む**こと。

- **拓本の右クリックメニュー**（v19.9・`#rubMenu` / `rubMenuOpen()`）。
  補正の画面（`view === "rub"`）で絵の上を右クリックすると出る。中身は状態で変わる。

      ふつう　　　ここを中心に回転補正をはじめる／トリミング／180° 反転／補正を終える
      回転中　　　✓ 回転を終える／ここを回転の中心にする／角度を 0 に戻す
      トリミング中　✓ トリミングを終える／全体に戻す

  ［ここを中心に…］は `startSpin()` のあと、押した場所で `setPivotAt()` を呼ぶ。
  上の帯のボタンはそのまま残してある。


- **はじめの画面**（v19.0・`#startWin` / `startOpen()`）。工事フォルダに保存された
  原稿（JSON）が無く、まだ何も作っていないときだけ出す。判定は
  `spApplyHandoff` の中で `autoOpenCase()` が false を返し、かつ `startEmpty()`
  （行も枠も拓本も無い）のとき。**開いている案件を上書きすることは無い**。
  あとから出したいときは［ファイル］の［▶ はじめの画面をひらく］。

      新規　　　1 枠をえらぶ　2 行をえらぶ　3 お客様の資料を登録
      追加彫刻　1 拓本を取り込む　2 お客様の資料を登録　3 行をえらぶ

  手順は `ST_STEPS` に書いてあるだけで、中身は既にある機能を呼ぶだけ
  （`frtPutBuiltin` / `tplApply` / `tplAdd` / `addGuideline` / `fDoc` / `fRub`）。
  **拓本も資料も、共有フォルダーの画像から選ぶ**（v19.1・`spImageList()` で
  工事フォルダと `資料` フォルダの画像を並べ、`spImageDataUrl()` でデータ URL に
  してから `loadRubImage()` に渡す）。

  **［パソコンから入れる］は、ファイルを共有フォルダーに置いてから登録する**。
  置き先は 工事フォルダ／`資料`（無ければ作る）。**4MB を超えると単純アップロードが
  通らない**ので、`docShrink()` が 3.2MB 超か長辺 2400 超の画像を
  **長辺 2400・画質 0.82 の JPEG** に縮めてから上げる（v19.2。名前は `.jpg` になる）。
  同じ名前がすでにあると PUT は**黙って上書きする**ので、`docFreeName()` で
  「○○（2）.jpg」に付け替える。案件ファイルに入るのは、あいかわらず
  置き場所（アイテム ID）と名前だけ。

  **スクリーンショットの貼り付け**（v19.3）。［📋 ここを押してから Ctrl+V］の欄を
  押すと `docPasteOn` が立ち、その間だけ貼り付けが**拓本ではなく資料**に行く
  （拓本の paste は `docPasteOn` のとき素通りする）。絵は `貼り付け_日時.png`
  （縮めたときは `.jpg`）で置く。絵でない貼り付けは、下の文字欄に入る。

  **文字で届いた内容**（v19.3）。文字欄に貼って、
  ［この文字を資料に入れる］＝ `文字資料_日時.txt` として置いて登録（`docsPutText`）、
  ［この文字を行に入れる］＝〔まとめて入力〕と同じ動きで、いま選んでいる
  1行ガイドラインへ 1 行 1 グループで入れる（`docsTextToRows` → `bulkApply`）。
  打ちかけの文字は `docTxtDraft` に控える ―― パネルを描き直すと消えるため。
  資料の窓は `.txt` を字のまま出す。

  **校正に重ねる**（v19.4・`S.docOv = {id,x,y,w,rot,op}`・mm）。
  重ねる絵は **データ URL**（`DOCIMG[id]`・v20.1）。窓で見るときの objectURL（blob:）は
  SVG の `<image>` で出ないことがあり、**何も出ないのに知らせも無い**状態になっていた。
  拓本と同じデータ URL なら確実に出る。読めていない間は**うすい四角と字**を出し
  （読み込んでいます／読めませんでした／つながっていないので読めません）、
  しくじったら資料のパネルとお知らせにも理由を出す（`DOCOV.err`）。
  資料の行の［重］で入り切りする。拓本とちがって**実測長が無いので、
  大きさも位置も手で合わせる**。絵をつかむと動き（`data-k="dov"`）、
  右下の青い四角で大きさが変わる（`data-k="dovs"`。縦横の比は保つ）。
  左右・上下・幅・回転・濃さは資料のパネルの欄でも打てる。
  **重ねた絵は画面で見るためだけ**。出力にも確認資料にも出ない
  （`renderBoth` にしか描いていない）。重ねた分も画面の入る範囲に足してある
  ―― 足さないと、置いたそばから画面の外に出てつかめなかった。
  案件を開き直したときは、`docOvLayer()` が絵を読み直してから描く。
  文字の資料（`.txt`）は重ねられない。

  一覧は**小さな絵（サムネ）で並べる**（v19.5）。SharePoint の一覧に
  `$expand=thumbnails` を付けて、返ってきた URL をそのまま
  `<img>` に入れるだけ（絵の中身は取りにいかない＝速い）。
  **`$expand=thumbnails(select=medium,small)` の書き方は通らない**
  （`400 Parsing OData Select and Expand failed` になる・v19.6 で直した）。
  それでも断られる置き場所があるので、`spChildren()` が**サムネ無しで取り直す**。
  **拓本の読み込みは時間がかかる**ので、押した札に回る印と「読み込み中」を出し、
  その間はどの札も押せなくする（v19.7・`ST.busy`）。何度押しても読みにいくのは 1 回だけ。サムネが無いものは 🖼、
  文字の資料は 文 の札。拓本を選ぶときは文字の資料を出さない。
  ［行をえらぶ］には、拓本があれば**いちばん左に拓本の縮小**を並べる（v19.5）
  ―― いまの彫り方が分からないと、どのテンプレートが合うか選べないため。

  一覧は**はじめの画面の中に開く**ので、
  選んでも窓は閉じない（v19.0 では左のパネルへ飛んで窓が消えていた）。
  **行は押すたびに増えない**（v19.1）。いつも `tplApply`（入れかえ）を使い、
  選び直すと前の行は残らない。テンプレートは名前だけでなく**下見**を出す
  （`tplPrevSVG()`。フォントは使わず、画面の字で縦に並べるだけの略図。
  大きさは 180×330（v19.7 で倍にした）。拓本の札も同じ大きさ。センターラインを足したもの（`linkTo` で
  行幅を借りている）と、文字の乗っていない線も出す ―― v19.2 で作り直した。
  枠は、行を持たないテンプレートのときだけ描く。いっしょに描くと幅が広がって
  行がつぶれるため）。
  **テンプレートは「無い」のか「まだ読んでいない」のかを出す**（v20.0・`stLoadBar()`）。
  読めていないのに「ありません」と出ていたので、区別がつかなかった。状態は

      まだ読んでいない　共有フォルダーの…は、まだ読んでいません。［🔄 読み込む］
      読み込み中　　　　回る印つきで「読み込んでいます…」（ボタンは出さない）
      読めなかった　　　読めませんでした：…　［🔄 読み直す］
      読めた　　　　　　共有フォルダーの… N 件　［🔄 読み直す］

  行（`TPL`）と枠（`FRT`）の両方に出す。本体アプリにつながっていないときは出さない。
  ［🔄］は `tplLoadSP(true)` / `frtLoadSP(true)` を呼び、終わったら `startRender()`。

  **［はじめる］の行き先**（v19.8）。拓本を取り込んであれば `startFix()` で
  **拓本補正**（`setSub("rub")` ＋ 拓本の画面）へ。頭・脚と実測長が決まらないと
  行の位置も文字の大きさも合わせられないため、そこを先に済ませてもらう。
  拓本が無ければ、行があれば文字編集、枠だけなら枠、どちらも無ければゴムシート。
  **どの手順もとばせる**。済みの判定は `stDone()`（枠・行・資料・拓本があるか）で、
  `renderMenuCounts()` から `startRender()` を呼んでいるので、
  何か入るとその場で ✓ に変わる。追加彫刻では行を `tplAdd`（足す）で入れる
  ―― 拓本に合わせた既存の行を消さないため。


- **パスで編集の Esc は、窓を閉じない**（v18.1）。点の選びを外すつもりで押すと、
  直しごと消えていた。Esc は（右クリックのメニューが出ていれば）そのメニューを閉じ、
  無ければ**点・輪郭の選びを外すだけ**。閉じるのは［取消］か［決定］。

- **モードは 6 つ**（v18.1）。［選ぶ・動かす］［点をまとめて選択］［点を足す］
  ［点を消す］［丸を置く］［線で切る］。［輪郭を消す］モードは外した
  （右クリックの［この輪郭を消す］と、［選んだ輪郭を消す］で足りる）。
  **［輪郭を選択］**（`PE.tool === "box"`・v18.3 で改名）は、**点の上から引いても囲みになる**。
  引かずに押しただけなら、押した所の**一辺**（となり合う 2 点）を選ぶ
  （v18.2・`peEdgeAt()`。まっすぐな線で測り、`upem × 0.05` まで拾う）。
  そのまま点をつかめば辺ごと動く（**すでに選んである点だけ**、つかむと動く）。
  `.pehit` / `.pehitc` の pointerdown を素通りさせて、台紙側の囲み処理に渡している。
  ［選ぶ・動かす］のままでも、何もない所から引けば囲める（今までどおり）。

- **右のメニューに説明文は置かない**（v18.1）。残っているのは中身が変わる案内だけ
  （`peWInfo` ＝ 画線幅の実測、`peRedInfo` ＝ 赤にしている輪郭の番号、`peName` ＝
  共有フォルダーでの名前）。［★ 作字登録］（＝ 上書き保存・緑で目立たせている）と
  ［1つ戻す］は**上のバー**（`.pehd`）にある。

- **右クリックのメニューは、使えるものだけ出す**（v18.2・v18.3）。出し分けは

      この点を動かす／この点を消す　　点の上、かつ その点が選択に入っていないとき
      ここに点を足す　　　　　　　　　点か線の上
      この輪郭の点をぜんぶ選ぶ　　　　点か線の上（v18.3）
      この輪郭を動かす／消す　　　　　点か線の上
      たて・よこにそろえる　　　　　　2 点以上を選んでいるとき（v18.3）
      選んだ N 点を消す　　　　　　　 1 点以上を選んでいるとき
      この字の点をぜんぶ選ぶ　　　　　まだぜんぶ選んでいないとき
      選択を外す　　　　　　　　　　　何か選んでいるとき
      モード切り替え　　　　　　　　　いまのモード以外
      表示を元の大きさに　　　　　　　拡大・移動しているとき

  **モードを変えても選択は消さない**（v18.3）。左のボタンでも右クリックでも同じ。
  ［たて・よこにそろえる］は選んだ点の **まん中** にそろえる（ハンドルも同じだけ動かす）。


- **お客様からの資料**（v18.0・左メニューの［資料］）。**絵は案件ファイルに入れない**。
  工事フォルダに置いたまま、案件ファイルには置き場所だけを覚える。

      S.docs = [{ id:"<アイテムID>", name:"...", drive:"<driveId>", at:"..." }]

  案件ファイルは SharePoint へ**単純アップロード（`PUT …/content`）で上げていて
  4MB までしか通らない**。拓本の画像（`rubSrc`）ももう入っているので、
  写真を base64 で入れると保存そのものが失敗する。だから置き場所だけにした。

  入れかたは 2 つ。［＋ パソコンから入れる］は工事フォルダの下の
  **`資料` フォルダ**（`DOCS_DIR`・無ければ作る）へ上げてから登録。
  ［＋ 工事フォルダから選ぶ］は工事フォルダと `資料` フォルダの画像を並べて選ぶ
  （同じものが 2 度出ないよう id で重複を落とす）。［×］は**この案件から外すだけ**で、
  ファイルは消さない。

  見るのは `#docWin`（右寄せの窓・見出しをつかんで動かせる）。校正のバーの
  ［📄 資料］か、パネルの［見る］［📄 資料を見る］で開く。◀▶ とキーの ←→ で送り、
  **ホイールで拡大縮小**（ポインタの下が動かないように scroll を直す）、
  **つかんで移動**、**ダブルクリックで窓に収める**、↻ で 90° 回転（v20.2）。
  ［＋］［−］［収める］［別ウインドウ］のボタンはやめた。倍率は見出しに数字で出す。
  読んだ絵は `DOCC` に objectURL で覚え、案件を替えるときに `docsForget()` で捨てる。

  **ロック中も資料は入れられる／見られる**（備考メモと同じ扱い。校正の中身ではないため）。
  つながっていないと絵は読めないので、その旨を窓に出す。


- **1文字の移動は十字パッド**（v17.3・`#charMenu` の `.padwrap`）。数を打つ欄
  （もとの `mDx` / `mDy`）はやめた。↑↓←→ で `adj[ci].dx / dy` を きざみ mm ずつ
  足し引きし、まん中の［中］で 0 に戻す。きざみは 0.1 / 0.5 / 1 / 5 mm から選ぶ
  （`padStep`。メニューを開き直しても覚えている）。動かした量は下に文字で出す。
  **後ろの送り**も同じ形（v17.4・［−］［0］［＋］）。きざみは十字パッドと共通。
  いちばん下の字には送りが無いので、ボタンは出さずにその旨を書く。
  **1文字目を動かしても、行の 上端・下端 と ほかの字は動かない**（`dy` は
  描くときに足すだけで、`layout()` の高さ・送りの計算には入らない）。

- **共有フォルダーの作字は、つながった時点で読んでいる**（`spApplyHandoff` →
  `fontDirLoad()` の中で `glyphLibLoad()`）。ただし通信に失敗すると読めないまま
  になるので、字の候補を開いたときにもう一度読みにいく（v17.3・`glibEnsure()`）。
  読んでいる間は「読んでいます…」と出す。手で読み直すのは
  ［🔄 読み込む］（候補の欄）と［作字を読み直す］（フォント・作字のパネル）。


- **右クリックは 2 とおりに分かれる**（v16.9・`ctxOnChars()`）。
  `charPicked`（左クリックで字を選んだかどうか）で決める。

      選んでいる字の上で右クリック　→ `openCharMenu()`（その 1 文字だけ）
      それ以外（未選択・別の字・字の外）→ `openGrpMenu()`（文字グループぜんぶ）

  `charPicked` は、字を左クリック（`#stage` / `#bothstage`）したときに `true`、
  字の外を押したときと、ほかのガイドラインへ移ったとき（`charSel = 0;` のところ）
  に `false`。選んでいる合図（赤わく）も `charPicked` を見て出している。
  ゴムシートと校正、どちらも同じ `ctxOnChars` を使う。

  `#grpMenu` の中身は **文字（打ち直し欄・v17.1）**、
  大きさ（横 mm・連動・縦 mm・縦を横と同じに・自動に戻す）、
  太さ（フォントの可変軸。無いフォントでは出ない）、字間（まとめて ±mm・
  細い字の前後を詰める・一文字ごとの調整をすべて解除）。
  **字間はグループに持っていない**。一文字ごとの `adj[i].gap` に、
  最後の 1 文字の手前までまとめて入れる。ぜんぶ同じ値のときだけ欄に出す。
  ［取消］は開いたときの `text / wMm / hMm / axes / adj` に戻す。

  **「字間 ±mm（まとめて）」は置かない**（v17.2 でやめた）。`layout()` は送りの
  調整をならしている（`mean` を引く）ので、ぜんぶの字に同じ値を入れると
  打ち消し合って **1 ミリも変わらない**。行の高さは 上端・下端 で決まっていて、
  字間はその中に収める形だから。字間を変えたいときは 上端・下端 か
  文字の大きさ を動かす。［細い字の前後を詰める］は字ごとに量がちがうので効く。

  **［拓本を重ねる］**（v17.2・`#gmOv`）は、この文字グループを選んでから
  校正へ移り、`S.bothOv` を立てる。同じ行に重ねている最中に押すと解除。
  拓本が無い／実測長が未入力のときは押せない。
  ロック中は `LOCK_ZONE` の `contextmenu` を止めているので、どちらも出ない。


- **テンプレートは「入れるもの」をレ点で選ぶ**（v16.3）。
  `data` に、選んだ分だけ入れる。

      ゴムシート　`data.sheet`
      枠　　　　　`data.frames` ＋ `data.glFrame`
      校正文字　　`data.centers` ＋ `data.groups`

  組み合わせで **文字だけ／枠だけ／枠＋文字／ゴムシートだけ** を作り分けられる。
  `tplApply()` は**入っているものだけ**入れかえること。校正文字を持たない
  テンプレートで `S.centers` を空にすると、いまの行が消えてしまう。
  `tplAdd()` も、校正文字を持たないテンプレートでは行を増やさず、
  枠とシートだけ足す。
  **組み込みの枠は `BUILTIN_FRT`**（v16.5・HTML の中に直書き）。共有フォルダーに
  つながっていなくても使える。消せない。入れると**ふつうの枠**になるので、
  ［枠］のサブメニューで 名前・幅・高さ・文字のはじまり を実物に合わせて直せる。
  数字を変えるときは `BUILTIN_FRT` を書き換えるだけでよい。いまの中身（v16.6）。

      8寸角　竿 戒名　　242×630　下げ 30
      9寸角　竿 戒名　　273×780　下げ 30
      尺角　　竿 戒名　　303×850　下げ 30
      8寸角　竿 施主_左　242×630　下げ 180
      9寸角　竿 施主_左　273×780　下げ 210

  **寄せはぜんぶ `ax:"right"`**（縦書きなので、枠の中で行を右はしにそろえる）。
  出す場所は 2 か所（v16.8）。テンプレート管理の枠テンプレートの一覧と、
  左メニューの**［枠追加］**（`openFrameMenu`・`data-a="bi"`）。
  どちらも `frtPutBuiltin(i, true)` を呼ぶだけ。
  ボタンは［追加］だけ（［置換］は使わないので外した）。

  「枠テンプレート（`FRT`）」は前からのデータのために残してあるだけで、
  新しく作るならレ点の「枠」だけで用が足りる。

  **どちらも、本体アプリとつながった時点で読む**（v16.4・`tplLoadSP(true)` と
  `frtLoadSP(true)`）。パネルを開いたときに読むだけだと、
  ［🔄 読み直す］を押さないと出ないように見える。
  読み直しボタンは、読めなかったときの手当てとして残してある。


- **確認資料の枠名は、枠の上に出る**。その分だけ表示範囲の上をあけること
  （v16.2・`renderDemo`）。あけないと、いちばん上の枠の名前が切れる。
  字の大きさは `vw * 0.022` で決まるので、**横幅が決まったあと**に
  `vy` と `vh` を直す（先に直そうとしても字の大きさが分からない）。

- **確認資料の JPEG 保存は、ロックするか聞いてから**（v16.2・`demoSaveAsk()`）。
  確認資料を出すのはお客様に見せるときなので、そのついでにロックを促す。
  すでにロック中なら聞かない。［キャンセル］ならロックせずに保存だけ。
  ［ファイル］メニューの［確認資料を JPEG で保存］も同じ口を通す。


- **ゴムシートの範囲選択**（v16.0・`shSel`）。**Shift ＋ ドラッグ**で四角を描き、
  かかった 1行ガイドラインを選ぶ。縦書きの行なので、**左右のかかり具合だけ**で
  決める（上下は見ない）。選んだ行はうすい青で塗る（画面だけ・`isPrint` では出さない）。

      何もない所をドラッグ　　　　囲んだ行を選ぶ（**Shift はいらない**・v16.1）
      Shift ＋ 何もない所をドラッグ　いまの選択に**足す**
      Shift ＋ 文字をクリック　　　その行を**足す／外す**
      何もない所をクリック　　　　選択を外す
      選んだ行の中の文字をつかむ　選んだぶんが**まとめて**動く
      選んでいない行をつかむ　　　その行だけ動き、選択は外れる

  `shSel` は案件に保存しない（そのときだけの選択）。
  受け口は `#stagewrap` の**取り込み（capture）**に置いて、
  画面スクロール（`makePannable`）より先に横取りしている。
  そのぶん、**ゴムシートでは左ドラッグの画面移動をやめた**（v16.1）。
  画面を動かすのは、まん中ボタン・ホイール・スクロールバー。


- **行を足す置き場所は 1 か所にそろえてある**（v15.5・`ADD_GAP = 6`）。
  縦書きなので**いまある行ぜんぶの左**へ、**外側どうしで 6mm** あけて置く。
  **紙からはみ出しても右へ回さない**（v15.8）。回すと行の並びが読めなくなる。
  はみ出したぶんは、あとで「左右 mm」か校正のつまみで寄せてもらう。
  同じ空け方をするのは 4 か所。

      addCenterLine()   センターラインを足す
      addGuideline()    1行ガイドラインを足す
      copyGuideline()   1行ガイドラインを複製
      テンプレート挿入   （`gap = ADD_GAP`）

  `addCenterLine()` は**自分のガイドラインの中だけを見てはいけない**。
  見ると、となりのガイドラインにちょうど重なる位置に置いてしまう。
  `copyGuideline()` のずらし量は「かたまりの**右はし**を、みんなの左はしの外へ」。
  左はしで計算すると、幅の広いかたまりが右へはみ出して重なる。

  **拓本があるときは `S.rub.centersPx` も合わせて入れること**（v15.6）。
  `syncCenters()` は「同じガイドラインの 2本目以降の x を、1本目との
  **画素の差**から計算し直す」ので、まん中の画素を入れると
  「1本目と同じ場所」に戻されて、せっかく空けた 6mm が消える。
  `addCenterLine()` は、決めた x を画素に直して入れている。


- **文字の入っていない行も、校正では場所を取る**（v15.4・`bothPack()`）。
  `layout()` は文字のない行を返さないので、以前は `half`／`cx0` に載らず、
  行を何本足しても**ぜんぶ同じところ（センターラインの生の x）に重なって**いた。
  1行ガイドラインにつき 1本だけ場所を取れば足りる。同じガイドラインの
  ほかのセンターラインは `renderBoth` の `bx()` が同じずれ量で追いかける。

- **自動で決まる文字の大きさは 30mm まで**（v15.4・`AUTO_H_MAX`）。
  `hAuto = 行の高さ ÷ 文字数` なので、1文字目を打った瞬間に
  行の高さ（例 500mm）がそのまま字の高さになって画面いっぱいになる。
  効くのは「縦 mm」を空にした（＝自動の）ときだけ。打ち込んだ値はそのまま。


- **校正の画面は 2 つのエリアに分かれている**（v15.1・`bothPack()`）。

      枠エリア　　x < 0 側。枠 0 の右はしが **−FGAP**。そこから左へ
      　　　　　　`f.w + FGAP` ずつ積む。**新しい枠ほど左**
      枠外エリア　x >= 0 側。枠に入れていない行と、拓本

  **枠エリアの右はしを 0 にしないこと**（v15.3）。枠外の 1本目の行は
  ちょうど x=0 から始まるので、0 にすると枠と文字がぴったり接して
  重なって見える。`FGAP` ぶん空けておく。

  **枠の場所は中身によらず動かない**。次の枠の右はしは「名目の幅（`f.w`）」で
  積むので、中身が広くて枠が左へ広がっても、ほかの枠はずれない。
  これで、行を枠に入れたときに**枠ではなく文字のほうが枠へ移る**。

  **`placeRun()` は「置き場所を覚えていない行」を 2 とおりに置く**（v15.7）。

      1つも覚えていない（まっさら・［位置を戻す］の直後）
      　　これまでどおり、左から ガイドライン間 mm で詰め直す
      すでに覚えている行がある（＝いま 1 本足した）
      　　**いちばん近い「置いてある行」と同じずれ量**にする
      　　＝ シートで空けた 6mm が、そのまま校正にも出る

  2 つめが要。走り書きの `c`（幅の足し算）から出すと、
  足した行は並び順で**いつも先頭（c＝0）**になるので、
  足すたびに同じ場所へ置かれて**ぜんぶ重なる**。
  「まっさらかどうか」は**その描画に入る前**の状態で決めること
  （描画の途中で 1 本置いた結果を見てしまうと、詰め直しが効かなくなる）。

  **並びを変えたら `S.bothOff` をぜんぶ捨てる**（v15.9・`glSeqMove()`）。
  覚えたままだと、メニューの順番だけ変わって**校正の文字は動かない**。
  捨てると、新しい並びで左から ガイドライン間 mm で詰め直される。

  引っ越しの要は `frameAssign()` で **`S.bothOff[g]` を捨てる**こと。
  `bothOff` は「校正でいちど決めた置き場所」を覚える仕組み（v12.9）なので、
  残したままだと枠に入れても文字が前の場所に留まる。

  **覚えていた拓本の場所（`S.rubX`）が、いまの文字のかたまりに食い込んでいたら、
  右へ逃がす**（v15.2・`renderBoth`）。`rubX` は絶対の位置を覚えるので、
  並べ方を変えると古い値のまま文字に重なってしまう。
  枠の中に入れている拓本（`S.rubFrame >= 0`）と、重ねているとき（`ov`）は別。

  拓本の置き場所（`pk.right`）は**枠の外の行の、実際の右はし**で測る。
  `placeRun` の戻り値（幅の足し算）で測っていたころは、行を枠に移すたびに
  値が変わって拓本まで動いていた。


- **備考メモの右クリックメニュー**（`#noteMenu` / `openNoteMenu()`・v14.5）。
  ゴムシートの `.notehit` を右クリックすると出る。縦書き 2 行 ⇄ 横書き、
  シートに入れる／入れない、文字サイズ、備考メモの欄をひらく。
  ロック中も使えるようにしてあるので、`contextmenu` の取り込みブロックに
  `.notehit` の抜け道を入れてある。

- **枠があるときも、枠の外の拓本を確認資料に出す**（v15.3・`showRub`）。
  もとは「枠があるなら、枠に入れた拓本しか出さない」だったが、
  校正では見えているのに確認資料では消えるので分かりにくかった。
  エリアが分かれたので、枠にかぶることはない。

- **確認資料は「位置調整」を入れなくても校正文字ぜんぶを動かせる**（v14.5）。
  ずらし量は `S.demoAll`（mm・表示だけ）。つかむ面は `data-demoall`。

      位置調整オフ　`data-demoall` の面を敷く → 文字ぜんぶが一緒に動く
      位置調整オン　`data-gl` の面を敷く　　 → 1行ガイドラインごとに動く

  **動かす単位は「枠」と「行」**（v15.0）。

      枠に入れた行（`S.glFrame[gl] >= 0`）　その枠ぜんぶ ＋ 枠の線も一緒に
      　　　　　　　　　　　　　　　　　　 `S.demoFx[枠番号]`・面は `data-demofx`
      枠の外の行　　　　　　　　　　　　　 その 1行ガイドラインだけ
      　　　　　　　　　　　　　　　　　　 `S.demoGx[ガイドライン番号]`・面は `data-demogx`

  枠の線は `demoFBox` の `bx` に `demoFx` を足して動かす。中身だけ動かすと、
  枠と中身の関係が崩れて実物に当てられない。
  拓本は動かない（`ix` はこれらを見ない）。戻す道は用意していない
  （逆へドラッグして戻す）。
  `demoAll` `demoDx` `demoRx` は廃止し、`migrate()` で捨てている。
  拓本の置き場所（`ix`）は `demoAll` を見ないので、重ね方だけが変わる。
  `dReset` は `demoDx` `demoRx` `demoAll` の 3 つとも 0 に戻すこと。


- **出力ボタンはゴムシートのバーの中**（v14.4）。［① 赤を出力］［② 黒を出力］［SVG］は
  上のバー（`#bar`）ではなく `#pvbar` の `.forSheet` にある。ゴムシートの画面でしか
  使わないので、ほかのタブでは自動で隠れる。`button.red` `button.blk` の色は
  どこに置いても効く（`#bar` 限定の指定ではない）。

- **文字はいつでもつかんで動かせる**（v14.4）。以前の「✥ 文字の位置」で入る
  モード（`#btnSheetMove2` と `#shOv` の帯）はやめた。`sheetMove` は
  **`true` で初期化して、もう切り替えない**。`sheetPad` も 30 のまま
  （動かす間だけ表示範囲を広げていたが、常時広げる）。
  `setSheetMove()` は古い呼び出しの受け皿として残してある。
  帯にあった［中央］は `#btnShHome`（［位置を戻す］）としてバーへ移した。
  これが無いと `clearSheetOff()` を呼ぶ道が無くなる。


- **共有フォルダーの作字は、勝手に上書きしない**（v14.1）。止めどころは 2 つ。

  ① **ロック中は書かない。** `glyphLibSave()` の頭で `locked()` なら投げる（最後の砦）。
  そこまで行く前に `glyphLibPushOne()` / `peCommitLib()` / `glibLibDelete()` の
  それぞれでも止めて、［✎ 修正（新Verへ）］を案内する。

  ② **同じ名前で形がちがうときは、見くらべて選んでもらう。**
  `glyphLibPushOne()` が共有を読んだあと、同じ `glyphKey(ch, font, name)` の
  ものがあって `samePathShape()` が偽なら、`openGlyCmp()`（`#glyCmp`）を出して止める。

      元のフォントに戻す　　この字の作字をやめる（`setAdj(g, ci, "path", null)`）
      いまの形を残す　　　　いまの形を「月2」として新しく足す
      　　　　　　　　　　　（`glyphLibPushOne(gi, ci, true)` ＝ asNew）
      共有の「月1」に合わせる　共有の形を案件に取り込む。いまの形は捨てる
      　　　　　　　　　　　（`setAdj` で共有の path を入れるだけ。共有は書かない）

  **どれを選んでも、共有フォルダーの「月1」は書き換わらない**（v14.3）。
  想定しているのは「共有の字に不具合があって、別の案件で直した」場面。
  案件の側が古い形を持っているのだから、案件が共有を上書きしては元も子もない。
  共有の字を直すのは［フォント・作字］の一覧から開いて直す道（`peCommitLib`）だけ。
  `glyphLibPushOne` の `force` はどのボタンからも渡していない（残してあるだけ）。

  **3 枚の絵は「選んだあとにゴムシートに出る形」**でそろえる。
  絵と結果がずれると、とたんに分からなくなる。
  直したのか、別の形を作ったのかは中からは判らないので、かならず選ばせる。

  **［やめる］は置かない**（v14.2）。「やめる」と何も起きず、名前だけ「月1」を
  名乗ったまま残る。あとで候補一覧から「月1」を押したときに共有の別物へ
  入れ替わるので、かえって分かりにくい。かならず 3 つのどれかを選ばせる。
  いちばん左の絵は元のフォントの字なので、`shapeOne` → `glyphPath` →
  `parsePath` で作る（`glibItems()` の "orig" と同じ）。

  ※ `glyphKey` には**フォント名が入る**。テストで共有の作字を手で仕込むときは
  `font` に実際のフォント名（`FR(0).name`）を入れること。空にすると別物として
  扱われ、見くらべ窓が出ずに「月1」が 2 つできる。


- **案件ファイルは 1 案件 1 ファイル、中に版をならべる**（v13.5）。名前は
  `caseFileName()` ＝「（家名）_彫刻原稿.json」。日付は付けない
  （付けると日が変わるたびに別ファイルになって、履歴が二重になる）。

      { v:2, savedAt, fonts:[], rubSrc:"data:…", cur:3,
        versions:[ { id, at, lock:{…}|null, status, note, state:{…S} }, … ] }

  **拓本の画像は `rubSrc` に 1 つだけ**置く。版ごとに複製すると、
  1 枚数MBが版の数だけ増えてファイルが壊れるほど大きくなる。
  `docPutCurrent()` が `state` から `rub.src` を抜いて `rubSrc` に寄せ、
  `applyVersion()` が戻す。

  **ロックした版は二度と書き換えない。** `docPutCurrent()` は
  「いまの版（`S.verId`）がロック済みなら、上書きせず新しい版を足す」。
  だから ロック → 解除 → 直す → 保存 で、自動的に次の版になる。
  ロックそのもの（`lockOn`）も保存を呼ぶので、ロックした校正は必ず残る。

  古い形（S を丸ごと入れたもの）は `docUpgrade()` で読めるが、
  **書き戻しはしない**。古いファイルを開いたときは `CASEF` を空にして、
  保存は新しい名前のファイルへ回す（既存のファイルには触らない）。

  `applyVersion()` は**版の記録の `lock` を正**とする（`state` の中の写しと
  ずれていても、版の記録どおりに開く）。

  `saveProjectLocal()`（パソコンに保存）は写しなので、**`S.verId` を戻す**こと。
  戻さないと SharePoint 側の版の番号とずれる。

  **［ファイル］メニュー**（`panFile` / `filePanel()`・v14.8 まで「ファイル管理」）で履歴を見る。
  中身は `CASEDOC`（最後に読んだ／書いた案件ファイル）を写しているだけなので、
  保存・読み込みのたびに `CASEDOC` を入れ替えること。
  ロック中も触れるように、`#panFile` は `#panSP` と同じく素通しにしてある。
  見出しは 履歴／エクスポート／インポート の 3 つだけ（v13.7）。
  パネルを開くと `fileLoadOnce()` が勝手に読む（`_fileBusy` で二重読みを止める）。
  **ステータスとメモは後から直せる**（`verEditStatus()`）。校正提出のあとで
  不備が見つかったときのため。直すのはラベルだけで、版の中身（`state`）は触らない。
  書き戻しは `casePut()`＝版を増やさずファイルをそのまま置き換える。
  履歴の行は `<details class="vrow">`（v13.9）。見出し（`<summary>`）は
  🔒／ステータス／版番号／日時／メモ／▽ の 2 行だけ。開き具合は `VOPEN` に覚え、
  描き直しても閉じないようにする。ステータスやメモを変えたときは
  `filePanel()` を呼ばずに、見出しの `.l1 span` と `.l2 .nt` だけ書き換える
  （呼ぶと開いている行が閉じる）。`.nt` は空でも必ず置いておくこと。
  無いと、メモを消してから入れ直したときに戻せない。

  ステータスは `STATUS_LIST`（お客様校正提出／（中止）お客様校正提出／誤操作／
  その他）からの**選択式**で、履歴の行の `<select>` をその場で変えるだけで保存される
  （v13.8）。空のときのために `statusOptions()` は先頭に「（なし）」を入れること。
  入れないと、ステータスなしの版が先頭の「お客様校正提出」に見えてしまう。

  **上のバーの［🔒 ロック］**（`#btnLock` / `lockBtnFill()`）は、ロック中は
  ［✎ 修正（新Verへ）］に変わる。表示の入れ替えは `applyLock()` の中で行う。

  その**左**に、いまの版を常に出す（`#barVer` / `barVerFill()`・v14.0）。
  ロック中だけ帯を出す作りだと、ロックしていないときに何も出ず、
  切り替わったかどうか分からなかった。3 とおり。

      ✎ 版 2　2026/09/04 10:33                     ふつう（灰）
      🔒 版 1　お客様校正提出　2026/09/04 10:33      ロック中（黄）
      ✎ 新Ver 準備中　保存すると版 2 になります      修正中（青）

  3 つめは `verPending()`＝**いまの版（`S.verId`）がファイルの中でロック済みなのに、
  画面はロックしていない**状態。［✎ 修正（新Verへ）］を押した直後がこれ。
  このとき履歴のいちばん上にも青い「新しい版（まだ保存していません）」を出す。
  **版が増えるのは保存したときだけ**なので、これを出さないと
  「修正を押したのに履歴が変わらない」と見える。
  同じ理由で、`verPending()` の間はロック済みの行に「・表示中」を付けない。

  **「ロックを解除」とは呼ばない**。解除したつもりなのに新しいファイルが
  できる、と読めてしまうため、**［✎ 修正（新Verへ）］**（`lockNext()`）にした。
  やることは同じで、ロックした版は履歴に残り、次の保存で版が 1 つ増える。


- **校正のロック**（v13.4）。`S.lock = null` か `{ at, status, note }`。`locked()` で見る。
  止めるのは**中身が変わる入口だけ**で、**位置合わせは通す**（これが決め事）。

      止める　文字の打ち込み・右クリックの 1 文字メニュー・その場の編集欄・
      　　　　サブメニューの入力欄とボタン・グループの並べ替え・
      　　　　**校正と拓本の画面でつかんで動かす**・ガイドライン間 mm・
      　　　　位置を戻す・拓本の反転／回転
      通す　　**ゴムシートと確認資料**でつかんで動かす・押して選ぶ（全画面）・
      　　　　**拓本のトリミング**（`#rbCrop` `#rbCropAll` と `.crophit`）・
      　　　　**備考メモ（`#panInfo` と右クリックメニュー）**・
      　　　　サブメニューのスクロールとたたみ欄・表示の切り替え・寸法・
      　　　　拓本を重ねる・印刷・SVG 出力・ファイルの保存／読み込み

  **トリミングもロックの外**（v14.6）。拓本の出す範囲は彫る内容を変えない
  （赤・黒の出力に拓本は入らない）ので通す。反転と回転は文字との重なり方が
  大きく変わるので止めたまま。つまみは `.crophit` を `LOCK_DRAG` の抜け道にする。
  **確認資料にもつまみを戻した**（v14.7）。`renderDemo()` の `cropping` は
  `cropMode && showRub`。［▣ トリミング］（`#dCrop`）は拓本の画面へ移らず、
  その場で `cropMode` を立てるだけ。ボタンの見た目は `demoCropFill()` が
  `bindDemoDrag()` のたびに直す。

  **備考メモはロックの外**（v14.5）。ロックで守るのは「文字の校正・形・位置・
  間隔」であって、備考メモは彫る内容ではない。だから `#panInfo` は
  `#panSP` `#panFile` と同じく素通しにし、`.notehit` の右クリックも
  取り込みブロックから外してある。

  校正（both）と拓本（rub）で動かせるのは、ガイドラインの並びや拓本の位置＝
  **校正そのもの**なので止める。ゴムシートと確認資料で動かせるのは紙の上の
  置き場所なので通す。この線引きが決め事。

  やり方は 2 つ。① `body.locked` を付けて CSS で **`#sub` の中の
  `input / select / textarea / button` だけ**を `pointer-events:none` にする
  （`#sub` そのものは触らない。触るとホイールでスクロールできなくなる）。
  `#panSP` は打ち消して素通しにする（解除ボタンがここにある）。
  校正・拓本のバーのボタン（`#bGap` `#bRubHome` `#rbFlip` `#rbRot` `#rbCrop`
  `#rbCropAll`）も止める。ゴムシートの `#btnSheetMove2` と確認資料の `#dAdj`
  `#dReset` は**ここに入れないこと**。
  ② `document` の**取り込み（capture）**で `contextmenu` / `dblclick`（範囲は
  `LOCK_ZONE`＝絵の 4 か所）、`pointerdown`（範囲は `LOCK_DRAG`＝
  `#bothwrap,#rubwrap` だけ）、`beforeinput`（`#panSP` を除く全部）、
  `dragstart` を止める。`pointerdown` は `stopPropagation` だけで
  `preventDefault` はしない — こうすると「つかんで動かす」は死ぬが、
  「押して選ぶ」（click）は校正の画面でもそのまま使える。
  ※ ロックの帯（`#lockBar`）は v14.0 で廃止した。絵の位置がずれなくなった。


- **実測チェック線は SVG（`printMode === "allc"`）にだけ入れる**（v13.2・`lenCheckMark()`）。
  拓本で実測した文字全長（`S.rub.realLen`）を、そのままの長さの青い線として
  紙の左の余白に引く。上端は `S.rub.originY`。紙に出したあと、この線に現物を当てれば
  縮尺が合っているか分かる。**プロッターへ渡す赤・黒（"red" / "blk"）には入れない**
  （線が 1本増えると、そのまま彫られてしまう）。

- **SVG は下見（`#svgPrev`）を見てから保存**（v13.2）。［SVG］で作って見せるだけ。
  実際に落とすのは［保存］を押したとき。下見の中の svg は mm 指定のままだと
  画面いっぱいになるので、`viewBox` の縦横比で枠に収まる px に直している（`svFit()`）。
  拡大縮小と移動は `SVZ {k,x,y}` を `#svZoom` の `transform` に当てるだけ（v13.3）。
  ホイールはカーソルの下の点が動かないように `x = px - (px - x) * (k1/k0)`。
  見出しの倍率ボタンとダブルクリックで、枠に収まる大きさへ戻る。

- **重ねているときは、拓本のセンターラインを 2通りに動かす**（v13.0 → v13.1）。
  見分けは `bothOv() && ci === bothOvAci`。

      合わせ先いがい　拓本の画素（centersPx）を動かす。文字は syncCenters で追う
      合わせ先　　　　拓本は動かさず、文字（S.centers[ci].x）のほうを動かす

  合わせ先のときは、拓本の場所が `ix = bx(aci) - bothAnchorPx(aci) * mp` と
  その線から逆算されているので、**文字を動かしたぶんだけ画素も同じだけずらして**
  逆算の答えを変えない。ずらす量は**画素にそろえてから mm に直す**（`dpx`）。
  ここで `syncCenters()` を呼ばないこと。2本目以降は動かさないのが目的で、
  呼ぶと 1回動かすごとに丸めのぶんだけ、じわじわずれていく。

- **校正のガイドラインの置き場所は `S.bothOff[g]` に留める**（v12.9）。
  `bothPack()` の `placeRun()` は毎回「文字のかたまりの幅」から置き場所を出していたので、
  **センターラインを 1本動かすと、親のセンターラインの文字も、ほかのガイドラインも動いた**
  （拓本に合わせる作業ができない）。いまは、はじめに決めた値を覚えてそこに留める。
  **枠の中での左右ずらし（`f.dx`）だけは、覚えた値に毎回足す**（あとから効かせたいので、
  `placeRun(list, from, fdx)` の第3引数）。ここを `at` に足し込むと、f.dx が
  覚えた値に焼き付いて効かなくなる。
  留めた値を捨てるのは、校正の［位置を戻す］（`#bRubHome`。拓本の場所もいっしょに戻す）と、
  **ガイドライン間 mm を変えたとき**（並べ方そのものを決める欄なので、捨てないと効かない）。

- **校正の拓本の場所は `S.rubX` に留める**（v12.8）。自動の置き場所は
  `pk.right`（文字のかたまりの幅）から出しているので、ガイドラインやセンターラインを
  動かすたびに拓本まで動いてしまい、**拓本に合わせる作業ができなかった**。
  いまは、はじめて描いたときの値を `S.rubX` に覚えて、以後そこに留める。
  捨てて置き直すのは校正の［拓本を右へ戻す］（`#bRubHome`）。別の拓本を読み込んだとき
  （`loadRubImageQuiet`）と［新規］でも null に戻す。**「重ねる」のときは留めない**
  （センターラインにぴったり合わせるのが目的なので）。

- **作字の共有フォルダーへの保存は 2通り**（v12.8）。`glyphLibPushOne(gi, ci, asNew)`。

      上書き保存（月1）  すでに付いている名前のものを書き換える
      新規登録           名前を外した写しを渡し、「月2」「月3」…として足す

  ボタンの出し分けは `peNameShow()`。名前が無いうちは［作字登録］だけを出し、
  名前が付いてから［上書き保存（月1）］と［新規登録］の 2つになる。
  形がまったく同じものが既にあるときは、どちらでも増えない（glyphLibPut の "same"）。

- **「この字を赤に」は単純なトグル**（v12.4・`toggleCharRed()`）。前は太さのしきい値
  （`thinMm`/`S.thinMm`）より細い輪郭だけを自動で赤に回していたが、その仕組みは廃止した。
  いまは押すたびに、その字の**輪郭ぜんぶ**を赤↔黒に切り替えるだけ。`redThinOne()` と
  `charMmPerUpem()`、しきい値の入力欄（`mThin`）、グループ一括（`mThinAll`）は削除した。

- **候補一覧は「作字＋異体字」をまとめて出す**（v12.7・`glibItems()`）。
  札は 3種類あり、`kind` で見分ける。

      "orig"  元の字（フォントのまま）      当てるもの：path を外す
      "path"  作字（この案件＋共有）        当てるもの：その path
      "vs"    異体字                        当てるもの：文字に異体字セレクタを足す
                                            （作字が乗っていると見えないので path も外す）

  当てる処理は `glibApply()` 1か所に集めてある。試し描き（ホバー）と決めるとき（クリック）で
  同じものを使うので、ここを直せば両方に効く。もとに戻すために `GLP.origPath` と
  `GLP.origCh` の 2つを覚えている（異体字は文字そのものを書き替えるため）。
  **共有フォルダーは自動では読まない**。右クリックのたびに通信すると重いので、
  ［🔄 読み込む］を押されたときだけ。絵は 26px（もとの半分）、名前だけで、
  どこにあるか・フォント名は出さない。

- **作字一覧（旧・別窓 `#glibPick`）は、いまは `#charMenu` の中の `#mLibBox`**（v12.5）。
  `openGlyphLib(gi, ci)` から cx/cy を外し、`glibRender()` は位置決めをせずに
  `#mLibBox` の中身を組むだけになった。GLP の仕組み（ホバーで試し描き→離すと戻す、
  押すと決める）はそのまま。決めたら異体字と同じく `closeCharMenu()` で窓ごと閉じる。
  `closeCharMenu()` は `GLP` が残っていれば `closeGlyphLib()` も呼ぶ（外側クリック・Escape対策）。
  外側クリックの監視は `#charMenu` 用の 1つだけに統合した（`#glibPick` 用は削除。
  残したままだと `#mLibBox` の中を押すたびに閉じてしまう）。

- **作字一覧を表示（B2）は元から同じ字だけ**。`glibCandidates(bare, gi, ci)` が
  `bareCh` で絞っている。「全部の一覧に見える」という指摘があったが、コードは
  最初から同じ字限定。紛らわしければ、開いたときの見出しに絞り込みの説明を足すとよい。

- **右クリックのメニュー（`#charMenu`）は「決定・取消」つき**（v12.3）。開いたときに
  `menuSnap = { adj: JSON.stringify(g.adj[ci]||{}), text: g.text }` を取っておき、
  **取消**はそこへ丸ごと戻す（異体字の変更もここで戻る）。フィールドの入力は今までどおり
  その場で `setAdj` → ライブ反映。**決定**はただ閉じるだけ（`persist()` のみ）。
  並びは 横/縦/連動 → 縦横そろえる → 既定の説明 → 左右移動/上下移動 → リセット →
  後ろの送り → 作字 → 細さ → 作字登録/元の字に戻す → 太さ/フォント → 異体字。
  「この文字の調整を消す」ボタンは削除（決定・取消に置き換え）。
  窓は開くたびに `WPOS.delete(el)` して位置をリセットしてから `winDrag()` を呼び直す
  （中身を毎回 `innerHTML` で作り直すため、.hd も毎回新しい要素になる）。

- **札の欄で `sel` を変えたら、太わくも手で動かす**（v12.2）。一覧を作り直せない
  （打っている欄が消える）ので、`.grow` の `on` クラスだけ付け替える。
  忘れると、太わくと下の編集がちがう文字グループを指す。

- **札の欄の blur では、すぐ `renderCols()` を呼ばない**（v12.1）。
  次に押した欄ごと作り直して消してしまうので、**2つめ以降の欄に入れなくなる**。
  `setTimeout(…,0)` で送らせ、行き先が `.gtxt` なら作り直さない。

- **連動のトグルは `.sw`**（v12.1）。`#pLink`（文字グループ）と `#mScLink`（右クリック）の 2か所。
  入っているかどうかは `on` クラス。文字は入れず、右の `.swlb` が「連動」の札。
  中身は今までどおり `S.linkWH` ひとつ。

- **ブロック間の寸法は 2周まわして測る**（v12.6）。①同じセンターライン（列）ごと、
  ②同じ 1行ガイドラインの中で列をまたいで。同じ組み合わせは `dimDone` で 1回だけ描く。
  ②だけにすると、間に別の列のブロックが挟まったとき同じ列どうしの寸法が消える。
  ①だけ（v12.5 まで）だと、センターラインを足して 1本に 1グループずつ置いたときに
  測る相手がいなくなり、寸法が 1本も出なかった。**どちらか片方にしない。**
  横に並んで上下がかさなるブロックは `dimV` が空を返すので、自然に飛ばされる。

- **校正の［寸法］が出すのは 3種類**（v12.0）。ぜんぶ `renderBoth()` の中の `dims`。

      文字ブロックどうしの縦の空き　　ブロックの中心に線。数字は右
      枠の上 → いちばん上の字　　　　枠の左はしの外に線。数字は左
      いちばん下の字 → 枠の下　　　　同上

  測るのは**墨の端**（`c.iy1`/`c.iy2`）で測る。枡で測ると見た目の空きと合わない。
  枠に入っている文字かどうかは `S.glFrame[glOf(ci)]` で見る。

- **窓を動かすのは `winDrag()`**（v11.9）。見出しの帯をつかむと `transform: translate` が付く。
  位置は `WPOS`（WeakMap）に持つだけで、保存はしない。帯の中のボタン・入力欄からは始めない。
  帯のダブルクリックでまん中へ戻る。窓を増やすときは `winDrag("#…の箱", ".…の帯")` を 1行足す。
  暗幕（`#pathEd` / `#glibWin` の background）は**うすい**。動かして下の原稿を見るためなので、濃くしない。

- **見出し `h3.sec` は帯**（v11.9）。ほかの字と同じ見た目に戻さない（見分けがつかないという指摘があった）。

- **作字の画面には 2つの入り方がある**（v11.7）。見分けは `PE.lib`。

      PE.lib が null　　案件の字を直す（これまでどおり）。［決定］で S.groups へ
      PE.lib が数字　　 共有フォルダーの作字を直す（openLibEditor）。
                       ［決定］で FDIR.lib.items[PE.lib].path へ書き戻し、そのまま SP へ保存

  `PE.gi` / `PE.ci` は共有のときは null なので、`S.groups[PE.gi]` を新しく足すときは必ず
  ぶんきさせる（いまは peTotalPct・peNameShow・peCommit・peShare の4か所）。
  `PE.base` は「開いたときの形」。空にすると `JSON.parse` で落ちるので、必ず入れる。

- **作字の一覧は別ウインドウ `#glibWin`**（v11.8）。中身は `glwRender()` が毎回組み直す。
  `data-i` は `FDIR.lib.items` の番号そのものなので、**しぼりこんでも番号はずれない**。
  消したときは、開いていれば `glwRender()` も呼ぶこと（一覧が古いままになる）。

- **共有フォルダーの作字は、いまは 1字ずつしか入れられない**（v11.7）。
  入り口は作字の画面の［この字を共有フォルダーに保存］と、字の右クリックメニューだけ
  （`glyphLibPushOne`）。まとめて入れる `glyphLibPush` と、まとめて当てはめる
  `glyphLibApply` は、分かりにくいという理由で削除した。

- **縦書きで倒す字は `V_ROT`**（v11.4）。かっこ・棒・点々のたぐいは、縦書き用の別の字が
  あるのではなく、**同じ字を 90度倒したものが正しい形**。`vRun()` が、その字の枠のまん中を
  軸に `rotate(90 …)` を掛けている。倒す字を増やすときは `V_ROT` に足す。
  （、。は倒さない。本来は右上に寄せるが、いまは動かしていない）

- **申し送りを縁の内側に押し込んではいけない**（v11.4 → v11.5 で撤回）。
  `noteGeom()` が切れない縁の中に押し込むと、**縁のぶんの余白へ動かせなくなる**
  （下に白いところが見えているのに動かない、という形で出る）。
  いまは **紙からはみ出さないところまで**しか収めない。
  縁にかかっているかどうかの知らせも出さない（v11.6・本人の指示。見れば分かるため）。
  縦書きの切り替えは、入れ物の高さが 3倍ほど変わるので **下端をそのままにして**動かす
  （`nVert` の change）。上端をそのままにすると、切り替えたとたん下へはみ出す。

- **申し送りの文面は、いつも備考メモからの自動（v11.3）**。`noteLines()` は `infoLines()` を
  返すだけになった。古い案件ファイルの `note.text` は読み込みのところで捨てている。
  置き場所（`note.x` / `note.y`）は残っていて、ゴムシートの画面でつかんで動かす。
  欄で打つ手だては無くしたので、直すなら `noteGeom()` を見る（横書き・縦書きの両方をここで出す）。

- **訂正マークは画面の中だけ**（v11.3）。`S.info.src` が納骨リストの値のひかえ。
  `infoFixed()` がちがう項目を返し、`infoFixShow()` が ✎訂正 の印と帯を出す。
  **ゴムシートには出さない**（本人の指示）。`src` が無い＝手入力だけの案件では、印も帯も出ない。

- **縦書き 2行は、右の列が 1列目**（v11.3）。割り方は `noteCols()`：
  行が 2本以上なら前半・後半で分ける（1列目＝納骨日・家名／2列目＝場所・その他）。
  縦に長すぎるときは `noteGeom()` が切れない縁に収まるまで文字を小さくする。

- **作字の名前は自動（v11.2）**。共有フォルダーの目印は `glyphKey(字, フォント, 名前)`。
  入れ口は `glyphLibPut()` 1つに集めてあるので、直すならそこだけ見る。

      すでに名前を持っている　→ その名前のものを書き換え（"upd"）
      形がまったく同じものがある → 何もしない。その名前を使う（"same"）
      どちらでもない　　　　　→ 「月1」「月2」…を付けて足す（"add"）

  名前は `path.name` にも書き戻すので、案件の側にも残る。`peCommit()` は
  **古い `path.name` を引き継ぐ**（形を直しても「月2」のまま）。ここを消すと名前が飛ぶ。
  v11.2 より前の名前なしデータは `glyphLibFixNames()` が読んだところで番号を振る。
  ［共有の作字を当てはめる］は、同じ字が何個もあるときは**いちばん若い番号**を入れる。

- **札（`.grow`）の中に入力欄を置くときは、行のクリックを止める**（v11.1）。
  `.grow` の `onclick` は `renderAll()` を呼ぶので、欄を押した瞬間に欄ごと作り直されて
  字が打てなくなる。`e.target.closest("input,button,select,textarea")` なら何もしない、で回避。
  あわせて、欄にフォーカスがある間は `row.draggable = false`（でないと字をなぞって選べない）。

- **出力の色は `printMode` の4通り**（v11.0）。

      "red"   赤だけ #FF0000     ［① 赤を出力］（プロッターのペン）
      "blk"   黒だけ #000000     ［② 黒を出力］（プロッターの刃）
      "all"   赤も黒も **ぜんぶ黒**（いまはどこからも呼んでいない。1色でよいときの残し）
      "allc"  赤は赤・黒は黒     ［SVG］（ほかのソフトで色で選び分けるため）

  `buildSVG()` の `allIn`（"all" と "allc"）で「両方入れるか」、`redG` の色で「赤にするか」を
  決めている。この2つは別の判断なので、片方だけ直すと黒1色に戻る。

- **［試し切り］は原稿とは無関係**（v10.9）。`testCutSVG()` が決まったかたちだけの SVG を作り、`#stage`
  に入れて `window.print()`、そのあと `renderStage()` でもとに戻す。`buildSVG()` も `printMode` も通らない。

      出るかたち　コの字 2mm ×10 ／ 丸 3mm ×5 ／ 四角 2・5・10mm ／ 1mm はばの線 ×3 ／ 直線・ななめ線
      紙の大きさ　いまのゴムシートのまま（プロッター側の用紙設定を作り直さないで済むように）
      線　ぜんぶ黒・fill なし・太さ PEN_MM（0.12mm）。`plotOff()` のずらしも効く
      **文字（ラベル）は入れない**。カッターだと、その字まで切ってしまうため

- **作字は「モード」と「操作」に分ける**（v10.8）。

      モード（押すと続く。もう一度押すと［選ぶ・動かす］に戻る）
          選ぶ・動かす／点を足す／点を消す／丸を置く／輪郭を消す／線で切る
          ※ 丸を置くだけは **1つ置くと自動で［選ぶ・動かす］に戻る**（置いた丸は選んだまま）
      操作（押すと 1回だけ）
          選んだ点を消す／選んだ輪郭を消す／赤・黒／重なりを1つにする／1つ戻す／元の字に戻す

  いまのモードは `peModeShow()` が 3つで見せる ――
  **札**（`#peModeTag`。`.pebody` の中に置く。`#peStage` の中に入れると
  `peRender()` の `innerHTML` で消える）、**ポインタ**（`#peStage` の `m-<tool>` クラス。
  × とハサミは SVG を data URI で貼っている）、**点の形**（消すモードは赤い ×）
- **文字を打つ所は 2つだけ**（v10.5）。行の構成の**札の中**（`.gtxt`）と、**まとめて入力**。
  下にあった「文字」の欄（`pText`）と［数字を漢数字に］（`pKanji`）は消した。
  札は掴んで動かせる（`draggable`）ので、中の入力欄には `draggable="false"` が要る。
  打っている間は `renderCols()` を呼ばない ―― 呼ぶと欄ごと作り直されて打てなくなる
  （直すのは `renderProps` `renderStage` `renderBoth` だけ。作り直しは blur のとき）
- **横と縦の連動（`linkWH`）の既定は「入」**（v10.5）。`S.linkWH` が未設定なら true。
  古い案件で false を入れてあるものは、その値のまま

- **センターラインの左右の決め方は、1本目と 2本目からで別**（v10.2。ここは何度も作り直した所）。

      センター 1-1（行のはじまり）
          枠の［1行目の位置］のふうせん（`frx:`）で決める。**自分のふうせんは持たない**
      センター 1-2 以降
          拓本があるとき … 拓本の上の線に合わせた**離れぐあいがそのまま入る**
                            （`syncCenters()`。1行ガイドラインごとに 1本目を基準にする）
          拓本が無いとき … 校正のふうせん（`cenx:<ci>`）か「左右 mm」（`.cx`）で手で合わせる

  だから `cenx:` のふうせんは、**2本目から** かつ **拓本で決まっていないとき**だけ出す。
  ぜったいに自動でそろえない（v9.9〜v10.0 の `packCentersInGl` は取りやめ）。
  `addCenterLine` は、いちばん左の線のすぐ左（重ならない所）に置くだけ。
  校正では、**文字がまだ入っていない線**にも線とふうせんを出す
  （上下は同じガイドラインの範囲、左右は同じガイドラインのずれ量を借りる。`bx`）。
- **拓本の上のセンターラインも、上下は文字の側にそろえる**（v10.3）。
  線は `colBox[ci]`（無ければ同じガイドラインの範囲 `glY`）のぶんだけ引き、
  つまみはその **すぐ上**（`ry1 − KN*1.5`）に置く。
  以前は切り抜き前の画像の上端（`iy`）を基準にしていたので、
  トリミングすると絵から離れた所につまみが出ていた
  行そのものの左右は、枠の［1行目の位置］のふうせん（`frx:`）で動かす。
  ※ v9.8 で「左右 mm」の欄と `cenx:` のつまみを付けたが、
  ふうせんと役目が重なるうえ、幅の外へ出せてしまうので取りやめた

- **左メニューの木が、選ぶ場所のすべて**（v9.4）。メインメニューに
  「拓本解析」「文字編集」「枠」のボタンは無い。`#mmGl` の中に

      枠 石塔 120×260      … `.frhd[data-f]`。押すと `selFrame(fi)`
        ガイドライン 1      … `.mmsub[data-k]`。押すと `selGl(k)`
        拓本               … `.mmsub.rubrow`。押すと `selRub()`
      枠の外               … `.frhd.out[data-f="-1"]`。押せないが、落とせる
      行追加 ＋ / 枠追加 ＋

  行も拓本も**枠の札へドラッグ**すると、その枠に入る（`frameAssign` / `rubToFrame`）。
  `frameSel` が選んでいる枠。枠のパネルは**その 1つだけ**を出す
  （以前は全部の枠のカードを縦に並べていた）
- **`setSub(null)` でサブメニューをぜんぶ閉じる**（v9.4）。枠を消したときなど、
  選んでいたものが無くなったら閉じる
- **枠のパネルから消したもの**（v9.4）：［＋ 枠を追加］（左メニューの［枠追加］へ）、
  「行をどの枠に入れるか」（各ガイドラインの「入れる枠」へ）、
  「拓本の置き場所」（拓本解析 ① の「入れる枠」へ）。
  置き場所を決める道は **拓本解析のえらび** と **左メニューのドラッグ** の 2つで、
  どちらも `rubToFrame(fi)` を通り、そのなかで `rubPanel()` も呼ぶので表示は一致する
  （v9.5 で枠側にもレ点を置いたが、同じことが 3か所になるので v9.6 で外した）

- **左メニューの［行追加］［枠追加］は同じ吹き出し（`#addMenu`）を使い回す**（v9.3）。
  中身を作るのは `openAddMenu()`（行）と `openFrameMenu()`（枠）。
  枠を足す道は `addFrameNew()` `copyFrame(i)` `frtUse(i, true)` の 3つで、
  枠パネルの［＋ 枠を追加］も `addFrameNew()` を呼ぶ（作りが 2か所に分かれないように）

- **拓本解析は「パネル＝結果」「拓本の画面＝操作」に分けてある**（v9.1）。

      パネル（#panRub）  ① 拓本データ … 取り込み・削除・ファイル名
                         ② 補正結果   … [✎ 補正する] と、反転・角度・トリミング・
                                         解像度・実測長・歪み・縮尺の一覧（表示のみ）
      拓本の画面（帯）   [↻ 180° 反転] ／ [◎ 回して合わせる]
                         ／ [▣ トリミング] ／ [全体に戻す] ／ [終了]

  拓本の画面は**拓本を直すためだけ**の画面（v9.2）。`rubRender()` の `showG` は
  `false` 固定で、センターラインも文字グループの上下線も出さない
  （`cen:` `grp:` のつまみもここには無い。文字との見比べは校正の画面で）。
  ガイドのレ点は帯から外し、`#rShowGuides` は隠したまま残してある

  帯は `.forRub`（`view === "rub"` のときだけ出る）。状態の色付けは `rubBar()`。
  ふうせん（`#rubOv`）はもう使わない（`rubOverlay()` は隠すだけ）
- **実測した文字全長の欄（`#rLen`）は `#rubwrap` の直下にひとつだけ置く**（v9.1）。
  `#rubstage` の中に入れると、絵を描き直すたびに作り直されて**打っている途中で消える**。
  `rubRender()` は場所（脚の線の下）と値を直すだけ。つなぐのも 1回だけ（`_bound`）
- **モードを抜けても拓本の画面に居残る**（v9.1）。校正へ戻るのは［終了］（`endFix`）だけ。
  `endRot` `endCrop` は画面を切り替えない

- **`bothRubX` は「ずらしたあと」の左はしを入れる**（v9.0）。これは拓本の画素 0 が
  校正のどこに来るかで、**ガイド線を引くのにも、つまみをつかむのにも**使う。
  `S.rubDx` を足す前に覚えていたので、ずらした分だけ食い違っていた。
- **［拓本を重ねる］（`ov`）のときは `S.rubDx` を足さない**（v9.0）。
  重ねるのはセンターラインにぴったり合わせるためのものなので、
  そこへ手のずらしを足すと合わなくなる。解除すればずらしは戻る

- **確認資料の拓本の置き場所は、校正と同じ式で出す**（v8.9）。

      枠の中 … その枠の中で、見えている範囲を真ん中に
      枠の外 … 文字のかたまりの右（`pkD.right + max(bothGap(),10)`）へ
      そのあと S.rubDx（校正のずらし）＋ S.demoRx（確認資料だけのずらし）を足す

  v7.7 で「確認資料は校正と同じ並びに」と直したとき、**文字側だけ** `bothPack` に
  そろえて、拓本の x は昔の式（`rubAnchor()` でセンターラインに合わせる）のままだった。
  そのため校正で決めた拓本と文字の位置関係が確認資料に出ていなかった

- **拓本の置き場所・広さは「見えている範囲」で計る**（v8.8）。
  校正は `rvL/rvT/rvW/rvH`、確認資料は `cx1..cy2`。`RUB.W`・`RUB.H` で計ると、
  **トリミングで切り落とした分まで場所を取り**、枠の上や横に白い空きができる。
  絵そのものを置く `ix`・`iy` は切る前の左上のままなので、
  `ix` を出すときは切った分（`cb.x1*mp`）を引いておくこと
- **校正の拓本は左右に動かせる**（v8.8。`S.rubDx`）。右はしから上へ伸びた線の
  **緑のふうせん**（`data-k="rubx:0"`）をつかむ。枠の `frx` と同じ作り。
  校正の見え方だけで、ゴムシートの出力・確認資料（あちらは `S.demoRx`）には効かない

- **トリミングは「拓本解析」で決める**（v8.5）。`cropMode` が真のあいだ、
  拓本の画面（`view === "rub"`）に赤い四角と 4辺のつまみを出す。
  終わると校正へ戻る（回転の `rotMode` と同じ作り）。
  範囲 `S.rub.crop` は**画像の画素**で持ち、`rubFlip180()` が裏返すので、
  ほかの目印（頭・脚・帯・センターライン）と同じ扱いになる
- **トリミングは校正と確認資料の両方に効く**（校正は v8.6 から。`bclip` / `dclip`）
- **切り抜き（`clip-path`）は、回す `<g>` の外側に付ける**（v8.5）。
  内側（`<image>` 自身）に付けると、切り抜きの四角も一緒に回ってしまい、
  **180°反転したときに、見えている赤枠と実際に切れる所が上下逆**になる。
  これが「反転してからトリミングすると動作がおかしい」の正体だった

- **枠の縦で、人が決めるのは「下げ」だけ**（v8.7）。上端 `f.y` は入力欄を持たず、
  `framesSyncY()` が計算で出す。**枠の上端の欄を足したくなったら、まずこれを読むこと。**

      拓本があるとき … 行の上端 ＝ 拓本の頭。   だから f.y ＝ 拓本の頭 − f.top
      拓本が無いとき … 行の上端 ＝ 行そのもの。 だから f.y ＝ 行の上端 − f.top

  `framesSyncY()` は `renderAll` `renderBoth` `renderDemo` `renderFrames` の頭で毎回通る
  （`framesFitRub()` は同じものの別名。呼んでいる所が多いので残してある）。
  下げを直したときは、拓本があれば**枠が動き**（文字は頭のまま）、
  無ければ**文字が動く**（枠はそのまま）。
  ※ ここは 3回作り直している。v7.2〜v8.3 は枠が固定で、拓本と永久に合わなかった。
  v8.4〜v8.5 は上端を直すと下げが入れ替わり、枠と文字の関係が変わってしまった。
  v8.6 は上端が中身ごと動いたが、そもそも上端は下げから出るので**欄が要らなかった**
- **校正の枠のつまみ（`frx:`）は左右と上下の両方**（v8.4）。
  左右＝1行目の位置（`f.dx`）、上下＝枠そのもの（`frameSetY`）

- **［画（丸）を足す］は「クリックした所に丸ができる」**（v10.7）。
  押した所を中心に、決まった大きさ（upem の 15%）の丸を **その場で作る**。
  そのまま引くと、押した所を中心に大きさが変わる（Shift でまん丸）。
  それまでは **8px 以上斜めにドラッグしないと何も出ず**、
  「ボタンは黒くなるのに何も起きない」と見えていた。
- **形を足す道具は、引いても入れたまま**（v10.6）。1つ引くごとに［選ぶ・動かす］へ
  戻していたので、続けて 2つめが引けず「押しても効かない」ように見えていた。
  終わりは、同じボタンをもう一度押す（`.pt` のボタンは押すたびに入・切が変わる）。
- **作字で形をひとつ足す道具は 丸だけ**（v8.2 で追加、v8.3 で四角をやめた）。
  `peDrawTool()` が真のあいだは、点や輪郭の上を押しても台紙側で受けて、新しい形を引ける。
  丸は `ovalContour()`。節点は 左・上・右・下 の 4つで、ハンドルの長さは半径の
  **0.5523 倍**（3次ベジエでいちばん円に近くなる長さ）。
  向きは四角と同じ**時計回り**にそろえる ―― `unionContours` は回る向きで
  穴かどうかを決めるので、逆にすると穴になってしまう

- **`spGetBlob` の大きさ確認は「足りないとき」だけ弾く**（v8.1）。
  `expect`（SharePoint が持っているファイルの大きさ）と**ぴったり同じか**を見ていたので、
  受け取った中身のほうが大きいと「途中で切れました」になり、
  **5回とも取り直して**いつまでも「読み込み中」に見えていた。
  途中の通信で圧縮されて届くと、開いたあとの大きさは置いてあるものと合わないことがある。
  案件ファイル（JSON）はよく圧縮されるので、ここに引っかかりやすい。
  いまは `b.size < expect` のときだけ取り直す
- **自動保存は「この端末のブラウザ」だけ**。`persist()` が
  `localStorage["chokoku.state"]`（拓本の絵は IndexedDB）へ毎回書く。
  **SharePoint の案件ファイルは［案件を保存］を押したときだけ**書き込む

- **画面のタブは ゴムシート／構成／確認資料 の 3つ**。拓本の画面（`view === "rub"`）は
  消していないが、タブには出さない（`#viewRub` に `hidden`）。
  **拓本をつかんで回すときだけ**［マウスで回す］が一時的に開き、［終了］で構成へ戻る。
  前回 `rub` で終わっていても、開くときは構成にする（`restore()`）
- **文字編集の並びは 行の構成 → 文字グループ一覧 → その字の中身** の順（v4.6）。
  行の構成（`#clist`）は**左のメニューで選んでいるガイドライン 1本だけ**を出す
  （`curGlKey()` で絞る）。ほかの行は左のメニューで選び直す。
  「センターライン未設定」の箱だけは、出している行に関係なく全部のグループを見て決める
- **古いテンプレート・案件はセンターラインに `gl` を持っていないことがある**。
  そのままだと `glOf` が 0 を返し、**ぜんぶが「1行ガイドライン 1」にまとまる**（v4.7 で修正）。
  `fillGl()` で並び順の番号を入れる。`tplApply` `tplAdd` `migrate` の 3か所で通す
- **消したもの**（v4.7）：文字グループの一覧カード（`#glist`。行の構成の中に同じものがある）、
  ［＋ ガイドライン追加］のボタン（左メニューの［行追加］→ 新規 に集約。
  はたらきは `addGuideline()`）、「いま 横…×縦… 字送り…」の説明文
- **構成の 頭・脚は `calTop`/`calBot`（拓本の画素の行）を決める**（v5.6／v5.7）。
  つまみは `cal:top` / `cal:bot`。つかんでいる間は `bothImgHold` で
  **拓本の絵の置き場所（iy・mp）を止める**ので、印だけが動く。
  **離したあとも同じ置き場所のままにする**（v5.7）。そのために
  `realLen = mp × (calBot − calTop)`、`originY = iy + calTop × mp` を入れ直す。
  こうしないと、離したとたんに較正がかかって絵が跳ね、
  合わせ直す→また跳ぶ、で**終わらなくなる**（v5.6 の不具合）。
  絵の大きさが変わるのは、**実測長の欄に数字を打ち込んだときだけ**。
  文字の行（`groups[].topY/bottomY`）はどちらでも動かない。
  ※ v4.0〜v5.5 は 頭＝`originY`、脚＝`realLen` を直接動かしていた。
  これだと拓本ぜんぶが動いてしまい、新規の較正に使えなかった
- **上のバーの［SVG］** は、**ぜんぶ黒の SVG を 1枚**落とす（`outSVG("all")`。v6.2）。
  プロッターへ渡すのは印刷（赤・黒）のほうなので、SVG は色で分けない。
  `printMode === "all"` は赤の組も黒のペン線で書く
- **細い所をペン（赤）に回す**（v6.2。案B）。`redThinOne(gi, ci, mm)`。
  輪郭ごとに `measureGlyph([c]).ink`（画線幅の中央値）を測り、
  `ink × その字の mm/upem` がしきい値（`S.thinMm`、既定 0.8mm）に足りないものを
  `path.red` に入れる。作字でない字は、そこではじめて作字にする。
  細い所が無ければ**作字にしない**（あとで字を差し替えられなくなるため）。
  mm/upem は縦横の**細いほう**で見る（安全側）。
  1文字修正のメニューの［この字を赤に］／［この文字グループぜんぶ調べる］
- **新しい行は拓本の 頭 のラインからはじめる**（v6.2）。`snapToRubTop(list)`。
  テンプレート呼び出し・テンプレート追加・行追加→新規 の 3か所。
  **いちばん上をそろえて、まとめて動かす**ので、行どうしの上下の関係は変わらない。
  拓本が無い・較正していないときは何もしない（`rubTopMm()` が null）
- **「文字の位置」は上のバーの［✥ 文字の位置］だけ**（v6.2 でゴムシートの
  サブメニューから削除。同じものが 2か所にあると迷う）。`#btnSheetMove` は無い
- **縦横の連動**（v5.8）。`S.linkWH`。入れておくと、横を変えると縦も、
  縦を変えると横も、同じ割合で変わる。文字グループの大きさ（`pW`/`pH`）と
  1文字修正（`mScW`/`mScH`）の両方で同じ覚え書きを見る。
  比は**打ち始めたところで覚える**（`pSeed()` / `mSeed()`）。
  打っている途中の値で取り直すと、少しずつ形がくずれる
- **1文字ごとの上下ずれ `adj[i].dy`**（v5.8。下が＋）。左右の `dx` と同じ扱いで、
  `cellTop` に足す。**ゴムシート・出力・構成・確認資料の 4か所**と、
  作字で拓本を下敷きにする `peRubFit()` に入れること。
  `hasAdj()` にも足さないと、一文字ごとの調整の一覧に出てこない
  （ついでに `peRubFit()` が `dx` を見ていなかったのも直した）
- **画面のタブは ゴムシート／校正／確認資料**（v6.0 で「構成」→「校正」に改名）。
  中の名前（`view === "both"`、`renderBoth`、`bothPack` など）はそのまま
- **校正の［寸法］**（v5.8〜v6.1）。`S.bothDim`。**縦の空きだけ**を出す。
  文字ブロック（＝文字グループ）の**墨の端**で測る
  （枡で測ると見た目の空きと合わない。`cells[].ix1…iy2`）。
  ※ 横の間は出さない（v6.1 で削除）。ガイドラインの位置も間隔も数値で
  決めているので、画面で測らなくても分かるため。
  横をやろうとして 2回しくじっている。v5.8 は詰めた並びの上に実寸の数字だけを
  書いた（矢印 6mm に「実 30.3」）。v5.9 は `bothPack()` を詰めないようにした
  （**ガイドラインのシート位置は重なっていることがあるので、行が重なって
  編集できなくなる**）。v6.0 は下に帯を作ったが、そもそも要らなかった
- **まとめて入力**（v7.8／v7.9。`bulkApply()` / `bulkFill()` / `bulkGis()`）。
  文字編集の上のテキスト欄に 1行ずつ書いて、**いま選んでいる 1行ガイドラインの中の
  文字グループ ①②③…** へ上から入れる（v7.9）。**ガイドラインは増やさない**。
  足りない文字グループは足し（`addGroupTo`）、多いぶんは文字を空にするだけ。
  ［数字を漢数字に］（`kanjifyDigits`）も欄の中で使える。
  ※ v7.8 は 1行＝1ガイドラインに入れていた
- **新しい文字グループの大きさは 20mm**（v6.5。`NEW_MM`。`mkGroup()` が `hMm` に入れる）。
  入れないと「上端〜下端にちょうど収まる大きさ」になり、行ごとにばらつく。
  **古い案件は `hMm` を持っているか 0 なので、見え方は変わらない**
- **確認資料の並びは校正と同じ**（v7.7）。`renderDemo()` も `bothPack()` の位置を使う
  （枠・行・拓本の位置関係をそろえる。拓本は枠に入れていればその枠の真ん中、
  枠を使っていなければ、これまでどおりセンターラインに合わせる）。
  ずらしは〔位置調整〕のレ点が入っているときだけ（`#dAdj`。既定は切）。
  **拓本の置き場所に `demoDx`（文字側のずらし）を混ぜないこと**（v8.0）。
  混ぜると文字と拓本が一緒に動き、いくらずらしても重なり方が変わらない
  ＝「位置調整が利かない」ように見える。拓本は `demoXBase()`（ずらし無し）＋ `demoRx`。
  拓本そのものも、位置調整のときだけつかんで動かせる（`.rubhit` / `data-rubdrag`。
  文字の面より先に置いて、文字を優先させる）。
  〔元に戻す〕（`#dReset`）で `demoDx`/`demoRx` を消して、校正と同じ並びへ。
  枠の名前も出す
- **確認資料を出す条件**（v7.6。`renderDemo()`）。
  枠が無ければ、これまでどおり**拓本があるときだけ**。
  枠があれば**枠と文字を出す**（拓本が無くてもよい）。
  枠と拓本がそろっているときは、**拓本を「枠の中」に置いているときだけ**拓本も出す
  （`S.rubFrame >= 0`。枠の外の拓本は、校正で横に並べている見本なので出さない）。
  確認資料の枠は、その枠に入れた行のまわりに置く（枠はシートの左右の位置を持たないため）
- **校正での行の並びは `S.glSeq`**（v7.5。右から左＝縦書きの読み順。`glSeq()`）。
  `bothPack()` も左メニューも、これで並べる。**シートの x は見ない／動かさない**。
  ※ v7.4 までは並べ替えで `S.centers[].x` を動かし、x の順で並べていた。
  ところが行はどれもシートの真ん中に置くのがふつうで、**x が同じだと
  「間隔を保ったまま置き直す」計算が成り立たず、何も動かなかった**
  （端の行だけ動いて見えるのはそのため）。ゴムシートに載せる位置は
  ✥ 文字の位置（`glOff`）で決めるものなので、並べ替えとは切り離した
- **枠 ＝ 石の彫刻できる面**（v6.4／v6.5）。`S.frames[] = {name,w,h,y}`、
  `S.glFrame[ガイドライン番号] = 枠の番号`、`S.rubFrame`（拓本を入れる枠。null＝枠の外）。
  **校正の画面にだけ出す**。ゴムシート・出力・確認資料には出さない
  （＝ 石の面であって、紙やゴムシートの枠ではないため）。
  並べ方は `bothPack()` の中。枠ごとに、その枠の行を 6mm 詰めで並べ、枠の幅の中で
  中央に置く。枠に入れていない行は、これまでどおり右へ続ける。
  中身が枠より広いときは、枠の幅ぶんを赤い線で重ねて知らせる。
  枠がひとつも無ければ `frames` が空なので、並べ方は前と同じになる。
  **枠だけ作った段階でも校正に出す**（v7.0）。文字も拓本も無いときの
  「文字を入れるか…」の案内は、枠もひとつも無いときだけ出す。
  **行を別の枠の行の所へ落とすと、その枠に入る**（v7.4。`moveGlTo()`。校正でも
  左メニューでも同じ）。枠が違うまま左右だけ入れ替えても、校正は枠ごとに並べるので
  見た目が変わらず「動いていない」ように見えるため。
  枠を選ぶところは 3か所（すべて同じ `S.glFrame` を見る）：左メニューの「枠」、
  文字編集の行の構成の見出し（`clFr_<gl>`）、そして左メニューのガイドライン一覧は
  **枠ごとにまとめて出す**（`renderMenuGl()` の `.frhd`）。
  上下は **`f.y`（枠の上端。v7.2 から固定・欄なし）と `f.top`（文字のはじまり。
  上端からの下げ。負なら枠より上）の 2つ**。
  行をそろえる先は `frameTextTop() = y + top`。`y` を動かすと**枠も行もいっしょに**動き、
  `top` を動かすと**行だけ**が枠の中で下がる。
  ※ v6.6 は `y` ひとつだったので、文字がいつも枠の上端にべったり付き、
  数字を変えても枠と行が同時に動くため「効いていない」ように見えた。
  行の上端は **枠（`y`＋`top`）と 拓本の頭（`rubTopMm()`）の 2つに合わせる**ことになる。
  枠のカードに、いまの行の上端と拓本の頭の差を出し、
  ［拓本の頭に合わせる］（`.frfit`）でも直せる（v6.9）。
  **拓本があるときは `framesFitRub()` が自動でそろえる**（v7.2）――
  取り込んだとき・頭を動かしたとき・実測長を変えたとき・枠を足したとき。
  枠の上端は動かさず、`top` だけで合わせる（`top` が負＝面から上にはみ出す、が見える）。
  枠の名前は**枠の外（上）**に出す（中だと校正の文字とかぶる。つまみ用の余白 `FRTOP` に載る）。
  左メニューの一覧は**行が 1本でも、枠だけでも出す**（v7.2）。
  並びは**シートの左右の位置に合わせ、いちばん上が右の行**（v7.3。縦書きなので
  右が 1行目）。コピーの行も位置の順（v7.4。札で分かるため）。※ v7.2 までは gl の番号順に出していたので、
  並べ替えても一覧が変わらず「動いていない」ように見えた（中身は動いていた）。
  **1行目の寄せ（中央／右から／左から）と 左右のずらし mm**（`f.ax` / `f.dx`）で、
  枠の中のどこから並べるかを決める。`dx` は校正の画面でも動かせる ――
  枠の中の行の**右はしの線を枠の外（上）まで伸ばし、その先の丸いつまみ**
  （`data-k="frx:<枠の番号>"`）をつかんで左右へ。つまみの分の余白は `FRTOP`。
  枠の名前は**枠の中**に出す（外に出すと画面のふちで切れる）。
  **枠テンプレート**（v6.7。置き場所は v7.0 で「テンプレート」のメニューの中へ）は、
  枠と、**その枠に入れてある行（センターライン・文字グループ）も**取っておく（v7.8）。
  中身は `data:{centers,groups,frames,glFrame}` ＝ふつうのテンプレートと同じ形にして、
  呼び出しは `tplApply()` / `tplAdd()` に通す（同じ道を使う）。
  そのあと `setSub("tpl")` でテンプレートの画面に戻すこと（tplApply は文字編集へ切り替える）。
  共有フォルダーの `枠テンプレート.json`（`FRT` / `FDIR.frtId`）。作りは
  テンプレート（`TPL`）と同じで、置き場所のファイルだけ別。〔置換〕〔追加〕〔×〕。
  枠は案件のテンプレートにも入る（`data.frames` / `data.glFrame`）。
  ただし **`tplApply()` は、テンプレートが枠を持っているときだけ入れかえる**（v7.1）。
  枠を先に作ってから文字だけのテンプレートを呼ぶ使い方で、枠が消えてしまうため。
  〔置換〕は入れかえ、〔追加〕は後ろに足して割り当ての番号をずらす
- **本体から受け取ったフォルダーは、無ければ作る**（v6.3。`spEnsurePath()`）。
  上から順に GET → 404 なら POST で作る（409 は開き直す）。403 はそのまま知らせる。
  工事関連から開くと `Folder_Pass/工事仕様/彫刻資料` を指してくるので、これが要る
- **説明文は `<details class="help">` で畳む**（v6.1）。ゴムシートのサブメニューは
  入力欄より説明のほうが長く、読みづらかった。増やすときも畳むこと
- **作字の輪郭ごとに「刃で切る（黒）／ペンで書く（赤）」を選べる**（v5.5）。
  `adj[i].path.red = [輪郭の番号…]`。作字の画面で輪郭を選び［赤にする］。
  `charPathSplit()` が黒と赤に振り分ける。**ゴムシートと出力だけ**分け、
  **構成と確認資料は全部まとめて黒**（`charPathD` のまま）。
  `path` ごと共有フォルダーの作字一覧に入るので、フォントに紐づいて残る。
  輪郭を消したら `peRedDrop()` で番号をずらす。
  ［重なりを1つにする］は番号が変わるので赤を外す
- **構成で拓本のガイドラインを動かしても、`syncCenters()` は呼ばない**（v5.4）。
  拓本の線は「拓本の見本の行」を指しているだけ。呼ぶと `S.centers[].x` が
  拓本から決め直されて、構成した文字の行まで動いてしまう。
  文字の行を動かすのは、名前の帯をつかんだ並べ替え（`reorderGl()`）だけ
- **`copyGuideline()` は「いまある行ぜんぶの外」に置く**（v5.4）。
  「コピー元の隣」に置いていたので、同じ行を 2回コピーすると**同じ場所に重なり**、
  並べ替えても動かないように見えていた
- **共有フォルダーから落とすものは、すべて大きさ（`expect`）を渡す**（v5.4）。
  渡さないと `spGetBlob` が「途中で切れた」を見つけられず、
  欠けた中身をそのまま読もうとして止まる（作字の一覧で起きていた）。
  作字（`FDIR.libSize`）・テンプレート（`FDIR.tplSize`）・案件・拓本・フォントの 5か所
- **確認資料の JPEG は、既定で工事フォルダー**（`spNeedFolder()` → `spPutIn()`）。
  つながっていないときと、しくじったときは、このパソコンに落とす。
  ボタンの右の ⤓（`dSaveLocal`）はいつでもこのパソコン
- **ファイル名は保存先で分ける**。工事フォルダーは `demoJPEGName()`＝
  「宇田川-確認資料-20260831-2330.jpg」のように漢字のまま。
  このパソコンは `demoJPEGNameLocal()`＝`safeName()` の英数字。
  **Chromium は `a.download` の漢字の名前を落として「download」にしてしまう**ため
- **［全体に戻す］［位置を戻す］のボタンは無い**（v5.3 で削除）。
  切り取りを全体に戻すのは、**トリミングのレ点を外す**（外している間は切らずに出す）
- **1行ガイドラインは、文字グループが 1つも無いと画面に出てこない**。
  左のメニュー（`glKeys()`）も行の構成（`curGlKey()` で絞る）も、
  文字グループから作っているため。だから `addGuideline()` は
  **空の文字グループを 1つ一緒に作る**（v5.2 で修正。それまで「新規」は
  何も起きないように見えていた）
- **コピーの行も、行の構成にふつうに出す**（v5.2）。それまでは
  「◯ のコピー」の札だけ出して打ち切っていたので、消すことも、
  文字グループやセンターラインを足すこともできなかった。
  コピーで紐づいた文字グループも `vis` に入れて並べる（カードに「コピー」と出る）
- **ゴムシートの「出す行」のレ点は `S.sheet.glHide`**（`glShown()` / `renderSheetGl()`）。
  外した行は `buildSVG` で字・線・つかむ面のどれも作らないので、
  **下見にも出力（赤・黒）にも出ない**。拓本・構成・確認資料には効かない。
  ガイドラインが 2本以上あるときだけツールバーに出る
- **一文字ごとの左右ずれは `adj[i].dx`**（mm。右が＋）。字を描く 3か所
  （`buildSVG` `renderDemo` `renderBoth`）で `cx + g.dx + AD.dx` として足す。
  つかむ面も `cells` の位置から出すので、ずらした字はそこを押せば選べる。
  **出力（赤・黒）にも効く**
- **起動時、前回の続きを戻すあいだ ％ を出す**（`BOOTP` / `bootProg()` / `bootStep()`）。
  フォント 1件ごとに 1つ、拓本は「読み込み」「下ごしらえ」の 2つとして数える。
  `await tick()` を挟まないと画面が描き変わらず、％ が見えないので注意
- **`loadRubImageQuiet()` は最後に `redraw()` を呼ぶ**。
  `rubRender()` だけを呼んでいたため、起動して構成の画面に戻ったとき
  拓本が出ず、タブを行き来しないと現れなかった（v4.9 で修正）
- **行を足すときは左へ**（縦書きなので次の行は左）。`addGuideline()` `tplAdd()`
  `copyGuideline()` の 3つとも、紙からはみ出すときだけ右に置く
- **ガイドラインの左右の並びはドラッグで入れ替えられる**（`reorderGl()` / `reorderGlTo()`）。
  左メニューの行（HTML のドラッグ＆ドロップ）と、構成の名前の帯（`glmove:` のつまみ）。
  いまの間隔をそのままに、左から置き直す。**中の並びは変えない**。
  なお、そのあと構成でガイドラインの縦の帯をつかむと `syncCenters()` が走り、
  左右の位置は拓本の位置から決め直される（元からの作り）
- **［− 削除］は 1行ガイドラインの見出しの右**（`delGuideline()`）。
  いちばん下ではなく、**いま出している行**を消す。文字グループの ✕ ともども確認を出す
- **消したもの**（v4.6）：行の見出し（`renderGlHead` ＝ 左右の位置 mm ／この行をコピー）と、
  センターラインの「中心 X mm」。**数字で X を入れる欄はもう無い**。
  左右の位置は 構成でガイドラインをつかんで動かす（`cen:` のつまみ）か、
  ゴムシートの［文字の位置］で決める
- **［1行ガイドラインと文字グループ］（旧 拓本解析の ④）は文字編集の中**。
  `#clist` の id はそのままなので `renderCols()` の中身は変えていない
- **左メニューの［行追加］**（`#mmAdd` ／ `openAddMenu()`）。押すと
  新規（`btnAddC` と同じ）か、どの行のコピーか（`copyGuideline()`）を選べる。
  `#mmAdd` も `.mmsub` なので、ガイドラインの並びを拾うループは
  `.mmsub[data-k]` で絞ること（絞らないと onclick を上書きされる）
- **構成で押したものに、左のメニューが追随する**。
  拓本の絵（`data-rub`）→［拓本解析］、ガイドラインの列（`data-gl`）と文字（`data-gi`）
  →［文字編集］＋そのガイドライン。当たり面は 拓本 → ガイドライン列 → 文字 の順に
  重ねてあるので、細かいほうが優先される

- **「構成」の校正文字は、ゴムシート上の位置では並ばない**。1行ガイドラインごとに
  左から詰め直す（`bothPack()`）。隣り合うガイドラインの**行幅の線どうし**のあいだが
  既定 6mm（`S.bothGap`、ツールバーで変更）。ただし文字が行幅からはみ出す列は
  文字の幅で詰める（隣と重ならないように）。
  高さ（mm）はシートのまま動かさないので、拓本と行の高さを見比べられる。
  出力（プロッター）には一切効かない。**表示だけ**
- **「構成」の線は、選んでいるガイドラインの分しか出ない**（`bothGl()`）。
  ほかのガイドラインの線は隠す。列を押すとそのガイドラインに切り替わる（`data-gl`）。
  **頭／脚**（実測した文字全長を決める線）だけは拓本ぜんぶに効くので出したまま
- **「構成」の ■ をつかむと線が動く**（`bindBothDrag()`）。
  `grp:<gi>:top|bot` は行の上端・下端（`topY`/`bottomY` mm）。拓本解析の色つき線と同じもの。
  `orig` は頭＝`S.rub.originY`（拓本ぜんぶが上下に動く）、`len` は脚＝`S.rub.realLen`
  （頭からの実測した文字全長。拓本が伸び縮み）、`cen:<ci>` はガイドラインの
  左右の位置＝`S.rub.centersPx`（拓本解析でつかむ線と同じ）。
  **較正そのもの（`calTop`/`calBot`）は動かさない**ので、`syncOriginY()` で
  行が勝手にずれることがない。ただし拓本解析で頭／脚を動かすと `syncOriginY()` が走り、
  手で決めた `originY` は上書きされる
- **つまみの大きさは「画面で見た px」で決める**（`KN = 16 / (MMPX * sc)`）。
  mm で決めると、細長い原稿は縮尺が小さく数 px になって掴めない（v4.0 の不具合）。
  左右に `GUT` の余白をとり、拓本の画面と同じで線の外につまみを出す
- **`bothMapper()` は、つかんだときに一度だけ位置を測る**（`rubMapper()` と同じ作り）。
  動かすたびに測り直すと、`renderBoth()` で作り直したあとの「外れた古い SVG」を
  測ってしまい、大きさ 0 → 計算できず、2回目以降の移動が全部無視される（v4.0/v4.1 の不具合）
- **つかんでいる間は `bothBusy` で表示範囲と縮尺を止める**（`bothBox` に覚える）。
  止めないと、動かすたびに絵の大きさが変わってつまみがポインタから逃げる。
  拓本の左はし（`bothRubX`）と `mmPerPx()` も、つかんだときの値で通す

## テストの注意

**ドラッグのテストは「動いた量」まで見ること。** v4.0/v4.1 は、最初の 1 回ぶんだけ
動いて残りが無視される不具合だったが、「値が変わったか」しか見ていなかったので
素通りした。`scratchpad/t14.mjs` のように、
画面の移動 px ÷（`bothBox.sc * MMPX`）＝ 期待 mm と突き合わせる。
縮尺は描き直すたびに変わるので、比べる直前に測り直すこと。

- **ゴムシートに残っているのは、出力の下見（赤／黒／塗って表示）と
  「文字の位置」だけ**。拓本を重ねるは「構成」へ移した（`vRub` は使わない
  隠し入力として残してあるだけ。`buildSVG` の重ねる分岐はもう通らない）。
  カットできない縁・グループの重なりの知らせ（`#overflow`）は消した
- **「構成」の拓本を重ねる**（`S.bothOv` ／ ツールバーの［拓本を重ねる］）。
  選んでいるガイドラインのセンターラインに、拓本の上の同じ線（`centersPx`）が
  重なるように拓本を動かす（`bothAnchorPx()`）。拓本は opacity 0.6、
  文字は fill-opacity 0.45 で透かす。ほかのガイドラインを押すと合わせ直す。
  重ねている間は、左右のつまみ（`cen:`）は出さない（横位置は合っているので）
- **ゴムシートの「文字の位置」は 1行ガイドラインごと**（`S.sheet.glOff[gl] = {x,y}`）。
  つかんだ字のガイドラインだけが動く。**ゴムシートと出力（赤・黒）にだけ効き、
  拓本・並べて・確認資料には効かない**。`glOffOf()` は buildSVG の中だけで使う。
  古い案件の `S.sheet.ox/oy`（全体のずらし）はそのまま残し、その上に足す形。
  「中央」で両方 0 に戻る

- **`S.rub.centersPx` と `S.centers` は数をそろえる**。拓本解析は centersPx をたどって
  線を描くので、ずれると線が出ない。`padCentersPx()` が面倒を見る
- **出力（プロッター）の線幅**は、縮小して置くパスだと縮尺のぶん細くなる。
  `penAttr()` で打ち消している。赤 `#FF0000`／黒 `#000000`、太さ 0.12mm、塗りなし
- **プロッターの位置ズレ**は「ゴムシート」パネルの位置補正で直す（端末ごとに localStorage）
- **共有フォルダーのファイル名**は濁点の書き方が違うことがある。`sameName()` で比べる
- **テンプレートと作字ライブラリ**は SharePoint の `/業務アプリ/アプリ使用フォント` に置く

## 拓本に合わせる（輪郭編集のサイドバー・いまは「テスト」表示）

すでに彫ってある字（拓本）と、これから彫る字が同じ文字のとき、書体のちがいが
となり合って見えるのを減らすための仕組み。**フォントの形を残したまま、拓本へ寄せる**
（拓本をなぞるのではない）。押す順番は上から下。

1. ［① 拓本を動かす］で、彫ってある同じ字をこの字の下に重ねる（手作業）
2. ［② 拓本に大まかに合わせる］（`peFitTest`）… 位置・大きさ・縦横比・傾き・回転。
   **押すと［① 拓本を動かす］は押せなくなる**（`PE.rubLock`）。合わせたあとに拓本を
   動かすと意味がなくなるため。［1つ戻す］で戻せば、また動かせる
3. ［③ 画線の太さを拓本に合わせる］（`peFitWeight`）… 「この字だけ ±%」を決める
4. 直したい点を選んで**右クリック→［選んだ ○点を拓本に寄せる］**（`peFitEdgeSnap`）。
   **向きは点ごとに自動**（墨の中にある点は外へ、外にある点は内へ）。
   サイドバーの［縁に寄せる条件］は、外へ広げる上限（`peOut`）と
   内へ引き戻す上限（`peIn`）の 2 つだけ

- **点が動かないときの理由は 3 つ**（v20.54 で調べた）。
  1. **外へ広げる上限（既定 0.35mm）に届かない**。墨の中の点は、輪郭に垂直な向きへ
     この距離まで探し、**縁が見つからなければ動かさない**。広げたい量がこれより
     大きい点は、そのまま残る。［外へ広げる上限 mm］（`peOut`）で変えられる。
     引き戻す向きは 1.5mm まで見るので、**縮む方はよく効くのに広がる方は効かない**、
     という見え方になっていた。ゴシックの「日」で実測：0.35mm では 12 点中 10 点が
     届かず、1mm にすると 7 点まで減った
  1b. **内へ引き戻す上限（既定 1.5mm）に届かない**（v20.56 で入力できるようにした）。
     墨の外にある点は、内向きにこの距離まで探して、墨に入らなければ動かさない。
     字が拓本よりずっと外にはみ出していると、1.5mm では届かず**外から寄せられない**。
     ［内へ引き戻す上限 mm］（`peIn`）で変えられる
  2. **点の位置と向き**。フォントの字は直線の所に両はししか点がなく、角の点の
     「垂直な向き」は となりの点を結んだ線の直角＝**斜め**になる。斜めに探すと
     墨の中を長く進むので、なおさら縁に届かない（実測で 2.9〜30mm）
  3. **となりとかけ離れた動きは捨てる**（`LIM`）。v20.54 まで 0.8mm 固定だったので、
     ［外へ広げる上限］を 30mm にしても、大きく動くべき点は全部ここで捨てられ、
     **上限を上げても動かなかった**。v20.55 で `max(0.8mm, 上限×0.6)` に連動させた
  4. **寄せても一致が良くならなければ、まるごと取り消す**。上限を大きくしすぎると
     遠くの縁に飛びついて一致が下がり、「良くなりませんでした」で 1 点も動かない
- **どれに当たったのかは、押したあとの【内訳】で分かる**（v20.55）。
  「動いた／墨の外にあって対象外／選んでいないので固定／○mm 探しても墨の縁に届かない／
  となりとかけ離れていたので捨てた」の点数を出す。うまくいかないときは まずこれを見る
- **点の四角は 6 割の大きさ**（v20.55。拓本が見えないという指摘）。
  つかむ範囲（`pehit`／`r*6`）は変えていないので、つかみやすさはそのまま
- **輪郭編集の「表示」に切り替えをまとめた**（v20.57）。
  塗りつぶし（`peFill`）／ポイント（`peShowPt`）／拓本（`peRub`）／
  拓本の縁（`peFitEdge`）／元の形を薄く出す（`peGhost`）。
  **ポイントを消しても、つかむ範囲は残る**ので、選ぶ・動かすはそのままできる
  （拓本を見ながら形を確かめるための切り替え）。
  **消していても、選んでいる点と選択の枠は出す**（v20.62）。
  どこを選んでいるか見えないと、右クリックの操作ができないため
- **点を増やす（`peDensify`／v20.52）は v20.54 で外した**（本人の指示）。
  必要になったら git 履歴の v20.52〜v20.53 にある
- **⑤ は「動かす量」をなめらかに解く**（v20.52）。それまでは、縁が見つかった点だけ
  動かし、見つからない点はその場に留めていたので、となり合う点の動きが食いちがって
  輪郭に角ができ、字の形が崩れていた。いまは
  `u[i] = (cf・d + λ(u[i-1]+u[i+1])) / (cf + 2λ)` をくり返して、
  見つからない点はとなりの動きを受け継ぐ。選んでいない点は 0 で固定するので、
  選んだ所のふちだけがなだらかに 0 へ戻る（`FIT_LAM` が なめらかさ。既定 1.5）
- **縁の位置は「ます」より細かく決める**（v20.52）。墨のますは 0.1mm ほどあるので、
  ますの真ん中で決めると点ごとに 0.05mm 食いちがってギザギザになる。
  3×3 でならした濃さの面（`fitSoft`／`FIT_BLUR`）を線でたどり、0.5 になる所を割り出す
- **「寄せる点」の中／外の選び（`peFitSide`／v20.53）は v20.58 で廃止**。
  向きは点ごとに自動で決まる（拾った墨の中にいるか外にいるかを、その点で見る）ので、
  人が選ぶ必要がなかった
- **「離れている点」まわり（`peFitPickFar`／`fitFarTool`／`fitEdgeDist`／
  「範囲で選ぶとき、離れている点だけ選ぶ」）は v20.58 で全部削除**（本人の指示）
- **拓本の拡大・縮小・位置を戻すも v20.58 で削除**。濃さは「表示」の拓本の下へ移した
- **「下にある墨は この字の ○倍の広さでした」で突き返されるとき**（v20.59 で 3 つ手を入れた）。
  1. **倍率は「字のそば」だけで数える**（`fitInkNear`／字を 1.5mm ふくらませた範囲）。
     紙ぜんたいで数えると、地のざらつきを拾っているだけで何倍にもなり、
     同じ字なのに突き返していた
  2. **小さすぎる塊は捨てる**（`fitDropSmall`／0.5mm 角未満）。地のざらつき対策
  3. **しきい値は 2 通り試して、字によく重なった方を採る**（`fitInkOne(..., flat)`）。
     「地ならし」は、まわり 8mm 角の明るさを引いてから決める方法。
     紙のむら・光のむらで片側だけ暗い拓本は、そのままだと地の半分を墨と読む。
     画の太さ（2〜3mm）より窓が大きいので、画の中まで消えることはない
  - 突き返すときは **そばの墨 mm²／この字 mm²／しきい値** も出す。
    どれが効いているか分からないときは、この数字を見る
- **学習用データを貯める**（v20.64。［この字を学習用に保存］）。
  拓本クリーン化 AI（U-Net）を立てる前から、教師データだけ貯めておくための仕組み。
  - 1 文字ぶんを **512×512 の 3 枚 1 組**で持つ。
    `raw`（拓本の切り抜き・濃淡）／`mask`（拾った墨に人の筆を重ねた正解）／
    `hint`（そのとき合わせていたフォントの字形）＋ `meta.json`
  - 3 枚とも**「字の座標の正方形」（字の枠を正方形にして 2 割ふくらませた所）から
    同じ写し方で作る**ので、1 画素もずれずに重なる。実測で、mask が白の所の raw の
    明るさ 196／黒の所 96 と、はっきり分かれることを確認（ずれていれば両方 129 になる）
  - **置き場は共有フォルダー**（v20.65）。テンプレートと同じ
    `/業務アプリ/アプリ使用フォント` の下に **`拓本学習データ`** を作り、
    **1 文字＝1 つの ZIP**（`pair_YYYYMMDD-HHMMSS_字.zip`／中身は
    `raw.png`・`mask.png`・`hint.png`・`meta.json`）で置く。
    テンプレートと同じ `fontDirId(true)`＋`spPutIn()` を使う
  - **つながっていないときは この端末の IndexedDB**（`chokoku_taku` / `pairs`）に貯め、
    ［この端末の分を共有フォルダーへ送る］でまとめて送る（送れた分だけ端末から消す）。
    テンプレートの `tplStore()` と同じ考え方。
    フォントの置き場（`chokoku` / `kv`）とは別の DB なので、互いに影響しない
  - **注意：共有フォルダーへの実際の書き込みは、この場では試せていない**
    （Graph の呼び出しを差し替えて、行き先・ファイル名・種類・大きさが
    正しく組み立てられることまでは確かめた）。実機で 1 文字ためすこと
  - ［学習用を書き出す］で **ZIP**（つめ込みなし・自前の `zipMake`）。
    `pairs/pair_NNNNN_{raw,mask,hint}.png` ＋ `_meta.json` ＋ `index.csv` ＋ `README.txt`。
    `unzip -t` が通ることを確認済み
  - **手がかりの絵は 2 枚**（v20.67）。どちらも「人が直す前」の形。
    - `hint1.png` … **元のフォントそのまま**（`PE.base`＝フォントから起こした形。
      `openPathEditor` の `origC` なので、過去に直した分も入っていない）。
      合わせ込みもしていないので、分かるのは「どの字か・どの書体か」だけ
    - `hint2.png` … **［② 拓本に大まかに合わせる］の直後**の形（`PE.fitSnap`）。
      ②を押していない組には**入らない**。`meta.hint1` / `meta.hint2` に有無が入る
    - **直したあとの形を手がかりにすると、それは答えそのもの**なので、
      拓本を見ずに写すだけのモデルになる。v20.65 まではそうなっていた
    - 切り出す正方形は hint2 があればそこから、無ければ いまの形から決める（`meta.boxFrom`）
    - **学習は手がかりが 1 枚も無くても回せる作りにすること**。README.txt にも書いた
      （足りない手がかりはまっ黒で埋める／学習中に 3〜5 割わざと まっ黒にする／
      検証は手がかり無しでも測る）
  - 切り出す正方形も hint と同じ形から決めるので、**直しても raw・mask・hint は動かない**
    （実測で、点を大きく動かしても 3 枚とも 1 バイトも変わらないことを確認）
  - 「この字だけ ±%」は形に入っていないので、**hint2 にだけ**足す（v20.66/67。
    hint1 は「フォントそのまま」なので足さない）。`meta.wpct` にも入れる
  - **hint を学習の入力 2 枚目に使うときは、学習中に 3 割ほど白紙に差し替えること**
    （そうしないと、拓本を見ずに hint を写すだけのモデルになる）。README.txt にも書いてある
- **拾った墨を筆で直せる**（`S.rub.paint`／v20.63。［墨を消す］［墨を足す］）。
  拾い方をいくら工夫しても、紙のふち・石の地・となりの字は残るので、最後は人が消す。
  - **持ち方は「拓本の画素の座標での丸」**（`{x,y,r,m}`。m は 0=消す／1=足す）。
    字の座標ではないので、**字を動かしても・拡大しても・別の字を編集しても、
    同じ場所を指したまま**。案件（JSON）に一緒に保存される
  - **1本も無ければ、拾い方は今までとまったく同じ**。塗りは `fitApplyPaint()` で
    最後に重ねるだけ。実測：塗って消して（［塗りを消す］）、ます数が
    4425 → 1661 → 4425 と、ぴったり元に戻る
  - 拾った墨の計算結果（`PE.inkC.ink0`）は取っておき、塗りだけを重ね直すので、
    筆を引いている間に拾い直しは走らない。字の枠にかかる塗りだけ見る
  - **輪郭にはいっさい触らない**（実測でパスが 1 単位も動かないことを確認）。
    塗り始めると［拓本の縁］を自動で出す（何を消しているか見えないと危ないため）
  - **塗りは何度でもやり直せる**。あとに塗った方が上になる（`fitApplyPaint` は
    うしろから見て、最初に当たった丸で決める）ので、消した所に［墨を足す］を
    重ねれば戻る。［塗りを1つ戻す］は最後の 1 本を、［塗りを消す］は全部を消す。
    実測：4425 ます → 消して 1661 → 足して 2878 → 1つ戻して 1661 → 全部消して 4425
  - **［① 拓本を動かす］も軽くしてある**（v20.74）。実測 **6044ms → 1030ms（5.9 倍速）**、
    止まる位置は同じ。引いている間は**拓本の絵の `transform` だけ**書き換える
    （`peRubTransform()`）。画面を作り直すと、写真の読み直し 65ms に加えて、
    置き場所が変わるたびに**墨の拾い直しまで走る**（`fitPrep` の合図に位置が入っているため）。
    拾った墨の絵は位置が古くなるので、引いている間は隠す。
    指を離したときだけ `peRender()` で拾い直して描き直す
  - **筆は軽くしてある**（v20.71。「こまかい」にしたら重くなった、の直し）。
    実測：同じ 16 本を引くドラッグが **4595ms → 823ms（5.6 倍速）**。効いた順に 3 つ。
    1. **塗るたびに拾い直していた**（`rubPaintBump` が `PE.inkC` を捨てていた）。
       しきい値・形態処理・塊の選り分けを毎回やり直していた。
       拾った墨 `ink0` は取っておき、塗りだけ重ね直す
    2. **画面ぜんたいを作り直していた**。作り直すと拓本の写真（数 MB の data URL）を
       読み直すので、**1 回 65ms** かかる。引いている間は
       `#peEdgeImg`（拾った墨の絵）の href を差し替えるだけにした（`fitEdgeURL`）。
       指を離したときだけ `peRender()`
    3. **1 コマに 1 回にまとめる**（`requestAnimationFrame`）。
       ポインタは 1 秒に何十回も動くので、そのたびに作り直すと追いつかない
    - 足した塗りは、その丸のまわりのますにだけ重ねる（`fitPaintAdd`）。
      取り消し・全消しのときだけ、ぜんぶ塗り直す
    - **数字は 1 つも変わっていない**（4425 → 消して 1661 → 足して 2878 →
      1つ戻して 1661 → 全部消して 4425）
  - **筆の効く範囲を丸で出す**（v20.68）。ポインタの矢印は消して、丸だけ見せる
    （消す＝赤／足す＝青）。丸の大きさは塗るときと同じ値から出しているので、
    **見えている丸＝実際に効く範囲**。`rubBrushUnits()`＝`rubBrushPx() * |PE.rub.ky|`
    （`ky` は「拓本の画素 1 つが字の単位いくつぶんか」。掛ける。割ると 1/100 の丸になる）。
    丸はポインタが動くたびに属性だけ書き換える（描き直すと拓本の絵まで作り直して重い）
  - サイドバーの学習まわりは **「AI学習」の見出し**にまとめた（v20.68）
  - **まわりの字を隠して切り出す**（v20.69。既定 入・残す幅 1.5mm）。
    行が詰まっていると、切り抜きの四角に となりの字が入る。手がかり無しで動かしたい以上、
    絵に よその字が写っていると「どれがこの字か」が決まらない。
    この字の枠＋残す幅より外は、**紙の色（そのまわりの平均）で塗りつぶす**。
    mask も同じ所で切るので、**よその字を筆で消して回る必要はない**。
    実測：1.5mm で絵の 30%、0mm で 56% が塗りつぶしになる。
    `meta.hideOut` / `padMm` / `hiddenPct` / `paper` に記録
  - **推論のときも同じように隠して渡すこと**（README.txt にも書いた）。
    学習と本番で切り方が違うと、塗りつぶしの境目を手がかりにしてしまう
  - **塗りは拓本に付く**ので、よその字を消したまま忘れると、その字を編集するときに困る。
    ［この字のまわりの塗りだけ消す］（v20.69）で、いま見ている字の切り抜きの中に
    入っている塗りだけ消せる（実測：2 本のうち近い 1 本だけ消えて、遠い 1 本は残る）
  - これは将来の AI（拓本クリーン化）の**教師データそのもの**になる。
    「元の切り抜き」＋「人が直した墨マスク」の組が、この塗りから作れる
- **墨のこまかさ**（`peInkFine`／v20.70。標準＝これまで／こまかい＝写真の限界まで）。
  ますは**拓本の写真の 1 画素より細かくはできない**（`fitGrid` が `|PE.rub.kx|` で
  頭打ちにしている）。実測（この拓本・12px/mm＝0.084mm）：
  標準 256 ます角・1ます 0.123mm（写真の 1.5 画素ぶん）→
  こまかい 377 ます角・1ます 0.0835mm（＝写真の 1 画素。ここが限界で、
  それ以上上げても数字は変わらない）
  - **ノイズは増えない**。細かくする前後で拾った墨は 107.8 → 110.2mm²（+2%）。
    地の粒を落とす条件（0.5mm 角未満・0.12mm の切り離し）が mm で書いてあるので、
    ますの細かさに関係なく同じだけ効く
  - 重さ：拾い直し 66→57ms、［②］270→260ms、切り出し 104→152ms。
    ただし**大きな字ほどますが増える**（`320*F` で頭打ち。標準 320 角＝10万ます、
    こまかい 640 角＝41万ますが上限）
  - **既定は「こまかい」**（v20.76。それまでは標準）。選ぶ所は「その他」の見出しへ移した
  - `meta.json` に `fine` / `mmCell` / `mmPerPx` を残す
- **拾う量を手で寄せられる**（`peInkAdj`／v20.62。「表示」の［拓本の縁］の下）。
  自動で決めたしきい値に ± を足す。＋で拾う量が減り、−で増える。
  実測（ゴシックの「日」）：−30 で 5989 ます（しきい値 95）→ 0 で 4425（125）→
  +45 で 3219（170）。［拓本の縁］を出しながら動かせば、何を墨と見ているか見て決められる
- **試したが入れなかったもの**：「画線の太さで縮めて、画より太い面を落とす」方法。
  彫ってある画はフォントの画より太いことが多く、フォントの太さで縮めると
  **画そのものが消える**（実測で本物の墨を 1570 ます削り、合わせが 45% → 27% に落ちた）。
  ゆるめると今度は何も落ちない。効くと確かめられなかったので入れていない
- **「一致 ○%」は、字のまわりだけで測る**（`fitIoUNear`／v20.73）。
  紙のふち・石の地など、遠くの塊まで分母に入れて測ると、
  **画面ではぴったり合っているのに「一致 11%」**のような数字になり、良し悪しが分からない。
  実測（紙のふちを足した拓本）：ぜんたいで測ると 43% → **15.9%** に落ちるが、
  字をふくらませた範囲だけで測れば **44.1% のまま**。
  字を遠くへ動かした場合は 14.5% と、ちゃんと下がる（区別はできている）
  - 探すとき（`score`）は今までどおり。**画面に出す数字と、低い一致の知らせ**を
    こちらに替えた（②・縁に寄せる の両方）
  - あわせて、探すときの墨も**字のまわりの四角の中だけ**に絞った（`fitInkWin`）。
    遠くの大きな塊に引っぱられにくくなる
- **筆で消した分は［②］にもちゃんと効く**（`fitPrep` が返す墨は塗り込み済み）。
  実測：大きく消すと墨が 4425→253 ます、②の結果も 45%→4% と変わる
- **見張りは「止める」から「知らせる」に変えた**（v20.72）。
  ［墨を消す］で地の粒を消すほど「この字の ○倍」の倍率は下がるので、
  **消すほど突き返される**という困った作りだった。いまは
  - 倍率が 0.25 未満／4 超 … 動かしたうえで【…おかしければ［1つ戻す］で戻せます】と出す
  - 一致が 35% 未満のまま大きく動かす … 同じく、動かしたうえで知らせる
  - **墨がほとんど無い（倍率 0.05 未満）ときだけ止める**（動かしても意味がないため）
  - 太さを合わせる・縁に寄せる の見張りは 0.12〜6 に広げた
  - 判断は人に任せる形。飛んだら［1つ戻す］で戻る
- **［② 拓本に大まかに合わせる］で字が遠くへ飛ぶことがある**（v20.61 で止めた。v20.72 で知らせるだけに）。
  紙のふち・石の地・となりの字など、字ではない大きな塊が墨として残っていると、
  一致が少しだけ上がるので、そちらへ吸い寄せられる。**動かせる幅は字の 60%** もあるので、
  字の半分ぶん飛ぶこともあった。歯止めは 2 つ：
  1. **大きすぎる塊は落とす**（`fitDropBig`／字の 2.5 倍を超える塊。
     落として何も残らないなら落とさない）
  2. **一致が 35% 未満で、しかも大きく動かす形（字の 20% 超）なら、動かさない**。
     小さな寄せ直しは今までどおり通す。実測：地の塊を作った拓本で 15%→28% の
     「改善」を止めて動かさず（0 単位）、まともな拓本では 14%→45% で今までどおり動く
- **［決定］が落ちていた**（`setAdj`／v20.92）。保存した JSON の `adj` は
  `[null, null, {…}]` の形になることがあり、**その字の枠が null** のところへ書こうとして
  「Cannot set properties of null」で止まっていた。**なぞった結果がそのまま消える**。
  空きなら `{}` を置いてから書く
- **なぞった輪郭は「点だけ」**（`i`/`o` が null）。`pathD` は節点に手（ハンドル）が
  あれば `C`、無ければ `L` を書くので、出力はそのまま**直線のつなぎ**になる。
  プロッターは元々まっすぐにしか動けず、フォントの曲線も出す前にドライバーが
  細かい直線に割っている。**実測（試しの拓本「久」31.5×32mm、upem 2048）**：
  輪 3 本・点 725 個・輪郭の長さ 249mm・1 区間は中央値 **0.30mm**（最小 0.11／最大 1.69）。
  ［縁の細かさ］を 0.15mm にすると点 315 個・中央値 0.54mm。
  刃は 0.25〜0.5mm 後ろにあるので、**0.05mm の細かさは刃が追えない**。
  切りに回すなら 0.1〜0.2mm あたりから試すこと
- **作字の画面と校正の画面で、拓本の縦横が違って見える**（`peRubFit`／v20.93 で説明を出した）。
  **不具合ではない。** 作字はフォントのます目（正方形の枠）で動くので、
  長体・平体の字では拓本を `kx/ky = 高さ÷幅` だけ伸ばして重ねている
  （そうしないと字と拓本が重ならない）。出力で元の割合に戻る。
  **実測**（試しの拓本「久」）：幅 32mm なら 見た目・出力・拓本 とも 0.967 で一致。
  幅 24mm（長体）にすると 見た目 1.267 ／ **出力 0.9505 ＝ 拓本 0.9505**。
  幅 40mm（平体）で 見た目 0.871 ／ **出力 1.0887 ＝ 拓本 1.0887**
  - ③ なぞる の上に、**何 % どちら向きに伸ばしているか**と、
    **その字の 幅 × 高さ mm**（どの設定でそうなっているか）を出すようにした。
    もとは ［文字サイズ］か［文字調整］の 幅・高さ（`r.w/r.h`・`AD.wmm/hmm`・`AD.sc/scw`）
  - **残っている本当の問題**：`fitGrid` の `mmCell` は `PE.rub.kx`（＝横）だけで
    決めている。ます目は正方形なので、**縦向きの mm は 高さ÷幅 の分だけずれる**。
    ［太さの補正 0.3mm］［捨てる島 mm²］も、AI へ渡す 512×512 の切り抜きも同じ。
    5% を超えるときは画面に断りを出しているが、**直すなら ます目を「実寸で正方形」に
    する**（x と y で ます の刻みを変える）。長体を強く使うなら要対応
- **なぞったあとでも、太さは変えられる**（④の［この字だけ ±%］）。
  **実測**（試しの拓本「久」）：±% を 10 にすると 画線幅 3.14 → 3.46mm・面積 +17.2%。
  ただし**すき間がつぶれない量に抑える**ので、この字では **+19% あたりで頭打ち**
  （`weightDelta` の `gap*0.35`。ink 201 単位／gap 55 単位）。上限は `WPCT_MAX` の 30%
  - ［画線の太さを拓本に合わせる］は、**なぞったあとは ほぼ 0%**になる。
    もう拓本そのものの太さだから。太らせたいときは ±% を使う
  - v20.95 で、この表示に **mm** を足した（「201 → 221 単位」では彫る側に伝わらない）
- **作字の右メニューを 3 つのタブに分けた**（v20.96）。
  **［１ 作成］［２ 修正］［３ 設定］**。1 枚に全部出していたころは、
  次にすることが上から下まで探さないと分からなかった。開くと必ず［１ 作成］から。
  - **［１ 作成］**＝拓本から字を作る流れだけ。
    ① 拓本を合わせる（拓本を動かす／**AI オート**＝まだ中身なし）
    ② 拓本トレース（**AI お任せ**＝①のモデル待ち／トレース訂正：拾う量 ±20・
      **［消す］［足す］のえらび**・縁のならし 0.5／AI 学習用に保存）
    ③ 形状補正（**縁を広げる mm**／**AI 書体補正**＝押すと墨が変わる入切）
    ④ 字を作る（墨の縁から この字を作る）
  - **②は「AI お任せ」か「手動補正」か、どちらかを選ぶ形にした**（v20.97）。
    間に「または」の区切りを入れ、見出しも［トレース訂正］→**［手動補正］**に
  - **［消す］［塗る］はえらびを 2 つに分け、横に並べた**（v20.97。「えらぶ…（消す）」では
    何のえらびか分からなかった）。太さ（0.7/0.5/0.3）も一度に決まるので、
    **［筆の太さ mm］の入力欄と［墨を消す］［墨を足す］のボタンは廃止**。
    太さは `PE.brushMm`（既定 0.5）が覚えている
  - **右クリックからも 消す／足す を選べる**。**［１ 作成］のときだけ**出る
  - **［縁を広げる mm］（`#peGrow`／v21.5）が動かすのは「拾った墨」**。
    ここは拓本をなぞっている段なので、**フォントの輪郭ではない**（本人の指摘で直した）。
    `fitInkMask` の中で `fitGrowMask`（距離で見る。四角い窓だと斜めが √2 倍ふくらむ）。
    赤い［拾った墨］がその場で太り、④はその墨から字を作る。
    ほかの数字の欄と同じ `onchange`、値は拓本ごと（`S.rub.inkGrow`）。
    - **ます 1 つきざみしか動かせない**（墨は ます目の 0/1 なので半ますは無理）。
      **ます目に丸めて、実際に動いた量を必ず言う**。
      実測（1 ます 0.147mm）：0.03 → 動かない（そう知らせる）／
      0.10・0.20 → どちらも 1 ます＝0.147mm で 176.3 → 198.1mm²／
      −0.10 → 155.2mm²／0 → ぴったり 176.3mm²。
      広げてから④を押すと、字は 181.5 → 203.5mm²（墨との一致 97%）
    - 細かくしたいときは［設定］の［墨のこまかさ］を「こまかい」に（ます 0.083mm）
    **％ではなく mm**。拾い方の癖で内側に入る量は画の太さによらず一定なので、
    ％だと太い画ほど大きく動いてしまう。＋で外へ、−で内へ。
    **画の太さは指定の 2 倍**増える（両側から広がるため）。
    実測（試しの拓本「久」・周長 176mm）：＋0.10mm で面積 181.5 → 199.8mm²
    （周長 × 0.10 ＝ 17.6mm² に対して 18.3mm²。角のぶんだけ多い）。
    −0.10mm で 164.0mm²（−17.5mm²）。
    **［画線幅］の数字で確かめてはいけない**。あれは横に走らせて測るので、
    斜めの画では実際より大きく出る（＋0.10mm で 0.22mm 増えたように見える）
    なぞった線は内側に入りやすいので、まとめて外へ出すためのもの。
    **歯止めは掛けない**（本人の取り決め。目で見て決める）。
    ②タブの［この字だけ ±%］（`#peW`・すき間で頭打ちする太さ調整）とは**別物**。
    実測：＋10% で面積 +16.2%・「画線の太さで 0.32mm」と帯に出る
  - **［AI 書体補正］（`#peAiShape`）は入切のボタン**。えらび［AI に通す］（`#peAiStage`）は廃止。
    - **入切ではない**（v21.12。本人の指示「AI書体補正ボタンをもう一度押すことでの
      戻す機能は無くして」）。押すたびに、そのとき見えている墨に対してかかる。
      戻すのは**［↶ 1つ戻す］**（`peSnapAi()` が前の `PE.aiInk`（無ければ null）を
      輪郭・塗りと**同じ 1 本の列**に積む。`{ai: 前の値}`）。
      `peEdgeRedraw()` は画面を作り直さないので、そこで
      `#peUndo` の `disabled` を自分で外すこと
    - **［消す］［塗る］は AI のあとにも効く**（v21.12。本人の指示「その後、消すや
      塗ることでの修正をできるようにして」）。`PE.aiInk.ink0` は AI が出した
      そのもので、`RUBPAINT_V` が変わるたび `fitApplyPaint(G, ink0)` をかけ直す。
      塗りは丸を置くだけなので、前からある丸をもう一度かけても結果は変わらない
    - **［AI 学習用に保存］のボタンは無くした**（v21.13。本人の指示
      「AI 学習用に保存は無しにして、AI書体補正を押すと保存される仕組み」）。
      `peAiShapeGo()` が AI に通す**前**に `takuSave(true)` を呼ぶ。
      押すボタンが 1 つ減り、**拾い終わった墨が必ず残る**。
      `takuSave(quiet)` は quiet のとき本当に黙り、
      代わりに**何が起きたかの一文を返す**ので、AI の知らせと 1 本にまとめられる。
      同じ字は同じ名前なので、直してもう一度押せば上書き。
      保存先の説明（`#peTakuInfo`）も③の AI ボタンの下へ移した
    - **保存するのは生の墨**（`fitPrep(true)`）。
      ここの mask は「人が直した墨」＝正解なので、
      **AI の出したものを正解として覚えさせない**（自分の出力を自分で学ぶことになる）
  - **④［現トレースから字を作る］は［画線］［塗り］［点］を入にする**（v21.13。本人の指示）。
    作った字が見えていないと確かめようがないため
  - **ゴムシートの画面（v21.18。本人の指示でまとめて整理）**
    - **文字の校正・修正はゴムシートでは行わない。［校正］タブで行う。**
      `#stage` の右クリックから `ctxOnChars()` を外した（`#bothstage` には
      付いたままなので、できることは減らない）。備考メモの右クリックだけは残す
      （備考メモはゴムシートに乗っているものなので）
    - ［位置を戻す］（`#btnShHome`）と `clearSheetOff()` を無くした
    - ［SVG］→ **［SVG出力］**
    - ［① 赤を出力］［② 黒を出力］は、となりのタブと**同じ高さ**にする。
      タブは 1px の枠＋上下 5px、ふつうのボタンは 7px で 4px 高かった。
      `#pvbar #btnPrintRed,#pvbar #btnPrintBlk{padding:5px 14px;}`。実測：どちらも 29px
  - **SVG に入れる線は「拓本実測値 ○○mm」**（v21.18。もとは「拓本文字全長」）
  - **備考メモの家名は「〇〇家」で出す**（v21.18）。欄には「久ヶ山」とだけ入れることが
    多いので、`infoLines()` が出すときに「家」を足す。すでに「家」で終わっていれば足さない。
    **欄に入れた文字そのものは変えない**（出すときだけ）
  - **タブは、どの画面でも帯の同じ所に出すこと**（v21.31。本人の指摘「校正を押した
    ときだけ位置がずれる」）。帯（`#pvbar`）は折り返す並びで、同じ行に背の高いもの
    （校正の［ガイドライン間 mm］の欄）があると、まん中そろえのせいで
    **タブだけ下へ落ちていた**（実測 60px → 68px）。
    `#pvbar .tabs{align-self:flex-start;}` で上にそろえる。
    実測：4 画面とも タブの上端 60px
  - **帯に操作の説明文は置かない**（v21.31。本人の指示）。
    校正の `#bInfo` と、ゴムシートの案内を外した。
    **拓本を重ねている間の知らせだけ**は残す（いまどの状態かが分からないと困るため）。
    帯も低くなった（校正 138 → 90px、ゴムシート 116 → 81px）
  - **タブの並びは 拓本補正 → 校正 → 確認資料 → ゴムシート**（v21.22。本人の指示）。
    作業の順に左から。拓本補正（`#viewRub`）は前は `hidden` にして「回す」ときだけ
    開く作りだったが、**ふだんから開けるように**した
  - **［行の見立て］は［AI補正］に改め、いまは押せない**（v21.27。本人の判断
    「文字の認識が全くできていない。後日 AI を使ったものに置き換える」）。
    見出しを「AI補正」にし、［この行を見立てる］は `disabled`。
    古い案件に控えが残っていても、**間違った枠を絵に重ねない**
    （`rubRender()` の `var LN = null;`）。
    中の仕組み（`rubLineMeasure`／`rubLineFixRot`／`rubLineHtml`）は
    **AI 版の比べ先として残す**。［帯をこの列に合わせる］は使える
  - **［拓本補正］を開いたら、はじめから回せる状態にする**（v21.27。本人の指示）。
    タブの `onclick` で `startSpin()`（絵があり、まだ回していないときだけ）
  - **回転の中心を決めていないときは、たて＝頭ライン（`calTop`）、
    よこ＝拓本の幅の中心**（v21.27。本人の指示）。前は絵のど真ん中だった。
    頭で留めれば、頭を合わせたまま傾きだけ直せる。
    実測：頭 54 → 中心 (300, 54)、頭を 200 にすると中心も 200 へ付いてくる。
    中心を決めたら、そちらが優先される
  - **回転補正のとき（v21.26。本人の指示）**
    - **回せるのは拓本の絵の上だけ**。`.spinhit` は `x=0 width=W` にする。
      前は左の余白（`-GUT`）まで受けていて、**枠の外を引いても回った**
    - **頭・脚（`calTop`／`calBot`）は回している最中も動かせる**。
      つまみを `calHits` に分け、`.spinhit` の**上**に置く（下だと押せない）。
      `bindDrag()` の `if (rotMode) return;` も、この 2 つだけ通す
    - マウスポインタは**ぐるりと回る矢印**（`.spinhit` に data URI の cursor）。
      白フチ付きなので黒い拓本の上でも見える。引いている間は `grabbing`
      （`#rubstage.spinning`。`bindSpin()` が付け外しする）
    - 実測：受け皿は x=0・幅＝絵の幅／独自のポインタが効く／
      回転中に頭を下へ引くと 54 → 143 と動く
  - **作字の１［作成］まわり（v21.39。本人の指示）**
    - ［AI オート］［AI お任せ］は**灰色で押せない**。中身がまだ無いのに押せると紛らわしい。
      なぜ使えないかは `title` で出す（押したら知らせを出す作りはやめた）
    - **「拾う量 ±」→「拾う厳しさ ±」**。`peInkAdj` は ＋ で**しきいを上げる**＝
      拾う墨が**減る**ので、「拾う量」は逆に読めていた。
      「厳しさ」なら ＋＝厳しい＝墨が少ない で向きと言葉が合う。
      下に「＋で少なめ、−で多めに拾います」と添える。**数の意味は変えていない**
      （変えると案件に入っている `S.rub.inkAdj` の意味が変わってしまう）
    - **［消す］［塗る］は、太さをそのまま押せる並び**（`.peink`）にした。
      えらび（`<select>`）だと開いて縦に並んだ中から選ぶので 2 手かかり、
      どちらのえらびかも分かりにくかった。いまは
      「消す 0.7 0.5 0.3」「塗る 0.7 0.5 0.3」の 2 行。
      いま使っている太さは濃く光る。**同じものをもう一度押すとやめる**。
      実測：3 つとも同じ高さで左から 0.7→0.5→0.3、消すが上・塗るが下
    - **墨の筆を使っている間は、右クリックに［ポイント（点）を出す］を出さない**。
      墨を直しているのに点の話が混ざるため（`inkMode`）
  - **作字の２［修正］から 3 つ外した**（v21.38。本人の指示）
    - ［画線の太さを拓本に合わせる］（`#peFitW`）… あまり使わない
    - ［拓本に寄せる｜外へ／内へ mm］（`#peOut`／`#peIn`）と、
      右クリックの［選んだ ○ 点を拓本に寄せる］… 使わない。
      欄を無くすと加減が決められないので、**右クリックの入口も一緒に外す**
    - 中の仕組み（`peFitWeight`／`peFitEdgeSnap`）は戻すときのために残してある。
      欄が無くても既定値（0.35／1.5）で動くように書いてあるので、そのままで落ちない
  - **［大まかに合わせる］は学習用に保存しない**（v21.38。本人の指示
    「大まかに合わせるは、学習用に保存しないはずです」）。
    この時点の墨はまだ人が直していないので、正解として貯めるには早い。
    `peFitTest()` の `takuSave(true)` を外した。保存は［AI 書体補正］のときだけ（v21.13）。
    ボタンの字からも「（学習用に自動保存）」を取った
  - **拓本が無ければ、作字の１［作成］は灰色で押せない**（v21.37。本人の指摘
    「作字のメニューで拓本が無く、単純に文字の修正の場合、どれが変更できる
    メニューなのか分からない」）。
    １の中身（拓本を合わせる／拓本トレース／形状補正／現トレースから字を作る）は
    **ぜんぶ拓本が要る**のに、押せるように見えていた。
    `peTabSync()` が `PE.rub` を見て `disabled` を付け、なぜ使えないかを `title` で出す。
    `peTab(1)` も拓本が無ければ２へ落とす。開くときは `peTab(PE.rub ? 1 : 2)`。
    実測：拓本なし＝作成が灰色で開くのは修正／拓本あり＝作成が押せて作成から始まる
  - **ゴムシートで、1 文字を選んだときの囲みは出さない**（v21.36。本人の指示）。
    ゴムシートは出来上がりを見る画面で、字を直すのは［校正］タブ（v21.18 で
    右クリックも外した）。囲みが出ると「ここで直せる」と見えてしまう。
    `buildSVG()` の `charPicked && r.gi === sel && i === charSel` の枠を消した。
    **行（グループ）の囲みは残す**（どの行を選んでいるかは要る）。
    出力には元から入らない（`isPrint` で分けてある）
  - **上の帯から［フォント読込］と、読み込んだフォントの選びを外した**（v21.35。本人の指示）。
    - 読み込みは 左の［フォント・作字］にある。どのフォントで組むかは、
      行ごとに［行の構成］の［フォント］（`#pFont` → `g.fontIdx`）で選ぶ。どちらも二重だった
    - 帯の選び（`.fchip`）が変えていたのは `curFont`＝**フォントを決めていない行の既定**。
      押すと黙って組版が変わるので、外した方が安全でもある。
      `curFont` は案件に保存していないので、もともと開き直すたびに変わっていた
      （読み込んだ最後のフォントになる）。**行ごとにフォントを決めておけば影響は無い**
    - `renderFontBar()` は欄が無くても落ちないように頭で `return`。
      `$("btnFont")` の結び付けも `if ($("btnFont"))` で包む（呼び先は `fontLoadOpen()` に残した）
    - 読み込んだかどうかは、左の［フォント・作字］の右肩の札（`#nFont`）で分かる
  - **［開く］の案件選びは、［設定］ではなく［ファイル］に出すこと**（v21.34。本人の指示
    「一般的なファイル管理は設定ではなく、ファイルメニューに入れてほしい」）。
    `spBrowseOpen("json", "file")`。`spBrowseRefresh()`／`spBrowseClose()` に
    `host === "file"` → `filePanel()` を足し、`filePanel()` の頭で `spBrowseHTML()` を出して
    `spBrowseBind(box)` を呼ぶ。`spPanel()` 側は `host === "sp"` のときだけ出す
    （でないと両方に出る）。実測：［開く］でファイルのパネルが開き、選び画面はそちらだけに出る
  - **［③ SVG出力］は ①赤・②黒と同じ形で色違い**（v21.34。本人の指示）。
    `button.svgb`（青）＋ `#pvbar #btnSvg{padding:5px 14px;}`。実測：高さ 29px で 3 つとも同じ
  - **確認資料の［JPEGで保存］は、押したら保存先を選ばせる**（v21.34。本人の指示）。
    前は「JPEGで保存」（共有フォルダー）と「⤓」（このパソコン）が並んでいて、
    ⤓ が何なのか分からなかった。`#pickMenu`（押した所の下に出る小さな選び窓）を足し、
    ［共有フォルダーへ］［このパソコンに保存］の 2 択にした。
    つながっていなければ共有フォルダーは押せない。外を押すか Esc で閉じる
  - **拓本の絵が読めなかったときは、黙って終わらないこと**（v21.33。本人の指摘
    「JSON のロードで拓本が読み取れないことが多々ある。リロードすると読める」）。
    - **拓本の絵は案件ファイルの中に入っている**（`rubSrc`。v2 の書き方では
      文書に 1 つだけ持ち、版ごとには複製しない）。**別に取りに行っていない**ので、
      「校正は読めたのに拓本だけ通信で落ちた」は起こらない。
      起きるのは**ブラウザが絵の読み取り（デコード）に失敗する**こと。絵が大きいほど起きやすい
    - `loadRubImageQuiet()` の `im.onerror` は黙って終わっていた
      （`applyProject()` は `done` を渡していなかったので、**何も出ずに絵だけ出ない**）
    - 直し：`applyProject()` が失敗を受け取り、**1 回だけ自動で入れ直し**、
      それでも駄目なら知らせる。`rubReload()`／`rubReloadSync()` を足し、
      **［🔄 拓本を読み直す］**を拓本のメニュー（`#btnRubReload`）と
      確認資料の帯（`#dRubReload`）に置いた。
      絵が入っているのに出ていないときは赤くして「拓本が出ていません（読み直す）」に変える
  - **絵だけ入れ直したときは、補正を消さないこと**（v21.25。本人の問い
    「JSON に拓本データが入っているが読み込めなかった場合、SP取込で元の JPEG を
    選んだらどうなるのか」）。
    - `loadRubImage()` は読み込むたびに**角度・較正線・帯・センターラインの位置を
      初期値に戻して**いた。別のパソコンで開いて絵だけ無い案件で JPEG を選び直すと、
      **補正がぜんぶ消えていた**
    - 見分け方：**いま絵が無く（`RUB.img` が null）、案件に補正が入っていれば
      「入れ直し」**（`rubHasWork()`）。絵があるまま選び直したら今までどおり取り替え
    - さらに `S.rub.imgW/imgH`（前の絵の大きさ）と違えば取り替え扱い。
      較正線も帯も塗りも「画素の位置」で持っているため
    - 実測：同じ絵を選び直すと 角度3.75°／較正120,800／実測321.5／帯250,340／
      塗り1本／しきい値140 が**すべて残る**。大きさの違う絵なら作り直す。
      絵があるまま選び直したら作り直す
  - **［拓本補正］のタブは、拓本が無いと押せない**（v21.24。本人の指示
    「押せないようにして、意味ないので」）。`rubTabSync()` が `disabled` を付け、
    なぜ押せないかを `title` で出す。拓本は左の［拓本］から取り込む
  - **ロック中でも、［フォント・作字］は使えること**（v21.32。本人の指摘
    「ロックしているファイルを開いたとき、フォント・作字のメニューの内部が
    ほとんど無効化されている。ここを変更しても文字は変わらないのでは」）。
    - ロックの見張りは `#sub` の入力とボタンを**丸ごと**止め、
      `#panSP,#panFile,#panInfo,#panDocs` だけ通していた。`#panFont` も通す
    - ここは案件の中身ではなく、**このパソコンと共有フォルダーの道具**。
      とくに **フォントは案件に入っていない**ので、読み込めないと
      **別のパソコンではロックした校正の字が出ない**（実害があった）
    - 共有の作字を書き換える／消すところには、もともと別の歯止めがある
      （`peCommitLib`／`glyphLibPushOne`／作字の削除。どれも `locked()` で断る）
    - `beforeinput` の見張りにも `#panFont,#glibWin` を足す（作字一覧の検索欄用）
    - 実測：ロック中に フォント・作字＝使える／行の構成・ゴムシート設定＝止まる／
      設定＝使える／共有の作字の書き換え＝断る
  - **ロック中でも、まん中ボタンは通すこと**（v21.29。本人の指摘「ロック中のファイルを
    閲覧する際に、マウスのセンターボタンを押すと警告がでて、移動ができません」）。
    ロックの `pointerdown` の見張りが `#bothwrap,#rubwrap` の中を丸ごと止めていたので、
    **画面を動かすだけのまん中ボタンまで止まっていた**。
    ロックは「中身を変える入口」を止めるためのもの。見るための操作は止めない。
    実測：ロック中、まん中ボタンで拓本補正・校正とも動き、知らせは出ない。
    左ボタンは今までどおり止まって知らせが出る
  - **［見立てる帯］の絵とつまみは外した**（v21.28。本人の指摘「拓本校正画面に
    見立てる帯の表示が残っています」）。［行の見立て］を止めたので、見せる意味が無い。
    ［帯をこの列に合わせる］も外した（押しても何が変わったか見えないため）。
    - **帯（`S.rub.bandL`／`bandR`）そのものは残っている。**
      しきい値の自動判定と列の検出は、いまもこの帯の中だけを見ている
    - AI に置き換えるときは、絵とつまみとボタンを戻すこと。
      元の作りは v21.24（`rubBandFit()` は関数だけ残してある）
  - （v21.24 の経緯）**帯は、絵の上でつかんで動かせるようにしてあった**。
    - しきい値も、列の検出も、［この行を見立てる］も、**ぜんぶこの帯の中だけ**を見る。
      ところが v21.23 まで、**帯を動かす操作が画面のどこにも無かった**
      （取り込んだときに 幅の 5%〜35% に置かれ、あとは `detectColumns(true)` 頼み）
    - 絵に青い帯と、上に 2 つのつまみ（`data-k="bandL"/"bandR"`）を出した。
      `bindDrag()` に 左 < 右 を保つ分岐を足した（実測：左を右の外まで引いても入れ替わらない）
    - ［帯をこの列に合わせる］（`rubBandFit()`）＝ `detectColumns()` の中から
      **いまの帯の中心にいちばん近い列**を選び、8% の余裕を付けて合わせる。
      実測：3 列のうち、帯を寄せた側の列に毎回合う
  - **［拓本補正］を押したら、左の［拓本］も開く**（v21.23。本人の指示）。
    この画面の設定（しきい値・較正・トリミング・行の見立て）は
    ぜんぶ左のパネルにあるので、タブだけ開いても何もできない。
    `setView("rub"); setSub("rub");`
  - **`setSub()` は毎回 `renderMenuGl()` を呼ぶこと**（v21.23）。
    前は「どのパネルも開かない」ときだけ呼んでいたので、
    タブから開いたときに**左の［拓本］に色が付かなかった**
  - **角度補正が入っている間は、回転の中心と縦の基準線をいつも出す**（v21.22。本人の指示）。
    `rubRender()` の `showPiv = !!rotMode || !!num(S.rub.rot, 0)`。
    どこを中心に、どの線を頼りに回したのかが、あとから分からなくなるため。
    **出すのは絵だけ**で、つかんで動かせる `.pivhit` は「回す」ときのまま
    （ふだんの画面で中心をうっかり動かさないように）。
    実測：角度 0 では出ず、3.2° を入れると中心の印と基準線が出て、つかむ印は出ない
  - **拓本補正の［実測した文字全長］の欄は、絵のまん中に付けること**（v21.22）。
    左右に余白を入れたので、`left:0;right:0` のままだと**欄だけ取り残される**。
    `st.offsetLeft + st.offsetWidth/2` に置く（実測：絵の中心との差 0px）
  - **拡大・縮小の中心は、マウスの先**（v21.20。本人の指示）。
    - 前は `scrollLeft` の倍率計算（`(scrollLeft + px) * k - px`）で寄せていた。
      これはゴムシートでは**左右だけ効かない**。拡大率で `#stagepad` の余白が変わるうえ、
      描き直しの最後に `shSideRoom()` が `scrollLeft` を置き直すため
    - 倍率の計算はやめ、**紙のどこを指していたか**（紙の幅・高さに対する割合）を覚え、
      描き直したあとに「その所がまたマウスの先に来る」よう寄せる。
      余白の付け方が変わっても必ず合う。`#stagewrap` では `shPanKeep()` も呼ぶ
    - 実測：200% から拡大・縮小を左上寄り／右下寄りで各 2 回、
      **ずれは縦横とも 0.4 画素まで**。紙の外を指していても（割合が −1.79 でも）合う。
      拓本補正でも同じく 0.4 画素まで
  - **どの画面も、いつでも左右に動かせること**（v21.19／v21.21 で 3 画面／
    v21.22 で拓本補正も。本人の指示
    「上下に動かすのは、マウスのセンターを押し込んで…左右も同じようにして」）。
    - まん中ボタンのドラッグは**はじめから左右にも効いていた**。効かなかったのは
      **動かせる余地が無かった**から。紙は 90mm × 700mm の細長いもので、
      ふつうの拡大率では窓より狭く、左右に 1px も動かせない
      （実測：100%・400% とも 0px。900% でやっと 212px）
    - `#stage` を `#stagepad` でくるみ、その**左右に余白**を入れた。
      余白 ＝ 窓の幅 − 紙の幅 ÷ 2。動かせる量が**いつも窓 1つぶん**になる
      （実測：100%／200%／400%／900% どれでも 836px）
    - **余白はスクロールする側（`#stagewrap`）に付けてはいけない。**
      `clientWidth` は自分の余白を含むので、余白を `clientWidth` から決めると
      ふくらみ続ける（実測：24px → 10965px まで暴走した）
    - 左右の余白が同じなので紙はまん中のまま。ただし「まん中」は `scrollLeft` が 0 では
      なく**動かせる量の半分**。描き直すたびに『まん中＋人が動かしたぶん』へ置き直す
    - 仕組みは 3 画面で共通（v21.21）。`ROOMS` に
      `stagewrap/bothwrap/demowrap` ＝ `#stagepad/#bothpad/#demopad` を並べ、
      `sideRoom(wid)`／`roomKeep(wid)` で扱う。人が動かした量は**画面ごと**に覚える。
      呼ぶのは `renderStage`／`renderBoth`／`renderDemo` の最後。
      **画面を増やしたら、ここにも足すこと**
    - 実測：ゴムシート 837px／校正 824px／確認資料 828px／拓本補正 823px 動かせる。
      どれも中ボタンで左へ 150px 引ける（右へ戻せる）。絵はまん中のまま（ずれ 0px）
    - ズームの寄せは、描き直しで絵が消えることがある（中身が無い校正・確認資料）ので
      **幅が 1px 以下なら寄せない**。0 幅で割るととんでもない所へ飛ぶ
  - **ホイールは拡大縮小だけ**（v21.30。本人の指示「ズームと Shift＋ホイールで
    左右移動は使いませんので削除して」）。v21.18 で足した
    「Shift＋ホイール＝左右」「タッチパッドの横すべり＝左右」は**外した**。
    左右に動かすのは、上下と同じく**まん中ボタン**（どの画面にも窓 1 つぶんの
    余白がある。`sideRoom()`）。実測：Shift＋ホイールでも横すべりでも横には動かず、
    まん中ボタンでは 140px 動く
  - **ZIP の取り込みも、日本語の名前を潰さないこと**（2026-09-14）。
    共有フォルダーに置く ZIP は `pair_<家名>_g0-3_<字>.zip`。
    取り込みは名前の日本語を `_` に潰していたので、
    **家名の字数と 行・番号が同じなら、別の案件どうしがぶつかって上書きし合っていた**
    （`pair_久ヶ山_g0-3_石` と `pair_田中山_g0-3_大` が どちらも `pair_____g0-3__`）。
    `_keep_name()`（`feedback` と同じ決まり）に替え、
    さらに **ZIP の中の `meta.json` の `key` を先に見る**ようにした。
    実測：直したあとは ぶつからない。`..` は `zip`、`../etc/passwd` は `_etc_passwd`
  - **拓本AI の絵の口は、日本語のフォルダー名を弾かないこと**（2026-09-13。本人の指摘
    「貯まったデータ① 読む（拓本）の文字の写真が表示されない」）。
    - ①の組は、この画面が `pair_<家名>_g0-3_<字>` という名前で送る（`takuKey()`）。
      `app.py feedback()` は `ord(c) > 127` を通すので、日本語のまま保存される。
      ところが絵を返す口 `img()` は ASCII だけの `SAFE` で見ていたので、
      **①の絵がぜんぶ 400 になって 1 枚も出なかった**。
      ②（合成）は `syn_00001` と ASCII なので出ていた
    - 直し：`_pair_dir(root, pid)`。名前の中身では決めず、
      **出来上がった道が root の中に居るか**で見る（`..`・`/`・`\` は弾く）。
      実測：`pair_久ヶ山_g0-3_石` は通り、`..`／`a/b`／`../etc` は弾く
  - **曲線の端の傾きは、となりの 1 点だけで決めないこと**（v21.17。本人の指摘
    「現トレースから字を作るで、このような傾きのエラーが出ました」）。
    - `traceCurveNodes()` は Schneider のやり方どおり
      `t1 = unit(run[1] - run[0])` としていた。Schneider は「なめらかに細かく
      採った点」を前提にしているが、こちらの元はます目の**階段**。
      角のすぐ先の 1 歩がななめだと、**そこだけ 45° の傾き**になる。
      3 次ベジエの形は端の傾きで決まるので、**まっすぐなはずの縁が大きくふくらむ**
    - 実測（合成した十字。ます 4／0.0835mm）：出の手が**弦から 44.6°**ずれ、
      縁が **7〜10 ます ふくらみ**、墨との一致 **79%**（まっすぐモードは 99%）。
      節点の位置は正しかったので、**位置を見ていても気づけない**
    - 直し：`runTangent(run, atStart, win)` で `win` ぶん離れた点まで見て向きを出す
      （角を見つけるときと同じ考え方）。run の 3 分の 1 より先は見ない
    - 直したあと：十字 **79% → 100%**、丸 100%、しずく（曲線＋角）100%
  - **解析をやり直す（消す）（v33.5。本人の指示
    「［拓本を解析］にリセットボタンを追加して。元に戻せなかった気がするので」）**
    - ［拓本を解析］の隣に［やり直す（消す）］。聞いてから消す
    - 消すのは **この拓本の見立て**だけ：1 字ずつの枠（手で直した列も）／列の構成／
      AI の墨／列ごとの字数
    - **消さない**：手本帳・ちがう帳・枠から覚えた 1 字の大きさ・較正・通す範囲・戒名の字幅
    - 聞く文に 消すものと残るものを数で出す
      （「枠 3 列・37 字（うち 手で直した 1 列）／列の構成 3 列…残る：手本帳 42 枚…」）
    - 実測：枠 2 列・構成・墨 → **ぜんぶ消える**／手本帳 1 枚・覚えた大きさ・較正 → **残る**
  - **枠を 足す・消す・割る・つなぐ（v33.4。本人の報告
    「文字枠がいつもうまくいきません」）**
    - これまで枠は **つまんで動かす・大きさを変える**しかできなかった。
      1 字ぶん足りない／かけらに枠が付いた／2 字が 1 枠／1 字が 2 枠 ── どれも直せず、
      **どれだけ AI が良くなっても 人が直しきれない**状態だった
    - ⑤ に 5 つ：［↑ 上に足す］［↓ 下に足す］［ここで割る］［下とつなぐ］［この枠を消す］
      - 足す枠の大きさ・間は **枠から覚えた 1 字の大きさ**（`learnBox`）を使う
      - 割る所は **墨のいちばん薄い行**（まん中 ±20% の中で探す）
      - つなぐと、グループ・決めた字は 上の枠のものを引き継ぐ
    - 直すと `boxFix` が立ち、**そのまま教師データ**になる（作り直しでも消えない／
      1 字の大きさも覚え直す）
    - 実測：3 字 → 割る 4 → つなぐ 3 → 足す 4 → 消す 3、手直し印も立つ
  - **拓本AI は このリポジトリの中にある**（`takuhon-ai/`。本人の問い
    「別リポジトリとはどういうこと？」に対する訂正）。
    `app.py`（API）・`unet.py`（墨のモデル。**出力 1 枚**）・`train.py`・
    `char_guess.py`（**字の見分けの下地が既にある**。ただし `app.py` に呼び出し口が無い）。
    サーバーは社内の GPU 機。学習を回すのはその機械の上
  - **手本帳を消さない仕組み／当て方を ずれに強く（v33.3。本人の報告
    「ちょっと精度がわるいです。また、学習したはずのデータが消えています」）**
    - **消さない仕組み**（`_tehSave`）：
      ・中身があったのに **0 枚で上書きする書き込みは断る**（前の中身を戻して知らせる）
      ・書く前に **1 つ前を控える**（`chokoku.tehon.bak`）
      ・⑤ に［1 つ前に戻す］（控えを足し合わせる）
      ・［手本帳を空にする］だけは `_clear` の印を立てて 0 枚にできる
    - **当て方**（`_tehIoUShift`）：ます目を 1 つも動かさずに数えていたので、
      かすれ・枠の切り方の差で 同じ字でも 0.2〜0.3 落ちていた。
      **±2 ます ずらして いちばん良い重なり**を採る（手本帳・ちがう帳とも）
      - 実測：かすれ＋枠ずれの「日」で **0.80**（当たりの線は 0.38）
    - **本筋の話**（本人の問い「AIを使った方がいいのではないですか」）：
      字を見分ける所は いま **64×64 の重なり比べ**なので、崩れた字に弱い。
      目印は 30 字ほどの**閉じた組**なので、**拓本AI 側に小さな分類器**を足すのが本筋。
      ただし別リポジトリの作り替えが要り、学習し直すまで効かない。
      当面は この画面の当て方を強くして しのぐ
  - **ちがう帳がフォントの読みに効いていなかった（v33.2。本人の報告
    「毎回同じ間違いをするのは、なぜですか。NGリストには入っています。同じ物が何枚も」）**
    - 真因：ちがう帳は **手本帳と見くらべるときだけ**効いていた。
      手本帳で当たらない字は **楷書のフォント**（`cgGuess`）で読むので、
      そちらは素通りし、**何枚 ちがうと教えても 同じ読みが出ていた**
      （印の丸が**灰色**なら フォントの読み）
      → `cgGuess` の候補からも、ちがう帳に 0.5 以上 似ている字を **外す**
    - 「同じ物が何枚も」：覚えるたびに 同じ形が積み上がっていた
      → **同じ形は 1 枚だけ**（覚え直しは置きかえ）。読み込み時に
        たまっている重複も ひとつにまとめる
    - ［この字を調べる］に「いまこの字は、手本帳ではなく **楷書のフォント**で
      読んでいます」を足した
    - 実測：同じ形を 3 回 ちがうと教える → ちがう帳 **1 枚**／
      同じ形を 3 回 覚えさせる → 手本帳 **1 枚**／
      フォントの候補から 十 が外れ、一・二 だけ残る
  - **ひと押し解析／大きさの持ち越し（v33.1。本人の狙い
    「理想は、拓本解析ボタンをクリックするだけで戒名や命日、といった構成を理解して、
      文字の長さや幅の設定を取得すること。学習が完了すれば個別の案件で、
      文字の枠を指定は行いません」）**
    - `rubAllAnalyze()`：④ の上の［拓本を解析（ひと押し）］。
      ① 列の構成を調べる（まだなら）② 列ぜんぶを AI に通して枠を作る
      ③ 手本帳で文字を見分けて 戒名／命日／俗名／年齢に分ける
      ④ 分かったこと（構成・字の大きさ・字送り・まとまりの字数）を 1 行で出す
      - 実測：「構成：上 3 列／枠：2 列・37 字／…字の大きさ 高さ 30.3mm・
        横 33.9mm・字送り 28.7mm／まとまり：戒名 37 字」
    - **覚えた 1 字の大きさを 案件をまたいで持ち越す**（`TEHON.box`。手本帳と同じ置き場＝
      ブラウザ＋SharePoint の控え）。新しい案件では それを出発点にし、
      ⑤ に「**前の拓本から持ち越した** 1 字の大きさ」と出す。
      その拓本で［この列の枠から覚える］を押せば置きかわる
      - 実測：覚えた 31.6mm → 案件の値を消しても **持ち越しで 31.6mm（印つき）**
    - **まだ案件ごとに要るもの**：較正（実測した文字全長）と 通す範囲。
      拓本AI の墨の良さは サーバー側の学習しだい
  - **手で教えたことを、作り直しても引き継ぐ（v33.0。本人の報告
    「違う文字の登録をしているのですが、同じ間違いを繰り返します。3回同じ登録をしています」
     「枠を変更し学習させ、再判定でいい結果も出たのですが、
       AI縁取りからやったところ、反映されていないところが多々あります」）**
    - 真因：`rubColPut` は **列をまるごと置きかえる**ので、AI 縁取りのたびに
      ・手で直した枠（`boxFix`）・手で決めた字（`pick`／`anch.hand`）
      ・手で付けたグループ（`gBy:"hand"`）が **消えていた**。
      同じ直しを何度もさせていたのは これ
    - 直し 3 つ：
      ① `rubColPut`：置きかえる前に、前の列の「手で教えたこと」を
        **いちばん近い新しい枠へ写す**（1 字ぶんより離れていたら写さない）
      ② `rubAllGo`：**手で直した枠の列は 作り直さない**
        （墨だけ新しくして、枠と決めた字はそのまま）
      ③ `rubAnchorGo`：**人が決めた読み（`anch.hand`）は消さない・上書きしない**。
        その字は見くらべを飛ばして、そのまま目印として使う
    - 実測：手直し列（枠の高さ 326px・決めた字「没」・手のグループ）と、
      枠は直していない列の決めた字「才」が、**AI 縁取りをやり直しても そのまま残る**
  - **大きさのばらつきを覚える／手で微調整（v32.9。本人の確認
    「同じ文字でも、数ミリの大きさ違いはあると思うのです。
      そのような場合、学習した枠は微調整できますよね」）**
    - 覚えるのは まん中の値だけでなく、**上下 1 割を落とした 小さい／大きい**も
      （`hMinMm` / `hMaxMm`）。`rubCharHSpread()` が **1.0〜1.4** の ゆるめ係数を返し、
      「2 字がくっついている」の見きわめ（`tall9`）と
      「つないでも 1 字ぶん」の上限を その分だけ ゆるめる
      - 実測：高さ 28・30・33 ます の列を覚える → 係数 **1.1**。
        少し大きい 1 字（33 ます）は **割られない**
    - ⑤ で **手で直せる**：高さ・字送りの mm 欄と ＋／− ボタン、［忘れる］。
      高さを直すと ばらつきの幅も同じ割合で動く。
      実測：＋ を押して 38.2mm → **39.2mm**
    - 青枠そのものは **墨から切る**ので 1 字ずつ大きさがちがってよい。
      覚えた値は「どこで割るか・どこでつなぐか」の**目当て**だと画面にも書いた
  - **枠そのものを学習の材料にする（v32.8。本人の問い
    「この枠を覚えるのは、学習してその結果が、枠の変更になればいいのですが。
      枠は学習されないのですか？」）**
    - v32.7 は「手で直した列は さわらない」だけで、**覚えてはいなかった**
    - `rubBoxLearn(L)`：その列の枠から **1 字の 高さ・横はば・字送り（mm）**を
      拾って `S.rub.learnBox` に覚える（拓本ごとに字の大きさが変わるので案件の中）
      - 枠をつまんで直し終わった時点で **自動で覚える**
      - ⑤ の［この列の枠から覚える］でも覚えられる（直さなくても、合っている列から）
    - 切り分け（`rubAiSplit`）は、**覚えた高さを いちばんの目当て**にする
      （無ければ 今までどおり ［戒名の字幅］）。
      「そのあたりの字送り」も、覚えた字送りと かけ離れていたら 覚えた方を使う
    - すき間の見きわめを **字の高さの 5 割 → 7 割**へ
      （実測：高さ 26 ます の「二」で すき間 14 ます＝54%。5 割では割れたままだった）
    - 実測：くっついた 2 字（高さ 14 ます× 2）が 覚える前は **1 かたまり** →
      覚えたあと **2 つに割れる**。「二」1 字・「三」1 字は 1 つのまま、
      ほんとうの「一 が 2 つ」は 2 つのまま
  - **平…日 が命日にならない／手で直した枠が消える（v32.7。本人の報告
    「枠を覚えません」「平で始まり日で終わる部分は命日になるルールが
      成立していません。平も日も認識されているのに」）**
    - **平…日 の真因 2 つ**：
      ① 年号（令平昭大）は **列の前半 4 割**にあるときしか採っていなかった。
        戒名と命日が 1 列に入っていると 平 が中ほどになり、外れる
        → **どこにあっても採る**
      ② `mark9` の歯止め「戒名ほど大きい字に当たったら止める」が効いて、
        **1 字も塗られなかった**（命日の字が戒名と同じ大きさの拓本）。
        この歯止めは「おわりしか分からないとき」のためのもの。
        **はじめ（年号）と おわり（日）の両方が分かったら、
        その間はまるごと命日**（歯止めを使わない）
      - 実測：5 字の列で 平（1 字目）と 日（5 字目）を覚えさせて手本合わせ →
        **1〜5 字目ぜんぶ命日**（前は 0 字）
    - **手で直した枠が消える**：［覚えたことで 再判定］は 墨から切り直すので、
      人がつまんで直した枠まで元に戻していた
      → 枠をつまんだ時点で `it.byHand` / `L.boxFix` を立て、
        **再判定では その列の枠をさわらない**（「手で直した枠なので そのまま（20 字）」）。
        作り直したいときは ④［この行を見立てる］（押すと印が外れる）。
        ⑤ にも「この列の枠は手で直してあります」と出す
  - **番号の無い列に よその墨を渡していた／登録窓も よその墨を見ていた
    （v32.6。本人の報告「再判定をしても3列中1列しかしない」
      「学習させて保存しましたとでるが、覚えた一覧は増えていない」）**
    - `rubBandOfLine` は **`!L.aiId`（番号を控えていない列）なら、
      いま画面にある墨をそのまま返す**決まりだった。
      v32.3 より前に `aiUrl`・`aiId` を失った列がこの道に入ると、
      **となりの列の墨**を受け取る。その墨で切り分けると 1 字も見つからず
      （実測：帯 67〜430 に よその墨 59068 ます → かたまり **0 個**）、
      「切り分けできず」で **その列だけ直らない**
      → **番号か 絵の場所が合ったときだけ**使い回す
    - `rgSave`（学習に登録）も `rubItemInk`（＝いま画面の墨）で手本帳に入れていたので、
      **ほかの列の字を登録すると 黙って覚えそこねて**いた
      → `rubItemInkOf(L, it)` に直し、入らなかったときは
        「手本帳には入れていません（…なので）」と理由を出す
    - **墨の控えが無い列は、再判定のときに その場で AI に通し直す**
      （古い案件を救うため。通せなければ その列だけ飛ばす）
    - 実測：1 列目の墨を消した状態で再判定 →
      「枠を作り直しました（2 列／**1 列は AI に通し直しました**）。
        1列目 18 字 → 19 字｜2列目 19 字 → 19 字」、墨も両列に戻った
  - **一覧の更新／覚えたらその場で出す（v32.5。本人の指示
    「覚えた一覧に更新ボタンをつけ、また、登録後に追加表示できるようにして」）**
    - 見出しに ［⟳ 更新］（手本帳を読み直して並べ直す）
    - `_tehSave()` の中で、**一覧が開いていれば その場で並べ直す**
      （覚えさせる・ちがう・捨てる・サーバーから戻す、どれでも増減が見える）
    - **いま覚えた 1 枚**（`_tehLast`）は **黄色いふち**＋「いま覚えた」と出し、
      その字の札を いちばん前に置く（10 分たてば ただの札に戻る）
    - 実測：一覧を開いたまま「十」を覚えると、**絵が 1 → 2 枚**に増え、
      先頭が「十」・「いま覚えた・比 1.00」と出る
  - **覚えた一覧（v32.4。本人の指示
    「学習済の一覧を見れるようにできれば、他に影響があっても判断つくのでつくって」）**
    - ⑤ の［覚えた一覧を見る］で右側の板（`#tehWin`。`[hidden]` の
      `display:none !important` を忘れずに）
    - 字ごとの札に、覚えている形を **56px の絵**で並べる（`_tehDraw`。
      64×64 の 0/1 をそのまま描く）。ちがう帳の札は**赤ぶち**
    - 1 枚ずつ **✕ で捨てられる**。捨てたら その場で `_tehSave` → 描き直し
    - `cgNormFrom` は字づらを**正方形に引きのばす**ので、絵だけでは
      「一」と「十」の見分けが付かない。**もとの 比（たて÷よこ）**を添えた
    - 見出しに 枚数・字数・ちがう帳・KB・サーバーの控えの日時
  - **読み直した墨に url が無く、列の墨が消えていた（v32.3。本人の報告
    「過去に登録しているのに墨がないといわれることが多いです」
     「覚えたことで再判定をすると、3列あると 1列しか判断せず、
       他の列は墨が無くなっています」）**
    - 根は 1 つ：`rgBandFromUrl`（控えの墨を読み直す所）が
      **`{map, mw, mh, sc}` しか返していなかった**
      - `RUB.aiBand` に入れて見立て直すと、`rubLineTakeAi` が控える
        **`aiUrl` が空**になり、**その列の青い墨が消える**。
        次からは読み直すこともできない → 再判定で 1 列しか残らない
      - `geo` が無いので `rubBandOfLine` が「別の向きの墨」とみなす取りちがえも
      → `url` / `geo` / `id` / `fromUrl` を持たせ、`rubBandOfLine` は
        **url が同じなら使い回す**ようにした
    - もう一つ：`rubItemInk` は **いま画面にある墨**しか見ないので、
      ［拓本ぜんたいを通す］のあと（最後の列の墨しか残らない）に
      **ほかの列の字をえらぶと「墨が読めません」**になっていた
      → `rubItemInkOf(L, it)`（その列の控えを読み直してから取り出す）を作り、
        ［この字として覚えさせる］［これはちがう］［この字を調べる］を それ経由に
    - 実測：2 列通したあと **1 列目の 2 字目を調べられる**（墨 22%）／
      再判定のあとも **両方の列に墨（aiUrl）が残り**、両方とも作り直される
  - **覚えても変わらない → 見ていなかった／調べる手立て（v32.2。本人の報告
    「覚えさせて、再判定しても、変わらないのですが／どうすれば調査できますか」）**
    - **真因の第一候補**：`rubAnchorGo` が 1 字ずつ見るのは
      **はじめ 3 字・おわり 8 字・まん中 6 か所だけ**だった
      （フォントぜんぶ＝数千字との見くらべが重かったときの名残）。
      そのため **列のまん中で覚えさせた字は、いつまでも見られない**
      （30 字の列なら 13 字が 1 度も見られていなかった）
      → **手本帳があるときは、ぜんぶの字を見る**（手本帳は数十枚なので軽い）
    - **調べる手立て**：⑤ に［この字を調べる］（`rubProbe`）。えらんだ 1 字について
      ・墨の % と 字づらの たて÷よこ
      ・手本帳の上位 5 つ（字・点・そのままの点・比・ちがう帳の点）
      ・当たりの線 0.38 に届いているか、届かない理由（比がちがう／ちがう帳が近い／
        そもそも似ていない）と、次に押すべきボタン
    - 実測：「二」の墨を調べる → 手本帳 0 枚なら「空です」、
      覚えたあとは「二 1.00（そのまま 1.00／比 0.87）→ 読めます」
  - **［覚えたことで 再判定］（v32.1。本人の問い
    「おしえた後、再度確認のために、再判定させたい場合はどうすればいいのですか」）**
    - これまでは ②枠の作り直しに **AI へ通し直す**しかなかった（遅い）。
      けれども **前に通した墨は列ごとに残っている**（`L.aiUrl`）ので、
      それを使えば AI 抜きで作り直せる
    - `rubRejudge()`：列ごとに `rubBandOfLine(L)` で前の墨を戻し、帯を その列に合わせ、
      `rubLineTakeAi()` で **枠を切り直す** → そのまま `rubAnchorGo()`（文字で見分ける）
    - 手で描いた列（`src:"hand"`）は さわらない。帯・較正の線は元に戻す
    - 手本がまったく無いときは、**枠の作り直しだけで止める**
      （そうしないと「手本がありません」の知らせで、作り直しの結果が消える）
    - 知らせ：「枠を作り直しました（3 列）。1列目 8 字 → 7 字｜…」
  - **切り分けの不安定さ（二＝一が2つ問題）（v32.0。本人の報告
    「墨は完璧なのに、例えば二という漢字が一が2つ並んだと判断されたり、
      正確に判断されたり不安定なのです」）**
    - 真因：`rubAiSplit` の ①「1 字の中の空きをつなぐ」は
      **そのあたりの字送り**と比べている。ところが かたまりが少ないと
      **字送りそのものが「二」の横棒どうしの間隔**になってしまい、
      つなぐ判断ができない（実測：「二」が 1 字だけの帯で **2 つに割れた**）
    - 直し 2 段：
      ① **漢字はだいたい四角い**（高さ ≒ 字幅）。字幅の半分にも足りない
        **平たいかけら**が、字幅の半分より近くに並び、つないでも
        字幅の 1.25 倍に収まるなら **1 字**とみる
        （つなぐ相手は平たいこと。つないだ側は 0.95 倍まで許す＝「三」のため）
      ② **手本帳に当ててみて、つないだ方が似ていれば つなぐ**
        （「二」を覚えさせてあれば 一＋一 より 二 の方が似る）。
        ＝ **青枠の切り分けも、覚えさせるほど確かになる**
    - 実測：「二」1 字だけ → **1 つ**（前は 2 つ）／「三」1 字だけ → **1 つ**／
      ほんとうに「一」が 2 つ（字送りぶん離れている）→ **2 つのまま**
    - irr.json：1〜3 列目が 28/25/18 字・字送り 18.9/22.0/30.8mm →
      **21/21/16 字・26.7/26.9/32.9mm**（そろった）
  - **満杯のときの減らし方（v31.9。本人の確認
    「これは案件ごとの使用ではなく、他の案件でも判断材料として使用できますよね」）**
    - 答え：**そのとおり**。v31.5 で手本帳を案件の外（ブラウザの持ち物）へ移し、
      v31.7/31.8 で SharePoint の共有フォルダに控えるようにしたので、
      **どの案件・どの拓本でも同じ手本帳で判じる**
    - ついでに見つけた不具合：満杯（400 枚）のときの減らし方が
      「その字に 1 枚でもあれば その字から減らす」だったので、
      **はじめての字を覚えるときは減らさず**、400 枚を超えて増え続けた
    - 直し：**いちばん枚数の多い字から 1 枚減らす**（もう じゅうぶん覚えている字）
    - 実測：50 字 × 8 枚＝400 枚（273KB）の満杯で、はじめての字「鶴」を覚えても
      **400 枚のまま・鶴は入る**
  - **控えの置き場を指定のフォルダに（v31.8。本人の指示
    「業務アプリ、アプリ使用フォント、内に拓本文字分析というフォルダを作って
      そこに保存して」）**
    - `/業務アプリ/アプリ使用フォント/拓本文字分析/拓本の手本帳.json`。
      フォルダが無ければ作る（`tehonDirId`。「拓本学習データ」と同じやり方で
      `fontDirId(true)` の下に作る）
    - 実測（にせ SharePoint）：フォルダを作って PUT → 置き場は
      **/業務アプリ/アプリ使用フォント/拓本文字分析/拓本の手本帳.json**、
      空にして［サーバーから戻す］→ **1 枚に戻る**
  - **手本帳を SharePoint に控える（v31.7。本人の指示
    「ブラウザではなくなる可能性があるので、SP等のサーバーにバックアップしてほしい」）**
    - 置き場は **工事フォルダではなく いちばん上のフォルダ**（`SP.cfg.rootPath`）の
      `拓本の手本帳.json` 1 つ。手本帳は案件ごとではなく **会社ぜんぶで 1 つ**の持ち物
    - `rubTehonBackup()`：**読んで → 足し合わせて → 書く**ので、
      別のパソコンで覚えた分を消さない。`rubTehonRestore()`：サーバーの分を足す
    - ⑤ に［サーバーに控える］［サーバーから戻す］。控えた日時も出す
    - **案件を工事フォルダに保存すると、ついでに控える**（`spSaveProject` の中。黙って）
    - 実測（にせ SharePoint）：控える → サーバーに 一×1＋日付／
      こちらを空にして戻す → 1 枚／
      サーバーに 十 が増え こちらが 二 を覚えた状態で控える → **一・二・十 の 3 つ**
  - **押すたびに 判じ直す／容量を出す（v31.6。本人の指示
    「訂正して、文字で見分けるボタンをもう一度押すことで、
      最新の状態で判別する様にしてほしい」「置き場所はブラウザで容量は大丈夫ですか」）**
    - 押し直しても **前の回のグループが残ったまま**で、
      「命日のあとの数字＝年齢」のような**後づけの決まりが効かなかった**
      （`allFree`（まだ g が付いていない）を見ているため）
    - 直し：グループに **誰が付けたか**の印（`gBy`）を持たせ、
      ［文字で見分ける］の頭で **手本合わせが付けたもの（`anch`）だけ消す**。
      ⑤ のボタンで**人が手で付けたもの（`hand`）は残る**
    - 容量：1 枚 683 字＝約 0.7KB。実測 **264 枚で 180KB**（400 枚で約 273KB）。
      ブラウザの置き場は ふつう **5MB** なので 1〜2 割どころか **2% 未満**
    - ⑤ に「手本帳：12 枚／ちがう帳：3 枚／**9.2KB**（上限 400 枚・約 280KB。
      置き場は 5MB ほど）」と出す
    - `persist()` の控えからは手本帳を外した（`chokoku.tehon` と 2 重に置かないため）
  - **手本帳は 案件をまたいでためる（v31.5。本人の問い
    「これは、AIの縁取り等の学習とは別で、覚えられるのですか？」）**
    - 答え：**まったく別物**。
      ［学習に登録］＝ 拓本AI サーバーへ絵の組を送り、モデルを鍛えるもの。
      手本帳＝ **この画面の中だけ**で、字の形と名前を並べて見くらべる覚え書き
    - ただし v31.4 までは `S.rub.tehon`（＝**案件の中**）に置いていたので、
      **別の拓本を開くと覚えたことが消えて**いた。字の形は拓本がちがっても
      使えるので、**画面の持ち物**（`localStorage("chokoku.tehon")`）に移した
    - 案件ファイルにも写しを入れ、案件を開いたときは **足し合わせる**（消さない）。
      `_tehLoad` / `_tehSave` / `_tehMergeFromCase`
    - 実測：覚える → 画面を読み直す → **残る**／別の案件（shibata.json）を開く → **残る**
  - **ちがう帳（v31.4。本人の問い「登録する必要のない漢字を間違えた場合、
    正しい物を教えるということはどのようにすればいいのですか」）**
    - 戒名の字などは、名前を覚えさせても手本帳が太るだけ。
      **「この形は ○ ではない」とだけ**覚える入れ物を足した
      （`S.rub.tehonNg = { "十": [...] }`。1 字 12 枚まで）
    - ⑤ の［これはちがう］（字をえらぶと押せる）。名前は要らない。
      押すと ① ちがう帳に入れる ② 読み違いの元だった手本を捨てる
      ③ その字の読みと印を消す
    - **しきい値では決めない**のが肝。「ちがう」と教えた形は ほんものの字とも
      よく似ている（実測：ちがう形と ほんものの十で **0.71**）ので、
      0.55 で切ったら **ほんものの十まで読まなくなった**
      → **手本（○）と ちがう帳（×）の、どちらに近いか**で決める
      （`iou <= ngV` なら その字としては読まない）
    - 実測：ちがう形 → **十（0.71）→ 読まない**／ほんものの十 → **十（1.0）のまま**
  - **手本帳の字は ぜんぶ見くらべ相手に／手本を増やす（v31.3。本人の問い
    「誤って判定された字を間違っていると教えたほうがいいのでしょうか？
      たとえば、子を十と間違っている物があります」
      ＋「新たに、月、平、令、昭、俗、名、歿、行、士、姉、女を追加して」）**
    - **答え：まちがいを教える必要はない。正しい字を教えるだけでよい**
      （`rubTeachHere` が、そのとき読み違いの元になった手本を捨てる）
    - ただし **穴があった**：見くらべ相手が「目印の字」（令平昭大明治／日没歿／
      行享／才歳／数字）だけだったので、**目印でない字（子など）は
      覚えても相手に入らず**、その墨はまた「十」に当たっていた
      - 実測：子の形 → **「十」（IoU 0.73）**。子を覚えても 目印だけなら **まだ「十」**。
        手本帳の字をぜんぶ相手にすると **「子」（IoU 1.0）**
    - 直し：`tehWant = 目印の字 ＋ 手本帳にある字ぜんぶ`
    - 手本を追加（本人の指定）：
      **年・月**（`mMid`。命日のまん中）、**俗・名**（`zHead`。俗名の目印）、
      **士・姉・女**（`kTail`。戒名のおわり＝居士・大姉・信女）。
      平・令・昭は `mHead`、歿は `mTail`、行は `nHead` に既にあった
    - 使い方も足した：
      ① 年・月が当たれば その前後を命日とみる（年号も日も かすれたとき用）
      ② 士・姉・女が当たれば そこから上へ **戒名**（大きい字なので
        「小さい字だけ」の歯止めは使わず、小さい字に当たったら止める）
      ③ 俗・名が当たれば そこから下を **俗名**
    - 早押しボタンを 32 字に（一〜百・年月日・才歳没歿・令平昭大・行享・
      士姉女信居・俗名・子）
  - **読み違いを直して覚えさせる／十と一を見分ける（v31.2。本人の報告
    「十を一と勘違いしているところなど多々ありました。
      修正や学習させるやり方はありますか」）**
    - **十 を 一 と読む真因**：`cgNormFrom` は字づらを**正方形に引きのばして**
      そろえるので、**平たい「一」と 四角い「十」の区別が消える**。
      実測：「一」の手本しか無い所へ「十」を見せると、**「一」（IoU 0.18）と読んだ**
    - 直し：手本に **字づらの たて÷よこ**（`_tehRatio`）も覚え、
      見くらべで効かせる（比が 2 倍ちがえば見ない／近いほど点はそのまま）。
      実測：同じ場面で **読まない（null）** に変わり、誤読しなくなった
    - **直して覚えさせる**（`rubTeachHere`）：⑤ に「読み違いを直す」欄を足した。
      字をえらんで **一二三四五六七八九十才歳没日** の早押し、または欄に 1 字入れて
      ［この字として覚えさせる］
      ① その字の読みを直す（印も直る）
      ② その墨を手本帳に入れる
      ③ **読み違いの元になった手本を捨てる**（この墨と 0.5 以上似ている、ちがう字の手本）。
        まちがいの元が残っていると、正しい手本を足しても また同じ読み違いをする
    - 実測：「十」を覚えさせたあと、同じ形を **「十」（IoU 1.0）** と読む
  - **読めた字に小さな印（v31.1。本人の指示
    「判別できた文字を記しして頂ければと、できていない文字は登録するので
      小さいマークでお願いします」）**
    - `rubAnchorGo` が当たった字を `it.anch = { ch, by:"teh"|"fnt", iou }` に控える
    - 絵では、枠の**左上に小さな丸**（枠の短い方の 5.5%）と、読めた字を小さく出す。
      **緑＝拓本の手本帳／灰＝楷書のフォント**。印の無い字＝読めなかった字
    - 知らせにも 列ごとに「読めた字 4 / 8（1=没・5=七・6=十・7=二）」と出す
    - 見立て直すたびに前の印は消す（古い印が残らないように）
  - **拓本の手本帳（v31.0。本人の話
    「フォントは楷書なので、学習している物が使えればいいのですが」）**
    - 手本合わせは フォントの字と見くらべていたが、石に彫ってあるのは
      楷書のフォントとは形がちがう。かすれた拓本では なかなか当たらない
    - `S.rub.tehon = { "没": ["<詰めた字づら>", …] }`。
      持ち方は フォントの手本と同じ **CG_N（64）×CG_N のます目**（`cgNormFrom`）
      なので、見くらべ（IoU）は同じものが使える。
      6 ビットずつ 1 文字に詰めて持つ（**1 枚 683 字＝約 0.7KB**）。
      1 字につき 8 枚まで、ぜんぶで 400 枚まで
    - 手本になるのは 2 つ：
      ① ［学習に登録］で登録した字（`rgSave`。サーバーに送るのと同時に覚える）
      ② ④ の候補から**人が決めた字**（`rubCandPick`）
    - `rubAnchorGo` は **手本帳を先に**見て、当たれば（IoU ≥ 0.38）その字とする。
      当たらなければ 今までどおりフォント。**フォントが無くても手本帳だけで動く**
    - ⑤ の欄に「拓本の手本帳：○ 枚（没×3・才×2 …）」と出し、空にするボタンも付けた
    - 検査：詰めて戻しても同じ／「十」「一」の手本を入れて見くらべ → どちらも正しく当たる
  - **グループ分けの精度（v30.9。本人の画面「この精度　何とかなりませんかね」
    ＝「没 七十二才」の列で、没は命日（緑）と当たったのに
      続く「七十二才」が戒名（赤）になっていた）**
    - 真因 3 つ：
      ① `rubAnchorGo` は 命日の末尾（日・没・歿）を **列の後ろ 3 割より後ろ**でしか
        拾わない。この拓本は **列の 1 字目が 没**（没／七十二才）なので拾えていた
        のは たまたまで、拾えない列もある
        → **8 字までの短い列は、どこにあっても拾う**
      ② 才・歳は かすれると手本に当たらない。当たらないと年齢にならない
        → **命日の末尾のあとに続く字は、戒名ではない**。数字が 1 つでもあれば
          （または残りが 6 字までなら）**年齢**とみる
      ③ 余った字の既定が「戒名」だった
        → その列に 命日／年齢があれば、余りは **俗名**（戒名にしない）
    - `rubGrpAuto`（位置で分ける方）も、**上下の境目を調べていない拓本では
      どの列も戒名**になっていた
      → 本人の経験則「戒名 30mm、命日・年齢はそれより小さい」を使い、
        **字幅が ［戒名の字幅］の 8 割より小さい列は戒名の列ではない**とみる
    - 実測（字 15mm・境目なしの列 4 本）：ぜんぶ戒名 → **ぜんぶ命日**
  - **［ぜんたい］は 紙の全幅にした（v30.8。本人の指摘
    「列の指定で、全体ボタンを押すと、帯が移動するのだが、
      このようになり、用紙の全幅ではないです」）**
    - ［ぜんたい］は `frameX()`（自動で見つけた**拓本の枠**）に合わせていた。
      本人の鉛筆拓では 枠 **51〜199mm**（紙は 0〜202mm）で、
      **字は 29mm から始まっている**＝ 字が枠の外。だから
      「ぜんたい」なのに紙の一部しか帯にならなかった
    - これが一連の「右半分になる」の**大もとの一つ**。枠が字を切っていた
    - 直し：
      ① ［ぜんたい］は **0 〜 紙の右はし**。枠が紙より 1 割以上狭いときは、
        「枠は ○〜○mm と見つけてあります…オレンジの線をつかんで直してください」
        と知らせる
      ② `rubAllGo` の `okL/okR` も、**範囲を決めていないときは紙ぜんたい**
        （`rubArea()` は範囲が無いと枠を返すので、そこでも切れていた）
  - **字より細い列は、字の幅まで広げる（v30.7。本人の画面
    「範囲を指定せず、全体で実行したところ、下側のおかしくずれています。
      これも同じ問題ではないでしょうか」）**
    - 半分は同じ（帯の中を切る）問題だが、もう一つ別の原因があった
    - 切れ目さがしが返すのは「**いちばん濃い たての帯**」で、字の幅とは限らない。
      実測（本人の 20260919_拓本.jpg を［列の構成を調べる］）：
      列は **64〜84.8mm と 122.9〜143.6mm（どちらも 20.8mm）**。
      字は 30mm なので、**字の 3 分の 2 の幅**しか帯になっていない。
      その細い帯で窓を置くので、窓も 1 字の枠も**字の片側に寄る**
    - 直し：`rubAllGo` で、字幅（［戒名の字幅］既定 30mm）の 9 割より細い列は、
      **まん中を動かさずに 字幅まで広げる**（となりの列・枠は越えない）
    - 実測：帯 20.8mm → **33.1mm**、1列目が **17 字・字送り 30.7mm**、
      枠の横 55〜91.4mm（列のまん中に乗った）
  - **測る所も帯の中を切っていた（v30.6。本人の画面「ここまで来ましたがまだです。
    念のための確認ですが、範囲を決める前の帯のラインと半分になってしまった
    ラインが同じ場所にありそうです」）**
    - **本人の見立てが当たっていた。** その線は **`frameX()`（枠）の左端 51mm**。
      この拓本の枠は 51〜199mm で、範囲 29〜65mm の左 2/3 が枠の外にある
    - 真因：`rubBandLimit()` が `out.L = max(0, frameX().x1, 範囲.x1)` としていた。
      v29.6 で「**絵に描く所**」は帯より内側に切らないよう直したが、
      **測る所**（`rubBandWide` → `rubAiMask` → 1 字の枠）はこの線引きのままで、
      **51mm より左の墨が、枠を作るときに落ちて**いた
    - 実測（本人の拓本・範囲 29〜65mm）：1 字の枠が どれも
      **横 50.6〜57.8mm**（7mm の短ざく）×6 個 → 直したあと
      **横 28.9〜64.9mm**（帯ぜんぶ）×3 個・字送り 22.9mm
    - 直し：`rubBandLimit()` の最後で **`out.L ≤ 帯の左`／`out.R ≥ 帯の右`** を保証。
      切ってよいのは帯の外（のりしろ）だけ
    - あわせて **くっついた 2 字を、経験の字の大きさでも割る**ようにした
      （かたまりが 2 つしか無いと「字送りの 1.45 倍」では割れない。
        実測：仁＋道 が 48mm の 1 かたまりのままだった。
        いまは ［戒名の字幅］の 1.5 倍を超える背丈も 2 字とみる）
  - **［拓本ぜんたいを通す］の側にも、同じ 3 つの狂いがあった（v30.5。本人の報告
    「何も変わっていませんが」＝ 板の計画は 窓 45mm・窓の横 25〜71mm と
      正しくなったのに、通す所は前と同じだった）**
    - 板（`rubAiPlan`）は直っていたが、**通しているのは `rubAllGo`**。
      そちらに 3 つ残っていた（本人の 20260919_拓本.jpg で実測）：
      ① `rubColsWanted()` が「範囲そのものが列」と決めた直後に、
        **`rubColMenu()`（調べた列）で上書き**していた
        → 範囲が字 1.8 つぶんより細いときは、上書きしない
      ② `okL = max(0, frameX().x1, …)` の**枠**が、範囲 28〜68mm の帯を
        **枠の左端 51mm から右へ押し出して**いた（v30.2 と同じ狂いが こちらにも）
        → 列が枠の中にあるときだけ枠に合わせる
      ③ 「広すぎる列は字送りのぶんに絞る」が、**字送り 4.9mm**（ほんとうは 27mm）
        という読み違いを物差しにして、帯を 41〜77mm へ動かしていた
        → 字 1 つの半分より狭い字送りでは絞らない／
          どう動かしても帯は その列（範囲）の中に収める
    - 実測：範囲 28〜68mm・調べた列 49〜65mm・枠 51〜199mm のとき、
      通す帯は **41.3〜77.4mm → 28〜68mm**（範囲ぴったり）
    - あわせて、1 列も読めなかったときは「画面に見えている枠は、
      前に通したときのものです」と知らせるようにした
  - **帯の幅を いちばん上の見当に戻した（v30.4。本人の報告「あまりかわらない」）**
    - 板の数字：帯 29〜68mm・**窓 20mm**・窓の横 **49〜69mm**・
      字の見当 39mm（帯の幅）・「※ これは前に通したときの窓です。
      いまの決め方なら 窓 45mm」
    - v30.3 の歯止め（字幅の 半分〜1.8 倍）でも、**17mm と測れば 窓 20mm** になり、
      転がり落ちを止めきれていなかった
    - 考え直し：AI の測りを**いちばん上**に置いたのは
      「1 列しか選べず、帯の幅から字幅を決められない」ときのための逃げ道だった。
      **v29.9 から 範囲そのものが列**なので、帯の幅は「人がこの 1 列だと決めた幅」
      ＝ 字幅 ＋ のりしろ で、いちばん確かな手がかり
    - 直し：**① 帯の幅（12〜60mm のとき）② AI の墨から測った字
      ③ 墨の幅（実測）④ 字送り／戒名の字幅** の順に。
      AI の測りの歯止めも 字幅の **0.7〜1.5 倍**へ狭めた
    - 板：**いまの窓と 25% 以上ちがう控えは出さない**（古い絵を見ながらでは
      直っているか分からない）。出さない理由も見出しに書く
    - 実測（本人の 20260919_拓本.jpg・帯 31〜68mm）：
      AI が 17mm と測っていても 28mm と測っていても、窓は **42.6mm（帯の幅）**・
      窓の横 **28.2〜70.8mm**（帯をぜんぶ覆う）で変わらない
  - **AI の測りが小さい方へ転がり落ちる（v30.3。本人の報告「悪化しました」）**
    - 板の数字：帯 31〜68mm・**窓 13mm**・24 枚・**字の見当 12mm（AI の墨から測った字）**
    - 真因：v29.7 で入れた「AI の返した絵から字の大きさを測る」が、
      `rubAiPlan` の**いちばん上の見当**になっている。
      いちど小さく測ると → 窓が小さくなる → AI は**字のかけら**しか見ない →
      また小さく測る、と **小さい方へ小さい方へ転がり落ちる**
    - 直し：**拓本の字幅（［戒名の字幅］。既定 30mm）の 半分〜1.8 倍**の中だけ信じる。
      外れた測りは 採らず、知らせに「○mm でしたが…使いません」と出す。
      しまうときも同じ歯止めをかける
    - 実測（本人の 20260919_拓本.jpg・0.08534mm/px・帯 31〜68mm・枠 51〜199mm）：
      12mm と測った状態でも 窓は **42.6mm（帯の幅）**・窓の横 **28.2〜70.8mm**
      （帯をぜんぶ覆う）。28mm と測ったときは 窓 **32.2mm（AI の墨から測った字）**
      ＝ まともな測りは今までどおり効く
  - **窓は かならず帯の中に置く（v30.2。本人の画面
    「青枠は多少広がりましたが、AIに送る画像が右半分のみになりました。
      青枠も左側は外れています」）**
    - 板の数字が答えだった：**帯 29〜67mm／窓の横 51〜81mm／枠 51〜199mm**
      （＋「枠に押されたので、帯のまん中へ戻しました」）
    - 真因：`frameX()`（拓本の左右の線）に窓を収める clamp。枠は
      「紙のふちに窓を取られない」ための目安でしかないのに、
      **人の描いた範囲が枠より左にある**と、窓を枠の中（51mm 以降）へ
      押し出していた。v28.9 の「6 割重ならなければ戻す」も、
      墨の重心が右に寄っていたので効かなかった
    - 直し 3 つ：
      ① **枠に合わせるのは、帯が枠の中にあるときだけ**（`inFr`）
      ② 墨の重心に寄せたあと、**窓を帯の中へ収める**
        （帯が窓より広い → `[q.x1, q.x2-side]` に clamp／
          帯が窓より細い → 帯のまん中）
      ③ 横に並べる見きわめを **窓の 1.25 倍 → 1.05 倍**へ
        （帯 38mm・窓 30mm のような「少しだけ広い」帯が 1 枚のままで、
          左のはしが通らなかった）
    - 実測（枠 400〜1050・帯 225〜525 ＝ 枠より左にはみ出す範囲）：
      窓 345px なら **203〜548**（帯のまん中。枠に押されない）、
      窓 230px なら **202〜432 と 340〜570 の 2 枚**で帯をぜんぶ覆う
  - **［拓本ぜんたいを通す］が mm の物差しを壊していた（v30.1。本人の画面
    「範囲を指定して、AI縁取りを実行した結果がこれです」＝ 枠が 2 つだけ、
      板には「前の通しの窓が **4mm**（12〜60mm の外）なので出していません」）**
    - **いちばん根の深い不具合**。v21.68 から `rubAllGo` の頭で
      `S.rub.calTop / calBot` を**範囲の たて に付けかえて**いた
      （「あとで元に戻すから較正はくずれない」という但し書き付きで）
    - ところが **`mmPerPx() = realLen ÷ (calBot - calTop)`**。
      付けかえている間は **mm の物差しそのものが変わる**ので、
      通している最中の mm 判断（窓 12〜60mm の歯止め、字の見当、戒名の字幅、
      10mm 未満の細い列を落とす…）が**ぜんぶ別の縮尺**で行われていた。
      範囲が短いほどひどく、実測では範囲 71mm ぶんで **窓 4mm**
      （＝ 12mm の歯止めが、真の 2.6mm ぶんの px で効いていた）
    - これが「窓が 5mm／7mm／9mm になる」「細い列になる」と、
      v28.8 以降ずっと追いかけていた症状の**大もと**。
      ［この帯だけ通す］では起きず、［拓本ぜんたいを通す］＋範囲のときだけ出る
    - 直し：**較正の線にはさわらない**。たての範囲は `rubBandQ()` が
      範囲と重ねて出すので、付けかえはもともと要らなかった
    - 実測（範囲 たて 900px）：通す前・最中・あとの物差しがすべて
      **0.12748 mm/px** のまま。窓は **29.3mm**
  - **人の決めた帯の中は切り落とさない（v30.0。本人の画面
    「AIには文字の左側は通っているのに、拓本上は塗られていません。
      AIの結果ではしっかりと認識しているのに、なぜ右半分になるのか」）**
    - 画面：AI の返事には **仁 がまるごと**出ているのに、拓本の上では
      **右半分（二）だけ**が青い。にんべん（亻）が消えていた
    - 真因：`rubAiTrimCols`。「字の列は 1 列たどると 字の数だけ**切れ目**があるが、
      地の塊はのっぺり続く」という見分け方で、**切れ目の多い所だけ残す**。
      ところが **たて 1 本の画（亻・丨）は切れ目が 1 つしかない**ので、
      字の一部なのに「地の塊」とみなされて落ちていた。
      はらいを救う伸ばし（芯の 4 割まで）も、離れた にんべんには届かない
    - 直し：`rubAiTrimCols(gmap, aw, ah, keepL, keepR)` に**帯の左右**を渡し、
      **帯の中は決して切らない**（切っていいのは、はみ出した のりしろだけ）
    - 実測（たて 1 本の画＋切れ目の多い列を作った検査）：
      左の画の墨 **0 ます → 3200 ます**（残るようになった）
  - **範囲そのものを列として通す／板に理由を出す（v29.9。本人の画面
    「青い枠出てきますし、AI縁取りを実行しても、AIに送る絵を見ると
      まだ通していませんと出ます」）**
    - 画面の知らせ：「1列目 帯 419〜455mm（幅 36.2mm）字送り 23.9mm
      帯の中の墨 **0.9%** 切り分けできず」。絵では、人の描いた範囲（緑）の
      **右半分だけ**に水色の窓が置かれていた
    - 真因：v29.8 で「範囲の中の列だけ」に絞ったが、**調べた列は拓本ぜんたいから
      見つけたもの**なので、範囲の中にあっても 範囲とずれる。
      その細い列を帯にしたので、墨が 0.9% しか出なかった
    - 直し：**範囲が字 1.8 つぶんより細ければ、範囲そのものが その列**
      （`rubColsWanted` の頭。字幅は `RUB.aiChar` → `_rubCharSide()` の順で見る）。
      実測（irr.json）：範囲 520〜720 → 通す列も **520〜720**、18 字
    - 「まだ通していません」の真因：`rubAiPreview` は計画が立たないと
      **`msg` を出してそのまま帰って**いたので、板には**前に開いたときの中身**が
      残っていた。直し：板にも「《窓を決められませんでした》理由」を出す。
      控えを使わなかったときも、その**理由**を見出しに出す
    - 水色の枠は **墨が 50 ます以上あるときだけ**にした（0.9% のような
      「ほとんど出ていない」ときは、結果ではないので出さない）
  - **範囲を決めたら、その中だけを相手にする／窓を横にも並べる（v29.8。本人の報告 4 つ）**
    - ①「AIの結果は左端は塗られている、範囲内なのに色が抜けているのはなぜ」
      - 真因 その 1：窓は字 1 つぶん（`side`）で、**帯のまん中に 1 つ置くだけ**。
        範囲が窓より広いと、**窓の外は AI に渡っていない**
      - 真因 その 2：`rubAiTrimCols` が「行から外れた墨」を落とすので、
        **いちばん濃い 1 本だけ残り、範囲の左はししか墨が出ない**
      - 直し：帯が窓の 1.25 倍より広ければ **横にも 6 割ずつずらして窓を並べる**
        （`PL.xs`）。多すぎるときは重なりを 0.95 に減らし、それでも 90 回を超えたら断る。
        横に並べたときは `rubAiTrimCols` を**かけない**
      - 実測（範囲 600px を横 6 つに割った墨の量）：
        直す前 **15.9 / 2 / 0 / 0 / 0 / 0 %** → 直したあと **22.9 / 30.7 / 26.5 / 27.4 / 37.9 / 24.9 %**
    - ②「範囲に指定すると 列の指定で１を選んでも色が変わらないが、画面上は変化している」
      ③「AI縁取りを実行しても AI には送信されず、［AI に送る絵を見る］の
        ［この帯を通し直す］をすると結果が出る」
      - 真因（②③ 同じ）：`rubColsWanted()` は `S.rub.colsTop`（**拓本ぜんたい**から
        調べた列）をそのまま返していて、**通す範囲を見ていなかった**。
        `rubColMenu()` も同じ。実測：範囲 520〜720 なのに、
        列は 125〜420／434〜729／744〜999（＝範囲の外）を通しに行っていた
      - 直し：`rubColsWanted`・`rubColMenu`・`rubAllGo` の 3 か所で
        **範囲の中の列だけ**に絞る。範囲の中に調べた列が 1 本も無ければ
        **範囲そのものを 1 列**として通す
      - 実測：範囲 520〜720 で［AI 縁取りを実行］→ 1列目 帯 237〜273mm・**18 字**
    - ④「AI縁取りを実行すると結果が出ず、青の枠が表示される。
        これは AI の結果が出ないと表示してはいけないのでは」
      - 直し：**墨が 1 ますも無ければ水色の枠を出さない**（`RUB.aiBand.ink > 0`）。
        ［AI 縁取りを実行］を押した時点で、前の通しの墨と枠を**先に消す**
  - **字の大きさを、AI が返した絵から測るようにした（v29.7。本人の案
    「この文字は、選択範囲で1列しかないので、幅が決められないなら、
      AIに枠をつけてもらってから、判断したらいいのでは。
      AIにははみ出た画像を出しているわけだし」）**
    - それまでの見当の順：帯の幅 → 字送り → 戒名の字幅（30mm）。
      **1 列しか選べない拓本では、帯の幅が字幅の手がかりにならない**
      （実測：帯 67px＝**9mm**。窓も字の切れはしになり、0 列しか読めなかった）
    - 重ねたあとの墨（`RUB.aiBand.map`）は**帯で切ってある**ので、
      そこから測っても帯より広くならない。**測るのは AI が返した絵そのもの**
      （窓ぶん・帯からはみ出したまま）にした
    - `_rubShotChar(img, side)`：返事を 128×128 に落とし、明るさのまん中で切って
      **墨の量の 5〜95% が入る幅**（`_rubSpanPct`）を字の大きさとする。
      山さがし（しきい値で run を歩く）は画のすき間で切れて **3mm** になった（実測）
    - **ふちの墨**（外 10% の枠）も見る。0.3 を超え、かつ幅もたても窓の 8 割以上なら
      「**字が窓より大きい**」とみて、窓を 1.6 倍にして通し直す
    - 測った字と窓が 1.35 倍／0.74 倍より離れていたら、**その場で 1 回だけ通し直す**
      （`AIBAND_RETRY`）。`RUB.aiChar` は `rubAiPlan` の**いちばん上の見当**
    - 実測（irr.json・にせ拓本AI）：帯 67px（9mm）で通す → 返事から **28×28mm** と測り、
      次の窓は **32mm（AI の墨から測った字）**。
      ［拓本ぜんたいを通す］は 2列目 43字・字送り 10.9mm → **21字・23.4mm**、
      3列目 59字・9.9mm → **15字・27.3mm** に直った
  - **細すぎる列を［拓本ぜんたいを通す］でも落とす／報告を mm に（v29.7）**
    - v29.2 の「10mm 未満の列は読み違え」は `rubColsScanTop` の中だけで、
      `rubColsWanted()` 経由（ぜんたいを通す）には効いていなかった
    - 列ごとの報告（`S.rub.allRep`）も px のままだったので、mm に直した
  - **青い墨が範囲の右半分にしか出ない（v29.6。本人の問い
    「なぜこの青い線が範囲を指定するのですか？」）**
    - 真因：AI の墨を描く範囲（クリップ）は `rubBandLimit()` で
      「**となりの列へはみ出さない**」ように切っている。
      ところが列さがしが外れて **範囲のまん中に「となりの列」がある**ことになると、
      そこで切られ、**範囲の右半分にしか墨が出ない**
    - 直し：**人が決めた帯（範囲）より内側には切らない**
      （`cx1 = min(cx1, q.x1)`／`cx2 = max(cx2, q.x2)`）。
      のりしろ（20%）を広げる／狭めるのは今までどおり
    - 実測：範囲 130〜420 のまん中に にせの列（260〜300）を置いても、
      通した所は **130〜420**（範囲をぜんぶ覆う）
    - この画面の AI の返事は **仁・道・信 がきれいに出ていた**。
      鉛筆拓でも、窓さえ正しければ読める
  - **板の中から通し直せるようにした（v29.5。本人の報告
    「21枚送りましたの後に、4枚出しました／画像の方には明らかに細い水色の窓で
      結果が来ています」）**
    - 何が起きていたか：**前の通しは古い窓（細い）**。v29.4 で控えは出さなくなったが、
      **絵に残っている青い墨と水色の枠は、その古い通しのまま**。
      板は「いまの窓」で 4 枚、絵は「古い窓」で 21 枚ぶん、と食いちがって見えた
    - 直し：板の上に ［**この帯を通し直す**］（`#aiPrevGo`）。
      いまの窓でもう一度 AI に通し、そのまま返事つきで並べ直す。
      見出しにも「絵に残っている青い墨も古いものです →［この帯を通し直す］」と書く
    - 実測：古い控え（窓 5mm）のまま開く →「《これから送る絵（前の通しは古い窓…）》
      窓 28mm／9 枚」→［この帯を通し直す］→「《通したときの絵》窓 28mm／9 枚」
      **返事つき 9 枚**
    - なお、この画面の送る絵は **1 枚に 1 字（仁・道・信）** になっていた。
      窓の決め方（v29.0〜v29.2）は効いている
  - **本人の画面は古い版だった（v29.4。画面の見分け方）**
    - 画面の知らせに「**墨 18 ます**」と出ていた。v29.1 からは **mm²** で出すので、
      その画面は **v29.0 以前**。窓 5mm は、その古い版で通した控え
    - 直し：**控えの窓が 12〜60mm の外なら、控えは並べない**（いまの窓で出し直す）。
      見出しに「《これから送る絵（前の通しは古い窓なので出しません）》」と書く
    - **版の見分け**：画面の題（`document.title`）と左上の版表示。
      ほかに「墨 ○ ます」＝ v29.0 以前／「墨 ○mm²」＝ v29.1 以降、
      「px」が残っている＝ v29.0 以前
  - **「窓が 5mm になった」＝ 前に通したときの控えを見ていた（v29.3。本人の報告）**
    - v29.1 で **窓は 12〜60mm** に縛ってあるので、**いま決める窓が 5mm になることはない**。
      ［AI に送る絵］は **通したときの控え（`shots` と `plan`）があれば そちらを並べる**
      （v28.6）ので、**古い通しの窓**がそのまま見えていた
    - 直し：
      ① 控えの窓と いまの窓が **2 割以上ちがえば**、見出しに
        「※ これは前に通したときの窓です。いまの決め方なら 窓 ○mm」と出す
      ② 板の上に ［**いまの窓で出し直す**］ボタン（`AIPREV_FRESH`）。
        控えを使わず、いまの決め方で送る絵だけを並べる
    - 実測（控えの窓をわざと 5mm にして）：
      「《通したときの絵》…窓 5mm…※ これは前に通したときの窓です。いまの決め方なら 窓 28mm」／
      ［いまの窓で出し直す］→「《これから送る絵》…**窓 28mm**」
  - **細すぎる列は落とす（v29.2。本人の問い「列さがしが外れてると
    なぜ 9mm の細い列になるの？」）**
    - 答え（なぜ細くなるか）：
      ① 列の幅は **見つけたかたまりの幅の中ほど**（`_rubColsFromRuns` の `w2`）。
        字ではなく**画 1 本**や鉛筆のこすり跡を拾うと、そのまま列の幅になる
      ② 何も拾えないときは、**見込みの字幅を 0.6 倍 → 0.35 倍**とゆるめて
        拾い直す（v23.3）。字 26mm の拓本なら **0.35 倍＝9mm** の
        かたまりまで列として通ってしまう
    - 直し：**10mm 未満の列は落とす**（`mmPerPx` があるときだけ。
      ［列の数］で人が決めたときは さわらない）。落としたら知らせに
      「細すぎる列（10mm 未満）を ○ 本 外しました」と出す
    - v29.1 の「窓は 12〜60mm」と合わせて、**細い列が窓に伝染しない**
  - **単位を mm にそろえた／窓は 12〜60mm に収める（v29.1。本人の指示と問い）**
    - 本人の言葉：「pxだとわかりずらいので㎜に単位を統一して」
      「変わってません。指定していないのに、なぜ窓 82px（7mm）になるのですか」
    - `mmT(px)` と `mmRange(a,b)` を足し、**拓本まわりの知らせ・パネル・
      ［AI に送る絵］**の長さを mm にした（較正が無いときだけ px のまま）。
      1 列 → 2 列の境目の欄も **mm**（↑↓ は 1mm ずつ）。墨の量も mm²
    - **窓 7mm の真因**：`rubBandKnown()`（調べた列に合わせた帯）だと
      **その列の幅をそのまま窓**にしていた。列さがしが外れて 9mm の列を作ると、
      窓も 9mm になる
    - 直し：**窓は かならず 12〜60mm**（字は 15〜30mm がふつう、という本人の話に合わせた歯止め）。
      広げた／縮めたときは、見出しに「帯の幅（調べた列）→ 12mm まで広げた」と出す
    - 実測：9mm の列に合わせても 窓は **12mm**（94px）。
      ふつうの範囲では 窓 28mm（字の見当 24mm＝墨の幅（実測））
  - **窓の大きさを、拓本の墨から実測する（v29.0。本人の問い）**
    - 本人の言葉：「文字の大きさがほぼ決まっているのに、なぜAIに送る画像が
      このようなものになるのですか」
    - 真因：窓は **帯（範囲）の幅**から作っていた。帯の幅は「人がどう描いたか」
      でしかないので、**字より細く描けば 窓も細く**なり、切れはししか写らない
    - 直し（順に見る）：
      ① 帯が **調べた列**（`rubBandKnown`）なら、その幅が字の幅
      ② そうでなければ、**帯のまん中の字が左右どこまで続いているか**を測る。
        まん中から左右へ、墨が「そのあたりのいちばん濃い所の 35%」を切るまで歩く
        （かたまり（run）で採ると となりの列とつながる。実測：帯 90px で 465px＝2 列）。
        まん中が すき間に当たっていたら、近くの山へ寄ってから歩く
      ③ 測った幅は **12〜60mm のときだけ**信じる（帯が拓本の高さぜんぶだと
        画のすき間を拾って 7mm になり、窓が細切れ＝「帯が長すぎます（128 回）」）
      ④ それも駄目なら、これまでどおり mm から（帯の幅 → 字送り → 戒名の字幅）
    - 見出しに **「字の見当 ○mm（もと）／墨の幅 ○px」**
    - 実測（`irr.json`。1mm = 7.8px）：**範囲を 290px / 90px / 45px と細くしても、
      窓は 217px / 231px / 223px（28〜29mm）でほぼ変わらない**（前は範囲の幅なりに細くなった）
  - **窓が範囲の右へずれていた真因は「枠」だった（v28.9。本人の画面
    「範囲しているのに、AIに送ろうとしているのが右側の1/3程度です」）**
    - 窓を置いたあと、**拓本の左右の枠（`frameX`＝`S.rub.frameL/frameR`）の中へ
      押し込む**clamp があった（v24.4。窓が拓本の外へはみ出さないように）。
      枠が範囲より右に引いてあると、この clamp が **窓を枠の左端まで右へ押す**。
      実測（再現）：枠 430〜999・範囲 130〜420 のとき、
      前の作りでは窓が **430〜763**（＝範囲の右わき）になっていた
    - 直し：押し込んだあとに **窓と帯の重なり**を見て、6 割に満たなければ
      **帯のまん中へ戻す**（枠の clamp より帯を優先）。
      実測（v28.9）：同じ条件で窓は **109〜442**（範囲のまん中）
    - ［AI に送る絵］の見出しに **「窓の横 ○〜○px／枠 ○〜○px」**（＋押し戻したときは
      「枠に押されたので、帯のまん中へ戻しました」）を出す。
      次に ずれたときは この数字で すぐ分かる
  - **窓の大きさを「字の大きさ（mm）」で決めるようにした（v28.8。本人の問い）**
    - 本人の言葉：「範囲全体の時のAIに送る大きさは何で決めているの？
      1文字がやく15㎜～30㎜とあるのに明らかに細かすぎ」
    - それまでは **帯の幅をそのまま窓**にし、あとから 4 つの条件で押し引きしていた
      （v24.9／v26.0／v27.8／v28.1／v28.7 と継ぎ足してきた）。
      帯が細ければ窓も細く（字の切れはし）、広ければ窓も広く（字が小さく写る）
    - いまの決め方（**mm で考える**）：
      ① 帯の幅が **10〜60mm** なら、それが その拓本の字の大きさ
      ② 外れていれば、**測った字送り**（10〜60mm のとき）
      ③ それも外れていれば、**拓本の戒名の字幅**（既定 30mm）
      窓はその **1.15 倍**。字の見当は **12〜60mm** に収める
    - 知らせと［AI に送る絵］の見出しに **「窓 ○px（字の見当 ○mm＝帯の幅／字送り／戒名の字幅）」**
      を出す。どこから来た数字かが分かる
    - 実測（`irr.json`。1mm = 7.8px）：
      ふつうの列 帯 37mm → 窓 **42mm**（字の見当 37mm＝帯の幅）／
      細すぎる帯 7mm → 窓 **25mm**（字の見当 22mm＝**字送り**。前は 7mm の切れはし）／
      拓本ぜんたい 137mm → 窓 **52mm**（字送り 45mm）／
      下の小さい列 13mm → 窓 **15mm**（帯の幅）
  - **送る絵が「字の一部だけの ぼやけた切れはし」になっていた（v28.7。本人の画面
    「おかしな証拠が出ました」）**
    - 画面の札：上から 720／750／780…と **30px きざみ**＝窓が 60px しかない。
      絵は字の縁の一部だけで、AI の返事も点々
    - 真因：**帯が範囲の外にあると、重なりが細い帯になる**。
      v28.0 で「範囲があれば その中に絞る」としたが、帯（前に選んだ列）が
      範囲からはずれていると、重なりが数十 px の**すきま**になり、
      窓もその幅（＝字の一部）になっていた
    - 直し（3 つ）：
      ① **範囲を描いたら、帯もその範囲に合わせる**（描いた直後に `bandL/R` を入れる）
      ② 帯と範囲の重なりが **どちらかの 6 割に満たないときは、範囲の幅**を使う
      ③ **窓が字幅の 0.55 倍より小さいときは、字幅の 0.9 倍まで広げる**（歯止め）
    - 実測：帯を 3 列目（700〜990）に置いたまま 1 列目に範囲（130〜420）を描くと、
      **帯 130〜420（幅 290px）・窓 290px・5 枚**（前は細帯 → 窓 60px）。
      範囲を描いた直後も **帯 430〜730・窓 300px・7 枚**
  - **返事が 1 つおきに「まだ通していません」になっていた（v28.6。本人の報告
    「評価できないから直して」）**
    - 真因：**通したときの窓と、開き直したときの窓が同じとは限らない**
      （字送りを測り直すと 1 段ぶんずれる）。窓の高さで突き合わせていたので、
      ずれた側が「返事なし」に見えていた
    - 直し：**通したときの控え（`RUB.aiBand.shots` と `plan`）があれば、そちらを並べる**。
      送った絵も そのときの窓（`plan.side`／`plan.cx0`）から作り直すので、
      **左右が必ずそろう**。見出しに《通したときの絵》／《これから送る絵》と、
      墨のます数も出す
    - 実測：わざと字幅を 30mm → 26mm に変えてから開いても、**9 枚すべてに返事**が並ぶ
      （前は 1 つおき）
  - **［ぜんたい］が［通す範囲］を無視していた（v28.5。本人の指摘）**
    - 本人の言葉：「範囲を決めているのに、列の指定で全体はおかしくないですか？
      指定した範囲が複数行あって、全体ならわかりますが。列の指定の全体は
      指定した範囲を無視してますよね」→ **そのとおり**。
      ［ぜんたい］は `frameX()`（拓本の幅いっぱい）に戻していた
    - 直し：**範囲（緑の四角）があれば、そこが「ぜんたい」**。
      ボタンの字も「**範囲ぜんたい**」に変わる。
      ［AI 縁取りを実行］の行き先の判定（ぜんたい＝`rubAllGo`）も同じ見方に
    - 実測：範囲なし →「ぜんたい」→ 帯 67〜999／
      範囲あり（130,200・290×620）→「**範囲ぜんたい**」→ 帯 **130〜420**・
      見る y **200〜820**、実行ボタンも「AI 縁取りを実行：範囲ぜんたい（1 列）」
  - **［AI に送る絵］が閉じない／返事も見たい（v28.4。本人の報告）**
    - 本人の言葉：「帰ってきた結果も表示して」「また、AIに送る絵ウインドウは
      閉じるを押しても閉じません」
    - **閉じない真因**：窓のじか書きの `display:flex` が `[hidden]` に勝っていた。
      この作りでは **窓ごとに `#xxx[hidden]{display:none !important;}` を書く決まり**
      （`#regWin` などと同じ）。それを書き忘れていた。
      ※ そのため、開く前から出っぱなしだった（本人の「全面に表示されて」も これ）
    - **返事も並べた**：通したときの返事（`/api/takuhon/clean` が返した絵）を
      `RUB.aiBand.shots` に 40 枚まで控え、板では **左＝送った絵／右＝AI の返事**
      で並べる。まだ通していなければ、送る絵だけ
    - 実測：はじめは `display:none`（出ない）／通してから開くと **10 枚**
      （送った 5＋返事 5）、説明は「1／上から 157px　左＝送った絵／右＝AI の返事」／
      ✕ を押すと `display:none` に戻る
  - **［AI に送る絵］が画面いっぱいで操作できなかった（v28.3。本人の報告）**
    - 本人の言葉：「AIに送る絵というウインドウが、全面に表示されて、操作できないです」
    - 直し：**右側の細い板**にした（幅 360px・画面の 44% まで。実測 360×876／画面 1500×950）。
      拓本もパネルも見えたまま。閉じるは **✕ ボタン**（`onclick` を HTML に直書きして、
      受け口の付けそこねでも閉じられるように）と **Esc**
  - **AI に送る絵を、そのまま見せる（v28.2。本人の指示）**
    - 本人の言葉：「AIに送る前に、何送っているのか画像を見せて」
    - ③ に ［**AI に送る絵を見る**］。送る切り抜きを 150px の札で並べて出す
      （`#aiPrevWin`）。見出しに **帯・窓の大きさ（px と mm）・枚数・字送り**
    - そのために、窓の決め方を **`rubAiPlan(trusted, quiet)`** に切り出した。
      ［AI 縁取りを実行］も この計画を使うので、**見える絵＝送る絵**
      （`rubAiBand` は `rubAiPlan` の返り値を使うだけになった）
    - 実測（`irr.json`・2 文字ぶんの範囲）：
      「帯 130〜420px・たて 200〜820px／窓 290px（37mm）／5 枚／字送り 26.4mm（校正から取った）」。
      計画の窓・枚数は 実行したときと**同じ**（墨 9356 ます）
    - 使い方：**字が窓いっぱいに写っていれば good**。
      小さく写っていれば窓が大きすぎ、切れていれば窓が小さすぎ・場所ちがい
  - **字幅の縛りが、大きい戒名の拓本で仇になっていた（v28.1。本人の画面）**
    - 本人の画面：「墨の背景で、戒名 2 文字を範囲指定しました」→
      **枠が点のように小さい**（字の上に乗っていない）
    - v27.8 で入れた「窓は 拓本の戒名の字幅（既定 30mm）の 1.35 倍まで」は、
      **戒名が 30mm より大きい拓本**だと窓が字の一部しか写さず、
      AI が点のような墨しか返さない
    - 直し：**帯（または範囲）が 字幅の 2 倍より広いときだけ**、字幅で縛る。
      1 列・数文字に絞った帯では、**帯の幅こそがその拓本の字の幅**なので、
      30mm の見当より確か
    - あわせて、**枠が小さすぎるときは そう言う**ようにした
      （「枠が小さすぎます（字の高さ ○px に対して 帯 ○px）。AI の墨が点のように
        しか出ていない見込みです」）
    - 実測（`irr.json`・2 文字ぶんの範囲 290×620px）：墨 9356 ます、
      **2 字（高さ 366px・254px）**。広い帯（拓本ぜんたい）でも今までどおり通る
  - **［通す範囲］が、手で押したときに効いていなかった（v28.0。本人の報告）**
    - 本人の言葉：「試しに戒名の文字 1 文字を範囲してしましたが、認識されません」
      「通常の墨の拓本でも、範囲指定うまくいきません」
    - 真因：**帯の たては 較正の線（＝拓本ぜんたい）のまま**だった。
      範囲（緑の四角）は 横も たても決めているのに、`clampRange()` の y は
      `calTop`／`calBot`。［拓本ぜんたいを通す］だけは中で `calTop/calBot` を
      範囲に差しかえていたので、**手で押したときだけ 範囲が無視**されていた
    - 直し：`rubBandQ()` で **範囲があれば、その中に絞る**（横も たても）。
      絞った先が 8px 未満になるときは、絞らない
    - あわせて **1 字だけでも見立てられる**ようにした（［この列の字数］に 1）。
      `rubAiSplit` の force は 2 字以上、`rubLineCoreAi` は「2 つ以上」で止めていた
    - 実測（`irr.json`）：範囲なし → 帯 y 152〜4341／
      **1 文字ぶんの範囲（140,200,260×300）→ 帯 x 140〜400・y 200〜500**、
      通した所も たて 200〜500（墨 1482 ます）、字数 1 で **1 字**見立て
  - **［列の数］の列が、字の無い所に立っていた（v27.9。本人の画面）**
    - 本人の画面：「通したのは 横 **594〜749px**・たて 660〜6995px、墨は **0 ます**
      （通した所の 0%）」＝ **字の無い所を列にしていた**
    - 真因：v27.6 の「墨のある所を n 等分」は、**鉛筆で取った拓本では効かない**。
      紙ぜんたいに こすり跡があるので「墨のある所」が紙いっぱいになり、
      等分すると 字の無い所が列になる
    - 直し：**字 1 つぶんの幅の窓をすべらせて、いちばん濃い所を n か所えらぶ**
      （選んだ所どうしは 字 1 つぶんは離す）。幅は 拓本の戒名の字幅（既定 30mm）から
    - あわせて、知らせに**列ごとの濃さ（%）**を出すようにした
      （「うすい列は字が無いかもしれません」）
    - 実測：`irr.json` で 列の数 3 → **154〜388／437〜671／729〜963**（本物の 3 列と合う）。
      右 70% を「こすり跡だけ（字なし）」に塗った拓本で 列の数 1 →
      **85〜319**（字のある左だけを選ぶ。前は字の無い所を選んでいた）
  - **広い帯で AI がほとんど読めなかった（v27.8。本人の画面「全然だめです」）**
    - 本人の画面：聞き窓「帯が広すぎます（**字送り 253px に対して 帯 1182px**）」。
      OK を押しても、青い墨が ほんの少ししか出ない
    - 真因：**窓（AI に渡す切り抜き）が帯の幅のまま**だった。
      `side = max(字送り×1.15, 帯幅)` で、帯 1182px なら窓も 1182px。
      字は窓の中で **5 分の 1** ほどに写るので、AI には読めない。
      しかも窓は**帯のまん中**に置くので、字が左寄りだと窓から外れる
    - 直し（3 つ）：
      ① **窓は字 1 つぶんまで**。拓本の戒名の字幅（既定 30mm）の **1.35 倍**で頭打ち
      ② **窓は「墨のまん中」に置く**（帯が窓より 1.2 倍以上広いときは、
        横の墨の重心へ寄せる）
      ③ **［列の数］で等分した列は、墨のある所まで縮める**
        （紙の白い所まで列に入れない）
    - あわせて、**字の大きさが分かっているときは「帯が広すぎます」で聞かない**
      （窓を縛ってあるので、そのまま通してよい。キャンセルすると何も起きず、
      本人がそこで止まっていた）
    - 実測：拓本ぜんたいを帯にして［この帯だけ通す］→ **聞かれずに通った**
      （墨 33659 ます）。ふつうの列（下1〜下6）は今までどおり
  - **上の列が絵に出ていなかった／字が重なる／下で勝手に分ける（v27.7。本人の報告）**
    - 本人の言葉：「文字がかぶって判断しずらく／下は2列と出るが2列ではない／
      列の上には何も出ていない」
    - ① **上の列の帯が、境目から下に描かれていた**。v27.6 で上の列の始まりを
      `y0: 0` と書いたため、`num(c.y0,0) || cy0` の **`||` で 0 が落ちて** `cy0`
      （＝下から見たときの上端＝境目）になっていた。番号も画面のまん中に出ていた。
      直し：上の列は **範囲の上 〜 境目**、下の列は **境目 〜 下**とはっきり書く
      （`num(c.y0, cy0)` に変えて 0 も通す）
    - ② **字の重なり**：緑の線の知らせと、下の列の番号が同じ高さにあった。
      下の番号は**帯の中**へ、緑の字は**小さく・少し上**へ。
      文も「ここから下は N 列」と短くした（N は下の列の本数）
    - ③ **［列の数］を入れてあるときは、下の分かれさがしをしない**。
      人が本数を知っているのに、こちらが勝手に 2 列に分けていた
      （本人「下は 2 列と出るが 2 列ではない」）
    - 実測：自動＝上の帯 y 152〜2303（3 本）・下の帯 y 2303〜4535（6 本）・
      番号 上1〜上3／下1〜下6。［列の数］1 →「**ぜんたい 1 列**です
      （［列の数］に 1 と入っているので、下で分かれていないかは見ていません）」、
      ボタンも 上 1／下 0
  - **絵の番号がメニューと食いちがっていた／［列の数］が効かなかった（v27.6。本人の報告）**
    - 本人の言葉：「校正を調べてもうまくいきません／また、メニューには上１２３　下１２
      と出るのですが、絵では下4があります。おかしいです」
    - ① **番号の食いちがい**：絵は `colsTop.cols`（上の列と下の列が**混ざった並び**）に
      1,2,3… と振っていた。メニューは `rubColMenu()`（上＝親ぜんぶ／下＝分かれた子だけ）。
      直し：**絵も `rubColMenu()` から出す**。番号も「**上1 上2 上3／下1 下2**」と書く。
      上の列の帯は**境目まで**、下の列の帯は**境目から下**だけに出す
    - ② **［列の数］を入れても、列さがしが従わなかった**。鉛筆で取った拓本は
      紙の上のこすり跡が たてすじに見えるので、自動では列を作れないことがある。
      直し：`列の数 n` が入っていれば、**墨のある所を n 等分**し、分け目を
      そのあたりの いちばん墨のうすい所へ寄せる（字数を手で入れるときと同じ考え方）
    - 実測（`irr.json`）：自動＝メニュー 上 3／下 6・絵の番号 [上1,上2,上3,下1〜下6]。
      列の数 1 → **1 本**（67〜998）／列の数 2 → **2 本**（67〜424／425〜998）
    - 鉛筆拓のような読みにくい拓本は、**［▣ 通す範囲］＋［列の数］**で人が決めるのが早い
  - **紙の外（真っ黒な所）を列にしていた（v27.5。本人の画面）**
    - 本人の言葉：「これが構成を判別できないのは AI のせいですか」
      （鉛筆で取った拓本。紙の右わきの**真っ黒な帯**に 4・5・6 列目が出ていた）
    - **AI のせいではない**。列さがしは画面の**明暗の差**から出しており、
      紙のふちや 紙の外のざらつきも「字らしい所」として拾ってしまう
    - 直し：見つけた列の**明るさ**を測り（`_rubColLum`）、
      **ほかの列の中ほどの半分より暗い列は紙の外**とみて外す（`_rubDropDark`）。
      ぜんぶ暗いときは さわらない。外したときは知らせに明るさを出す
    - 実測：`irr.json` はそのまま 3 列。右 28% を真っ黒に塗ってから調べると、
      **1 本外して 2 列**（「紙の外とみて 1 本 外しました（明るさ 14／ふつうの列は 127）」）
    - 本筋の直しは **［▣ 通す範囲を決める］か トリミング**で紙の中だけにすること。
      これは その手間を減らすための保険
  - **命日・年齢の大きさの見方を訂正（v27.4。本人の訂正）**
    - 私が書いた「戒名は 30mm ほど、命日・年齢はその半分ほど」は**まちがい**。
      本人の言葉：「命日・年齢は戒名より小さい、半分は最も小さいイメージです」
      （＝半分は いちばん小さいときの話。ふつうは その間）
    - 手本合わせの歯止め（`small9`）を **0.72 → 0.92** にした。
      ここは「**戒名の字に当たったら止める**」ためのものなので、
      戒名とほぼ同じ大きさの字だけを外せばよい
    - 実測（`irr.json`）：上の列は戒名のまま、7列は「日」から 12 字ぶんだけ命日（変わらず）
  - **手本合わせが、戒名まで命日にしていた（v27.3。本人の報告）**
    - 本人の言葉：「戒名は一番上で文字幅30㎜の制限のはずが、下にいき、
      上のほとんどが命日と判別されています」
    - 真因は 2 つ：
      ① **境目より上だけの列（戒名の段）にも手本を当てていた**。
        そこに命日も年齢も無いのに、空似で塗られる
      ② 「日・没・歿」だけ見つかって年号が見つからないとき、
        **1 字目から その字まで**を命日にしていた（＝列の上ぜんぶ）
    - 直し：
      ① 境目より上で終わる列は**手本を見ない**（戒名のまま）
      ② はじめが分からないときは、**その字から上へさかのぼるだけ**（12 字まで）
      ③ **字の大きさで歯止め**（本人の言う「文字幅 30mm の制限」）。
        戒名は 30mm ほど、命日・年齢はその半分ほどなので、
        **`字幅 30mm × 0.72` より大きい字に当たったら そこで止める**
        （`small9`／`mark9`／`markBack9`）
    - 実測（`irr.json`・にせ拓本AI）：
      1列（上・字幅 32.4mm）→ **戒名 15 字**／5列（上・33.9mm）→ **戒名 7 字**
      （前は上のほとんどが命日になっていた）。
      7列（下）→ 俗名 2＋**命日 12**＋俗名 12（「日」から 12 字ぶんだけ）
  - **AI に通さなくても枠を作れる（v27.2。本人の指示）**
    - 本人の言葉：「枠取りを実行しなくても、枠を追加できるようにして、
      文字を認識できない拓本があった場合、学習する手段がないので」
    - それまで枠（1 字のます）は **AI の墨から見立てた列**にしか作れなかった。
      AI が読めない拓本こそ教材にしたいのに、その道が無かった
    - 足したもの：④ の［**✎ 手で枠を描く**］（`rubHandStart`）。いまの帯に
      **人が枠を描くための列**（`src:"hand"`）を作って、そのまま枠描きに入る。
      枠が 1 つも無い所で描きはじめたときも、自動でこの列を作る
    - `src` は **"ai"（AI の墨から）／"hand"（人が描いた）** の 2 つ。
      絵・列えらび・学習の画面は `rubIsBoxLine()` で**どちらも同じに**扱う
    - 学習画面は、**AI の墨が無ければ しきい値で拾った墨**を出す（前からの作り）。
      そこを塗って直せば、そのまま登録できる
    - 枠のない列でパネルが落ちていたのも直した
      （`rubLineHtml` が `a.deg.toFixed` で例外。実測：`PAGEERROR`）
    - 実測（AI に一度も通さずに）：［✎ 手で枠を描く］→ 列 1 本（`src=hand`）→
      枠 3 つ → 右クリック［学習画面でひらく］で**ひらいた**
      （「この列の AI の墨がありません。いまはしきい値で拾った墨を出しています」）
  - **上のボタンに、分かれていない列が出ていなかった（v27.1。本人の報告）**
    - 本人の言葉：「これを構成をしらべたら、このメッセージは出るのですが、
      メニューは上1行、下5行になりました。4行目は判断しづらいですが、
      上3行は見つけてほしいです」
      （知らせは「上は 4 列、下は 5 列です（1 本が下で 2 つに分かれます）」）
    - 真因：**上のボタンを「下で 2 つに分かれた列」だけ**から作っていた。
      分かれていない 3 列は上に出ず、下のボタン（`ct.cols` ぜんぶ）に紛れこんでいた
    - 直し：`rubColMenu()` を足して 1 か所で決めるようにした。
      **上＝調べた上の列ぜんぶ（`topCols`）／下＝分かれた子の列だけ**。
      パネル・押したときの受け口・`rubBandGroup`・`rubColKeyOf`（字数の合い印）・
      `rubAllGo` の回る順・`rubGrpAuto` を、ぜんぶこれに寄せた
    - 分かれていない列を選んだときは、**上から下まで 1 本**として見る
      （`rubBandQ` は `sp` の列しか切らないので、そのまま）
    - 実測（柴田。1 本だけ下で分かれる拓本）：
      上 **4** ボタン／下 **2** ボタン。上2（分かれていない）→「この帯 1 本」・
      見る y 486〜5464（ぜんぶ）／上4（分かれている）→「この列（上＋下 2 本）」・
      見る y 486〜4896（境目まで）。`irr.json` は 上 3／下 6 のまま
  - **行年・享年は無いことが多い → 数字をさかのぼる（v27.0。本人の指摘）**
    - 本人の言葉：「行・享（行年・享年）　これは無いことが多いす」
    - 年齢は「七十八歳」のように **数字＋才/歳**。そこで **才・歳から上へ、
      数字であるあいだ さかのぼって** 年齢のはじめを決める（5 字まで）。
      手本の数字は `RUBANCH.num`＝**一二三四五六七八九十百廿卅**
    - 数字も見つからないときだけ、大きな空き／才・歳の 4 字手前を使う
    - ざっと見くらべる相手（`anchIdx`）にも数字を入れた。でないと
      数字が「見こみなし」で飛ばされ、さかのぼりが効かない
    - 実測（`cgGuess` を差しかえて「彌太郎七十八歳」と見立てたことにして筋道を確かめた）：
      **俗名 3 字＋年齢 4 字**（年齢 4〜7 字目）。行年・享年 なしで当たる
  - **手本合わせ（③）で命日と年齢を決める（v26.9。本人の話と指示）**
    - 本人の言葉：「命日の先頭は令、平、昭、大、で最後は日、没、歿／
      年齢は、最後は才、歳」「やりましょう」
    - 拓本の字を**読む**のではなく、**決まった数文字だけ**と見くらべる。
      手本は `RUBANCH`：命日のはじめ **令平昭大明治**／命日のおわり **日没歿**／
      年齢のはじめ **行享**（行年・享年）／年齢のおわり **才歳**
    - くらべ方は作字の候補出しと同じ（`cgGuess` の IoU）。
      **フォントぜんぶの中で上位 12 位に入っていれば当たり**とみる
      （1 つの点数で決め打ちにしない）
    - 速さの工夫：**2 段がまえ**。まず手本 13 字だけと ざっと見くらべ、
      IoU 0.20 に届かない字は飛ばす。見る字も **はじめ 3 字・おわり 8 字・
      まん中のとびとび 6 字**だけ（列ぜんぶは見ない）
      （実測：40 字ぜんぶ 14.9 秒 → 10.7 秒 → **8.7 秒**／7 列）
    - 付け方：年号〜日・没・歿を **命日**／行年・享年〜才・歳を **年齢**
      （行年が見つからなければ、大きな空きか 才・歳の 5 字手前から）。
      残った字は 下の列なら **俗名**、上なら **戒名**
    - パネル ⑤ は ［位置から見当を付ける］（v26.8）と［**文字で見分ける**］の 2 つ
    - **フォントが要る**（手本を作るため）。無いときは、そう知らせる
    - 実測（にせ拓本AI・`irr.json`・IPAゴシックを手本に）：**1 / 7 列**で当たり
      （7列目「命日 〜14 字目（日）」）。**にせ AI の墨は荒れているので、
      これは仕組みが動くことの確認まで**。本物の拓本AI での確かめが要る
  - **文字のグループ分け（v26.8。本人の指示「次は文字列のグループ化です。
    戒名や命日、俗名、年齢を分けられるようにしてほしい」）**
    - **1 字ずつに `g`（グループの合い印）**を持たせる：
      `kai` 戒名（濃い桃）／`zok` 俗名（青）／`mei` 命日（緑）／`nen` 年齢（橙）／`hoka` その他（紫）
    - 絵では**枠の色**がグループの色になり、変わり目に**名前**が出る
    - パネル ⑤ に：［グループの見当を付ける］（`rubGrpAuto`）と、
      **戒名／俗名／命日／年齢／その他／なし**のボタン。
      字をえらんでいなければ **列ぜんぶ**、えらんでいれば **その字から下だけ**に付く
      （「彌太郎／行年七十八歳」のように、1 列に 2 つ入ることがあるため）
    - 自動の見当（こちらで分かっている作りだけから）：
      **境目より上＝戒名／下は 同じ親の中で 右＝命日・左＝俗名**。
      下の列だけ、**字送りの 1.6 倍より広い空き**があれば そこから下を **年齢**
    - **上（戒名）の列は切らない**（v26.8 の実測。切ると戒名の列に「命日」が付いた）
    - 本人の言葉「4 の順番も例外もあり決まっていないと思った方がいい」なので、
      **手で直せること**が前提。ボタンで上書きできる
    - 実測（にせ拓本AI・`irr.json`・ぜんたい通しのあと）：
      1列 戒名／2列 命日／3列 俗名＋年齢／4列 命日／5列 戒名／6列 俗名＋年齢／7列 命日＋年齢。
      **左右の見分け（右＝命日）は合っている**。字数のばらつきは にせ AI の墨のせい
  - **AI 縁取りのボタンを 1 つに／墨を消すが効かなかった（v26.7。本人の指示）**
    - 本人の言葉：「AI縁取りの実行は１つのボタンでいいです。列の指定で、
      全体を選んでAI縁取りを選択する場合と、列を指定する場合で、決まるので、
      分ける必要がないからです」「また、墨を消すボタンは押しても効果ないです。治して」
    - ③ は **［AI 縁取りを実行：○○］** 1 つだけにした。○○ は ② の選び方で決まり、
      **押す前に字で出る**：
      「拓本ぜんたい（6 列）」／「この列（上＋下 2 本）」／「この帯 1 本」。
      中では ぜんたい→`rubAllGo()`／それ以外→`rubBandGroupGo()`
    - **［AI の墨を消す］が効かなかった真因**：`RUB.aiBand` を空にしていたが、
      絵には**見立てた列ごとの絵（`L.aiUrl`）も重ねて**いたので、見た目が変わらなかった。
      直し：`rubShowAi` で **出す／隠す の切りかえ**にした（墨は学習画面でも使うので捨てない）。
      ボタンの字も「AI の墨を消す」⇔「AI の墨を出す」で入れかわる
    - 実測：下3 を通したあと押すと 絵の画像が **2 枚 → 1 枚**（拓本だけ）になり、
      もう一度押すと 2 枚に戻る。ぜんたいを選んで実行すると **7 列ぶん**見立て
  - **AI補正のパネルを並べ直した／字数は列ごと（v26.6。本人の指示）**
    - 本人の言葉：「UIを整理しましょう、列の校正を調べるが一番上で、列の指定つぎ、
      そこに全体の選択も入れる。その下はAI縁取り、文字判別　と続けばいいです。
      文字数は列を列ごとに入れられた方がいいのではと思います」
    - 並びを **① 列の構成 → ② 列の指定 → ③ AI 縁取り → ④ 文字判別** にした
      （見出しを 4 つ入れてある）
      - ① 字幅（mm）・列の数・［列の構成を調べる］・結果・境目の欄
      - ② 上／下の番号ボタン＋**［ぜんたい］**（`data-k="all"`。帯を拓本の幅いっぱいに）、
        いまの帯（幅と見ている高さ）、［通す範囲を決める］
      - ③ ［拓本ぜんたいを通す］［この列（上＋下N本）を通す／この帯だけ通す］［AI の墨を消す］
      - ④ ［この行を見立てる］、字数の欄、列ごとの字数、見立ての一覧
    - **字数は列ごとに覚える**：`S.rub.lineNs`（合い印は 上 `t0,t1…`／下 `b0,b1…`／
      調べていない帯は `x<左>_<右>`）。`rubLineNKey()`／`rubLineNAt()`／
      `rubLineNGet()`／`rubLineNSet()`。前の版の `S.rub.lineN`（1 つだけ）も拾う
    - ④ に**列ごとの小さな欄**を並べた（上 1 2 3／下 1〜6）。まとめて入れられる
    - 実測：下1 に 4・下3 に 3・上2 に 6 を入れて、列を選び直しても
      それぞれの数が出る（控え `{"b0":4,"b2":3,"t1":6}`）。
      下1 を選んで通すと **4 字**に割れた
  - **選んだ列（上＋その下）を まとめて通す／知らせの太字（v26.5。本人の画面）**
    - 本人の画面の知らせ：「通したのは **横 72〜413px・たて 152〜2453px**」。
      つまり **上の列（戒名）だけ**を通していた。v26.3 で 上を選ぶと
      下も光るようにしたのに、**通すのは上だけ**のままだったのが食い違いだった
    - 直し：`rubBandGroupGo()`。帯が上の列そのものなら、
      **上＋その下の列を順に** AI に通して見立てる（同じ人の 1 かたまり）。
      ボタンの字も「**この列（上＋下 2 本）を通す**」に変わる。
      ふつうの帯（下の列・調べていない帯）は今までどおり 1 本だけ
    - 実測（にせ拓本AI）：上1 を押して ボタン → 3 本通して **2 本**見立て。
      できなかった 1 本（上の戒名）は墨がつながっているため。
      知らせに「［この列の字数］に数を入れて…」と出る
    - **`msg()` が `textContent` だった**ので、知らせに書いた `<b>` が
      **そのまま文字で**見えていた（本人の画面写真で判明）。
      `<b>`／`<br>`／`<i>` を含むときだけ `innerHTML` で入れるようにした
  - **「下の段は色が変わりません」→ 通した所と墨の量を数で出す（v26.4）**
    - 手元（にせ拓本AI）では**下の列でもちゃんと通っている**（実測：下1 を押して
      帯 125〜255px・幅 130px → 墨は**境目より下だけに 13118 ます**、
      境目より上は 0 ます）。つまり作りは合っていて、**本人の画面で何が
      起きているかが分からない**のが問題だった
    - そこで、通したあとの知らせに数字を足した：
      「通したのは **横 ○〜○px・たて ○〜○px**（たて ○〜○mm）、
        墨は **○ ます**（通した所の ○%）」。
      墨が 50 ます未満なら「**墨がほとんど出ていません**。AI が読めていないか、
      帯の場所ちがいです」と言う
    - 絵にも **水色の破線の枠**で「AI に通した所」を出す（`RUB.aiBand.box`）
    - これで、色が変わらないときに
      **AI が読めていない／別の所を通している／すでに青かった**のどれかが分かる
  - **上の列を選ぶと、その中の下の列も光る（v26.3。本人の指示）**
    - 本人の言葉：「上の1列目を選ぶと、その範囲にある下の1，2もONになる様に
      しただけで選ぶことはまずないので」
    - 上の 1 列（戒名）と、その下にぶら下がる 2 列は**同じ人の 1 かたまり**。
      選ぶのは上だけで足りる、という本人の見方に合わせた
    - 実測：上1 → 下 1,2 が ON／上2 → 下 3,4／上3 → 下 5,6。
      下を直接押したときは その 1 本だけ
    - 本人の話（**まだ手を付けていない**）：「絵で枠を移動して、この帯を通すのと、
      通す範囲を決めるはほぼ同じ目的で使用します。つまり、拓本がはみ出して
      うまく取れていない場合、外す為か、1列をターゲットにしたい場合です。
      機能をまとめてでもいいし、列の校正がうまくいくなら不要になりますが」
      → **帯と［通す範囲］をまとめる**のは、列の見つけ方が固まってから
  - **［この帯だけ通す］だけ うまくいかない（v26.2。本人の報告
    「拓本全体を通すではうまくいきましたが、この帯だけ通すはうまくいきません」）**
    - 真因①：［拓本ぜんたいを通す］は列を**信じて**（`trusted`）通していたが、
      手で押した帯は信じていなかった。同じ帯でも「**帯が広すぎます**（字送り ○px に
      対して 帯 ○px）」と聞かれ、**やめると何も起きない**
    - しかも その聞き文には、**もう無いボタン**（［列を探す］［近い列に合わせる］）が
      書いてあった（v25.8 で消した）
    - 直し：`rubBandKnown()` を足した。いまの帯が**調べた列（上の親・下の子）**に
      8 割以上そろっていれば、ぜんたいと同じく信じて通す。聞き文も直した
    - 真因②：**拓本を回すと、前に調べた列は使えなくなる**（`geo` が変わる）。
      黙って「ぜんたい」に戻るので、帯は上下まるごとを見てしまう
      （実測：下5 の列で 36 回 → **74 回**の窓になり、上の戒名まで通していた）。
      直し：パネルに**赤い知らせ**を出す（「拓本を回したので、前に調べた列は
      使えません。もう一度［列の構成を調べる］を押してください」）
    - 実測（にせ拓本AI・`irr.json`・**聞かれたら必ず「キャンセル」**を押す形で確かめた）：
      上 3 列・下 6 列とも **9 本ぜんぶ通った**（見立ては 上 1 本＋下 6 本＝7 本。
      通らなかった上の 2 本は墨がつながっているため。［この列の字数］で切れる）
  - **「結果は同じです」の真因は 3 つあった（v26.1。本人の報告）**
    - ① **下のボタンが `RUB.cols` から作られていた**。そこは［列の数］を
      入れると**等分に割った列**に化ける。すると「下」を押しても幅が
      **親の列のまま**になり、`rubBandQ()` は「上（戒名）」と判じて
      下の字はいつまでも見立てられない
      （実測：［列の数］に 3 → 下3 を押して 帯 670〜999・y 152〜2303）。
      直し：下のボタンは **調べた列（`S.rub.colsTop.cols`）そのもの**から作る。
      押したときの受け口も同じ並びを見る
    - ② **［拓本ぜんたいを通す］が、上か下の どちらかしか通していなかった**。
      `rubColsWanted()` の返す 1 組だけを回していたため。
      直し：上下で作りが変わる拓本では、**上の列（親）＋下の列（子）**を
      つないで回す（`blk: "top"/"bot"`）。となりの列は**同じかたまりの中**で見る
    - ③ **下の列を見立てると、上の列の見立てが消えていた**。`rubColPut()` が
      **横位置だけ**で「同じ列」と判じて置きかえていた。上下で作りが変わる拓本では、
      上の戒名と下の命日が同じ横位置にある
      （実測：9 列通して 6 列しか残らなかった）。
      直し：`band` に **T・B（たての見た範囲）**を持たせ、
      たてが半分も重ならなければ**別の列**として足す
    - 実測（にせ拓本AI・`irr.json`）：ぜんたいを通すと **9 列**（上 3・下 6）を回し、
      **7 列ぶん**が残った（上 2・下 5。残り 2 列は墨がつながって切り分けできず）
    - パネルに **「いまの帯は ○○を見ています（上から ○〜○px＝○〜○mm）」**を出した。
      上・下・ぜんたいのどれを見ているか、目で確かめられる
  - **下の列で「見立てられません」＝窓が 2 字ぶんだった（v26.0。本人の報告）**
    - 本人の言葉：「下の文字は見立てられませんでした」
    - 真因：AI に渡す**窓の大きさ**が、校正の見当（大きい戒名）から来ていた。
      下の命日・年齢の列は字が半分ほどなので、**窓に 2 字入る**。
      2 字が 1 つの墨につながり、`rubAiSplit` が 1 かたまりしか返せない
    - 実測（`irr.json` の下の列）：帯 100px に対して窓 **201px＝2.01 倍**／
      帯 163px に対して 235px＝1.44 倍／帯 130px も 校正の 171px に
      置きかえられて 1.5 倍
    - 直し：**窓は帯の幅を大きく超えない**。`side > 帯幅 × 1.3` なら
      `側 = 帯幅 × 1.15` にする（1 列ぶんの帯なら、字の大きさ＝帯の幅）
    - 実測（直したあと。にせ拓本AI）：下 6 列とも枠が出た
      （2 字／3 字／24 字／6 字／35 字／26 字。**数はにせ AI の墨しだい**）
    - あわせて、切り分けできないときの知らせに**数字**を出すようにした
      （帯 ○×○px／AI の墨 ○ます・○%／かたまり ○ 個）。
      画面の数字だけで見当が付けられるように
  - **帯が上下どちらの かたまりかを、幅で決める（v25.9。本人の報告「効いていません」）**
    - 本人の画面：帯を **1 列ぶん（親の列）**に合わせると、上の「居士」と
      下の「健／令和」が**同じ帯の中**に入ったままだった
    - v25.8 の `rubBandQ()` は「帯が**分かれた子の列**に 6 割以上重なるときだけ」
      下へ寄せていた。**親の列に合わせる使い方**では、子の幅は親の半分なので
      条件に届かず、何も起きていなかった
    - 直し：**帯の幅で どちらの かたまりかを決める**。
      親とほぼ同じ幅（7 割以上）→ **上だけ**（`y2 = yCut`）／
      それより細い（子の列）→ **下だけ**（`y1 = yCut`）
    - 列ボタンも **2 段**にした：「上（戒名） 1 2 3」「下（命日ほか） 1〜6」。
      押すと帯とともに**見る高さ**も変わり、知らせにも px と mm を出す
    - **もう 1 つ隠れていた不具合**：`renderCols()` の側に
      `document.querySelectorAll(".colb")` で **onclick を差しかえる**古い受け口が
      あり、上の列を押しても下の列に合っていた（実測：上2 を押すと 255〜420px＝下2）。
      `.colb:not([data-k])` に変えて、拓本パネルのボタンには手を出さないようにした
    - 実測：上2 → 帯 x 434〜729・y 152〜2303（＝戒名だけ）／
      下4 → 帯 x 566〜729・y 2303〜4341（＝命日だけ）
    - 残り：**上のかたまりは「字を 2 つ以上見つけられません」**になることがある
      （にせ拓本AI で確認。戒名が縦につながって 1 かたまりに見える）。
      そのときは［この列の字数］に数を入れる（v25.1）
  - **列さがしを 1 つのボタンに／下の列でも見立てられるように（v25.8。本人の指示）**
    - 本人の言葉：「UIを整理しないと操作できないので ①列の見極めで、上からと
      下からの調査が別々ですが、1つにして、どのような構成になっているか判別して／
      近い列に合わせる と 列を探す、全体にもどすは不要／この行を見立てるで、
      下の2列になった場合も文字囲ってほしい　変化なしです」
    - ［列の構成を調べる］（`rubColsStudy`）1 つにまとめた。中では
      ① 上の行で列を見つけ ② 下で 2 列に分かれていないか ③ 変わり目の高さ、
      までを続けて行い、**知らせは 1 つ**にする
      （「上は 3 列、下は 6 列です（3 本が下で 2 つに分かれます）。
        変わり目は 上から 2303px（294mm）＝緑の線」）。
      分かれていなければ「ぜんたい 3 列です（上から下まで変わりません）」
    - ［いちばん上の行から列を見つける］［下で分かれていないか見る］の
      2 つのボタンは消した（関数 `rubColsScanTop`／`rubColsBotSplit` は残す）
    - ［近い列に合わせる］［列を探す］［全体に戻す］も消した（本人の指示）。
      列は下の**番号ボタン**で帯に合わせる。`rubBandFit`／`detectColumns` は
      ほかからも呼ぶので関数は残してある
    - **下の 2 列で［この行を見立てる］が効かなかった真因**：帯は**横（x）しか
      決めていない**。`clampRange()` の y は較正の上下（＝拓本ぜんたい）なので、
      下の列を選んでも**上の戒名（大きい字）まで帯に入っていた**
    - 直し：`rubBandQ()` を足した。いまの帯が「下で 2 列に分かれた列」に
      重なっていれば（重なり 6 割以上）、**境目から下だけ**を返す。
      `rubAiBand` と `rubLineTakeAi` の入り口をこれに差し替え。
      分かれた列だけに `sp` と `y0` を持たせてある（分かれていない列は上から丸ごと）
    - 実測（`irr.json`・にせ拓本AI）：帯の上端が 2303px（＝境目）になり、
      下の列でも枠が出た（列1 4 字／列3 10 字／列5 34 字。
      **数はにせ AI の墨しだい**なので、本物での確かめが要る）
  - **境目さがしを作り直した（v25.7。本人の指示「2へお願いします」）**
    - v25.4〜v25.6 は **帯 1 段ずつ**「割れているか」を見ていた。字幅を変える
      たびに 2197／3515／2528／3582px と飛び、使いものにならなかった。
      1 段ぶんの墨では地のざらつきに負ける
    - 直し：**下から帯を積み上げて**（重ねて）、そのたびに「まだ割れているか」を
      見る。列さがしと同じやり方なので、同じだけ確かに出る。下から積んでいる
      あいだは割れたまま。**戒名（大きい字）が入ったとたんにすき間が埋まる**ので、
      そこが境目。3 段つづけて埋まったら打ち切り（もう戻らない）
    - 実測（`irr.json`・字幅 30mm）：上端 2335px までは **3 列とも ○**、
      2217px で 2 列が ×、2099px で 3 列とも ×
    - 出た境目：字幅 25mm **2343px**／30mm **2303px**／35mm **2558px**
      （本当は 2400〜2500px くらい。**字 1 つぶんの半分**まで詰まった。
      v25.6 までは 2197〜3582px と ばらばらだった）
    - 校正から推した 267px のときだけ 3391px と外れる。既定の 30mm なら合う
    - **ためして駄目だった手**（v25.7 でも試した。もう一度やらないため）：
      - たての run の高さ（30mm → 15mm）で見る → **駄目**。`_rubRowProf` を
        足して測ったが、字が画ごとに切れて run が 43〜55 本に割れる
        （高さ 65,23,21,67,35… となり、大きい字と小さい字が見分けられない）
      - 帯ごとの「いちばん広いかたまりの幅」→ **駄目**。下でも となりの列と
        くっついて 0.8〜1.0 になる列がある
    - `_rubRowProf()`（たての「字らしさ」）は残してある。使い道が出たときのために
  - **拓本の戒名の字幅を、経験の 30mm で決める（v25.6。本人の話）**
    - 本人の言葉：「戒名の文字幅は 30mm 前後が多い」「もちろん経験値なので、
      多少の過去のフォントによるばらつきはあると思いますよ」
    - それまでは **校正（これから彫る原稿）の字**から拓本の字幅を推していた。
      拓本は過去に彫った石なので、**原稿の字の大きさとは関係がない**
    - `rubKmWmm()`（既定 **30mm**。0 を入れると今までどおり校正から推す）。
      `_rubCharSide()` はこれを返し、`_rubCharSides()` は
      **30 → 36 → 24 → 15mm** の順に候補を持つ（ばらつきと、下の小さい字のため）
    - パネルに［拓本の戒名の字幅（mm）］（`#rKmW`。いま何 px として見ているかも出す）
    - 実測（列の本数は変わらない＝入れても壊れない）：
      `irr.json` 自動 267px／30mm 235px／25mm 196px／35mm 275px のどれでも
      **上 3 → 下 6**。柴田も **上 4 → 下 5** のまま
    - ただし**境目はばらばら**（2197／3515／2528／3582px）。境目の見立ては
      字幅ではなく**谷さがしの弱さ**が原因。次は「字幅が 30mm → 15mm に
      変わる高さ」で境目を出す（本人と相談ずみの ②）
  - **境目は 6 列とも同じ高さ。ずれは手で直す（v25.5。本人の答え）**
    - 本人の言葉：「ずれてますね。答えとしては、6列は開始と終わりは同じ位置です」
    - v25.4 は列ごとに出していた（実測 3470／3671／2331px）。ばらつく理由は
      **下の列の片方に字が少ない所では谷が消える**から（「建」「太」の列は 2 字だけ）
    - 直し：**1 本にそろえる**（中ほどの値）。全部の列に同じ `yCut` を持たせる
    - **ためして駄目だった手**（残しておく。もう一度やらないため）：
      - いちばん上（小さい y）を採る → **1929px**。戒名の「峰」「朗」のような
        左右に分かれる字でも谷が出るので、上に行きすぎる
      - 分かれ目のすき間だけを見る（帯の墨と くらべる）→ **3604px**。
        下の列の片方が空になる所（「三郎」の下など）で、くらべる相手が
        半分になり 当てにならない
      - 生の画素で「字と字のすき間」に寄せる → **効かない**。拓本の地は
        ざらざらで、行ごとの白の数は 180〜300 のあいだを行ったり来たりするだけ
        （実測：2050〜2650px のどこにも谷が無い）。`_rubTopProf` を細い帯で
        呼ぶのも駄目（局所コントラストの輪が 帯より大きい）
    - いまの見立て：**中ほどの値＋すき間へ寄せ**で 2197px。本当は 2400〜2500px
      くらい（士 の下と 健 の上のあいだ）なので、**字 1 つぶんほど上**に出る
    - そこで **手で直せる欄**を足した（`#rCutY`。px 入力と ↑↓ ボタン、
      1 押しで 字の 4 分の 1）。動かすと緑の線と むらさきの帯がついてくる。
      本人の言う「自動化し、手で微調整という形」
    - **確かに直すなら、この帯を AI（拓本AI）に通してから見るのが筋**。
      生の画素では、これ以上は難しい
  - **1 列から 2 列に変わる高さをさがす（v25.4。本人の指示）**
    - 本人の言葉：「あとは上の1列と下の2列の境界の判別ができるようになれば
      いいのですが」
    - やり方：下から 1 字ぶんの帯を 半字ぶんずつ上へずらし、**その帯でも
      同じ所で 2 つに割れているか**を見る（`valleyOf`）。割れなくなった所が境目。
      2 段つづけて割れなければ、そこで止める。列ごとに別々に出す
      （戒名の長さがちがうので、境目も列ごとにちがう）
    - 出しかた：列の帯を**その境目から下だけ**に出す。境目は**緑の線**
      （列ごとは太い線／ぜんたいの中ほどは細い破線）。知らせには px と mm
    - 実測（`irr.json`）：1列目 3470px／2列目 1929px／3列目 2197px（中ほど 2197px＝280mm）。
      絵と見くらべて、戒名の終わりと合っている
  - **下で列が分かれていないかを見る（v25.3。本人の案）**
    - 本人の言葉：「私のイメージでは、上の文字から、列数を判断して、下の列から、
      1列ままか、2列に分かれているかの判断をさせるイメージでした。
      なので、下の列数は今回では 6 になるはずです」
    - v25.2 の「下から読み直す」は、これとは別もの。**下だけで読み直すと
      上と同じ 3 本**しか出なかった（下の 2 列はすき間が数 px しか無い）。
      正しくは **上の列を土台に、その中だけを見て 1 列か 2 列かを決める**
    - `rubColsBotSplit(useAi)`。上の列（`S.rub.colsTop`。無ければ先に上から探す）
      ごとに、下から 1 字ぶんずつ 6 段ぶんの墨を重ねた横の並びを見る
    - **しきい値では分けられない**（実測）。まん中の墨は 0 にならない
      （まん中 .13〜.28／両わき .69〜.96）ので、ふつうの切り方だと 1 本のまま。
      **谷さがし**にした：幅の 38〜62% でいちばんうすい所が、両わきの山の
      **5 割より低ければ** そこが分かれ目。分けるのは **2 つまで**
    - 実測（`irr.json`）：上 3 本 → **下 6 本**
      （119〜257／257〜422／428〜568／568〜731／737〜850／850〜999）。本人の見立てと同じ
    - 実測（柴田）：上 4 本 → 下 5 本（右はしの見切れた列が分かれた）
    - 谷の窓を 30〜70%・深さ 6 割にすると、柴田が 7 本に増えた（字の中の
      すき間を分かれ目と読み違える）。38〜62%・5 割で両方とも合う
  - **下の行からも列をさがせる（v25.2。本人の案）**
    - 本人の言葉：「上の行をを見つけることをやっているので、下の行も見つけるように
      してはどうですか？」／「下２行でおかしいので、数値を入れてもよくならないです」
    - 上は戒名で 1 列、下は命日と建之で 2 列、という拓本はざらにある。
      上から見つけた列は下では使えないので、帯が 2 列またぎになり、
      字数を入れても直らない（切り分けの前に、列がまちがっている）
    - 足したもの：［いちばん下の行から列を見つける］（`rubColsScanTop(true, true)`）。
      `_rubBotStart` で**下のふちから上へ**のぼって いちばん下の字の下ふちを見つけ、
      そこから上へ 1 字ぶんずつ読む。`S.rub.colsTop.dir = "bot"` を持たせて、
      パネルの字と 絵の帯（`y0`/`y1`）を下向きに出す。
      **ボタンは v25.3 で ［下で分かれていないか見る］に置きかえた**
      （関数は残してある。下だけを読み直したいときに使える）
    - 実測（`irr.json`・範囲を下 45%）：上から 3 本（12〜365／365〜719／719〜1072）／
      下から 3 本（123〜416／435〜727／746〜1039）。下の方が字に沿っている
  - **「一」が枠から落ちていた（v25.2。本人の報告「一が選択されないです」）**
    - 原因：`rubAiSplit` の最後で「そのあたりの字送りの 2 割より背が低いかけらは落とす」
      としていた。横 1 本の字（一）は背が字送りの 1 割ほどしか無く、**かけらと同じ高さ**
    - 直し：**横はば**で見分ける。列の幅の半分より横に長ければ、かけらではなく字として残す
    - 実測（合成した墨・字 3 つ。まん中が背 8 ます・横 33 ます の「一」）：
      v25.1 = **2 個**（一が落ちる）／v25.2 = **3 個**
  - **字数を手で入れたら、その数ちょうどに切る（v25.1。本人の申し出）**
    - 本人の言葉：「文字数であれば、テストで私が数えて入力します。全く読めていない、
      都築家の下半分の２列になってる部分を検証として使いたいのですが」
    - 拓本は**過去に彫った石**なので、原稿の字数は当てにならない（本人の指摘）。
      墨がつながっている列は、こちらの谷さがしでは何字か決められない
    - 足したもの：拓本パネルの［この列の字数（分かるとき）］（`#rLineN` →
      `S.rub.lineN`。空 or 0 で今までどおりの自動）。［この行を見立てる］に流れる
      （`rubLineTakeAi` → `rubLineCoreAi(M,q,step,mpp,nWant)` → `rubAiSplit(M,nWant)`）
    - 切り方：墨のある上端〜下端を n 等分し、**切れ目ごとに ±0.3 字送りの中で
      いちばん墨の薄い行**へ寄せる。谷が無くても必ず n 字ぶん出る。
      ずれた枠は今までどおり手でつかんで直せる
    - 実測（にせ拓本AI・`irr.json`・帯 425〜734＝これまで切り分けできなかった列）：
      自動＝切り分けできず／6 と入れる＝**6 字・字送り 89.74mm**／
      12 と入れる＝**12 字・字送り 43.72mm**
  - **1 字だけ通すと、その列の墨が消えていた（v25.0。本人の報告）**
    - 本人の言葉：「右クリックで 1 文字を AI に通すと、その列の他の文字にあった、
      全体で通すで出した墨が全部消えます」
    - 原因：`rubAiOne` が `RUB.aiBand` を土台にしていた。`RUB.aiBand` は
      **最後に通した列の墨**しか持っていない（v23.0 で分かっていたこと）。
      列 1 の字を通すと、**列 3 の墨を土台に**して、それを列 1 の絵として
      書き込んでいた＝列 1 の墨が丸ごと入れかわる
    - 直し：**その列の青い絵（`L.aiUrl`）から読み直して土台にする**
      （`rgBandFromUrl`。学習画面と同じやり方）。控えを持っているので**写しを取る**。
      `RUB.aiBand` を使うのは、それが**その列の墨**（`aiId` が一致）のときだけ
    - 実測（柴田）：通しのあと 列1 33672 ます → 1 字通したあと **37952 ます**（増えた）。
      列2・列3・列4 の絵は**そのまま**

  - **窓は「字 1 つぶん」に近づける（v24.9。本人の画面の数字で判明）**
    - 本人の画面（帯の中で数えた墨）：
      1列目 字送り 669px **12.5%**／2列目 408px **11.5%**／3列目 250px **23.5%**
    - 窓 `side = max(字送り×1.15, 帯)` は、列ごとに 1 字ぶん・1.8 字ぶん・2 字ぶん に
      なっていた。**1 字ぶんの列だけ、墨が倍**出ている。
      ①「読む」は**1 字ぶんの切り抜き**で覚えているので、窓を大きくすると
      字が小さく写り、読めなくなる
    - 直し：**測った字送りが 校正の見当（`rubPitchPlanPx`）の 1.35 倍を超えたら、
      校正に合わせる**（`side = max(帯, 校正の見当×1.15)`）。
      小さすぎる方は v24.3 で置きかえずみ。v24.4 の「帯×1.6」の頭打ちは これに置きかえた
    - 実測（本人の値で計算）：列1 窓 769 → **308px**、列2 469 → **286px**、
      列3 300 → 300px（変わらず）。**3 列とも 字 1.00 個ぶん**になる
    - **手元の簡易な AI では、2 列目の切り分けが通らなくなった**（墨の出方が
      本物とちがうため）。本人の数字（12% 対 23.5%）を採ってこちらを選んだ。
      **本人の画面で悪くなるようなら戻すこと**

  - **通しの報告の「墨 ○%」を、帯の中だけで数えるようにした（v24.8）**
    - それまでは**拓本ぜんたい**を分母にしていたので、帯の広さがちがう列どうしを
      比べられなかった（同じ濃さでも 2.8% と 6.1% に見える）
    - 実測：1列目 21.5%／2列目 23.2%（前は 6.4%／6.3%）。列どうしを見くらべられる

  - **字の枠の右クリックを、メニューにした（v24.7。本人の指示）**
    - 本人の言葉：「文字枠を右クリックして、メニューを追加して、その部分だけ AI に
      通せる機能も選べるようにして、学習もメニューで選べるように」
    - 枠の上で右クリック → **⬛ この字だけ AI に通す／✎ 学習画面でひらく／
      🗑 この枠を消す**（その下に いつもの「拓本の補正」）。
      v22.7 の「いきなり学習画面」はやめた
    - `rubAiOne(hit)`：枠の 1.2 倍を ①「読む」に通し、**いまの青い墨に重ねる**
      （濃い方を採る）。`RUB.aiBand.map` を書きかえ、青い絵（`url`）を作り直し、
      その列の控え（`L.aiUrl`／`aiSc`／`aiId`）も入れかえる。
      墨がまだ無ければ、その字ぶんだけの墨を新しく作る
    - 実測：メニュー 3 つが出る／「1 列目の 3 字目だけ AI に通しました
      （この枠の中の墨 6763 ます）」で青い絵が入れかわる／枠を消すと 13 字 → 12 字

  - **拓本のパネルも、ロック中に使えるようにした（v24.6。本人の報告）**
    - 本人の言葉：「AI補正（準備中）以下のボタンが押せないです」
    - v24.5 では**絵の上の操作（JS の見張り）だけ**を通していた。
      左のパネルは **CSS** で止まっていた：
      `body.locked #sub input/select/textarea/button { opacity:.5; pointer-events:none; }`
      （`#panSP`／`#panFile`／`#panInfo`／`#panDocs`／`#panFont` だけ例外）
    - 直し：例外に **`#panRub`** を足した。ただし **［入れる枠］（`#rubFrSel`）は
      シートの中身が動く**ので、止めたまま
    - 実測：ロック中でも `btnColsTop`／`btnAiBand`／`btnBandAll`／`btnBandCols`／
      `btnArea`／`rColN` が押せる（実際に押して「帯を全体に戻しました」まで確認）。
      作字のパネルは押せないまま
    - **教訓**：ロックは **JS の見張り**と **CSS** の 2 か所でかかっている。
      片方だけ直しても通らない

  - **ロック中でも、拓本の解析はできるようにした（v24.5。本人の指示）**
    - 本人の言葉：「あなたの提案でいいので、拓本の AI 補正をロック中にも使えるようにして」
    - `lockRubOK(t)` を足し、`pointerdown`／`contextmenu`／`dblclick` の
      ロックの見張りで **`#rubwrap` の中は通す**。打ち込みも
      `#rubFix,#rubData,#rubBrowse,#regWin,#aiFixBox` は通す
    - **止めたままにしたもの**
      - 較正 … 頭・脚のつまみ（`data-k="calTop"／"calBot"`）と
        実測した文字全長（`#rubLenBox` は通す先に**入れていない**）
      - 回転 … `startSpin()`／`rPivot`／`resetRot()`／`rubFlip180()` に `locked()` の門番
      - トリミングは v14.6 から ロック中も動かせる。そのまま
    - **落とし穴**：［拓本補正］のタブは、開くと `startSpin()` で
      **はじめから回せる状態にする**作り（v21.27）。そのままだと
      ロック中に拓本を見るたび断りが出るので、**ロック中はだまって始めない**
    - 実測（ロック中）：列さがし 4 本／帯のつまみ 通る／列の数の欄 打てる／
      絵の右クリック 通る（メニューも出る）／拓本ぜんたいを通す 走る。
      較正のつまみ・回転・実測した文字全長 は止まる。
      ロックを外すと、いつもどおり `rotMode = spin` で開く

  - **【真因その 2】拓本の外を白で埋めていた＋窓が大きくなりすぎていた（v24.4）**
    - 本人の画面（v24.3 の列ごとの数字）：
      「1列目 帯105〜413（308px） **字送り 669px** AI の墨 **0.8%** ／
        2列目 … 字送り 408px AI の墨 4% ／ 3列目 … 字送り 250px AI の墨 6.1%」
      → **左端の列だけ、AI がほとんど墨を出していない**
    - 分かったこと
      1. 字送りを **倍に読み違えて 669px**（本当は 350px ほど）。
         窓 `side = max(pitch*1.15, 帯) = 769px` は 帯（308px）の **2.5 倍**
      2. 窓の左はしは **x = −125**。`rubCropPng` は拓本の外を **白（255）で埋めていた**
      3. 拓本は**字が白・地が黒**。外の白い所が AI には**大きな字**に見え、
         そこに墨が出る。帯の外なので `rubAiTrimCols`／切り取りで捨てられ、
         **帯の中には ほとんど残らない**＝ AI の墨 0.8%
    - 直し
      - `rubCropPng`／`rubCropCv`：拓本の外は **`S.rub.invert` に合わせて地の色**で埋める
      - 窓の大きさに上限（**帯の 1.6 倍**か 校正の字幅の 1.3 倍の大きい方）
      - 窓を **拓本の枠（`frameX`）の中に収める**
    - 実測：上下で作りが変わる拓本 3 列とも読める（列1 が 5 字・108.3mm）。
      柴田の拓本 4 列とも読める。上限を「帯そのもの」にしたら読めなくなったので **1.6 倍**にした

  - **通しの結果を、列ごとに数字で出す＋小さすぎる字送りは校正の値にする（v24.3）**
    - 本人の画面：［この帯だけ通す］で「帯が広すぎます（**字送り 66px** に対して
      帯 284px）」「拓本から測った**字送り 8.4mm**」。その列の字は 44mm ほどなので、
      **地のざらつきの細かい周期を拾っている**
    - 直し①：`rubAiBand` で、測った字送りが**校正の見当の 0.5 倍より小さい**ときは
      校正の値に置きかえる。**大きすぎる方は置きかえない**
      （倍音は窓が大きくなるだけで害が小さい。実測：両方置きかえたら
      読めていた列が読めなくなった＝3 列 → 2 列）
    - 直し②：［拓本ぜんたいを通す］の終わりに、**列ごとの中身**を出す
      （帯○〜○／字送り○px／AI の墨○%／字○・字送り○mm、または「切り分けできず」）。
      `S.rub.allRep` にも控える。どの列で何が起きたかを、写真 1 枚で確かめられる
    - 実測（上下で作りが変わる拓本）：「1列目 帯95〜425（330px） 字送り 669px
      AI の墨 6.4% 字 6・字送り 103.5mm ｜ 2列目 … ｜ 3列目 …」

  - **【真因】通しだけ帯を 8% 広げ、紙の白いふちに届いていた（v24.2。本人の報告）**
    - 本人の言葉：「列の見極めは完璧でした。拓本全体を通すだと左側の縁取りが
      うまくいかず、1 行目を指定し、この帯だけ通すを実行するとうまくいきます」
    - **2 つのボタンで帯の決め方がちがった**
      - 列の札を押す … `bandL = c.a`／`bandR = c.b`（そのまま）
      - 拓本ぜんたいを通す … `c.a - 8%`／`c.b + 8%`（**のりしろを付けていた**）
    - 左端の列では、その 8%（＋ AI へ渡すときの 20%）が**こすっていない紙の白いふち**に
      届く。拓本の字は**白**なので、紙のふちも AI には墨に見える。縦に長い塊になるので
      `rubAiTrimCols` が**そちらを本物の列と勘ちがい**し、字の列の方を 0 にする
      → 「左だけ青が出ない」。札から通すと のりしろが無いので、そこに届かない
    - 直し：① `rubBandLimit()` を **拓本の枠（`frameX`）と通す範囲**まで狭めた
      ② `rubAllGo` の のりしろも、**となりの列と拓本のふちの手前**で止める
      ③ `rubBandLimit()` が**自分の列をとなりと見なす**あたり（帯の中心と列の中心が
      ずれるため）を、重なりぐあいで見分けるよう直した
    - 実測（上下で作りが変わる拓本）：通しの切り取りが 札と ほぼ同じになった
      （列1 67〜427／札 67〜425、列2 424〜736／425〜734、列3 733〜999／734〜999）。
      **通しで 3 列とも読めるようになった**（前は 1 列だけ）。
      列1＝6 字・字送り 103.5mm（上段の「寒巌健蘊居士」6 字と合う）
    - 柴田の拓本も 4 列のまま（帯が 0〜289 → 34〜280 と、ふちを含まなくなった）

  - **見つけた列を、絵の上に出す（v24.1。本人の指示）**
    - 本人の言葉：「いちばん上の行から列を見つけるで、結果が画面で見れるようにして」
    - いままでは本数と番号の札だけで、**どこを列と見たのかが絵で分からなかった**
    - `S.rub.colsTop` に `y0`／`y1`（見た所の上下）と `side`（使った字の大きさ）を
      足し、`rubRender` で**むらさきの帯**（#6A4AC8）と番号を出す。
      見た所（上から何字ぶん）は濃いむらさき、その下は うすく
    - パネルに［**絵に出す**］のチェック（`rubShowCols`。はじめは入）と、
      列ごとの **左〜右 px と幅 px** を出す
    - 実測（上下で作りが変わる拓本）：列 3 本（119〜422／428〜731／737〜999 px、
      幅 303／303／262 px）が、番号つきで絵に重なる

  - **帯が となりの列に食い込んでいた（v24.0。本人の報告）**
    - 本人の言葉：「一番左側の文字がほぼ読めていません。この理由はわかりますか」
    - 帯は はらいの先を落とさないよう **左右 2 割ぶん外へ広げて** AI に渡している
      （v21.64）。ところが この拓本は列と列が **6px しか離れていない**
      （119〜422／428〜731／737〜999）。広げた先に**となりの列が丸ごと入る**
    - すると 2 つのことが起きる
      1. `rubAiTrimCols` は「切れ目の多いまとまり **1 つだけ**」を残して
         あとは 0 にする。**となりの列の方が墨が多ければ、こちらの列が消える**
         → 本人の画面で「左の列だけ青が無い」のは これで説明が付く
      2. 消えなかった場合も、となりの字は高さがずれているので
         **字と字のすき間が埋まる**。実測：すき間の行 **0／2095**・かたまり 1 個
         → 「AI の墨から字を切り分けられませんでした」
    - 直し：`rubBandLimit()` を足し、**となりの列とのまん中**で広げるのを止める。
      `rubBandWide()` と `rubAiBand` の切り取りの両方に効かせる
    - 実測：帯 25〜516 → 25〜437／334〜825 → 413〜746。柴田の拓本は 4 列のまま
    - **試してやめたこと**：切る高さ（`rubAiSplit` の `mx*0.04`）を上げれば
      すき間が作れるが、**6 字の列が 99 個のかけらになった**。
      地のざらつきが残る墨は、切る高さでは直せない。直すなら AI の墨そのもの

  - **上下で作りが変わる拓本を、知らせるようにした（v23.9。本人の指摘）**
    - 本人から届いた案件（`b9de741e`。20260907_拓本.jpg 1073×4535）は、
      **上が戒名 3 列（字幅 34mm）／下が命日と俗名で 6 列（18〜19mm）**。
      下では **名前の列と命日の列が すき間なく**並ぶ（実測：6 列が 3 つの塊に見える）
    - 足したもの
      - `_rubCharSides()`：校正にある字幅を、大きい順に px で並べる
      - `_rubColsScore(cols, side)`：間隔のそろい＋幅の合いぐあい（小さいほど良い）
      - `_rubColsSplitWide()`：幅が字幅の 1.5 倍を超える列を、**いちばんうすい所**で割る
        （うすさが ふだんの 0.35 倍以下でなければ割らない＝字のまん中を割らない）
      - `_rubRunsOf(prof, n, side, mul)`：切る高さを外から指定できるようにした
    - **えこひいき**：ふだんは校正のいちばん大きい字幅を使う。ほかの字幅に乗りかえるのは
      「いつものが点数 0.5 超（＝うまくいっていない）」かつ「乗りかえ先がその半分より良い」
      ときだけ。**これを入れないと、ふつうの拓本で 4 本が 6〜7 本に割れた**（実測）
    - **知らせ**：`sideUsed` で下の方（55%・80% の高さ）のかたまりの幅を測り、
      **どちらも上の 0.7 倍未満**なら「下の方は字が小さくなります（上 ○px／下 ○px）。
      範囲を上下に分けて…（下は ○ 列に見えます）」と出す。
      列の本数で比べると、ふつうの拓本でも いらぬ知らせが出たのでやめた
    - 実測：この拓本 → 上 3 列（正しい）＋「上 303px／下 119px」の知らせ。
      柴田の拓本 24 通り・実案件 6 通り → **前と同じ結果のまま**（割りすぎない）
    - **まだできないこと**：下の 6 列を自動では取れない（名前と命日がくっついているため
      3〜4 本になる）。下だけを範囲にして **［列の数］に 6** を入れると、
      6 本（67〜232／213〜387／368〜542／524〜698／679〜853／834〜999）に等分できる

  - **②へ渡す墨は、この字の画だけにする（v23.8。本人の問い）**
    - 本人の言葉：「枠の外まで修正できるようになると、上下や隣の文字の扱いは
      どうなるのですか？ 入らないようにするために枠の中だけに絞ったのですが」
    - 答え：**①と②で、となりの字の扱いは逆**。
      ①「読む」＝**窓の中の墨ぜんぶ**が正解（となりの字も墨。1 字に切るのは枠の仕事）。
      ②「整える」＝**この字 1 つの形**が正解なので、となりの字は入ってはいけない
    - これまで ②には `RG.ink`（窓ぜんぶ）を渡していた。合成データ（`make_synth.py`）は
      1 字だけの絵なので、**②には よその字が混じった絵が入っていた**
    - 直し：`rgInkOfChar()` を足した。**塊（つながった画）ごと**に見て、
      枠にかかっていないものを落とす。**四角く切らない**（切ると また直線の跡が残る）。
      枠は 6% 内側で見る（となりの字が枠にかすっただけで残らないように）。
      ［② に通す］・［② に登録する］・［縁取りから直す］の 3 つとも これを使う
    - 実測：①の墨 41611 ます → ②へ渡す墨 24317 ます（となりの字 17294 ますを外した）。
      枠より上の墨 7786 ます → 0 ます

  - **枠のしばりは、はじめは切る（v23.7。本人の指摘。v23.2 の考え直し）**
    - 本人の言葉：「参考の左上や左にあるのは**枠の跡**で、**枠外が消えないために
      縦線になっています**」
    - v23.2 の「枠の外はさわれない」は**害の方が大きかった**。枠の中の地を消すと、
      枠の外は消えずに残り、**枠の境目が まっすぐな線**になって正解に入る。
      拓本にない直線を教えることになる
    - 直し：`lockOut` は **はじめ false**（枠の外も直せる）。チェックは
      ［**枠の中だけ直す**］に変え、入れたときは「枠の境目がまっすぐな線になって
      残ります」と知らせる
    - あわせて、登録のときに **`box`（512 の中の x1,y1,x2,y2）を一緒に送る**ように
      した。`app.py` の `feedback()` は それを `meta.json` に覚える。
      いまは使わないが、**「枠の外は採点しない」**を拓本AI に入れるときに要る
      （`train.py`／`data.py` で loss から外す）。※ 事務所で `update.sh` が要る
    - **v23.2〜v23.6 のあいだに、枠の中だけ直して登録した組は、枠の跡が
      入っている可能性がある**。ダッシュボードで正解に直線が見えるものは
      〈使う〉を外すか、登録し直す
    - 実測：はじめ＝しばり切・枠の外で消せる。［枠の中だけ直す］を入れると
      枠の外で消えない。登録の中身 `box=43,91,469,421` が送られている

  - **【真因】範囲が小数だと、墨がぜんぶ 0 になっていた（v23.6）**
    - 本人の画面の数字で確定：「範囲の幅 **740.062295081967px**／字の見込み 240px／
      **字らしい所 7px・いちばん濃い所 0.00**／いちばん広いかたまり 0px」
    - 範囲はマウスで引くので **`x`・`w` が小数**になる。そのまま画素の並びを指すと
      `d[小数]` が **undefined**。AI の帯を数える所
      `if (dd[(y*w2 + x) * 4] > 100)` は、y が 0 の行だけ番地が整数なので、
      **1 行ぶんしか数えない**＝どこも 0 に近い値になる。
      これが「字らしい所 7px・いちばん濃い所 0.00」の正体
    - **範囲なしのときは `frameX`／`clampRange` が整数**なので、たまたま起きなかった。
      「範囲あり＝失敗／範囲なし＝成功」の本当の理由はこれ（v22.6・v23.4・v23.5 の
      見立ては いずれも外れ。ただし どれも別の弱点の手当てにはなっている）
    - 直し：① `rubArea()` は **かならず整数**で返す ② 描いた範囲も丸めて覚える
      ③ `_rubTopProf` の画素の番地を丸める ④ AI の帯は `acc.width`（整数）で数える
    - 実測：本人の範囲そのもの（幅 740.062295081967px）で **3 本**（161／425／676px）。
      小数の範囲 3 通りとも通る。ぜんぶ墨の帯で前の数え方を真似ると
      **いちばん濃い所 0.00**（本人の画面と同じ症状）
    - **教訓**：画素を指す値は、入り口（`rubArea`）で整数にそろえる

  - **切る高さを、読めるまで下げる（v23.5。本人の報告のつづき）**
    - 本人の言葉：「緑の線で範囲指定した部分が分かるはずですが、これではだめなので
      しょうか？ 範囲指定した上の線のすぐ下に 1 文字目があります」
      → **上のふちは合っていた**。v23.4 の見立ても外れ
    - 新しい見立て：`_rubRunsOf` の切る高さは **まわりの中ほどの濃さ × 0.45**。
      範囲を字にぴったり合わせると、どこも濃いので**中ほどが上がり**、
      残るかたまりが細切れになって「幅 side×0.4 未満」で ぜんぶ捨てられる。
      範囲なしだと 黒い余白のぶん中ほどが下がるので、たまたま読めていた。
      **範囲あり＝失敗／範囲なし＝成功 の説明が これで初めて付く**
    - 直し：`_rubRunsAt(sm, n, side, lim)` に切り出し、
      切る高さを **0.45 → 0.30 → 0.20 → 0.12** と下げて、
      かたまりが出た所で止める（今までの高さで読めるものは、今までどおり）
    - 実測：50px おきに濃淡が入れ替わる並び（範囲をぴったり合わせたときの形）で
      **0 個 → 1 個**。柴田の拓本 24 通りは前と同じ結果のまま

  - **範囲の上のふちは、どこでもよいことにした（v23.4。本人の言葉）**
    - 本人の言葉：「右端の 1 行が見切れているので外したいだけなのですが」
    - **範囲は 横をそろえるために描くもの**で、上のふちは たまたまそこになっただけ。
      v22.6 の `_rubTopStart` は 5 字ぶんしか下がらなかったので、
      上のふちが字より ずっと上だと、まだ読めなかった
    - 直し：**範囲の下まで（40 段まで）下がって探す**。見込みの字幅も 0.6 倍まで
      ゆるめて見る。段は 0.5 字ずつ
    - 実測（柴田。右端の列を外した幅 820px）：上のふちを y=0／200／486／1000 の
      どこにしても **3 本**（157〜162／424〜446／701〜731px）。150〜190ms

  - **列さがしが読めないときの逃げ道と、数字の出る知らせ（v23.3。本人の報告）**
    - 本人の言葉：「通す範囲をきめてから、いちばん上の行から列を見つけるを
      実行するとエラーが出ます」（v22.6 の手当てのあとも出た）
    - 見立て：`side`（＝校正の字の大きさ ÷ mm/px）は**見込み**でしかない。
      拓本の字がそれより小さいと、`_rubRunsOf` の「幅 side×0.4 未満は捨てる」で
      かたまりが 1 本も残らない
    - 直し①：読めなければ **見込みを 0.6 倍・0.35 倍にして拾い直す**。
      拾えたら「字が見込み（○px）より小さかったので ○px で見ました」と知らせる
    - 直し②：それでも読めないときは、**数字を出す**
      （範囲の幅／字の見込み／字らしい所の数／いちばん濃い所／いちばん広いかたまり）。
      どこで詰まったかが分かる。あわせて**範囲ぜんぶを 1 列として置き**、先へ進める
    - 実測：60px 幅の列が 240px ごと（見込み 240px）→ **4 本 見つかる**
      （144px で見た、と知らせる）。まったくの無地 → 数字入りの知らせが出て、
      範囲ぜんぶが 1 列になる。ふつうの拓本（柴田）は 4 本のまま変わらず

  - **①は枠の外をさわれないようにした（v23.2。本人の指示）**
    - 本人の言葉：「学習時に誤って消さないように、枠外は編集不可にした方がいいのでは？」
    - `rgBoxPx()`（枠の 512 での場所）と `rgLocked()` を足し、`rgPaintAt` で
      筆の届く範囲を枠の中だけに切る。**①のときだけ**（②は その字 1 つぶんの
      形なので、縛らない）
    - 枠の外は **rgba(255,255,255,.40) を敷いて**、見えるが直せないと分かるようにした
    - 直す用があるときのために、［**枠の外も直す**］の チェックを足した
      （初期は切。入れると「となりの字は消さないでください」と知らせる）
    - 実測：枠の外で消す → 墨 41090 のまま変わらず。枠の中で消す → 41090 → 40142。
      ［枠の外も直す］を入れてから枠の外で消す → 40142 → 39109
    - **残っている弱点**：枠の外は「AI が出したまま」が正解として保存される。
      そこに AI のまちがい（地のざらつき）があると、**まちがいを正解として教える**。
      直すには 拓本AI 側に「ここは見ない」region（loss から外す）を入れる必要がある。
      枠を `meta.json` に書いて `train.py`／`data.py` で外す、が筋
    - 使えない組は、**ダッシュボードの「使う／使わない」**（`pair_off`、`off` の
      空ファイル）で学習から外せる。消さなくてよい

  - **①はとなりの字を消さない。字の枠を学習画面に出す（v23.1。本人の指摘）**
    - 本人の言葉：「枠内ではなく、枠の外の他の文字も入っていて、どこまで修正して
      いいかわかりません。他の文字を消してしまうと、消すことを覚えてしまうと
      マズいですよね」。**そのとおり**で、これまでの案内が**間違っていた**
    - ①「読む」は `raw.png → mask.png` の**画素ごとの学習**（`train.py`／U-Net）。
      本当の画を「墨ではない」と教えると、同じ見た目の所に**正反対の正解**が
      混ざり、AI は迷うだけになる。**1 字に切り分けるのは枠（`rubAiSplit`）の仕事**で、
      AI の仕事ではない
    - 直し①：説明を書きかえた。「**となりの字は、墨なら消さないでください**。
      消すのは、地のざらつき・紙のしわ・傷など**字でないもの**だけ」
    - 直し②：学習画面の絵に、**この字の枠を黄色い点線**で重ねた（`rgDraw`）。
      切り抜きは枠の 1.2 倍なので、となりの字が入るのは当たり前。
      枠は**目印**であって、直してよい範囲ではない（枠の外も直してよい）
    - **v23.1 より前に「となりの字を消して」登録した組は、教材として害がある**。
      拓本AI のダッシュボードで見つけて消すか、登録し直す
    - 前の案内文（消すように書いてあった）は v21.63 から v23.0 まで

  - **どの列でも AI の墨で学習できるようにした（v23.0。本人の指摘）**
    - 本人の言葉：「拓本補正の画面上では残っているのに、学習で消えているのは
      理解しがたいです」。**そのとおり**だった
    - 中身：`RUB.aiBand` は **1 列ぶんしか持たない**（列を通すたび上書き）。
      いっぽう拓本補正の画面は、列ごとに取ってある**青い絵**（`L.aiUrl`）を
      ぜんぶ重ねて出している。だから画面には残るのに、学習では消えていた
    - 直し：`rgBandFromUrl(url, sc)` を足した。青い絵の**すけ具合（アルファ）が
      AI の濃さ そのもの**なので、そこから墨を取り出す。`rgOpen` は
      ① いま持っている墨（その列のものなら）→ ② その列の青い絵 → ③ しきい値
      の順に使う。`rgInk` の 3 番目の引数は 真偽から**墨そのもの**に変えた
    - 縮め具合は `L.aiSc`（絵を作ったときの `SC`）を刻んで使う。
      `mw / RUB.W` で出すと丸めの分ずれて、拓本の下の方で 2〜3px 食いちがう
    - 実測（4 列を通したあと）：**4 列とも「AI の墨」**で開くようになった
      （前は最後の列だけ）。列 1 の字の中心で 列 1 の絵＝255・列 4 の絵＝0。
      いま持っている生の墨と、絵から読み直した墨は **262144 ます すべて一致（ちがい 0）**

  - **Delete で枠を消す／墨の出どころをはっきり出す（v22.9。本人の指示と疑問）**
    - 本人の言葉：「文字の枠を選択して、デリートボタンで削除できるようにして。
      これは AI が出した画像ではないですよね」（学習画面のまっ青な墨の写真つき）
    - **Delete／Backspace** で、選んでいる字の枠を消す（`rubItemDel`）。
      拓本の画面のときだけ・欄に入力中は効かない・学習画面をひらいている間も効かない
    - **本人の疑問は当たっていた**。［拓本ぜんたいを通す］は `RUB.aiBand` を
      列ごとに上書きするので、**最後の列の墨しか残らない**。ほかの列の枠から
      学習画面をひらくと `fresh` が偽になり、**しきい値で拾った墨**が出る。
      知らせは一度出るだけで見落としやすかった
    - 直し：①の説明のすぐ下に、`inkFrom` で書き分けて出す。AI なら
      「ここに出ているのは **AI が出したそのまま**です」、しきい値なら
      **赤字で**「これは AI の墨ではありません。しきい値（明るさ）で拾った墨です」
    - 実測：枠 19 → 18（Delete）。選んでいない／欄に入力中／学習画面中は消えない。
      通しのあと 1 列目の枠をひらくと `inkFrom=hand` で赤字が出る

  - **学習画面は AI の墨をそのまま出す（v22.8。本人の指示。v22.7 のざらつき落としを取り消し）**
    - 本人の言葉：「拓本補正の画面と学習画面は同じでいいです。AI で間違っているなら
      手で直すので、学習画面はアルゴリズムで作ったということですよね？ AI でいいです。
      AI の代わりに人ではなく、間違ったところを指摘するので」
    - **こちらの計算で掃除すると、AI の間違いが見えなくなる**＝教材にならない。
      v22.7 で足した `rgInkClean()` は丸ごと外した。`rgInk()` は元どおり
      `A2.map > 127` をそのまま出す
    - 学習画面の説明に「ここに出ているのは **AI が出したそのまま**です」と足した
    - 実測：学習画面の墨 24807 ます ＝ AI の墨をそのまま数えた 24807 ます（ぴったり同じ）
    - **教訓**：AI の出しぶんを人が直して貯めるのが学習データ。その途中に
      こちらの後処理を挟まない（挟むなら AI 側に入れて、学習にも効かせる）

  - **枠を右クリックで学習画面へ／学習画面のざらつきを落とした（v22.7。本人の指示）**
    - 本人の言葉：「学習させるために、ノイズがある物を優先的に行いたいので、
      枠を右クリックで学習画面へ行けるようにしてほしい。また、学習画面が
      縁取りがノイズだらけで、拓本補正画面と違っています」
    - **右クリック**：拓本の絵の上で右クリックしたとき、字の枠の中なら
      `rgOpenAt()` で**その字の学習画面**をひらく（`rubItemAt()` で当たりを取る。
      よその列の枠なら、その列に移ってから）。回転・トリミングの最中は
      右クリックに別の役目があるので奪わず、**メニューの先頭に
      ［この字を学習画面でひらく］**を出す
    - **ざらつき**：拓本補正の画面は AI の濃さを**そのまますけ具合**にして
      重ねているので、うすいざらつきは ほとんど見えない。学習画面は
      `A2.map > 127` で切って まっ青にしていたため、**同じ墨なのに**
      ざらつきだけ くっきり出ていた
    - 直し：`rgInkClean(gv, N)` を足した。① **二段しきい値**（うすい 110 で
      つながりを見て、濃い所＝おおむね 200 以上 を含む塊だけ採る）
      ② いちばん大きい塊の 6% 未満、または 0.04% 未満の かけらは落とす。
      落とした数は学習画面の説明に出す（［塗る］で足せる）
    - 実測（柴田の拓本・4 列を通して 4 列目の 6 字目）：墨 35385 → 28814 ます
      （**ざらつき 6571 ます＝19% を落とした**）。字の画はそのまま残る。
      列の境目に出ていた縦一列の斑点が消えた

  - **通す範囲を決めると列さがしが失敗する のを直した（v22.6。本人の報告）**
    - 本人の言葉：「通す範囲を決めるで範囲を確定させた後、いちばん上の行から
      列を見つける を押しても『字のかたまりを見つけられませんでした』。
      範囲をやめるとうまくいきます」
    - 原因：範囲は ふつう **字より少し外**に描く。その上のふちから
      1 字ぶんの帯を数えていたので、上の何段かが まるごと**地**になり、
      字が 1 つも出てこなかった（AI は 2 段しか見ないので特に当たりやすい）
    - 直し①：`_rubTopStart()` で、**字が始まる高さを手元で先に探す**
      （範囲の上のふちから 1 字の 3 分の 1 ずつ、5 字ぶんまで下げて見る）。
      下げたときは 知らせに「範囲の上のふちより ○px 下から読みました」と出す
    - 直し②：AI で読めなかったとき／**1 本で範囲いっぱい**になったとき
      （AI の墨が地まで塗ったとき こうなる）は、**手元でもう一度**見直す（4 段）
    - 実測（柴田の拓本 1074×5739・字 240px）：範囲の上を 486px ずらして
      画像のてっぺん（y=0）から取っても **246px 下がって 4 本（157／419／692／910px）**。
      AI がまっ黒を返す／地まで塗る どちらでも 手元に切り替わって 4 本。
      範囲なし・範囲あり・上下左右ずらし 24 通りで 前と同じ結果

  - **字の枠を明るいピンクに／AI補正を押したら［回して合わせる］を切る（v22.5。本人の指示）**
    - 枠の色：いま見ている列 **#FF2D9B（明るいピンク）**、ほかの列 **#FF9ECF（うすいピンク）**、
      **選んでいる 1 字だけ #FFD400（黄）**。線も太くした（0.0014〜0.004 → 0.0025〜0.006 × 幅）。
      前は青（#1B6FD6）と水色（#8AA7C8）で、黒っぽい拓本の地に沈んでいた
    - AI補正の節を `#aiFixBox` でひとまとめにし、**その中を押したら `rotMode` を切る**。
      画面の作り直しは `setTimeout` で**その押しが済んでから**（すぐ作り直すと、
      押したボタン自体が消えて 動きが起きない）。節の外のボタンでは切れない
    - 実測：［回して合わせる］入 → ［全体に戻す］／［いちばん上の行から列を見つける］を押す
      → `rotMode` が null になり、ボタンの動き（AI 14 窓）も ちゃんと走った
  - **列さがしを「1 字ぶんの帯 × 何段」にした（v22.4。本人の指摘）**
    - 本人の言葉：「1 文字ではだめですか／文字数によって かなり間隔が空くので、
      行によって読めないことが想定されます」。**そのとおり**だった
    - 前は「上から 2 字ぶん」を ひとまとめに見ていた。列ごとに字の大きさも
      始まる高さもちがうので、その帯が たまたま**字と字のすき間**に当たった列は
      抜け落ちる
    - 直し：**1 字ぶんの帯を上から順に何段か見て、段ごとの値の いちばん大きいものを採る**
      （どれか 1 段でも字があれば列とみなす）。段の数は 手元 4 段／AI 2 段
      （AI は 1 段 7 窓なので 14 窓。拓本ぜんたいの 98 窓に比べれば軽い）
    - 実測（柴田さんの拓本）：1 字ぶん／1.5／2／3 字ぶん、2 字目だけ、3 字目だけ、
      4 字目だけ — **どれでも 4 本**（中心は 148〜171px のあいだで少し動く。拓本の傾きぶん）
    - **範囲の上のふちがずれていても大丈夫**になった。実測：範囲の上を
      1 字ぶん・2 字ぶん 上にずらして（1 段目が字の無い所に当たる状態にして）も **4 本**
    - 手元だけなら **132ms**
  - **通したら まっ黒になる／帯が壊れる を直した（v22.3。本人の報告）**
    - **まっ黒の正体**：AI の墨の絵は「下敷きを黒でぬりつぶし → 墨を白で描く」で作るのに、
      **青＋透け具合に直す処理が入っていなかった**（但し書きには書いてあるのに、
      v21.57 の時点ですでに無い）。不透明な黒い絵を拓本の上に重ねていた。
      直して「色は青・濃さをそのまま透け具合」に。実測：**96% がすき通る**ようになった。
      合わせて `inkMm2` の数え（`n1`）も、ずっと 0 のままだったのを直した
    - **帯が壊れる**：`rubColsScanTop` が見つけた本数を `S.rub.colN` に書いていたため、
      `rubColsWanted` が「人が本数を入れた」とみなして**等分に割り直し**、
      せっかく見つけた**場所**が捨てられていた
      （実測：34〜277／277〜541／541〜805／805〜989 → 34〜287／258〜526／… ）
    - **帯寄せ（`rubBandSnap`）が帯を 253px → 86px に潰していた**。拓本は 字が白・地が黒で、
      いちばん濃い所は字ではなく地。もとの 6 割より細くなる寄せは捨てるようにした
    - **絞りすぎ**：字送りを読みそこねた列で 帯が 33px（字の 8 分の 1）まで絞られていた。
      校正の字の大きさを下限にした
    - **こちらが決めた帯は、幅で断らない**（`rubAiBand(noAsk, trusted)`）。
      「帯が広すぎます」は**人が 2 列ぶんを帯にした**ときのための見張りで、
      列さがしが出した帯にまで効いていた（実測：正しい 288px の列が門前ばらい）
    - 実測（柴田さんの拓本・にせ AI で通し）：直す前は 3 列が壊れて 1 列が門前ばらい →
      直したあとは **4 列とも通る**
  - **［いちばん上の行から列を見つける］を足した（v22.2。本人の案。**これが本筋**）**
    - 本人の言葉：「まず一番上の文字を AI で通して、文字のセンターが分かれば、行数が分かる」
    - **v22.0 の「校正の列数を使う」は まちがい**だった。拓本には**すでに彫ってある列**が
      並んでいて、校正には これから彫る 1 列しか入っていない
      （実測・柴田さんの案件 `20260903.json`：**拓本 4 列・校正 1 列**）
    - やり方：**いちばん上の 2 字ぶんの帯だけ**を見て、字のかたまりを拾い、
      その**間隔**から列を割り出す。上の行はどの列にも必ず字がある。
      かすれている列も、間隔から補える
    - 字の拾い方は 2 とおり。**AI**（窓を横にずらして ①「読む」に通す。実測 7 窓で済む）と、
      **手元**（まわりより明るい所＝字。**画の太さより小さい塊は捨てる**。
      実測：白い画素 31999 → 塊を捨てて 24406。これをしないと地のざらつきで一面が字に見える）
    - **ならしはすき間より細かく**。±30px でならしたら 70px のすき間が埋まって
      4 本が 2 本になった。字の 16 分の 1（±15px）にして直った
    - 実測（柴田さんの拓本 1074×5739）：**4 本**／中心 156・409・673・897px（間隔 264px）。
      **73ms**（AI なし）。絵に重ねて目でも確認ずみ
    - 優先順：①［列の数］に人が入れた数 → ②いちばん上の行から見つけた列
      → ③センターライン → ④切れ目 → ⑤校正の列数 → ⑥並びの周期
  - **字送りが読めないときに止まるのをやめた／列の場所はセンターラインから（v22.1）**
    - 本人の報告：3 列とも「字の並び（字送り）を読めませんでした」で止まった
    - ① **列の場所はセンターラインから**（`rubColsFromCenters`）。拓本に切れ目が無い以上、
      列の**場所**も拓本からは読めない。拓本の上のセンターライン（`S.rub.centersPx`）は
      人が合わせたものなので、いちばん確か。帯の幅は その列の字の幅（`layout().w`）× 1.3。
      **センターラインが近すぎて重なるときは使わない**（拓本に合わせていない見込み。
      実測：この案件は 3 本が 83〜114px しか離れておらず、字幅 31.5mm なので使えない）
    - ② **字（白）の並びでも測る**（`rubCharProf`）。地を一面にこすってあると
      「墨の量」はほとんど揺れないので周期が立たない。**まわりより明るい所＝字**を数える。
      紙の余白もスキャナの黒も一様なので 0 になる。墨で読めなかったときだけ通る。
      実測：字の列にぴったりの帯で 350px＝29.2mm（正解）。25〜47ms
    - ③ **それでも読めなければ、校正の字送りを使って通す**（`rubPitchPlanPx`）。
      止めるより、校正の値（字の高さ × 1.15）で窓を置いて AI に通す方がよい。
      帯の幅から、その列がどの大きさの列かを当てる（列の帯 ≒ 字送りの 1.3 倍）。
      知らせに「校正から取った字送り 29.1mm」と出す
    - 実測：直す前は 3 列とも 0 窓（門前ばらい）→ 直したあとは 3 列とも AI へ届く（98 窓）
  - **列の本数は「校正の列数」から取る（v22.0。本人の指摘。**これは作りの前提のまちがい**）**
    - 本人の言葉：「**石の地が一面にこすってあるのが通常です。白い切れ目など存在しません**」
    - これまでの作りは `detectColumns`＝**墨の切れ目から列を見つける**だった。
      拓本は石の面をぜんぶこするので、**列と列のあいだに白い切れ目はできない**。
      つまり、これまで「読めませんでした（石の地が一面にあると…）」と
      **例外あつかいで断っていた方が、ふつうの拓本**だった
    - 直し：**何列あるかは校正の側が知っている**（字の入っている行が使っている
      センターラインの本数＝`rubColNFromPlan`）。それを本数にして等分に割る。
      切れ目から見つけた列は、**校正の列数と一致するときだけ**そのまま使う
      （合わなければ、こすり跡を列と読み違えている見込みが高い）
    - 優先順：①［列の数］に人が入れた数 → ②切れ目から見つけた列（校正と数が合うとき）
      → ③校正の列数 → ④並びの周期からの見当
    - 実測（20260804_拓本.json）：校正は col 0／1／2 の 3 列。
      直す前は「列を切り分けられませんでした」→ 直したあとは 3 本に等分
    - 等分に割るので、**［範囲］（緑の四角）を石の彫ってある面に合わせる**必要がある。
      知らせにもそう書いた（範囲にスキャナの黒い所が入っていると、そこも 1 列になる）
  - **通している間の進み具合を出し、中止できるようにした（v21.99。本人の報告
    「拓本ぜんたいを通すを押すと フリーズしたようになる」）**
    - 右上に黒い小窓（`#aiProg`）を出す。**列 2 / 3 ／ AI に通しています 12 / 28 ／ 経過 41 秒**。
      ［中止］で、そこまでで止められる（実測：4 / 28 で押して 4 回で止まった）
    - 止まって見えていた所：① 列をさがす（`detectColumns`＋字送り）② 列ごとの字送り
      ③ AI へ 1 窓ずつ。**①②は数秒かかる同期の計算**なので、その前に `uiTick()` で
      画面を 1 回描き直させる。はさまないと、押してから終わるまで画面が固まる
    - ［列の数］が効かないことがあった（本人の報告「認識しませんでした」）。
      `onchange` は**欄から出たときにしか起きない**ので、数字を入れてすぐ
      ［拓本ぜんたいを通す］を押すと届かなかった。`oninput` でも受け、
      さらに `rubColsWanted` が**画面の欄の値も見る**ようにした
      （実測：欄に 3 と入れただけで 3 本に分かれ、進み具合も「列 1 / 3」と出る）
  - **実物の拓本（20260804_拓本.jpg）で通してみて、字送りの測り方を直した（v21.98）**
    - **見つけたこと 1：列ぜんたいを一度に測ると、字送りが半分以下に出る**
      （実測：ぜんたい 234px＝19.5mm → 28.3 字ぶん。本当は 17〜19 字）。
      紙の折り目・地の荒れ・字の大小が混ざって、本当でない周期が勝つ。
      **直し**：4 字ぶんくらいの窓に分けて測り、**まん中の値**を採る
      （実測の窓：330／341／345／348／359／360／369／398px → 348px＝29.1mm）
    - **見つけたこと 2：墨の広がりから「字の大きさ」は測れない**。
      拓本は **字が白・地が黒**なので、いちばん濃い所は字ではなく地。
      `rubInkSpan` は帯の 4 分の 1 に貼り付いた値を返していた
      （実測：帯 690px → 192px。帯を狭めるほど 138／99／85px）。
      これを「字の大きさ」として周期を正していたので、
      **字送りが半分・4 分の 1 に落ちていた**（355 → 174 → 87px）
    - **直し**：見当は **校正の側が知っている字の大きさ**（`layout()` の `h` × 1.15）から。
      案件に 2 種類（戒名・日付）あれば両方を見当にする。
      見当から 4 割以上はなれた答えしか無いときは、見当を当てにしない
    - **結果**（同じ拓本・帯をいろいろ変えて）：
      直す前 234／264／176／174／116／87／176px → 直したあと **348／350／352／348／348／347／352px**。
      どの帯でも 29.0〜29.4mm でそろう
    - **見つけたこと 3：墨から見つけた「列」は、こすり跡ぜんたいになる**
      （実測 690px＝58mm。字は 30mm）。そのままでは「帯が広すぎます」で 1 列も通らない。
      **直し**：［拓本ぜんたいを通す］のときだけ、広すぎる列を **まん中に字送りの 1.4 倍**だけ残す
      （実測：765〜1565 → 918〜1412。字の列にぴったり乗る）。**手で決めた帯はさわらない**
    - まだ残っていること：この拓本は**右はしにスキャナの黒い所**が写り込んでいて、
      それを墨として数えてしまう（しきい値・墨の広がり・帯寄せが右へ引っぱられる）。
      ［トリミング］で切ると安定する
  - **学習用の組の名前に、案件ごとの合い印を入れた（v21.97。本人の指摘）**
    - 本人の指摘「すでに同じところからの文字があった場合、保存できないのでは」。
      **保存はできるが、前の組に黙って上書きされていた**
    - 組の名前は「施主名＋行＋何字め＋字」だけだった。**施主名が同じ（同姓）か空**だと、
      **別の案件の同じ場所が同じ名前**になり、前の組が消えていた
      （共有フォルダーの zip も同じ名前なので、そちらも置きかわっていた）
    - 直し：案件に合い印（`S.info.uid`。8 文字。案件の JSON に入る）を持たせ、
      名前を `pair_<施主名>-<合い印>_g<行>-<何字め>_<字>` にした。
      **同じ案件で入れ直したときは今までどおり上書き**（それが狙い）
    - あわせて、サーバーが **`replaced`（前の分を置きかえたか）** を返すようにし、
      知らせに「新しく入れました」／「同じ所の前の分に上書きしました」と出す
    - 注意：**v21.96 までに登録した分は名前が変わる**ので、同じ字を入れ直すと
      上書きではなく**もう 1 組**増える。要らない方は 拓本AI の一覧で「使わない」に
  - **説明文を短くし、効かない欄は使えなくした（v21.96。本人の指示）**
    - ［消す］［塗る］の下の注意書き（「太さ mm を押すと…」）を**外した**
    - **［角とみなす角度］は「まっすぐ（点だけ）」のとき入力不可**にした（うすく出す）。
      まっすぐは角を見つける必要がないので、入れても何も変わらなかった。
      合わせて、その下の言い添えも「まっすぐ」のときは隠す
    - なぞりの説明 **126字 → 21字（17%）**（「角が丸い→角度を小さく。ぎざぎざ→大きく。」）
    - 幅と高さが違うときの説明 **126字 → 32字（25%）**
      （「縦長（31.5×32.0mm）／拓本を 1.6% 横に広げて表示」）。
      くわしい言い分（ます目が正方形だから・出力は元に戻る・mm は横の物差し）は
      **`title`（指を当てたときだけ出る）**へ移した
  - **重さ・青が赤くなる・保存先が分からない を直した（v21.95。本人の報告 3 件）**
    - **マウスが重い** … 別の色のふちを描く `fitEdgeURLOf` が、毎回 `PE.inkC = null` に
      していたので、線を 1 本描き直すたびに しきい値・形態処理・塊の選り分けまで
      やり直していた。**別の取り置き（`PE.inkCAlt`）に分けて使い回す**ように。
      実測 `peEdgeRedraw` **234ms → 7〜13ms**、`peRender` **357ms → 59〜77ms**
    - **消す・塗るで青線が赤線になる** … `peInkRefresh` と 筆を引いている間の `draw()` が、
      青の層（`#peEdgeImg`）に `fitEdgeURL()` を**色なし**で描き込んでいた。
      色なしの既定は赤。どちらも `peEdgeRedraw()`（色と層を分けて描く）に通すように。
      実測：直す前は 消した／塗った あと青の層が rgb(220,38,22)＝赤、直したあとは
      rgb(30,110,220)＝青のまま
    - **取り置きに ます目の枠の見分けを足した** … `key` は輪郭を見ていないので、
      ［パス化］のあとも前の枠の取り置きを返していた（実測：青だけ 768×774 のまま、
      赤は 674×711）。枠（w/h/x0）が変わっていたら作り直す
    - **拓本AI へ送れたかを必ず言う** … 前はこの端末に貯めるだけでも「保存しました」と
      出て、送れなかったことが分からなかった（本人の報告「拓本AI のページから
      そのデータが確認できません」）。**入れ先（① 読む／② 整える）とサーバーの組数**、
      送れないときは**その理由**（住所が空／http の住所）を出す。
      あわせて **どの一覧に出るか**（①＝「学習用（dataset/pairs）」、
      ②＝「②整える・拓本から・学習用」）も知らせに足した
  - **［パス化］のあと 縁取（赤）が横しまになる のを直した／［AI オート］にも済マーク（v21.94。本人の報告）**
    - 因：［パス化］は `PE.contours` を なぞった輪郭に入れかえるので、ます目の枠
      （`fitGrid`）の幅・高さ・ますの大きさが変わる（実測 768×774 → 674×711）。
      AI の墨（`aiInk.ink0`）は**前の枠の並び**なので、新しい幅で読むと 1 行ずつ
      ずれて**横しま**になっていた。枠は `keyNG` に入っていない（輪郭を見ていない）
      ので、気づけなかった
    - 直し：`aiInk` に そのときの枠（`gr` ＝ x0/y1/cell/w/h）を覚えさせ、`fitAiInk` が
      **長さ違いに気づいたら `fitInkRegrid` で置き直す**（ますの真ん中どうしを字の
      座標で突き合わせる）。枠が分からないときは AI の墨を捨てて拾い直す
    - 実測：直す前 赤の点 29770・横に全幅（0〜674）→ 直したあと 693・輪のまま
      （最長の並び 38、字の箱 229×229）
    - ［AI オート］にも **✓ 済**（`PE_AUTO_OK`。`PE_AUTO_TRACE` は確かめの窓を
      閉じるときに消えるので、済マーク用に別に持つ。［取り消す］と字を閉じたら戻す）
  - **ボタンの字をまん中に、2 文字ぶん字下げ、数字の欄は 1 行に（v21.93。本人の指示）**
    - ボタンは **まん中そろえ**＋**左に 16px（2 文字ぶん）**の字下げ。見出しと
      同じ左端だと、どちらも見出しに見えていた
    - **「済」が付いても折り返さない**（`width:auto;min-width:50%;white-space:nowrap`）。
      実測：92px → 110px に伸びるだけで、1 行のまま
    - ［アルゴリズム精度 ±］［縁を広げる mm］［縁のならし mm］は **`.f.inl` で 1 行**に
    - タブ・筆の並び・確かめの箱のボタンは、この指定より**強い指定で打ち消す**
      （`#pathEd .peside #peTabs button` など。しないとタブが半分幅になってはみ出す）
    - **保存の前に確かめる**（デバッグ用。`ASK_BEFORE_SAVE = true`）…
      入れ先（①／②）・字・組の名前を出して、いいえ なら保存しない。
      要らなくなったら `ASK_BEFORE_SAVE` を false に
  - **ボタンの色を琥珀にし、幅を半分にした（v21.92。本人の指示）**
    - 実行ボタンの色は **琥珀**（`.go1` ＝ #B45309 に白文字／`.go2` ＝ うすい琥珀地
      #FFF7EC に琥珀の枠と文字）。［拓本を動かす］にも `.go2` を当てた
      （`peRender` が class を作り直すので、そこでも付け直す）
    - **［現トレースから字を作る］→［パス化］** に改名（知らせの文言もそろえた）
    - **幅いっぱいだと見出しに見える**ので、右の欄のボタンと数字の欄を**半分の幅**に
      （実測：欄 207px に対して 92px）
  - **②をもう一度押すと線が赤くなる／濃さが青にしか効かない を直し、
    実行ボタンに色を付けた（v21.91。本人の報告・指示）**
    - **赤くなる原因は 2 つ**
      1. ①（拓本解析）を押していないとき、赤い線が**いま画面に出ている墨**
         （＝②の形）を描いていた。→ 赤はいつでも**拓本から拾った墨**
         （①が無ければ しきい値の墨。広げる前）にした
      2. 字を開いて**最初に②を押したとき**、新しい墨の `keyNG` を
         **元に戻したあとの取り置き**から取っていたので空になり、
         `fitAiInk` が「拾い方が変わった」とみて**結果を捨てて**いた。
         2 回めでやっと効く（＝1 回めは赤いまま）。→ 通した場の取り置きから覚える
    - **濃さは 2 本とも**効くようにした（前は青だけ書き換えていた）。
      つまみは［書体補正（青）］の下に置き、**2 本に共通**
    - 重ね順は **赤が下・青が上**（字にするのは青の形なので、隠れないように）
    - タブ名を **「パス／改行／修正」** の 2 行に
    - **実行ボタンの色**（本人の指摘「白だとテキスト入力と勘違い、黒だとラベル」）…
      画面で使っている 赤・青・緑 とぶつからない**藍緑（ティール #0E7490）**。
      `.go1`＝その段のいちばんの実行（AI オート／現トレースから字を作る。白文字）、
      `.go2`＝個別の実行（AI 拓本解析／AI 書体補正。白地＋ティールの枠と文字）。
      済マーク（緑）は、この色より優先
  - **１［作成］タブを組み替えた（v21.90。本人の指示どおり）**
    - **① 拓本を合わせる**（拓本を動かす）／**② 文字制作**（AI オート＋確かめの箱、
      現トレースから字を作る）／**③ 個別モード**（AI 拓本解析／AI 書体補正／
      現トレースから字を作る）／**③ 手動モード**（アルゴリズム精度 ±、消す、塗る、
      縁を広げる mm、縁のならし mm）
    - ［拾う厳しさ ±］→ **［アルゴリズム精度 ±］**、［現トレースから字を作る］は
      ②と③の 2 か所（`peTrace`／`peTrace2`。中身は同じ `peTraceGo`）
    - **線の名前と色をそろえた** … **赤＝縁取**（拓本から拾った墨。広げる前）／
      **青＝書体補正**（②を通したあとの形）。［表示］のレ点も
      ［縁取（赤）］［書体補正（青）］に。②を通していなければ青は出ない
    - **済マーク**（`peStepMark`）… 実行の済んだボタンに「✓ …済」を付けて緑にする。
      戻せば印も消える
  - **確かめの箱を短くし、タブ名を直した（v21.89。本人の指示）**
    - 箱は「AI オート完了／① AI 拓本解析 OK／② AI 書体補正 OK／③ 縁を ○ ます拡大」
      と［取り消す］だけ。**下の説明文は外した**（mm は出さず、ます だけ）
    - **［修正］タブ → ［パス修正］** に名前を変えた（画面の文言もそろえた）
  - **手で①②を押したときも、赤と青の 2 本を出すようにした（v21.88。本人の指示
    「手動で作られる線も、オートと同じように赤と青にして、左の表示でレ点で
    表示非表示できる様にして」）**
    - ［表示］に **［縁取り（青）］** のレ点を足した。［拾った墨］は
      **［拾った墨（赤）］**と書き変え、色を付けた
    - **赤と青は、別々に出し入れできる**（青の層を `fitTraceEdgeLayer` に切り出し、
      赤（`fitEdgeLayer`）と別に足す）
    - ①の墨は `peTraceInk()` で探す … ①を押した直後はそれ自身／②のあとは②が
      控えている `base`／オート中は `PE_AUTO_TRACE`
    - ①②を押したら、青のレ点は**自動で入**にする（切れば、切れたまま）
    - 登録の知らせが、字を作った知らせで消えていたので、**続けて出す**ようにした
    - 実測：開いた直後は赤だけ／①②のあとは赤青／どちらのレ点も別々に効く
  - **［AI 書体補正］を押すたびに字が太っていたのを直した（v21.87。本人の報告
    「AI 書体補正が何度も押せてしまって、押すたびに文字が大きくなる不具合あり」）**
    - **原因**：②に渡していたのが**いま画面に出ている墨**だった。2 回めは
      「②の出した形＋縁を広げた分」が入力になるので、通すたびに太る
    - 直し：**②に渡すのは、いつでも「①の墨（縁を広げる前）＋人の直し」**にした。
      ①の墨は②の中に `base` として控える（3 回め以降も同じ入り口）
    - あわせて `gwC0` は 0 ます で覚える（入り口を広げ量 0 にそろえたため。
      ［縁を広げる］の分は、このあと差として効く）
    - 実測：②に渡した墨は 3 回とも **30034 ます**（①の墨そのもの）。
      出来上がりも 3 回とも同じ
  - **［AI オート］の使い勝手を、本人の書いたとおりに直した（v21.86）**
    - 通したら **画線（この字の線）を消す**。AI の墨と 2 本の線だけを見て決める場なので
    - 知らせは短く … 「AI オート完了／① AI 拓本解析 OK／② AI 書体補正 OK／
      ③ 縁を ○mm（○ ます）拡大」。**ボタンは［取り消す］だけ**
    - **登録は［現トレースから字を作る］を押したとき**（直していれば、そのときの墨で
      ①／②に振り分ける）。直すときは、そのまま［消す］［塗る］を押せばよい
      （通した直後から直せる状態にしてある）
    - **［AI お任せ］→［AI 拓本解析］** に名前を変えた
    - **取り消してもう一度オートを押すと、青い線が出なかった**のを直した
      （前は「直してから登録する」を押さないと直しモードに入らず、線が出なかった）
    - 字を作るときは、画線を出し直す
  - **直した所によって、①と②を自動で振り分けるようにした（v21.85。本人の案
    「縁取りラインと校正ラインを表示させ、校正ラインのみ修正がある場合は②、
    縁取りまで修正する場合は①。縁取りまで変更している場合は校正ラインも
    変わっているので、②の学習は行わない」）**
    - 直しモードでは、**青＝縁取り（①が読んだ墨）／赤＝校正ライン（いまの仕上がり）**
      の 2 本を重ねて出す（`fitEdgeURLOf` で、別の墨の**ふちだけ**を色を変えて描く）
    - 登録の振り分け（`fitInkCount` で、直しを外した墨と ます の数をくらべる）
      - **青が動いた** → ①へ（②には入れない。入れるものが変わっているため）
      - **赤だけ動いた** → ②へ（入れる＝①の墨／正解＝直した形。`set=shape_rub`）
      - **どちらも動かない**（そのまま登録） → ①へ（「これでよい」と認めた組）
    - **筆は丸いので、②だけ直したつもりでも①の縁に少しかかる**（実測：②だけの
      ます を 1 つ消したら①も 3 ます 動いた）。**0.5%（最低 30 ます）未満は
      「動いていない」**とみなす
    - 実測（①は円／②は円＋離れた点 の にせ AI）：そのまま登録 → ink ／
      ②だけの点を消して登録 → **shape_rub**（青 0 ます）／
      ①の墨まで消して登録 → ink（②には入らない）
  - **「直してから登録」が、①の墨ではない物を保存していたのを直した（v21.84。
    本人の指摘「直してから登録だと、どちらの AI の修正か分からなくないですか？」）**
    - **原因**：②を通すと `PE.aiInk` は②の形に置きかわる。`takuShot` は
      `aiInk.from === "trace"` のときだけ AI の墨を使うので、②のあとに切り出すと
      **しきい値で拾った墨**が保存されていた（①の墨でも②の形でもないもの）
    - 直し：①の墨を `PE_AUTO_TRACE` に取っておき、登録のときは**必ず①の墨**
      （＋人の直し、広げ量 0）で切り出す。いま画面が②の形でも同じ
    - **どちらを見て直しているか**を選べるようにした（［見ている：仕上がり／①の墨］）。
      直し（消す・塗る）は拓本の画素に付くので、**どちらを見て直しても両方に効く**
    - 実測：切り替えで `PE.aiInk` と広げ量が入れ替わる（①の墨＝広げ量 0）／
      登録のときに切り出されるのは、画面が②でも **①の墨**
  - **［AI オート］の確かめに「直してから登録する」を足した（v21.83。本人の指摘
    「点が出てしまった場合など、手動で修正する場合に、このままにするを押すと、
    良い物として学習保存されてしまう。戻ると、手作業が増えます」）**
    - えらびを 3 つにした … **［このまま登録する］／［直してから登録する］／［取り消す］**
    - ［直してから登録する］は、**AI の形はそのまま残して、保存だけ待つ**。
      人が［消す］［塗る］で直してから［いまの墨で登録する］を押す。
      AI をやり直す手間も、直しのやり直しも要らない
    - どちらで登録しても、残るのは**縁を広げる前**の墨。直したあとに登録するときは、
      いったん広げ量 0 に戻して切り出し、すぐ元の値に戻す（`peAutoSave`）
    - 実測：直しモードに入った時点では保存 0 回／墨と広げ量はそのまま／
      塗りを 1 本足して［いまの墨で登録する］→ 保存 1 回・広げ量 0.1 に戻る
  - **［AI オート］が手で 3 つ通したのと同じ結果になっていなかったのを直し、
    確かめを画面の中に移した（v21.82。本人の指摘「手で通した物と AI オートでは
    出来上がりが違います」「確かめの窓が文字の上で固定なので、文字の上側が確認できず」）**
    - **原因**：［縁を広げる］の欄は**前の字の値が残る**。0.1 のまま②を通すと、
      ②が「そのときの広げ量」を覚えるので、最後に 0.1 を入れても**差が 0 で広がらない**。
      → オートは**いったん 0 に戻してから**①②を通し、最後に 0.1 にする
    - 実測（同じ字・同じ返事）：手で 3 つ **23267 ます** ／ オート（直す前）22561 ます →
      **直したあと 23267 ます**（絵も同じ）
    - 確かめは `confirm` をやめ、**作字の画面の中**（［AI オート］のすぐ下）に出す。
      字が見えないまま決めさせては、確かめにならない。
      ［このままにする］で保存して進み、［取り消す］でその場で元に戻す
  - **［AI オート］の学習用の保存は「はい」と答えてから、広げる前の墨で（v21.81。
    本人の指示「学習用に保存は、選択肢で「はい」と答えた場合にして、広がる前の
    情報を学習して」）**
    - ②の中の保存は、オートのときは止めた。**確かめで「はい」と答えてから**保存する
    - 保存するのは、**①の直後に取っておいた切り抜き**（`takuSave(quiet, shIn)` を足した）。
      確かめは縁を広げたあとに出るので、そこで切り出すと**広げたあとの墨**を
      覚えさせてしまう
    - 実測：はい → 保存 1 回／いいえ → 保存されない（回数が増えない）
  - **作字の［AI オート］を動くようにした（v21.80。本人の指示「作字画面の AI オートの
    有効化してください。AI お任せと、AI 書体補正、縁の幅を広げる 0.1 を実行する
    イメージです。実行後、確認のメッセージを入れて、戻れるようにもして」）**
    - 押すと **① AI お任せ → ② AI 書体補正 → ③ 縁を広げる 0.1mm** を続けて通す
      （②のところで、今までどおり学習用にも保存される）
    - **3 つまとめて 1 手**として控える（`PE_AUTO` の間は内側の `peSnapAi` を止め、
      終わってから `{ai, grow, auto}` を 1 つだけ積む）。
      ［↶ 1つ戻す］で**縁の広げ量も一緒に**元へ戻る（`peUndoGo`）
    - 通したあとに**確かめの窓**を出す（何をしたか 3 行＋実際に広がった mm）。
      「いいえ」なら、その場で押す前の状態に戻す
    - 実測：はい → 墨あり・縁 0.1・控え 1 手（ai/grow/auto）／［↶ 1つ戻す］→ 元どおり／
      いいえ → その場で元どおり
  - **青い枠をつかむのは左ボタンだけにした（v21.79。本人の指示「青い四角をつかむのは
    左クリックだけでいい。センターは画面移動で使います」）**
    - `bindDrag` だけボタンを見ていなかった（ほかの つかむ所は前から左だけ）。
      まん中ボタンでも枠をつかんでしまい、**枠の上では画面を動かせなかった**
    - ここで止めずに返すようにしたので、まん中ボタンは `makePannable` に届いて
      **枠の上からでも画面が動く**
    - 実測：左で引くと枠が動き（129,42 → 161,74）、まん中で引くと**枠は動かず**
      画面が動く（横 422 → 482）
  - **登録の墨が字とずれることがあったのを直した（v21.78。本人の報告
    「学習に登録でズレていました、回転ズレですかね」）**
    - **原因は 2 つ。どちらも「前に作った墨を、いまの拓本に当てていた」**
      1. **拓本を回したあと**（［この傾きで回転補正する］など）。`rebuildRub` は
         回転を焼き込んで `RUB.data` を作り直すが、**AI の墨（`RUB.aiBand`）と
         1 字ずつの枠はそのまま残る**ので、字とずれる
      2. **別の列を通したあと**。［拓本ぜんたいを通す］では **最後の列の墨**しか
         残らないので、前の列の枠に当てると**よその列の墨**になる
    - 直し：拓本の向きを `rubGeoKey()`（回転＋ずらし）で覚え、AI の墨には
      **通し番号 `id`** を付けた。列（`S.rub.lines[]`）にも `geo` と `aiId` を控える
    - `rebuildRub` で向きが変わったら **AI の墨を捨てて知らせる**。枠には
      `stale` の印を付け、**登録の窓は開かない**（ずれた正解を貯める方が悪い）
    - 列がちがうだけのときは、**しきい値で拾った墨**に切り替えて開く
      （その旨も知らせる）。AI の墨で直したいときは、その列の帯で通し直す
    - 実測：ふつうは AI の墨、番号がちがえば しきい値＋知らせ、回したあとは
      墨が消えて枠は登録できない
  - **字送りの取り違えを「字の大きさ」で直し、断る前に人に聞くようにした
    （v21.77。本人の報告「帯が広すぎます（字送り 179px に対して 帯 357px）」）**
    - 自己相関は、本当の周期の 2 倍・3 倍でも、半分でも よく合ってしまい、
      **合い具合の大小だけでは決まらない**（実測：本当の字送り 300px で 合い 0.13、
      2 倍の 600px で 0.44。合いだけで採ると 598px と出る）
    - そこで **漢字はほぼ正方形**という当たり前を使う。字送りは ふつう
      **字の幅の 1.0〜1.5 倍**。見つけた周期の 1/4〜3 倍のうち、**その見当に
      いちばん近いもの**を採る（合いが 0 に近いものは採らない）
    - 実測：三・日・目（よこ画が多い字）・戒名 とも **299〜300px**（本当は 300px）。
      手で決めた帯 140／180／240px でも 149px（本当は 150px）
    - **断る前に聞く**ようにした。帯を手で 1 列に合わせてあるのに断られると
      進めなくなる。［拓本ぜんたいを通す］の中では聞かない（今までどおり）
    - 実測：2 列を 1 つの帯に入れると、今までどおり「広すぎます」と出て聞いてくる。
      「いいえ」で止まり、「はい」でそのまま通る
  - **筆の丸を、ポインタに付けた（v21.76。本人の指示「塗るや消すときの範囲を
    マウスポインターにして、作字と同じように」）**
    - 作字の画面（`peBrushC`）と同じ見た目。**塗る＝青／消す＝朱**、うすい塗り
      （18%）と細い輪、まん中に狙いの点。**矢印そのものは消す**（丸が筆の先になる）
    - 拡大しているときは、丸も一緒に大きくなる（効く範囲そのものを出す）。
      筆の太さを変えると、その場で太さが変わる。絵から外れると消える
  - **塗りを半透明にできるようにした（v21.75。本人の指示「塗の色も半透明が
    できるようにして」）**
    - ［色の濃さ］10〜100%（既定 60%）。べた塗りだと**下の拓本が見えず**、
      どこを直すのか見きわめられない。縁取りの輪郭（朱）は目印なので、いつも濃いまま
    - あわせて、**筆を動かすたびに横の欄まで作り直していたのをやめた**
      （`rgDraw` と `rgSide` に分けた）。作り直すと**すべり台を引いている途中で
      外れる**（濃さ・筆の太さ・倍率の数字は、その場で書きかえるだけにした）
    - ズーム（v21.74）は**まん中のダイヤル（ころがし）**で効く。1〜12 倍
    - 実測：濃さのつまみを続けて引いて 30%（表示も 30%）、筆も 44 まで引ける。
      ころがすと 1 → 1.4 倍、拡大したまま塗れて［戻す］も効く
  - **登録の窓にズームを付けた（v21.74。本人の指示「ズームも出来るようにして」）**
    - 絵は 512 の下敷き（`RG.buf`）にいったん作り、そこから**拡大して写す**。
      ます目のまま大きくしたいので、なめらか化は切る
    - **ころがすと、指している所を中心に**大きくなる（1〜12 倍）。
      ［−］［＋］［全体］［動かす］も置いた
    - 動かすのは ［動かす］／**まん中ボタン**／**右ボタン**／**Shift を押しながら引く**
    - 筆の位置はズームを見て直してある（実測：4 倍・左上 100,100 で まん中を押すと、
      絵の **164,164** に塗れる。Shift で引いたときは**塗らずに動くだけ**）
  - **②を直すとき、直す前の縁取りを「輪郭の線」で出すようにした（v21.73。本人の指示
    「整えるが崩れすぎていて直すのが厳しい。もう少し、縁取りの状態を目立つように
    して輪郭だけ強調させてほしい。チェックの為、非表示も出来るようにして」）**
    - 直す前の縁取りを**薄い灰色の塗り**で敷いていたが、緑と重なって見分けがつかない。
      **輪郭のますだけ**取り出して（`rgEdge`。1 ます では細いので 3 ます 幅にする）
      **朱の線**で緑の上に重ねる
    - **［縁取りの輪郭を出す］**で消せる（AI の形だけを見て確かめるため）
    - **［縁取りから直す］**を足した。②が崩れすぎているときは、AI の形を捨てて
      **縁取りそのもの**から始められる（［戻す］で元に戻せる）
    - ②が崩れるのは、②が**合成（フォントを荒らしたもの）だけで覚えている**ため。
      拓本の墨は見たことがない。`dataset/shape_rub` が貯まったら
      材料＝拓本 で学習し直して比べること
  - **手で決めた帯を、こちらで狭めてしまっていたのを直した（v21.72。本人の報告
    「手動で幅を決めて［この帯だけ通す］を押すと、帯の幅が勝手に狭くなり、
    帯が広すぎると出て進めません」）**
    - v21.70 で「断る前に帯を墨へ寄せる」を入れたが、**人が決めた幅を変えるのは
      行きすぎ**だった。寄せるのは、こちらが等分に割った帯（［拓本ぜんたいを通す］）
      だけにした
    - あわせて `rubPitchPx` の**上限を帯の幅に戻した**。墨の幅を上限にも使っていたので、
      墨が細く出た帯では**上限が本当の字送りより小さくなり**、字送りが小さく出て
      「帯が広すぎます」と断られていた。**下限だけ**墨の幅から決める
      （下限・上限とも、v21.69 以前よりゆるい側にだけ動く）
    - 実測（地がざらざら・字送り 150px の列。手で帯を 140／180／240px にして押す）：
      字送り **149px**、3 つとも**通る**。押しても**帯は動かない**
  - **1 字ずつ学習に登録する窓を作った（v21.71。本人の案「拓本から縁取りを確認して
    登録、縁取りから書体を整えて登録。このような形をとれば、2 つの学習が手に入る」）**
    - **1 つの字から 2 組**取れる。①読む＝拓本の切り抜き → 人が直した**縁取り**
      （`dataset/pairs`）／②整える＝その縁取り → 人が**整えた形**（`dataset/shape_rub`）
    - ［学習に登録］の **［1 字ずつ登録する］** から開く。窓の中で
      **①で縁取りを直す →［① 読む に登録］→［② に通す］→ 形を直す →
      ［② 整える に登録］→［次の字］** の 4 段
    - 筆は［塗る］［消す］［戻す］（20 手まで）。字の候補があれば押して選べるが、
      **字が決まっていなくても登録できる**（拓本には普通に出てこない字が多い）
    - 切り抜きは**枠の 1.2 倍**（`takuShot` と同じ切り方）。墨は **AI の墨**があれば
      それ、無ければ しきい値。どちらから始めたかは画面に出す
    - 主なもの：`RG`／`rgOpen`／`rgRender`／`rgSide`／`rgPaintAt`／`rgShape`／
      `rgSave`／`rgKey`／`rubCropCv`／`rgInk`／`rgMaskCv`／`bindRegWin`
    - **②の正解に書体のグリフを写さないこと。**②が「フォントを描き写せば当たる」
      と覚え、彫った職人の形が消える（`train.py` にも同じ注意がある）
    - サーバー側：`feedback` に `set`（ink／shape／shape_rub）を足し、
      ②の材料は合成（`dataset/shape`）と拓本から（`dataset/shape_rub`）で分けた。
      学習は `mix`（syn／rub／both）と `val`（物差し）を別々に選べ、
      `noswap` を付ければ**比べるだけで差し替えない**
    - 実測（にせ拓本 5 字）：窓が開き、筆で足す→戻すが効き、①は `set=ink`、
      ②は `set=shape_rub` で raw.png／mask.png の 2 枚が飛ぶ。［次の字］で①に戻る
  - **地がざらざらの拓本で字送りが読めていなかったのを直した（v21.70。本人の報告
    「拓本ぜんたいを通すで読めず、列の数を入れても読めず」＋実物の拓本の写真）**
    - **原因 ①**：`estimatePitch`（自己相関）が**平均を引いていなかった**。
      石の地や紙の目があると墨の量がどこも底上げされ、**どの間隔でもよく合う**。
      実測（実物に似せた 3 列・本当の字送り 150px）：**884px** と出ていた。
      → 平均を引くようにした（v21.69 で列の間隔にやったのと同じ直し）
    - **原因 ②**：字送りの上下限を**帯の幅**から決めていた（下限＝幅の 0.45 倍）。
      ［列の数］で等分に割った帯は字よりずっと広いので、**下限が本当の字送りを
      追い越し**、読めなくなる。→ `rubInkSpan` で**帯の中の墨の幅**を測り、
      そこから上下限を決める
    - **原因 ③**：等分に割った帯をそのまま AI に渡していた。窓は字送りの 1.15 倍
      なので、**帯が広いと窓が数字ぶんになり、一面が青くなるだけ**で切れない。
      → `rubBandSnap` で**帯を中の墨にぴたりと寄せてから**通す
    - **周期の取り違えの見分け方を直した**（`rubBestLag`）。「同じくらい合えば
      短い方」（9 割）では足りなかった。実測：本当の 150px で 合い 0.24、
      2 倍の 297px で 0.30。→ **本当の周期なら、その 1.5 倍の所では合わない**
      （山と谷が入れちがう）ことで見分ける（実測：222px で 0.09）
    - 実測（地がざらざら・3 列・本当の字送り 150px／列幅 160px）：
      直す前 **字送り 884px・帯 420px（一面が青くなるだけ）** →
      直したあと **帯 166〜179px・字送り 148〜149px（3 列とも通る）**
    - **読めなかった列は、理由まで出す**ようにした（`aiBandNo`／`RUB.aiWhy`）。
      「読めず」だけでは、どこで止まったのか人にもこちらにも分からない
    - ［帯を AI に通す］も、**断る前に帯を墨に寄せてみる**（寄せた帯は画面に残る）
    - **寄せるのは「等分に割った帯」だけ**（`RUB.colsSplit`）。列を見つけられて
      いるときの帯はもともと列ぴったりなので、さわらない。寄せた幅が
      **帯の 4 分の 1 を切ったら、あきらめる**（同じ字が縦にそろっていると
      たての画の所だけを列とみてしまう。実測：幅 226px の行が 29px になった）
  - **［列の数］を入れなくても、列の本数を見当づけられるようにした（v21.69。
    本人の指摘「［拓本ぜんたいを通す］と全体を切り分けられませんでしたとでました」）**
    - **考え方**：列の**切れ目**が読めなくても、**並びの周期**は残っている。
      たての墨の量（`projX`）の自己相関で**列の間隔**を出し、
      **本数 ＝ 範囲の幅 ÷ 間隔** で見当づける（`rubColPitch`）
    - **周期の取り違えを直した**（`_corrAt`／`rubBestLag`）。自己相関は本当の
      周期の 2 倍・3 倍でもよく合う。実測：間隔 260px を **517px** と出した。
      半分・1/3・1/4 も確かめて、**同じくらい合うなら短い方**（9 割が目安）を採る
    - **地のざらつきを引く**。石の地が一面にあると墨の量がどこも底上げされ、
      **どの間隔でもよく合って**しまう。実測：ざらつきを入れると **173px**
      （ただの雑音）と出た。**平均を引いて**出っぱり・へこみだけで合わせると直った
    - 実測（4 列・間隔 260px のにせ拓本）：ざらつき なし → **259px／4 本**、
      ざらつき 強 → **259px／4 本**（直す前は 173px／6 本）
    - 見当をつけたときは、**その旨と間隔を知らせる**
      （ちがうときは［列の数］を入れてもらう）
  - **通す範囲を決められるようにし、列が切れないときの逃げ道も作った
    （v21.67／v21.68。本人の指摘「拓本全体が色が変わるだけで進みません」
    「範囲指定させて、その中の全行を取得する様にして」）**
    - **原因**：石の地が一面にある拓本では、列と列の間でも墨の量が落ちきらず、
      `detectColumns` が**拓本ぜんたいを 1 本の列**として返していた。
      そのまま通すと窓が拓本の幅いっぱいになり、**全面が青くなるだけで 1 字も切れない**
    - `rubColsWanted()` … **広すぎる列は列とみなさない**（字送りの 1.8 倍 または
      枠の 55% を超えるもの）。実測：3 列を丸ごと帯にすると字送りの見当が
      **2 倍にずれて**、2.2 倍では通ってしまった。1.8 倍にした
    - 見つからないときは **［列の数］** を入れて**等分に割る**（墓誌のように
      等間隔ならこれで十分合う）。**数を入れてあれば、そちらを優先**
    - `rubAiBand` も、**帯が字送りの 1.8 倍より広ければ断る**。
      何が起きているか分かる知らせを出す
    - **［▣ 通す範囲を決める］**（v21.68）… 拓本の上でかこむと、その中の列だけを
      通す。たての範囲も合わせる（頭・脚は元に戻すので較正はくずれない）。
      拓本の端で**見切れている行を外せる**
    - 実測：3 列の にせ拓本で、範囲なし → **3 列（5/5/4 字）**、
      まん中だけをかこむ → **1 列（5 字・帯 277〜503）**
  - **拓本ぜんたい（列ぜんぶ）を通して見立てられるようにした（v21.66。本人の指示
    「1 行だけでなく全体的に見れるようにして」）**
    - **［拓本ぜんたいを通す］** … 列を見つけ、1 列ずつ
      **帯を合わせ → AI に通し → 見立てる** をくり返す。終わったら帯は元に戻す
    - `S.rub.lines` を「新しい順に 6 行」から **「左からの列の並び」**に変えた
      （`rubColPut`。同じ帯なら置きかえ、帯の左で並べ替え、24 列まで）
    - 絵には**列をぜんぶ出す**。いま見ている列は濃く、ほかは薄く。
      **ほかの列を押すと、その列に切り替わる**。つかむ所（枠を直す所）は
      **いま見ている列だけ**（ほかの列まで触れると押しまちがえる）
    - メニューに**列えらびのボタン**（1・2・3…）
    - 実測（3 列の にせ拓本：5 字・5 字・4 字）：**3 列とも 5/5/4 字**と数え、
      帯も 3 本それぞれに合った。枠の絵は 14 個（＝字の数）、
      つかむ所は選んでいる列のぶんだけ
    - **ハマり所**：列えらびのボタンに字の候補と同じ `.cb` を付けたため、
      あとから配る `.cb` の割り当てが列えらびを**上書きしていた**。
      `.cb:not(.colb2)` で分けた
  - **枠を描いて足せるようにし、帯は四角いつまみでだけ動くようにした（v21.65。本人の指示）**
    - **［✏ 枠を描いて足す］** … 押している間だけ、絵の上のドラッグが
      「回す」ではなく「枠を描く」になる。描き終わると 1 字の枠として足す。
      **描いている間は 1 字の枠のつかむ所を出さない**（出すと、既にある枠の上から
      描き始められない）
    - **帯のつまみは四角の所だけ**（本人の指摘「枠を調整するときに、帯の枠が
      移動してしまう」）。前は帯の線ぜんたい（絵の高さいっぱい）を受けていたので、
      1 字の枠を直そうとして帯をつかんでいた。実測：つかむ所の高さは
      **絵の 2%**（前は 100%）。線の上で引いても帯は動かない
    - **枠は「その字の墨にぴたり」が正解**（本人の質問への答え）。
      登録の切り抜きは **枠の 1.2 倍**なので、ゆるいと字が小さく写って
      学習済みのものと見え方がずれ、きついと はらいが切れる。
      となりの字が入り込んでいても**枠はこの字だけ**に合わせる（画面にも書いた）
  - **枠を人が直せるようにし、字の幅が見切れるのも直した（v21.64。本人の指摘）**
    - **見切れ**：切り落とし（`rubAiTrimCols`）が「切れ目の多い列」だけを残すので、
      **はらいの先のような細く薄い所が落ちて**いた。芯を決めたあと
      **墨が続くかぎり外へ伸ばす**（芯の幅の 4 割まで）ようにした。
      あわせて、窓の絵を帯ぴったりで切ると はみ出したはらいが落ちるので、
      左右に帯の 2 割の のりしろを取る（`rubBandWide`）。
      **見るのも測るのも同じ枠**でないと ます目と画素の対応がずれるので、
      `rubLineTakeAi` が広げた枠を `rubAiMask` と `rubLineCoreAi` の両方に渡す
    - **枠を直す**：青い枠の中をつかむと動き、4 辺をつかむと大きさが変わる。
      ［枠を足す］［選んだ枠を消す］も。直したら `rubLineRestat()` が
      字送り・傾きなどを出し直す（枠から出しているので、直せば数字も変わる）
    - **AI の切り分けがずれた所こそ教材**（本人の指摘）。直した枠で登録すれば、
      「ここまでがこの字」を教えられる
    - 実測：枠 6 つ・つかむ所 30 か所（1 枠 5 か所）。下辺を引くと 138→186px に伸び、
      字の高さが出し直された。足す／消すも効く
  - **拓本メニューに［学習に登録（準備中）］を足した（v21.63。本人の指示）**
    - ［この行を見立てる］のあと **［字の候補を出す］** で、1 字ずつの枠の墨を
      `cgGuess` にかけ、**似ている順に 8 つ**をボタンで出す。押して選ぶ。
      もう一度押すと「?」（分からない）に戻る
    - 選んだ字を並べた文字列を「いまの並び」として出す
    - 枠の墨は `rubItemInk(it)`（`RUB.aiBand.map` を枠で切る）
    - 実測（フォントで「慈光院妙信徳寿大姉」を縦に描いたにせ拓本）：
      **見立てが 9 字**、候補の **1 位が 9 字とも正解**で、
      並びは **慈光院妙信徳寿大姉** になった。ボタンは 9×8＝72 個。
      ちがう候補を押すと その字だけ入れ替わる
    - **※ ここまでが今回。** このあと 1 字ずつ作字の画面で墨を直して
      学習用に保存する流れ（案A）は次の段
  - **墨から「どの字か」の候補を出す仕組み（v21.62。本人の案。拓本の登録を楽にするため）**
    - `cgBank(fi)` … フォントが持っている字（`face.collectUnicodes()`）のうち
      漢字とかなを、**64×64 の同じ形にそろえて並べる**。フォントごとに 1 回だけ
    - `cgGuess(ink, w, h, fi, k)` … 拾った墨を同じ形にそろえ、**重なり（IoU）**で
      上位 k 個を返す
    - **OCR は使わない。** 石の地・かすれ・楷書や隷書は印刷とはちがい、
      読み違えたまま静かに登録されるのがいちばん困る。
      こちらは持っている字形と見くらべるだけなので、
      **フォントに入っている旧字・異体字はそのまま候補に出る**
    - 実測（IPAゴシック 10,326 字）：**並べるのに 3.0 秒**（1 回だけ）、
      **1 字あたり 105ms**。同じ書体の字を 2% 荒らして当てて
      **1 位 67〜86%／上位 8 位 95〜100%**
    - サーバー側にも同じ道具（`takuhon-ai/char_guess.py`）。貯まった組で
      答え合わせができる。こちらは**ちがう書体どうし**で 1 位 57%／上位 8 位 95%
  - **離れている画を、②につながせない（v21.61。本人の指摘
    「この例は、実行すると線がつながります」＝行の 彳 と 亍 がつながった）**
    - ②は「書体らしい形」に寄せるので、**もともと離れている画をつなぐ**ことがある。
      いっぽう **石の欠けで途切れた画をつなぐのは正しい**。両方を分けたい
    - 分けるのは**すき間の広さ**。`fitCutBridges(inM, outM, ...)` が
      ① 元の墨を つながりごとに番号を振り、
      ② 地を「どの塊にいちばん近いか」で塗り分け（ボロノイ）、
      ③ **ちがう塊の境目**で、**元の墨から `fitGapCells` 以上離れている**ます だけ削る。
      狭いすき間は境目でも削らないので、欠けの修復は残る
    - しきい（`fitGapCells`）は **0.25mm**（すき間の幅で 0.5mm ほど）。
      小さなかけら（ぜんたいの 2% 未満）は塊として数えない
    - **②にだけ かける。** ①（お任せ）は拾い出しそのものをまかせる所なので、
      見くらべる元の墨が無い
    - 実測：すき間 1.25mm を橋渡ししたもの → **8 ます削って 1 つ → 2 つに分かれた**。
      途切れ 0.25mm を埋めたもの → **削らず、つながったまま**
  - **AI の墨にできた小さな穴をふさぐ（v21.60。本人の指摘
    「書体補正を入れると、文字に穴が空いたりしています」）**
    - `fitFillHoles(m, w, h, maxCells)` … **外（絵のふち）とつながっている地を
      先に塗りつぶし**、残った地が「穴」。ひとかたまりずつ広さを見て、
      小さいものだけ埋める。①②どちらの AI の結果にも かける
    - しきい（`fitHoleCells`）は **2.0mm² 未満**。「日」「口」の中は
      25mm の字なら 10mm² を超えるので、間違えて埋めることはない
    - 実測（墨の中に 4 ます の穴と 64 ます の空き、外の地）：
      **4 ます の穴だけ埋まり**、64 ます の空きと外の地はそのまま
    - 埋めた数は知らせにも出す（「小さな穴 2 か所をふさぎました」）
  - **［AI お任せ］から人が直した墨は、そのまま学習用に保存する（v21.59。
    本人の指摘「［AI お任せ］後、人で補正した物であればいいと思うのですが」）**
    - v21.58 では「AI の出した墨は保存しない」の決まりをそのまま当てて、
      お任せの墨も外していた。**硬すぎた。**
      人が見て［消す］［塗る］で直したものは、**だれが最初に描いたかに関わらず正解**。
      直す手間が減るぶん、組を早く貯められる
    - `takuShot()` は `PE.aiInk.from === "trace"` のとき `fitPrep(false)`＝
      **見えている墨**を保存する。`meta.inkFrom` に `"trace"`／`"hand"` を残す
    - **［AI 書体補正］（②整える）の結果は今までどおり保存しない。**
      あれは「書体らしい形」で、**彫ってある形とはちがう**。①に混ぜてはいけない
    - 拓本AI の画面の組に **［AI発］の札**を出す（`/api/takuhon/pairs` が `inkFrom` を返す）。
      伸びが止まったときに、その組だけ［使わない］にして学習し直せる
  - **［AI お任せ］の墨は学習用に残らない、と画面で言うようにした（v21.58）**
    - 学習用に保存するのは いつも **`fitPrep(true)`＝人が拾って直した墨**で、
      AI の出したものは保存しない（自分の出したものを自分で学ぶことになる。v21.12）
    - ところが v21.49 で［AI お任せ］を足したため、**お任せの墨を見たまま
      ［AI 書体補正］を押すと、見えている墨とはちがう墨が保存される**という
      落とし穴ができていた。データを貯めるときに気づけないと、質の悪い組が貯まる
    - `PE.aiInk.from = "trace"` で印を付け、お任せを押したときと、
      その状態で保存したときの両方で、その場で言う
  - **行の外に出た墨を落とすようにした（v21.57。本人の画面で、行の左わきに
    青い塊が出て、枠が字と合っていなかった）**
    - ① **窓の絵は帯の中だけに描く**。窓は字 1 つより大きいので、帯の外
      （となりの列・石の地・紙のふち）までモデルに渡る。そこに出た墨まで
      受け取っていた。`ag.clip()` で帯の四角に切る。実測：帯の外の墨 **0**
    - ② **帯の中でも、行から外れた墨を落とす**（`rubAiTrimCols`）。
      **墨の量では見分けられない**（実測：地の塊は たて 1 列で 900、
      字の列は 800 で、**塊の方が多かった**）。見分けるのは**切れ目の数**。
      字の列は 1 列たどると字の数だけ切れ目があるが、地の塊は のっぺり続く。
      切れ目の多い列のひと続きのうち、墨のいちばん多いまとまりだけ残す
    - 実測（字の列 x120〜240 ＋ 左わきに縦 900 の塊）：残したのは **x108〜251**、
      **左わきの塊は 0 になり**、字の墨（96,000）はぜんぶ残った
    - 青の絵も同じ所で落とす（**見ているものと 測るものをそろえる**）
  - **1 字ずつの切り分けを、字送りでの等分から「そのあたりの字送りで見る」に
    変えた（v21.56。本人の指摘「2文字を1文字としてカウントしている」
    「文字の大きさは一律ではない。戒名と命日で大きさは変わることがほとんど」）**
    - 前は**行ぜんたいを 1 つの字送りで等分**して振り分けていた。
      戒名と命日で大きさがちがうので合わず、詰まった所では 2 字が 1 ます に入っていた
    - いまは AI の墨の **よこ 1 行ぶんの量**から切れ目を拾い、
      ① **そのあたりの字送りの半分より近い切れ目はつなぐ**（「二」「三」「日」対策。
      すき間の広さでは字の切れ目と見分けられない。小さい字の字間より広いことがある）
      ② **そのあたりの字送りの 1.45 倍より背が高ければ割る**（くっついた 2 字）
    - そのあたりの字送り（`localPitch`）は、前後 3 つぶんの間隔のまん中の値。
      **行のとちゅうで大きさが変わっても、その場の間隔で見られる**
    - 大きさが 2 つに はっきり分かれるときは、**大 ○字 ○mm ／ 小 ○字 ○mm** と
      分けて出す（`sizes`）。まとめた値だけでは どちらともちがう数字になる
    - 実測（にせ拓本：戒名 6 字・高さ 100px・字送り 140px。うち 3 字目は「二」
      〈2 本の横画・間 34px〉、4-5 字目は すき間 4px でくっつけた。
      ＋ 命日 4 字・高さ 50px・字送り 70px）：
      **10 字と正しく数え**、「二」は 1 字（高さ 80px）のまま、
      くっついた 2 字は 100px ずつに割れ、
      大きさの組は **大 6 字 17.54mm ／ 小 4 字 8.77mm**（＝ 100px と 50px）
  - **AI の墨から「この行を見立てる」を作った（v21.55。本人の確認
    「完ぺきではないですが、文字の輪郭はしっかりとらえています」を受けて）**
    - 見立ての後ろ半分を `rubLineCore(M, q, step, pitch, mpp)` に切り出した。
      **墨の絵（M）さえあれば同じ**なので、しきい値の墨でも AI の墨でも使える
    - `rubAiBand` が、色を付ける**前の濃さ**を `RUB.aiBand.map` に取っておく。
      `rubAiMask()` がそれを ます目に読み直す（0.5 で切るだけ。拾い直さない）
    - `rubLineTakeAi()` … 字送り（自己相関）→ AI の墨 → `rubLineCore`。
      控えに `src:"ai"` を付け、**AI で見立てた分だけ**絵に青い枠を出す
      （しきい値の古い控えは出さない。v21.27 の判断のまま）
    - 表（字の高さ・幅・画線・字送り・字間・傾き…）と
      ［この傾きで回転補正する］を メニューに戻した
    - 実測（にせ拓本・8字・字送り 140px・0.1754mm/px）：
      **字 8 字／高さ 17.54mm／幅 31.58mm／字送り 24.56mm／字間 7.02mm／傾き 0.00°**
      と、仕込んだ値どおり。1 字ごとに 14px ずらした行では **傾き 5.71°**
      （atan(14/140) と一致）、［この傾きで回転補正する］で −5.71° が入った
  - 拓本メニューの見出しを **［AI補正（準備中）］** にした（v21.54。本人の指示）。
    通して見せる所までしか出来ていないため
  - **回している間も、つまみをぜんぶ触れるようにした（v21.53。本人の指示
    「回して合わせるが有効でもマウスでポイントを操作できるようにして」）**
    - 回している間は `hits` を丸ごと `pointer-events:none` の中に入れていた。
      頭・脚（`calHits`）だけを受け皿の上に出していたので、
      **ガイドライン・行の上下・帯のどれにも触れなかった**
    - `hits` を回す受け皿（`.spinhit`）の**あと**に置き、`bindDrag` の
      「回転中はほかを止める」判定も外した
    - 実測：回している状態（`rotMode="spin"`）で帯のつまみを右へ引くと
      帯が 200 → 310 に動き、**角度は 0 のまま**。つまみでない所を引けば
      これまでどおり回る（−10.3°）
  - **帯が一度も見えていなかった不具合を直した（v21.52。本人の指摘
    「ボタンを押しても表示されない」）**
    - 帯の絵を `if (!rotMode)` の中に入れていた。ところが［拓本補正］のタブは
      **はじめから回せる状態で開く**（v21.27）ので、`rotMode` はいつも入っている。
      つまり **ふつうに開いたときは一度も出なかった**。帯の値は変わっていたのに、
      絵に何も出ないので どこを見ているのか分からない、という状態だった
    - 回している間も出すようにし、`bindDrag` でも `bandL`／`bandR` は
      回転中に動かせるようにした（頭・脚と同じあつかい）
    - 実測：タブのボタンで開いた直後に `rotMode = "spin"`、それでも帯の絵と
      つまみが出ることを確かめた
  - **帯（左右の範囲）を絞る手立てを戻した（v21.51。本人の指摘
    「帯を絞り込むことが出来ないです」）**
    - v21.28 で外していたが、［帯を AI に通す］が **1 行だけの帯**を要るようになった
    - 絵の上：オレンジの帯＋左右のつまみ（`data-k="bandL"／"bandR"`。
      bindDrag の受け皿にそのまま乗る）。**出すのは拓本補正の画面だけ**。
      回している間は出さない
    - メニュー：いまの帯の幅（mm／px）／［近い列に合わせる］（`rubBandFit`）／
      ［列を探す］（`detectColumns` → 番号のボタンで選ぶ）／［全体に戻す］
    - 実測（3列のにせ拓本 900×1200）：列を **[60,240] [360,540] [660,840]** と
      3本とも正しく見つけ、2本目のボタンで帯が [360,540] になった
  - **拓本の［AI補正］に「帯を AI に通す」を入れた（v21.50。本人が進め方を選択）**
    - ①「読む」のモデルは**字 1 つぶんの切り抜き**で覚えているので、行ぜんたいを
      一度に渡しても当たらない。**字送りの長さで窓を切り、1 つずつ通して、
      半分ずつ重ねてつなぐ**（重なりは濃い方＝`lighten`）
    - 字送りは `rubPitchPx()`（`rubLineMeasure` から切り出した自己相関）。
      **ここは前から効いている**。壊れていたのは そのあとの塊拾いの方
    - 窓の絵は `rubCropPng()`。512 の白黒で、明るさはそのまま（`takuShot` と同じ。
      反転はかけない）。手がかりは渡さない（帯のどこが何の字かは分からないため）
    - 出てきた墨は**青で拓本に重ねる**だけ。**測らない**。
      モデルがいまどこまで読めているかを目で確かめるためのもの
    - 実測（にせ拓本 300×1200・字送り 140px）：字送りを **140px と当てた**。
      11 か所に区切って 11 回とも通り、絵が重なった。［AI の墨を消す］で消える
    - ［この行を見立てる］は灰色のまま。AI の墨から測る作りは この次
  - **［AI お任せ］に中身を入れた（v21.49。本人の指示「試しに使ってみたい」）**
    - ①「読む」のモデルへ、**拓本の写真そのもの**を渡す。②（［AI 書体補正］）は
      人が拾った墨を渡すが、こちらは拾う前の写真を渡して、拾い出しごとまかせる
    - 渡す絵は **`takuShot()` が作るもの＝学習用に貯めているものと同じ作り**。
      覚えたときと違う見え方の絵を渡さないため。手がかりは hint1 のみ
    - 返事を ます目へ戻すのは `traceShotToMask(G, img, box)`。②の `traceAiToMask` とは
      切り抜く四角が違う（②は `traceBox`、①は `takuShot`）ので別にした。
      そのため `takuShot()` の返り値に `box:{x0,y1,side,n}` を足した
    - 受け取りは ②と同じ `PE.aiInk`。［↶ 1つ戻す］で戻せて、［消す］［塗る］も効く
    - ［AI オート］は中身が無いままなので、灰色で押せない
  - **4 画面ぜんぶの帯を、校正と同じ組み立てにした（v21.48。本人の指示）**
    - 拓本補正＝**向き**（180°反転・回して合わせる）／**範囲**（トリミング・全体に戻す）／
      **仕上げ**（終了）
    - 確認資料＝**表示**（ガイド・列を白地に・拓本を反転）／**拓本**（トリミング・
      全体に戻す・読み直す）／**書き出し**（JPEGで保存）
    - ゴムシート＝**出す行**（`#shGl`。ガイドラインが 2本以上のときだけ出す。
      1本のときは `box.hidden = true` で囲みごと消す）／**表示**（赤・黒・塗って表示）／
      **出力**（①赤・②黒・③SVG）
    - 並べ方は `BARMAP` で画面ごとにタブと帯を対にし、`bBarAlign(v)` が
      **そのタブの字の位置**に左の空きを合わせる。`setView()` から毎回呼ぶ
    - 囲みの高さは **4 画面とも 34px**。出力の 3 つだけ背が高かったので
      囲みの中では余白を 4px → 3px に。`#pvbar .bgrp[hidden]{display:none}` も追加
      （囲みを `display:flex` にしたせいで `hidden` が効かなくなっていた）
    - 確認資料の「文字をつかむと動きます…」の**説明文はやめた**。
      動かした量（＋2.0mm など）があるときだけ出す
    - 実測：4 画面とも帯の左が タブの左と一致（507／587／641／721px）。
      3 回ずつ開き直してもずれない
  - **開くたびに帯が右へずれる不具合を直した（v21.47。本人の指摘）**
    - `bBarAlign()` が、**前に入れた左の空きを足したまま**測っていた。
      そのため［校正］を開くたびに同じぶんずつ右へ動いていた。
      **先に `paddingLeft` を 0 に戻してから測る**ように直した。
      実測：5 回開き直しても 587px のまま（前は毎回 +587px）。窓の大きさを
      変えたあとも 587px で一致
    - 「選んでいるガイドラインに拓本を重ねています…」の**知らせは全部やめた**
      （本人の指示）。`#bInfo` の欄ごと消した。チェックの入切が状態を表している
  - **校正の帯の見た目を直した（v21.46。本人の指摘）**
    - 「グループ名と内容が同じフォント同じ色で区別がつかない」
      → 見出し（表示・配置・資料）を **白ぬきの小さな札**にした
      （10px・字間 .12em・`--muted` の地に白。中身は 11.5px・`--text`）。
      囲みの地も `--sunk` にして、帯の地と分ける
    - 「枠の高さが違う」→ 原因は **配置** の中の `label.f` が `display:block` のままで
      〈ガイドライン間 mm〉と数値欄が**縦に積まれていた**（40px）。
      `#pvbar .bgrp label.f{display:flex}` で横に並べ、
      囲みは `min-height:32px` ＋ 親を `align-items:stretch`。実測 **3 つとも 32px**
    - 「校正の文字の下あたりから右側に並べて」→ `.forBoth` を `width:100%` で
      **タブの次の行**に落とし、左の空きを `bBarAlign()` が［校正］タブの位置から
      測って入れる（窓の大きさが変わっても合う。実測 587px で一致）
  - **校正の上のメニューを、種類ごとにまとめた（v21.45。本人が案を了解）**
    - 帯を 3 つの囲み（`#pvbar .bgrp`）に分けた。
      **表示**＝ガイド／列を白地に／拓本を反転／拓本を重ねる／寸法、
      **配置**＝ガイドライン間 mm／置き場所を自動に戻す、
      **資料**＝📄 資料
    - ［拓本を重ねる］［寸法］は入切なので、**ボタンからチェックに**した。
      `bOverFill()` は `b.checked = !!(can && S.bothOv)`、`b.disabled = !can`
      （文字と色を変える書き方はやめた）。受け手は `.onclick` → `.onchange`。
      重ねている間の知らせ（`#bInfo`）はそのまま残す
    - ［位置を戻す］は **［置き場所を自動に戻す］** に名前を変えた。
      中身は前と同じで、**つかんで動かした置き場所（`S.bothOff`）と
      拓本の置き場所（`S.rubX`／`S.rubDx`）を捨てて、自動の並べ方に戻す**もの。
      **ガイドラインの並び順（`S.glSeq`）は変わらない**ので、説明にもそう書いた
    - タブ（拓本補正／校正／確認資料／ゴムシート）の位置は 4 画面とも同じまま
      （実測：507／587／641／721 px で 4 画面一致）
  - **作字まわりの手直し（v21.44。本人の指示）**
    - 拓本の濃さの既定を **0.5 → 1（いちばん濃く）**（`peRubFit` の `op` と `#peRubOp` の value）
    - 右クリックの［消す］［塗る］を、**右のメニューと同じ並び**にした
      （前は「0.7 消す」…と 6 つ縦並び）。`#peMenu .peink` に同じ見た目の CSS。
      `data-a` は `ink-del-0.7` のままなので、受け手はそのまま
    - **拾い方の数を変えたときの描き直しを、まとめて 1 回にした**
      （`peInkLater()`。160ms 待つ）。矢印を続けて押すと、そのたびに ます目ぜんぶを
      拾い直していた。実測：**8 回押して 8 回 → 1 回**
    - `fitIntegral()` を `Float64Array` → **`Int32Array`**（中身は 0/1 なので足りる）。
      実測：ならしが こまかさ3 で **110ms → 77ms**、こまかさ2 で 63ms → 42ms
  - **AI の墨は「濃淡」で受け取り、画素の間を割って読むこと**（v21.43。本人の問い
    「AIの書体補正時にガタガタをなくすことはできますか」）。
    - サーバーは前から `soft=true` で **0〜1 の確からしさ**を返せた（v20.91／
      `SPEC-輪郭と補正.md` にもそう書いてある）のに、画面は `soft=false` で
      **0/1 に潰して**受け取り、さらに**いちばん近い画素をそのまま**取っていた。
      512 の絵は字の 88% ぶんなので 1 画素は 25mm の字で 0.055mm ほど。
      その角がそのまま段々として残っていた
    - 直し：`soft=true` で受け、`traceAiToMask()` が**まわり 4 つの濃さから割り出す**
      （たて・よことも線形）。**両方そろって初めて効く**
    - 実測（ななめの縁。ます目は AI の画素より細かい設定）：
      0/1＋近い画素 **0.720** ます → 0/1＋間を割る 0.541 →
      濃淡＋近い画素 0.711 → **濃淡＋間を割る 0.179**（4 分の 1）
    - サーバーが古くて 0/1 を返しても落ちない（割る先が 0 か 255 になるだけ）
  - **なぞりの既定を変えた**（v21.42。本人の指示「縁の解像度を上げることと、
    文字作成時になぞる様にするのをデフォルトに」）。
    - ［なぞりの形］＝**傾き付き（curve）**を既定に。
      実測（丸）：まっすぐ 点 20 個／一致 99%、傾き付き **点 4 個**／一致 99%。
      点が減って縁がなめらかになる（プロッターも速い）
    - ［墨のこまかさ］に **3＝いちばんこまかい**を足して既定に（`FIT_FINE = 3`）。
      実測：写真が細かいとき ます目 F2→10.9 が **F3→7.3**。
      ただし**写真の画素で頭打ち**（`fitGrid` の `Math.abs(PE.rub.kx)`）。
      実測：粗い写真では F2・F3 とも 12.0 で変わらない。
      どれだけ細かくなったかは帯の「1 ますは ○.○○○mm」で分かる
    - 下の帯の案内が古かった（無くした［墨の縁から この字を作る］
      ［選んだ点を拓本に寄せる］の名前が残っていた）ので書き直した
  - **［縁をなだらかにする］は弱めに、押した回数で加減する**（v21.41。本人の指摘
    「効きすぎます」）。もとは `K = 0.5` を `ROUND = 2` 回。
    実測：**1 回でぎざぎざが 12.00 → 0.00**（完全に消える）、点が最大 0.088mm 動き、
    本物の形まで丸めていた。`K = 0.2`／`ROUND = 1` にした。
    実測（直したあと）：ぎざぎざ 12.00 → 7.21 → 4.33 → 2.60 → 1.56、
    1 回の動きは 0.029mm → 0.018 → 0.011 → 0.006mm。
    あわせて、**動かした量を mm で帯に出す**（`peMmPerUnit()` が 0 なら出さない）
  - **AI の墨への塗りも、足した分だけ塗り直すこと**（v21.40。本人の指摘
    「AI書体補正の後に、手動で補正を行うと重いです」）。
    - 拾ったままの墨（`fitPrep`）は、はじめから「足しただけなら足した分のまわりだけ」
      の軽い道（`fitPaintAdd`）を通っていたのに、**AI の墨だけ毎回
      `fitApplyPaint` で ます目をぜんぶ塗り直していた**
    - 実測（760×900 ます、丸 70 本）：**1 手 89ms → 0ms**、
      筆の一引き（30 手）で **2660ms → 2ms**
    - 戻した・減らしたとき、［縁を広げる］が変わったときは今までどおり作り直す
    - **落とし穴**：塗りが 1 本も無いと `fitApplyPaint()` は**同じ配列をそのまま返す**。
      そのまま `fitPaintAdd` で書き換えると **AI の元の墨（`ink0`）を壊す**ので、
      `A.ink === base` なら写しを取る。実測：軽い道とぜんぶ塗り直しの差 0 ます、元の墨も無事
  - **［縁を広げる］も AI 書体補正のあとに効く**（v21.16。本人の指摘
    「AI書体補正した後で、縁を広げるを押すと、補正前に戻ります」）。
    - 拾った墨の取り置きの合言葉を 2 つに分けた。`keyNG`（広げる**以外**の拾い方）と
      `key = keyNG + "/g" + 広げる mm`。
      AI の結果は `keyNG` だけで見張るので、**広げるを変えても捨てない**
    - AI を通したときの広げ量（`gwC0`）を覚えておき、
      いまの広げ量との**差だけ** `fitGrowMask()` で AI の墨のふちを動かす。
      毎回 `ink0` から作り直すので、行き来しても誤差が積もらない
    - mm → ます の換算は `peGrowMm()`／`peGrowCells(G)` に一本化した
      （`fitInkMask` と AI 側で ずれないように）
    - 実測：AI の墨 100 ます → +0.3mm(3 ます) で 236 → −0.2mm(2 ます) で 36 →
      0 に戻すと **ぴったり 100**。拾い方そのものを変えたときは今までどおり捨てる
  - **回転を変えたら、塗り（`S.rub.paint`）も一緒に動かすこと**（v21.15。
    本人の指摘「作字の文字を消したり足したりした後で、拓本の回転補正などを行うと、
    塗ったり消したりした場所がずれます」）。
    - 塗りの丸は「拓本の画素」で持っているが、その画素の枠は
      **回転をかけたあとの絵**（`rebuildRub()` が
      `translate(off) → 中心まわりに rot 回転 → 画像を置く` で作る）。
      だから角度を変えると絵だけが動き、**塗りが置き去りになってずれていた**
    - `rubFrameSt()`（いまの枠）／`rubPtToSrc()`／`rubPtFromSrc()`／
      `rubPaintRemap(f0, f1)` を足し、
      **いまの枠 → 元の写真 → 新しい枠**と移し替える
    - 呼ぶ所は 4 つ。`setRot()`／`resetRot()`／`rubLineFixRot()`（見立てた傾きを入れる。
      `setRot` を通らない直書き）／`rubFlip180()`（ほかの座標は裏返していたのに
      塗りだけ置き去りだった）。**角度をいじる所を足したら、ここも足すこと**
    - 実測：傾き 3.5°／−7.25°／0.1°きざみ 100 回／180°反転／0 に戻す、
      回転中心を隅に寄せて off が動く場合も含め、**ずれは 0.014 画素まで**
  - **③④に説明文は置かない**（v21.13／v21.14。本人の指示
    「縁を広げる mm と AI書体補正の説明は不要」「［AI 書体補正］と
    現トレースから字を作るの解説は不要」）。`#peTakuInfo`（保存先の説明）も外した。
    `takuShow()` は**欄が無くても落ちず、貯まった分の自動送りは続ける**こと
    - **押した時点で、拾った墨そのものを差し替える**（v21.9。本人の指示
      「ai書体補正を押すと拾った墨を変化させて、もう一度押すと on/off できるイメージで、元に戻して」）。
      前は「入にしておいて、④を押したときに中で通す」だったので、
      **何がどう変わるのか画面で見えず**、④の結果だけで判断するしかなかった
    - 入のあいだ `PE.aiInk = { key, pv, ink }` が拾った墨の代わりになる。
      `fitPrep()` の返りを `fitAiInk()` が通すので、**帯・輪郭の絵・④が全部同じ墨を見る**
    - `key` は `PE.inkC.key`（拾い方ぜんぶ）、`pv` は `RUBPAINT_V`（消す・塗る）。
      拾い方（`key`）が変わったら**黙って差し替えを続けず**、捨てて元の墨に戻し、
      帯で 1 回知らせる（`PE.aiDrop`）。見えているものと④で作るものを必ず一致させるため
    - サーバーやモデルが無いときは入にならない（`traceAskAI` の note をそのまま出す）。
      ④は `P.ai` を見るだけで、**もう AI を呼ばない**
  - **AI に渡す絵は、学習と同じ切り取り方にすること**（v21.10。ここを間違えると
    AI は何もできない）。`make_synth.py fit_box()` は
    **字をぴったり切って正方形にし、絵の 88%（まわり 6% ずつ空け）に置く**。
    AI が見たことがあるのはこの置き方だけ。
    - v21.9 まで `traceMaskToPng()` は**ます目の枠をそのまま 512 に伸ばして**いた。
      `fitGrid()` の枠は字のまわりに `0.45*(bb.w+bb.h)` の余白を取るので、
      正方形の字なら**枠の 53% ほど**にしかならない（学習は 88%）。
      画の太さが**画素で 1.7 倍細い**ものを渡していたことになる。
      U-Net は大きさを選ぶので、細い線を線と見なさず太らせて団子にする。
      枠が正方形でなければ**縦横の比まで崩れていた**
    - 直した形：`traceBox(G, ink, n)` が墨の四角 → 正方形 → 絵の 88% の写し方
      （`sx0/sy0/side/s/off`）を返し、`traceMaskToPng`／`traceAiToMask` が
      **同じ写し方とその逆**で行き来する。実測：字が枠の **88.1%**、
      縦横の比は保つ、行って戻して**一致 100%**。
      まわり 6% にはみ出した分も、ます目には余白があるので受け取れる
    - サーバー（`app.py _read_gray`）は 512 にそろえるだけで、切り直さない。
      **切り取り方をそろえるのは、こちら（画面）の役目**
    - **枠は「字の四角」から取る。墨の四角ではない**（v21.11）。`make_synth.py` は
      `tgt = fit_box(崩した字)` → `inp = degrade(tgt)` の順なので、
      学習の入力は「**正解の枠の中にある、88% より小さい墨**」。
      v21.10 は拾った墨の四角を 88% に伸ばしていたため、
      かすれて短く拾えた墨ほど大きく引き伸ばされ、そのうえ AI が
      画をあるべき長さまで伸ばすので、**二重に伸びて枠からはみ出していた**
      （本人の指摘「かなり伸ばしてしまっていますが 予定無いですか」）。
      こちらで「正解の枠」に当たるのは **`G.bb`＝いまの字の輪郭**
      （フォントから起こして拓本に合わせてある）。墨がかすれても枠は動かない。
      墨のはみ出しは 25% までなら枠を広げて入れ、それ以上は字の枠のまま
      （遠くの地のごみで枠が壊れるのを防ぐ）。
      実測：字の枠が絵の **88.1%**、墨（字の枠の 60%）は絵の **53%**
      ＝学習の入力と同じ見え方、行って戻して**一致 100%**
  - **［捨てる島 mm²］は画面から廃止**（えらび［消す］で消せるため）。
    ただし**中では 0.3mm² のまま効かせる**（`TRACE_MIN_MM2`）。
    0 にすると、フォント補正の「太らせる前に小さすぎる塊を落とす」が働かず、
    地の粒が太って輪が増える（実測 3 本 → 7 本）
  - **［縁の細かさ］は［３ 設定］へ移し、既定を 0.05 → 0.15 にした**（点 725 → 315）
  - **［塗りをぜんぶ消す］（`#peInkClr`）も廃止**
  - **［拾った墨］にも濃さのつまみを付けた**（`#peEdgeOp`／v20.98）。拓本と同じ形。
    赤は拓本の上に重ねているので、拓本が薄いと赤が強すぎ、濃いと赤が埋もれる。
    絵は作り直さず `opacity` だけ書き換える（拓本の data URL を読み直さないので軽い）
  - **［まわりの字を隠して切り出す］［字のまわり 残す mm］は［３ 設定］へ**（v20.97）。
    ふだん触らない値なので、作る流れからは外した
- **［塗りつぶし］［ポイント（点）］［元の形を薄く出す］は［画線（この字）］の下**
  （v21.2）。1 文字ぶん右へ寄せて、支配下だと分かるようにした。
  - **画線を切ると、3 つもいっしょに切れて、触れなくなる**（字を出していないのに
    「塗りつぶし」だけ入っている、が起きないように）
  - **画線を入に戻すと、切る前の入切に戻る**（覚えておく。実測：ポイントだけ切った
    状態で画線を切って戻すと、ポイントは切のまま）。字を開き直したら忘れる
  - 左の欄を 150 → 168px に広げた（1 文字ぶん寄せると、字が折り返したため）
- **なぞった結果の帯が、一瞬で消えていた**（v21.6 で直した）。
  拾った墨の層は描き直すたびに `#peFitInfo` へ書くので、
  ［現トレースから字を作る］の結果（点の数・ずれ・直した分）が
  **その場で上書きされて読めなかった**。v21.0 で［拾った墨］を既定オンにしてから
  こうなっていた。`PE_INFO_HOLD` を立てて、墨をいじったときだけ墨の説明に戻す
- **「ポイントが墨のふちからずれる」のは［フォントで補正する］（既定オン）のせい**。
  **実測**（試しの拓本「久」・1 ます 0.147mm）：
  - **補正 切**：ずれは中央 0mm・最大 **0.147mm（1 ます）**。なぞり自体はずらさない
  - **補正 入**：中央 0.147mm・9 割 0.356mm・最大 **0.442mm**
  - 帯に **【墨のふちからのずれ】中央 ○mm／9 割が ○mm 以内／最大 ○mm** を出すようにした
    （`traceDevMm`）。墨どおりにしたいときは［修正］タブで補正を切る
- **［表示］の入切は、開くたびに決め直す**（`peViewPreset`／v21.0）。
  前に開いた字の入切がそのまま残るので、**作っていない字なのに見え方が違う**
  ということが起きていた。
  - **まだ作っていない字** … 拓本と拾った墨だけ。字の線・塗り・点は出さない
    （これから拓本から作るので、フォントの形は邪魔になる）
  - **作った字を直すとき** … 元の形を薄く出す だけ切る。ほかは出す
  - **拓本が無い字** … それだと何も見えないので、今までどおり字の線を出す
- **［ポイント（点）］を消しているときは、右クリックに点の操作を出さない**（v21.0）。
  点が見えていないのに「この点を消す」が出るのは筋が通らない。
  代わりに**［ポイント（点）を出す］**を 1 つだけ出す。
  輪郭の操作（動かす・消す）と、墨の 消す／塗る はそのまま出る
- **傾き付き（曲線）でなぞれるようにした**（`traceCurveNodes`／v21.3）。
  ［３ 設定］の**［なぞりの形］**で「まっすぐ（点だけ）／傾き付き（フォントと同じ）」。
  既定はまっすぐ（今までどおり）。
  - やり方は Schneider の当てはめ。①角を見つける →②角と角の間を 1 本の曲線で
    当てはめる →③いちばん離れた所が誤差を超えたら、そこで割ってやり直す
  - **角は「少し離れた点」との向きで測る**（`findCorners`）。すぐ隣で測ると
    ます目の階段のかどを角と取り違える。［角とみなす角度］（既定 40 度）で変えられる
  - **ます目より細かい誤差を求めてはいけない**。マーチングスクエアの縁は
    半ますきざみの階段なので、細かくすると階段のかどが角になって曲線にならない。
    **実測：［縁の細かさ］0.05mm で 墨との一致が 79% に落ちた**。
    誤差は 0.6 ます・下ならしは 0.4 ます・角を見る幅は 3 ます を下限にして直した（→ 94%）
  - **手（ハンドル）の向きと長さの決め方**
    - **向き**＝端の傾き。角では隣の点への向き（`t1`／`t2`）、
      割った所では前後の点を結ぶ向き（左右で符号を逆に）
    - **長さ**＝最小二乗で出す（`bezFit`）。点の並びにいちばん近くなる長さ
    - **長さには歯止めを掛ける**（v21.8）。最小二乗は長さに上限が無いので、
      ほとんど真っすぐな並びだと解が暴れて手だけが伸びる。
      **実測：弦 2.54mm に対して 146mm（57 倍）の手が 1 本あった**。
      曲線そのものは点の近くを通るので、誤差では気づけない。
      **弦の 1.5 倍で止める**（3 次ベジエ 1 本で表せるのは 90 度ほどの弧で、
      そのときの手は弦の 0.6 倍ほど。超えるなら割って当てはめ直せばよい）
  - 実測（試しの拓本「久」・0.15mm）：**まっすぐ 113 点・一致 97%／
    傾き付き 83 点・一致 97%**（手の歯止めを掛けて 94% → 97% に上がった。
    暴れた手が輪郭をゆがめていた）。手の長さは 弦の 中央 0.59 倍・最大 1.28 倍。
    0.05mm なら 傾き付き 143 点・一致 99%。どちらも 0.4 秒ほど
  - **［広げる］［太さ ±%］も曲線のまま効く**（実測：＋10% で面積 +13.9%）。
    ［決定］→開き直しでも 83 点すべて傾き付きのまま残る
  - `pathD` は節点に傾きがあれば `C`、無ければ `L` を書くので、出力はそのまま通る
    （実測：出力の形が C 83・L 0）
- **なぞる前に、拓本AI に通す**（`traceAskAI`／v20.91）。［③ 拓本をなぞる］の
  **［AI に通す］**で選ぶ。**使わない**（既定）／**① 読む**（拓本→綺麗な墨）／
  **② 整える**（書体らしく）／**①＋② 両方**。AI を通した墨を、そのあとの
  「フォントで補正」→「なぞる」に渡す。
  - 送り先は［拓本AI］に入れた住所（`takuhonAiUrl`／`takuApiBase`）。
    サーバー側は `POST /api/takuhon/clean` の `stage=ink|shape|both`。
    **②は手がかり（フォント）を見せない**（フォントを描き写す近道を覚えさせないため）
  - **つながらなくても作業は止めない**。住所が無い・サーバーが落ちている・
    ②のモデルがまだ無い、のいずれでも**そのまま今までどおりなぞり**、
    帯に「【AI】…」と理由を出す（実測：住所なし／落ちている／モデルなし／① 成功、の 4 通り）
  - **墨が 1 割未満に減ったら、AI の結果は使わない**。学習が足りないモデルは
    真っ黒を返すので、そのまま通すと「墨が見つかりません」しか出ず原因が分からない
    （実測：まっさらな重みの②は 219 → 0 mm²。使わずに元の墨でなぞった）
  - **画の太さの歯止め**（v20.91）。目あての太さが**字の 1/3 を超えたら測れなかったもの**
    として太らせをやめ、帯に知らせる。墨が塊になると途方もない値が出て、字が真っ黒に太る
- **フォントで補正するのは やめた**（v21.7。`traceFix`／`fitFontMask`／`fitParts` を削除、
  ［フォントで補正する］［太さの補正］［欠けの補完］も廃止）。
  かすれを直すつもりで、**墨のふちから 最大 0.44mm ずらしていた**（実測。
  中央 0.147mm・9 割 0.356mm）。ここは「彫ってある字をそのまま使う」ための道具なので、
  フォントの形に寄せるのは悪い方に働く。**欠けは②の［塗る］で人が直す**。
  本人の言葉：「フォントで補正する はやめましょう、悪影響しかないです」
  - 帯には **【墨のふちからのずれ】**（`traceDevMm`）を**いつも**出す。
    実測：補正をやめたあとは 中央 0.00mm・9 割 0.15mm・最大 0.15mm
    （＝1 ます。墨は ます目の 0/1 なので、これが原理的な下限）
- **［③ 拓本をなぞる］墨の縁から、この字の形を作る**（`traceGo` / `traceToContours`／v20.89）。
  彫ってある字を**そのまま**この字の形にする。**前の行と同じ字を合わせるときの本筋**
  （`takuhon-ai/SPEC-輪郭と補正.md`）。仕様の「作る順番 1（輪郭化）」がこれ。
  - **マーチングスクエア**（`traceLoops`）。ますの間を**線形に割って**交点を出すので、
    ます目（0.0835〜0.147mm）より細かい縁が取れる。
    のちに AI の 0〜1 の濃淡をそのまま渡せば、さらに滑らかになる（同じ関数で動く）
  - **穴は自動で逆回り**になる（マーチングスクエアの性質）。囲みの中が抜ける。
    実測：輪（外 45・穴 20）で 2 本・符号が逆
  - `rdpClosed` … 点の間引き。**閉じた輪のままダグラス・ポイカーを掛けると輪ごと消える**
    （始点＝終点で線の長さが 0 になり、どの点も「線の上」と判定される。実測で 2 点になった）。
    **いちばん遠い点で 2 つに割ってから**掛ける
  - **半ますの足し忘れに注意**。ますの値は「ますの真ん中」のものとして扱うので、
    字の座標に直すときは `+0.5`。忘れると 0.5 ます（0.04mm）ずれる
    （実測：四角の一致 96.7% → **100%**）
  - 設定は 2 つ。**［縁の細かさ mm］**（既定 0.05。これ以内のでこぼこは同じ点に）と
    **［捨てる島 mm²］**（既定 0.3。地のごみを落とす）
  - 結果は窓の下の帯に 3 つの数で出す。**点の数**（多いとプロッターが遅く角がなまる）／
    **墨との一致**（なぞれているか）／**元の形との一致**（低くて当たり前。参考値）。
    輪が 4 本以上なら「地のごみが残っている見込み」と添える
  - **［↶ 1つ戻す］で戻る**（`peSnap` を先に積む。実測で 595 点 → 元の 18 点）
  - 実測（試しの拓本「久」、縁のならし 0.15mm、捨てる島 2mm²）：
    **点 509・輪 3・墨との一致 97%**／細かさ 0.02→647 点、0.05→595、0.15→212、0.4→89 点。
    **プロッターに出すなら 0.15mm あたり**が現実的
  - まだ**補正は入っていない**（仕様の 2）。地のごみで輪が増えるので、
    いまは②で墨を消してから押す。**AI が入れば、そこが楽になる**
- **学習データを、拓本AI のサーバーへ直接送る**（`takuPostAI`／v20.87）。
  共有フォルダー（SharePoint）への保存は**今までどおり**。控えは二重に持ったうえで、
  **保存と同時に 1 組ずつサーバーへも送る**。ZIP を書き出して運ぶ手間が丸ごと消える。
  - 住所は本体アプリの［🧠 拓本AI］と同じ `takuhonAiUrl`（同じ出所なので localStorage を読める）
  - 送り先は `POST /api/takuhon/feedback`（サーバーに最初からある受け口）。
    `key` を付けるので、**同じ案件・同じ字なら上書き**される
  - **https でないと送らない**。この画面は https なので、http のサーバーへは
    ブラウザが混在内容で止める。`takuApiBase()` が http を見つけたら**空を返して黙って送らない**
    （実測：https のページ＋http の住所 → 空／https の住所 → そのまま返す）
  - **サーバーが落ちていても、保存は成功する**。送れなかったことだけ知らせる
  - 下ごしらえ（サーバー側を https にする。既存の 443→Ollama には触らない）：
    `sudo tailscale serve --bg --https=8443 http://127.0.0.1:8077`
    → 住所は `https://…ts.net:8443/`。**［🧠 拓本AI］の住所もこれに変える**
  - 実測：アプリから 1 文字保存 → サーバーの `pairs` が 0 → 1 組になることを確認
- **［形がちがいます］の窓を作り直した**（`openGlyCmp`／v20.86）。
  共有フォルダーに同じ名前の作字があって、形がちがうときに出る窓。
  - **3 枚 → 2 枚**。［元のフォントに戻す］（`gcOrig`）は**外した**。
    作字をやめるために押す場面が無い
  - **右上に［✕ やめる］**（`gcX`）。**Esc でも閉じる**。
    押し間違いでこの窓が出たときに、何もせず抜けられる
  - **右の札の向きを逆にした**。
    v20.85 まで：共有の形を**この案件に取り込む**（`gcTake`）
    v20.86 から：**いまの形で共有を上書きする**（`gcOver` → `glyphLibPushOne(gi, ci, false, true)`）
  - **上書きは 2 段構え**。押しただけでは書かない。
    「共有の形そのものが置き換わる／これまでに作った案件の形も変わる／元に戻せない」を
    出して、もう一度たずねる。**「いいえ」なら窓は開いたまま**（選び直せる）
  - 実測：✕ と Esc で閉じる／確かめで「いいえ」＝何も呼ばれず窓も開いたまま／
    「はい」＝ `asNew:false, force:true` で呼ばれる、まで確認
- **［墨訂正］のえらび**（`peInkFix`／v20.90）。②の［縁のならし mm］の下。
  「0.7／0.5／0.3 消す・足す」の 6 つから選ぶと、**筆の太さと消す／足すが一度に決まる**。
  太さを打ち直してボタンを押す、を毎回やるのが面倒だったため。
  ボタン（墨を消す／墨を足す）を直に押したときは、えらびを空に戻して食い違わないようにする
- **窓ぜんたいで拾うキーは、入力欄に入っている間はさわらない**（`typingIn`／v20.85）。
  **［まとめて入力］で ← → が効かない**という申し出の原因。
  **［資料］の窓を開いたままだと**、`docWin` のキー処理が ← → を
  「前の資料・次の資料」に取り、`preventDefault()` まで掛けていた。
  窓を閉じていれば起きないので「たまにおかしい」に見えていた。
  - 直し方：`typingIn(e)`（`INPUT` / `TEXTAREA` / `SELECT` / `contenteditable` なら真）を
    **窓ぜんたいで拾っている 2 か所**の頭に足した
  - **同じ取りこぼしがもう 1 か所あった**。パス編集の `Delete`／`Backspace`／`Ctrl+Z` は
    `pathEd` が開いてさえいれば効いていたので、**［拾う量］などの数の欄で
    Backspace を押すと、数字ではなく「選んだ点」が消えていた**。こちらも同時に直した
  - 実測（資料を開いた状態で ← ← ＋ Shift+←）：
    直す前 カーソル 5 のまま・選択なし → 直したあと 3・選択 [2,3]（閉じたときと同じ）。
    絵の上での ← →（資料の送り）、絵の上での Backspace（点を消す）は**今までどおり効く**
  - Escape だけを拾っている残りの処理（グループ menu・備考メモ・SVG・拓本 menu・
    開始画面・作字一覧）は、入力欄で Esc を押しても害が無いので、そのままにしてある
- **住所に付いてくる引用符を落とす**（`_stripWrap`／v1.9.427）。
  `"https://…ts.net"` のように**前後に `"` が付いたまま**保存されると、別の住所として
  扱われて**必ず失敗する**。資料や JSON の出力からそのまま貼ると起きる（実際に起きた）。
  - 落とすもの：`" ' “ ” ‘ ’ 「 」 『 』 < > ＜ ＞` と前後の空白。何重でも剥がす
  - **保存のときだけでなく、読むときにも通す**ので、**すでに入っている分も直る**
    （設定を入れ直さなくてよい）
  - 効く先は **AI の接続先・拓本AI の住所・フォント編集の住所**の 3 つ
  - 実測：9 通りの書き方（引用符・全角引用符・かぎかっこ・山かっこ・前後の空白・
    末尾の `/`・それらの重ね）すべてが同じ住所になることを確認
- **AI（Ollama）の接続先を全員で共有し、届かないときは知らせる**（v1.9.426）。
  - **`ai_endpoint_openai` を `SP_UI_KEYS` に載せた**。一人が入れれば全員に届く。
    **APIキー（`ai_apikey_*`）は今までどおり端末ごと**で、共有しない
  - **［💬 AIチャット］が［⚠️ AIチャット］（橙）に変わる**（`aiPingCheck` / `aiPingPaint`）。
    メニューを出したときと、チャットを開くときに、`/api/tags` を静かに叩いて確かめる。
    6 秒で打ち切る。**provider が Ollama のときだけ**見る（Claude などは確かめない）
  - **つながらないときの助言を、接続先に合わせて出す**（`aiNetHint`）。
    これまでは何が起きても「OLLAMA_ORIGINS=* で起動してください」と出していたが、
    実際に起きた原因は **どちらも CORS ではなかった**：
    ① この端末の Tailscale が動いていない ② トンネル（cloudflared）が落ちて HTML が返る。
    接続先が `…ts.net` なら Tailscale の確かめ方、そうでなければトンネルの確かめ方を出す。
    設定の［接続テスト］とチャットの窓、**両方**で同じ助言が出る
  - 経緯（2026-09-11）：`cloudflared.service` が `disabled` のままで、再起動で上がらず
    `ai.shojisekizai.jp` が **530（元のサーバーに届かない）**。アプリには
    「Failed to fetch」→「`Unexpected token '<'`（Cloudflare のエラー画面 HTML）」と出た。
    **Tailscale の `…ts.net` に切り替えて復旧**。この経路には Dify も Open WebUI も無関係
- **ビューアーから拓本AIの状況を開ける**（`index_b.html` v1.9.425／`openTakuhonAi`）。
  メニュー上部の **［🧠 拓本AI］**。会社のパソコンで動いている `takuhon-ai` の
  状況画面（`http://…:8077/`）を別窓で開く。
  - **右クリック（スマホは長押し）でURLを入れ直す**（`setTakuhonAiUrl`）。
    フォント編集と同じ作り。入れた値は `takuhonAiUrl` として `SP_UI_KEYS` に載るので、
    **一人が入れれば全員に届く**
  - 住所は端末で違う。サーバーと同じパソコンなら `localhost:8077`、
    ほかからは **Tailscale の住所**。全員に配るなら Tailscale の方を入れる
  - **画面の中に埋め込んではいない**。ビューアーは https、あちらは http なので、
    iframe も fetch もブラウザが止める（混在内容）。**別窓で開くのは通る**
- **［行の見立て］＝彫ってある 1 行を測って数字にする**（`rubLineTake` / `rubLineMeasure`／v20.83、v20.84）。
  校正を作るときの**手本の客観データ**。左右の線（`clampRange`）で 1 行ぶんの帯を囲んで押すと、
  **字の高さ・幅・画線の太さ・字送り・字間・行の中心・傾き・ばらつき**を mm で出し、
  拓本の上に青い枠と番号を出す。控えは `S.rub.lines` に**新しい順で 6 行ぶん**残り、
  2 行目からは**前の行との差**を並べる（0.15mm を超える差は朱色）。
  ［この傾きで回転補正する］で `S.rub.rot` に入る
  - 測り方：よこの墨の量の**自己相関で字送り**（`estimatePitch`）→ 帯を白黒にする →
    細いつながりを切る（0.15mm の開き）→ **塊**を拾う（4 つながり。2mm² 未満は捨てる）→
    字送りより 1.3 倍高い塊・行の中心から ±0.7 字より外の塊を落とす →
    **区切り目をまたぐ塊が最少になる ずらし**で ます に振り分ける →
    ます ごとに 1 字。画線の太さは **2×面積÷ふち長**、傾きは**中心 x を中心 y で回した最小二乗**
  - 投影（よこ 1 列の合計）だけで上下を決める作り方は**捨てた**。
    地のざらつきに埋もれて字の高さが半分（4.26mm）になった。**塊で数える**方が確か
- **行の見立ての「墨の見え方」は 3 段構え**（`rubBandMask`／v20.84）。
  上にあるものから使う。**① `RUB.clean`（AI＝U-Net が起こした墨。拓本と同じ大きさの 0/1 の並び。
  まだ無いので今は飛ばす）／② しきい値＋［拾う量］**。そのうえに**筆で直した所**を重ね、
  **［縁のならし］**でならす。**1 字ずつの拾い方と同じ設定**なので、手で直した分がそのまま効く。
  - なぜ：地のざらつきを相手に見立てが戦っていた。それは U-Net に消させるものそのもの。
    `takuhon-ai` が動いたら `RUB.clean` に入れるだけで、見立てがきれいな墨で測れる
  - 実測（試しの拓本。かなり地が荒い）：ならし 0 で 字19・高さ31.4・**幅35.6mm**（幅が高さを超える＝地を拾っている）、
    **ならし 0.4mm で 字18・高さ30.0・幅29.4mm**（幅が高さの内に収まる）。
    0.8mm まで上げると 字15 に減り、画線の太さが 1.37 → 2.16mm に太る（つぶれる）。**0.3〜0.4mm あたり**
  - まだ残る弱み：拓本の右にある黒い帯・紙のふちが枠に入る字があり、
    高さの幅が 17〜49mm と開く。**U-Net の墨に替えれば直る見込み**
- **知らせは窓の下の帯**（`#peFitInfo` ／ `.pemsg`／v20.79）。
  拾った墨の内訳・合わせ込みの結果・内訳の数字は、サイドバーの細い所ではなく
  **窓の幅いっぱい**で読む。`#peFitInfo` の id はそのままなので、書いている所は前と同じ
- **［拾う量 ±］［縁のならし mm］［墨のこまかさ］は「この拓本をどう読むか」の設定**。
  効く範囲は **いま編集している字の枠のまわりだけ**（`fitGrid` が作る ます。実測で
  36.9×39.9mm。拓本ぜんたいは 197×888mm）。**拓本の画素そのものは 1 つも変えない**
  （実測で `RUB.data` が変わらないことを確認）ので、拓本の画面・構成・出力には影響しない。
  ただし**設定は 3 つとも拓本につく**ので、別の字を開いても同じ値で拾う
  （字ごとではない）。v20.82 から `S.rub.inkAdj` / `inkSmooth` / `inkFine` として
  **案件と一緒に保存**し、開き直しても続きから使える
- **［縁のならし mm］**（`peInkSmooth` ／ `fitSmoothMask`／v20.80、v20.81 で mm に）。
  **拾った墨の側**をならす。**まわり ○mm の中で墨が半分より多ければ墨**という見方に
  置き換える（積分画像で 2 回まわす。1 回だと四角い窓のくせが残って角がカクッとする）。
  ○mm より小さい出っぱり・へこみが消え、大きくするほど丸くなる。
  - v20.80 は「回数」だったが、3×3 の多数決は 2 回で頭打ちになり、
    それ以上ならせなかった。**窓の大きさ（mm）で決める**形に変えた
  - 実測（1ます 0.084mm）：縁のます 2238 →（0.05mm）1799 →（0.2mm）1604 →
    （0.3mm）1270 →（0.5mm）1151。広さは 110.2 → 108.0 → 106.8mm²
    （0.3mm を超えると角が丸まって少し減る）
  - 効かせすぎると囲みがつぶれるので、0.1〜0.3mm あたりから試す。0 で元どおり
- **［縁をなだらかにする］**（`peSmoothEdge`／v20.79。④ 手で直す。**字の輪郭の側**）。
  拓本に寄せたあとの輪郭は、拓本のふちの粗さを拾って波打つ。
  となりの点との まん中へ 5 割寄せるのを 2 回。点を選んでいれば その点だけ。
  実測：わざと波打たせた輪郭で、波の量 6502 → 1861 → 1170（押すたびに効く。
  点を選んだときは、ほかの点が 1 単位も動かないことも確認）
- **［拾う量 ±］［墨のこまかさ］［墨を消す／足す］を触ると、［拾った墨］を自動で出す**（v20.79）。
  何を拾っているか見えないまま数字だけ動かしても決められないため
- **表示の入切は、絵のすぐ左の帯**（`.peleft`／v20.78）。
  画線・塗りつぶし・ポイント・元の形・拓本（濃さ）・拾った墨。
  作業中に何度も触るので、右のサイドバーから移した。
  モードの札（`#peModeTag`）は帯にかぶらないよう `left:162px` へ
- **取り消しは［↶ 1つ戻す］に一本化**（v20.78）。`PE.undo` に
  `{c:輪郭のJSON}` と `{p:塗りの本数}` を同じ列で積み、
  形の編集も 墨の塗りも やった順に戻す。**筆の一引きが 1 手**（`peSnapPaint`）。
  ［塗りを1つ戻す］［この字のまわりの塗りだけ消す］は外した。
  実測：9 本引いて［1つ戻す］1 回で 0 本に戻り、続けて形の編集も戻る
- **［塗りをぜんぶ消す（拓本ぜんたい）］は作業のやり直し**。
  この拓本に塗った分が**ほかの字のぶんも含めて**消え、拾い方が元どおりになる。
  押すと確かめる（v20.78）
- **サイドバーは作業順に並べてある**（v20.77／v20.78 で表示を左へ）。
  ① 拓本を合わせる（拓本を出す・濃さ・拓本を動かす）／② 拾った墨を直す（縁・拾う量・筆）／
  ③ 字を拓本に合わせる（大まかに・太さ・この字だけ ±%）／④ 手で直す（モード・寄せる上限・赤／黒・重なり）／
  表示／AI学習／その他／戻す
- **［③ 大まかに合わせる］で学習用に自動保存**（v20.77）。
  名前は **案件＋グループ＋番号＋文字**（`takuKey`）で決まるので、
  **あとで直してもう一度保存すれば上書き**になる（共有フォルダーは同じ名前に PUT すれば置き換わる。
  この端末の貯め置きも、同じ `key` があれば差し替える）。
  実測：③を 2 回押しても 1 組のまま（0 → 1 → 1）
- **拾う量・こまかさを変えたときは、拾った墨の絵だけ作り直す**（`peInkRefresh`／v20.77）。
  実測 154ms → 97ms。残りの 97ms は しきい値が変わったための拾い直しそのもの
- **表示に［画線（この字）］**（`peShowInk`／v20.76）。切ると字の線・塗り・元の形を出さない。
  拓本だけを見たいときのため。点とつかむ範囲は残るので編集はできる
  （実測：切ると path が 7→3・塗り 0、つかむ範囲は 16 のまま、拓本の絵はそのまま）
- **貯まった学習用データは、つながったときに自動で送る**（v20.76）。
  ［共有フォルダーへ送る］のボタンは外した。作業ごとに 1 回だけ試す（`TAKU_AUTO`）。
  ［書き出す］［消す］は「その他」へ移した
- **サイドバーから外したもの**（v20.75。本人の指示）：モードの［輪郭を選択］、
  操作の［選んだ点を消す］［選んだ輪郭を消す］。
  **消す処理は関数として残してある**（`peDelSelNodes` / `peDelSelContours`）。
  **Delete キー**で今までどおり消せる。右クリックの［輪郭を選択］も外した
- **［元の形を薄く出す］が AI学習の中に取り残されていた**（v20.68 で並べ替えたときの取りこぼし）。
  v20.75 で「表示」へ戻した
- **右クリックのメニューは v20.60 で削った**（本人の指示）。
  たて／よこにそろえる・選んだ点を消す・選択を外す・この字の点をぜんぶ選ぶ は無くした。
  **点を選んでいるときは、モードの切り替え（点を足す・点を消すなど）も出さない**
- **注意：墨の拾い方（しきい値）が甘いと、拓本の地の斑を追いかけて輪郭が波打つ**。
  ［テスト：拾った墨のふちを出す］で、何を墨と見ているか目で確かめてから寄せること

## 残っている宿題

### ~~ロック中でも、拓本の解析はできるようにする~~（**v24.5 で実施ずみ**）

- 本人の言葉：「彫刻原案のアプリでロックされていても、拓本の解析はできるようにして。
  後でまとめて実行して」
- いまの作り：`locked()` のとき、`#rubwrap`（拓本の絵）の中の
  pointerdown／contextmenu／dblclick／打ち込みを止めている（`LOCK_ZONE`／`LOCK_DRAG`）。
  そのため**帯を動かす・範囲を描く・右クリック**ができない
- **通してよいと思うもの**（校正の中身を変えないから）
  - 帯の左右を動かす／列の札／通す範囲を描く・やめる
  - いちばん上の行から列を見つける／拓本ぜんたいを通す／この帯だけ通す
  - この行を見立てる／1 字ずつの枠を直す／学習に登録する
- **止めたままにした方がよいと思うもの**（校正の寸法に響くから）
  - **較正**（実測した文字全長・頭／脚の線）… mm/px が変わると、原稿の寸法の見え方が変わる
  - 回転・トリミング・180°反転 … 拓本そのものの向きが変わる
- やるときに、この線引きでよいか本人に確かめる


### プロッターの用紙の長さ（2026-09-08 の相談。**本人が「後で検討します」。着手していない**）

- **分かっていること**
  - アプリが出す中身は毎回まったく同じ（4回続けて出して、印刷に渡す中身のハッシュが一致）。
    PDF も 90×780 mm で出ている。**アプリ側の揺れではない**
  - レ点が入っていない行（隠したガイドライン）は**出力に入っていない**（実測で確認済み）
  - 中身は紙の**上下まんなか**に置く。だから 700 → 780 にすると、字が 40mm 下へ動く
  - 90×1000 のシートを入れたら出力できた。700 は通り、780 は不安定
  - v20.50／v20.51 で、出す前に「紙 90 × 780 mm で出します。線は … に入っています」と
    見せ、紙からはみ出しているときは止めるようにした（`inkExtentMm()`）
- **試せる順番**
  1. **780 の紙を入れて、アプリの長さは 700 のまま出す**。
     ただし中身が 40mm 上へ動く（上下まんなかに置くため）ので、位置の確認が要る
  2. **上 25／下 50 の「カットできない縁」を狭める**。使える長さ ＝ 高さ − 上 − 下。
     700 なら 625mm、780 なら 705mm。**広げるのではなく狭める**と使える長さが増える
  3. それでも足りなければ、**［① 赤を出力］［② 黒を出力］の並びに
     「紙の長さを中身に合わせる」のチェックを足す**。上端を止めたまま下だけ切り詰めるので、
     字の位置は動かない（例：780 → 640mm ほど）。**未実装。本人の返事待ち**
- 本人の言葉：「700mmの用紙で700で通っているので。780の用紙なら700なら良いと思いますが」

- `data_kugayama.js`（264名の氏名）が **git の履歴に残っている**。HEAD からは消したが
  履歴の削除は未実施（本人の明示的な承諾が要る）
- このリポジトリは **公開**。`HANDOFF.md` に運用アカウントのアドレスと
  SharePoint の各種 ID が書かれている
- さくらインターネットへの移設（手動アップロードの方針）。
  移設したら `loadMapDataFromScript` のURLを書き換え、Entra ID にリダイレクトURIを追加
- `gaichu-portal/index.php`(v1.2.2)、`upload.php`(v18)、`submit.php`(v3) のアップロード
- 工事関連データタブの「彫刻記入用紙返却／彫刻校正確定／彫刻納品」ゾーン

- **AIサーバー（Ubuntu）が 2026-09-12 12:49 ごろ、突然落ちた**。`journalctl -b -1` の
  終わりに**片づけの記録がいっさい無い**（`Stopping…`／`Reached target Shutdown` が無い）ので、
  ソフトではなく**電気が切れた**側。学習中に何度も起きるなら電源の容量を疑う
  - **同じ日にもう一度落ちた。** `nvidia-smi -pl` は**再起動で元に戻る**ので、
    一度下げても意味がない。`takuhon-ai/nvidia-powerlimit.service` を入れて、
    起動のたびに 300W を掛け直すこと
  - 対策として `train.py` を**1 エポックごとに `runs/last_<名前>.pth` へ保存**するようにした。
    同じ `--name` で回し直せば続きから。それまでは**最後に 1 回しか保存しておらず、
    落ちると学習が丸ごと消えていた**

- **一覧のサムネが出るまで遅い**（納骨・工事／v1.9.428 で手を入れた）。
  もとの作り：開くたびに ① Graph の `$batch` でサムネの住所を聞き（20 件ずつ・3 並列）、
  ② その住所（CDN）から絵を落とす。**毎回ゼロからやり直していた**。
  - **large（最大 800px）をもらっていた**。枠は 104px なので桁違いに無駄。
    `$expand=thumbnails($select=c240x240,medium,small)` にして、
    **240px → 176px → 96px の順**で選ぶようにした（`_lthumbPick`）
  - **絵そのものを手元に取っておく**ようにした（`_lthumbFetch`／`_lthumbPreload`。
    添付と同じ IndexedDB・30 日。鍵は `lthumb:<FolderPass>`）。
    2 回目からは **$batch も CDN も通らない**ので、開いた瞬間に出る。
    サムネを差し替えたときは、その場で手元の分も入れ替える
  - **取っておくのは「未完了」のものだけ**（本人の取り決め／v1.9.429。
    `_lthumbUndone`：工事は `endinfo` が入っていたら完了、納骨は `_x524a__x9664_` が true なら完了）。
    終わった案件は、**絵を落としにも行かず、住所のまま出す**（手元がふくらまない）。
    サムネを差し替えたときも、完了のものは取っておかず、あれば捨てる
  - サムネの住所は 1 時間ほどで切れるので、**住所ではなく絵を**取っておくのが肝心
  - `blob:` の絵は `?_r=` を付けて取り直せないので、`_listThumbError` で
    そのときはダウンロードへ回す

## 触ってはいけないもの

- `docker compose down -v`（Dify のデータが消える）
- `tailscale funnel`（インターネット全体に公開される）
- LLMサーバー・管理PCの Tailscale ログアウト

---

## v33.6 — AI補正のパネルを「作業の順」に並べ替えた

本人の指示「AI補正（準備中）のメニューが複雑すぎて、作業順に並べたいです。
説明文は不要で、直感的にわかる様にしなければなりません」。

**新しい並び（承認ずみ）**

1. **準備** — 実測した文字全長（できています／まだです）・戒名の字幅（`rKmW`）
2. **範囲** — ［▣ 範囲を描く］（`btnArea`）／［範囲をやめる］（`btnAreaClr`）
3. **解析** — 大きく［拓本を解析］（`btnAnalyze`）＋［やり直す（消す）］（`btnAnalyzeClr`）
   ＋結果 1 行（`S.rub.anaLast.tx`）
4. **直す**（`<details id="aiFix4">`。枠があるときは開いた状態）
   - 右上に小さく［AI の墨を出す/消す］（`btnAiBandClr`）［AI に送る絵］（`btnAiPrev`）
   - 4a **列** — ［列を調べ直す］（`btnColsStudy`）・列の数（`rColN`）・絵に出す（`rShowCols`）・
     境目（`rCutY`／`btnCutUp`／`btnCutDn`）・列チップ（`.colb`）・
     ［ここだけ AI に通す］（`btnAiGo`）・帯の情報
   - 4b **枠** — ↑足す/↓足す/割る/つなぐ/消す（`.bxb`）・［この列だけ作り直す］（`btnLineTakeAi`）・
     ［✎ 手で描く］（`btnHandBox`）・字数（`rLineN`／`.lnn`）
   - 4c **読み** — `rTeachCh`・［これは この字］（`btnTeach`）・［ちがう］（`btnTeachNo`）・
     ［調べる］（`btnProbe`）・早押し 32 字（`.teq`）
   - 4d **まとまり** — 戒名/俗名/命日/年齢/なし（`.grpb`）＋列ごとの一覧
   - 4e ［もう一度 解析］（`btnRejudge`）
5. **覚えたもの**（`<details id="aiFix5">`。既定で閉じる）— 手本帳の枚数・
   ［一覧］［控える］［戻す］［1 つ前］［空にする］・1 字の大きさの微調整（`rLbH`/`rLbP`/`.lbb`/`btnLbClr`）・
   ［この列から覚える］（`btnBoxLearn`）

**名前の付け替え**：AI 縁取りを実行 → ［ここだけ AI に通す］（ふだんは 3 の［拓本を解析］に吸収）／
この行を見立てる → ［この列だけ作り直す］／覚えたことで 再判定 → ［もう一度 解析］／
列の構成を調べる → ［列を調べ直す］／これはちがう → ［ちがう］／この字を調べる → ［調べる］。
［位置から見当を付ける］（`btnGrpAuto`）と［文字で見分ける］（`btnAnch`）は
解析の中で自動で通るので、画面からは隠した（受け口は `display:none` のまま残してある）。

**作りの変更**

- `rubGrpHtml()` を 4 つに分けた：`rubBoxFixHtml()`（4b）・`rubReadHtml()`（4c）・
  `rubGrpPickHtml()`（4d）・`rubMemHtml()`（5）。id は前のままなので、
  受け口（`rubPanel()` の下半分）は触っていない。
- 説明の文（`hint` の長い段落）は消して、すべて `title=`（ツールチップ）に移した。
- `rubAllAnalyze()` が「**列を調べました／列は前のまま**」を出すようにした（`did9`）。
  結果は `S.rub.anaLast` に残り、3 ─ 解析 の下に 1 行で出る。
  ［やり直す（消す）］（`rubAnalyzeReset`）で消える。

**確かめたこと**：`node --check` と Playwright で、偽の拓本を入れて `rubPanel()` を描き、
22 個の id（`rKmW` 〜 `aiFix5`）がすべて出ること・`pageerror` が 0 件であることを確認。

---

## v33.7 — 並べ替えの手直し（範囲の微調整・右クリックのメニュー）

本人の指示 4 つ。

1. **「実測した文字全長」の行は不要**（その上のメニューで確認できる）→ 1 ─ 準備 から外した。
   残るのは 戒名の字幅（`rKmW`）だけ。
2. **範囲は描いたあとも微調整でき、解析したら動かせない**
   - 緑の四角の **4 辺につまみ**（`data-rarea` → `bindRubArea`）。トリミングのつまみと同じ作り。
   - パネル 2 ─ 範囲 に **左右上下の ± ボタン**（`.arb`。1 押し 1mm＝`1/mmPerPx()` px）。
   - 受け口は `rubAreaEdge(k, v)` 1 つ（つまみも ± も同じ所を通る）。帯（`bandL/bandR`）も一緒に動く。
   - **錠**：`rubAreaLocked()` ＝ 字の枠を持つ列が 1 つでもあれば true。
     このとき つまみを描かず、［▣ 範囲を描く］［範囲をやめる］は disabled、
     `rubAreaDrawTog` / `rubAreaClear` も断る。緑の字は「この中を通しました（解析ずみ）」。
     ［やり直す（消す）］（`rubAnalyzeReset`）で枠が消えれば、また動かせる。
3. **4d まとまりの段は削除**。`rubGrpPickHtml()` ごと消した。
4. **枠を右クリック**（`rubMenuOpen` の hit の枝）に、次をまとめた。
   - 枠：↑ 上に足す／↓ 下に足す／✂ ここで割る／⇵ 下とつなぐ／🗑 この枠を消す
     （`rubBoxAdd(-1/1)`・`rubBoxSplit`・`rubBoxJoin`・`rubItemDel`）
   - まとまり：戒名／俗名／命日／年齢／その他／なし（`data-a="g:<k>"` → `rubGrpSet(k, hit.i)`。
     **その字から下**に付く。いま付いているものは色を反転して見せる）
   - この字：⬛ この字だけ AI に通す／✎ 学習画面でひらく（前からあるもの）
   - どれも **押した枠を先にえらんでから**通す（`pickHit()`）ので、別の列の枠でも効く。

パネルの段は 1 準備／2 範囲／3 解析／4 直す（4a 列・4b 枠・4c 読み・4d もう一度 解析）／5 覚えたもの。

**確かめたこと**：Playwright で ± ボタンが範囲を 8px（=1mm）動かすこと、
解析ずみにすると `rubAreaLocked()` が true・つまみ 0 個・［範囲を描く］が disabled になること、
右クリックのメニューに 17 個のボタン（bx-up〜g:・ai1・learn・補正）が出ること、
［命日］を押すと `items[0].g === "mei"` になること、`pageerror` 0 件を確認。

---

## v33.8 — 枠の角（ななめ）と、直した列を拓本AI に登録する口

### 1. 文字枠を ななめに直せる（本人の指示「角をクリックしたら斜めでも修正できるように」）

- つまみに **4 すみ**（`li:<i>:tl / tr / bl / br`）を足した。辺（t/b/l/r）より
  **あと**に出しているので、角では角が勝つ。えらんでいる枠は角が小さな黄色い四角で見える。
- カーソルは `.lihit.d1`（↘↖）と `.lihit.d2`（↗↙）。
- `bindDrag` の動かす所に「名前が 2 文字なら ななめ」の枝を足した（たてと よこを いちどに）。
- 実測（Playwright）：右下の角を 30px 引くと 幅・高さとも +37.5px（絵の倍率ぶん）。

### 2. 直した列を、そのまま拓本AI に登録できる（本人の指示「AIに登録できるように進めましょう」）

1 字ずつの［学習に登録］とは**別の教材**。**列まるごと**を送る。

**画面**（4 ─ 直す の下）
- ［⬆ この列を AI に登録］（`btnLineReg`）／［ぜんぶの列を登録］（`btnLineRegAll`）

**送るもの**（`rubLineReg`）
- `raw_image`：その列の切り抜き（枠ぜんぶを囲み、まわりを 12% つける。
  横 384px・たて 3072px を上限に縮める）
- `ink_image`：AI が読んだ墨（`L.aiUrl` を同じ切り抜きに合わせたもの。無ければ送らない）
- `boxes`：切り抜きの画素での `[{x,y,w,h,ch,g}]`＝**人が直したあとの枠と読み**
- `key`：`line_<案件>-<caseUid>_c<列番号>`（**同じ列を送り直すと上書き**）
- `char_line`（読みをつなげたもの）・`mm_per_px`・`note`（手で直した列か）

**サーバー**（`takuhon-ai/app.py`。置き場は `dataset/lines/<key>/`）
- `POST /api/takuhon/line` … `raw.png` / `ink.png` / `meta.json` を書く。
  返りは `{status, saved_id, replaced, n, lines}`。**学習は回さない**（貯めるだけ）。
- `GET /api/takuhon/lines` … 一覧（id・字数・日付・読み・墨の有無）
- `GET /api/takuhon/lineimg/{pid}/{raw|ink}.png` … 画像
- `GET /api/takuhon/status` に `lines`（貯まった列の数）を足した。
- 枠の読み取り `_line_boxes()` は、数でないもの・大きさ 0 のものを捨て、200 枠まで。
  `_line_dir()` は名前を洗って、置き場の外へ出られないようにしてある。

**確かめたこと**：にせのサーバー（127.0.0.1:8899）を立て、Playwright から
［この列を AI に登録］を押して `POST /api/takuhon/line` が飛ぶこと、
`boxes` に 3 枠ぶん（「令」「和」「」）と `key=line_案件-…_c1` が入ることを確認。
サーバー側は `_line_boxes` / `_line_dir` を直に呼んで、
おかしな枠を捨てること・`../etc` を断ることを確認。

**次にやること**：この材料で拓本AI 側を作る。
（a）`unet.py` を 2 出力にして「字の中心」を出す → 枠を AI に描かせる
（b）`char_guess.py` を `/api/takuhon/guess` として開き、手本帳と合わせて読む

---

## v33.9 — AI 登録の確かめ（目で見て確かめる板）

本人の指示「どの様に登録されたか、可視化する様にしてほしいです／現在の拓本の墨だしに
紐づける形でしたよね。枠と文字の学習できたかの確認と、学習資料の目視チェックを
このモック画面でもいいので確かめたい」。

**入口**：4 ─ 直す の［🔍 登録したものを見る］（`btnRegView`）。板は `#regWin`。

**板の中**
- 上の切りかえ：［いまの拓本］（`regTabNow`）／［サーバーに貯まった分］（`regTabSrv`）
- ［AI の墨を重ねる］（`regInk`）／［枠と読みを出す］（`regBox`）／［更新］（`regReload`）
- 1 列 1 枚の札。**送るものを そのまま 1 枚の絵に描く**（`rubRegDraw`）
  - 拓本の切り抜き（`rubLineShot` が作るもの＝実際に送る画像）
  - AI の墨を**水色で重ねる**（`rubLineInkShot`。掛け合わせ、濃さ 45%）
  - 枠は**まとまりの色**（戒名＝桃／命日＝緑 ほか。`rubGrpOf`）
  - 枠の左上に**番号**、右上に**読み**（読めた字は緑・読めていない字は灰の □）
  - 下に「3 字（読めた 2 字／読めない 1 字）／読み：令和□／手で直した列／墨あり・なし／
    切り抜き 86×264px／登録ずみ 09-21 12:30（または まだ登録していません）」
  - 札ごとに［⬆ この列を登録］（`.regOne`）
- 見出しに「1 列／3 字（読めた 2 字）。登録ずみ 1 / 1 列。」と出る。
  拓本AI の住所が入っていなければ、その理由も赤で出る（`takuAiOffNote`）。
- たての長い列でも板からはみ出さないよう、札の絵は **高さ 58vh** で縮めて見せる。

**登録した日付**：`rubLineReg` が通ると、その列に `L.regAt`（ISO の日時）を控えて
`persist()` する。札の「登録ずみ」はこれ。案件を開き直しても残る。

**サーバー側**（`takuhon-ai/app.py`）
- `GET /api/takuhon/line/{id}` を足した（その列の `meta.json`＝枠と読み）。
  板の［サーバーに貯まった分］は、一覧（`/lines`）→ 1 件ずつ `meta`（枠）と
  `raw.png` / `ink.png` を読み、**いまの拓本と同じ描き方**で並べる。
  これで「送った通りに貯まっているか」を目で突き合わせられる。

**確かめたこと**：にせのサーバーを立てて Playwright で
①［登録したものを見る］→ 札 1 枚・「読めた 2 字」・［この列を登録］で `L.regAt` が入り
見出しが「登録ずみ 1 / 1 列」に変わること、
②［サーバーに貯まった分］が一覧＋meta＋画像を読んで同じ形で 1 枚出ること、
③ 画面の写真（枠の色・番号・読みの □）を目で確認。`pageerror` 0 件。

---

## v34.0 — 「枠が反映したのか」を判断できるようにした

本人の問い「枠が登録されたものが反映したのかはどう判断できるの／
字を読めたかどうかの説明はあるけど」。

**大前提（画面にも黄色で出した）**：**枠は まだ AI が描いていない**。
いまの枠は「AI の墨 → 覚えた 1 字の大きさ → 手本帳でつなぐ・割る」で切っている。
`/api/takuhon/line` は教材を貯めているだけで、学習はまだ回していない。
したがって判断できるのは次の 2 つで、どちらも札に出すようにした。

### ① 登録した枠と、いまの枠が同じか
- 送るときに **切り抜きの位置**（`crop` ＝ 拓本の画素での x,y,w,h と縮めた率）も送り、
  サーバーの `meta.json` に控えるようにした（`app.py` の `line()`）。
  これが無いと、あとで同じものさしに戻せない。
- 板では、登録してある枠をサーバーから読み（`rubRegSrvBoxes`）、
  **いまの切り抜きの画素に直して白い点線で重ねる**。
- 数も出す（`rubRegMatch`。重なり IoU 0.6 以上＝同じ、0.2〜0.6＝ずれた）：
  「登録した枠とくらべて：**3 枠は同じ**」／
  「**2 枠は同じ**／1 枠はずれた → ちがうので、もう一度 登録すると新しい枠に入れかわります」
- 読めなかったときは「登録した枠は読めませんでした（サーバーに つながらないか、古い登録です）」。
  v33.9 までに登録した分は `crop` が無いので、この行が出る（送り直せば出るようになる）。

### ② いまの枠を決めているのは何か
- 札に「**枠のもと：**手で直した／覚えた 1 字の大きさ 30.2mm（前の拓本から）／
  手本帳 12 枚／字数の指定 17 字」と出す（`rubRegWhy`）。
  何も効いていなければ「AI の墨のすき間だけ」と出る。

**確かめたこと**：にせのサーバー（枠と crop を控えて返すもの）で、
①［この列を登録］→ 確かめの板が「3 枠は同じ」と出すこと、
② 枠を 1 つ 40px ずらすと「2 枠は同じ／1 枠はずれた」に変わり、
白い点線が元の位置に残ることを画面の写真でも確認。`pageerror` 0 件。

**次の段階（ここが済めば「AI が枠を描いた」と言える）**
`unet.py` を 2 出力（墨＋字の中心）にして、`dataset/lines` で学習し、
`/api/takuhon/clean` が中心も返すようにする。そうなれば札に
「AI が描いた枠」対「人が直した枠」の一致率を出せる。

---

## v34.1 — **枠を AI に描かせる**（拓本AI 側も作った）

本人の指示「今の枠が役に立たないので、登録しても無視されるから AI にしようという
結論になった／AI は何を出してくれるの」→「お願いします」。

### AI が出すもの
列の切り抜きを渡すと、**1 字ずつの枠**が返る。

```
POST /api/takuhon/boxes   file=列の切り抜き.png（＋ ink=AI の墨.png）
→ {"boxes":[{"x","y","w","h","score"}], "n", "w", "h", "ms", "model":{...}}
```
x,y,w,h は**渡した絵の画素**なので、アプリはそのまま青枠にできる。

### 作ったもの（takuhon-ai/）
- **`boxnet.py`** … 枠の U-Net（墨の `unet.py` とは別物）。
  入力 2 枚（拓本／AI の墨）、出力 3 枚：
  `center`（字の中心ほど明るい点の絵）・`w`・`h`（字の幅・高さ ÷ 絵の横幅）。
  `find_boxes()` が山の頂を拾って枠にする。
  **頂を探す窓は 絵の幅の半分**（実測で判明：9 画素で探していたときは 1 字の中に
  頂が何十も立ち、200 個の枠が重なって F1 0.08 だった。直して F1 0.94）。
  そのうえで重なり（IoU 0.3）で間引く。
- **`boxdata.py`** … `dataset/lines` を読む。列の切り抜きを**横 128px にそろえ**、
  枠から正解の 3 枚を作る（山はガウス、大きさは山の近くだけ）。
  **`zoom()` で「字が小さく写った切り抜き」も見せる**（0.55〜1.05 倍）。
  これが無いと、本番の切り抜き（帯を広げたもの）で**枠を小さく見積もる**
  （実測：高さ 90px の字を 27px と答えた）。
- **`train_box.py`** … 学習。1 割を検証に取り分け、**枠の当たり具合（F1）**で測る
  （人が直した枠と IoU 0.5 以上で当たり）。良くなったときだけ `runs/box_current.pth`
  を差し替え、世代は `runs/box_YYYYmmdd-HHMM.pth` に残す。`python3 train_box.py --epochs 80`
- **`app.py`** … `POST /api/takuhon/boxes`（上記）、枠モデルの読み直し `_reloadb_if_new`、
  `/status` に `box`（モデルの様子）を追加。墨が渡されなければ①のモデルで作る。

### アプリ側（chokoku-genko.html）
- 4b に **［🤖 AI に枠を描かせる］**（`btnBoxAi` → `rubBoxAiGo`）。
  いまの帯を **のりしろ 12%** で切って送る（教材と同じ切り方。
  ［この行を見立てる］の 20% では字が小さく写り、枠が大きくなる）。
  返った枠から列を作り、`rubColPut` で入れかえる。切り抜きより広い枠は切り詰める。
- `rubLineStats()` を切り出して、AI の枠からも字送り・傾き・大きさを出せるようにした。
- 列に `boxBy:"ai"` と `boxModel`（版の F1）を控え、確かめの板の「枠のもと」に
  **「AI が描いた枠（F1 0.94 の版）」**と出る。これで
  **AI の枠かどうか・どの版か**が画面で分かる。

### 確かめたこと（この環境で実際に動かした）
1. にせの列 14 本を作り、`train_box.py --epochs 150 --base 8` で学習 → **F1 1.00**
   （直す前は 0.08。原因は上記の「頂を探す窓」）。
2. `uvicorn` で `app.py` を起こし、`POST /api/takuhon/boxes` に列を投げて
   枠 8 個が正しい位置で返ることを確認。
3. Playwright で彫刻原稿アプリから［🤖 AI に枠を描かせる］を押し、
   **7 字ぶんの枠が引かれ**（高さ 98.6px・正解 90px、先頭 y=21.1・正解 20）、
   知らせに「AI が枠を描きました（7 字／361ms／この版の当たり具合 F1 1）」、
   確かめの板の「枠のもと」に「AI が描いた枠（F1 1 の版）」と出ることを確認。
   `pageerror` 0 件。

### GPU 機ですること
```
cd takuhon-ai
python3 train_box.py --epochs 80          # dataset/lines から学習
```
材料は彫刻原稿アプリの［この列を AI に登録］で貯まる。目安は **30 列で使いはじめ、
80〜100 列で安定**。学習が済めば `box_current.pth` が置かれ、サーバーは自動で読み直す。

### まだやっていないこと
- 読み（字を見分ける）の口 `/api/takuhon/guess`（`char_guess.py` を開く）
- `dash.html` に枠の学習の様子を出す（`runs/progress_box.json`・`curve_box.jsonl` は書いてある）

---

## v34.2 — 枠を **いくつも選んで**、その分だけ登録する

本人の話「［ぜんぶの列を登録］だと効率が悪い。命日と年齢を重点的に登録するので、
戒名や俗名は同じ文字がなかなか出ない」→「シフトキーやコントロールキーを押して
複数の枠を選択して登録ができるといいです」。

**答え（考え方）**：枠の AI は「どの字か」を覚えていないので、**戒名の列も教材になる**
（字が大きい・画数が多い＝いちばん割れやすい所の教材）。繰り返しが効くのは
**読み（手本帳）**の方なので、そちらは命日・年齢だけでよい。
目安：命日・年齢は全部、戒名は 1 基につき 1 列。

**操作**
- **Ctrl（⌘）を押しながら枠を押す** … 1 つずつ 足す／外す
- **Shift を押しながら押す** … いま選んでいる字から そこまで まとめて
- えらんだ枠は **だいだい色で太く**出る（`rubSelSet`）
- 右クリックに［同じまとまりを選ぶ（命日 など）］［この列をぜんぶ選ぶ］
  ［⬆ えらんだ ○ 字を登録］［選びをやめる］を足した
- 4b に［⬆ **えらんだ枠を登録**（○ 字）］（`btnLineRegPick`）。選んでいなければ押せない
- ふつうに押す（修飾キー無し）と、いままでどおり 1 つだけ選んで つかんで動かせる

**作り**
- `rubSelSet`（いまの列の 何字目か の並び）と `rubPick1 / rubPickTog / rubPickRange /
  rubPickGrp / rubPickAll / rubPickClear / rubPickOn`。列を移ると空になる。
- `rubLineShot(L, idxs)` … idxs があれば**その枠だけ**を囲って切り抜く。
  返りに `part` を付ける。
- `rubLineReg(L, idxs)` … 名前は `line_<案件>-<uid>_c<列>_p<始>-<終>x<字数>` として
  **えらんだ分は別々に貯まる**（列ぜんぶの分を上書きしない）。
  列ぜんぶを送ったときだけ `L.regAt`（登録ずみ）、えらんだ分は `L.regPart` に字数を足す。
  控えの note に「えらんだ分」と入る。

**確かめたこと**：Playwright で Ctrl＋4 字目 → Shift＋6 字目 で `rubSelSet=[3,4,5]`、
ボタンが「⬆ えらんだ枠を登録（3 字）」になること、押すと実際のサーバーに
`line_案件-…_c1_p4-6x3` として 3 字ぶんが貯まること（`meta.json` の note に「えらんだ分」）、
右クリックの［同じまとまりを選ぶ］で戒名の 3 字 `[0,1,2]` が選ばれることを確認。`pageerror` 0 件。

---

## v34.3 — 学習画面が真っ黒だった（id のぶつかり）

本人の報告「枠を右クリックして学習画面で開くと真っ黒で何も映っていないです」。

**真因**：v33.9 で足した［AI 登録の確かめ］の板に **`id="regWin"`** を付けたが、
**学習画面がすでに同じ `regWin` を使っていた**（HTML に同じ id が 2 つ）。
`document.getElementById` は**先に出てくる方**を返すので、`rgOpen()` の
`$("regWin").hidden = false` は**確かめの板の方**を開けていた。
その板は開いた時点では中身が空なので、**黒い板だけが出ていた**。

**直し**：あとから足した方の名前を変えた。
`regWin → aiRegWin`、`regInfo → aiRegInfo`、`regBody → aiRegBody`、
`regTabNow/regTabSrv/regInk/regBox/regReload/regClose → aiReg…`。
学習画面（`regWin`・`rgCv` ほか）は**元のまま**。

**確かめたこと**：Playwright で `rgOpenAt({ci:0,i:0})` を呼び、
学習画面が開き（`regWin` が表示・確かめの板 `aiRegWin` は閉じたまま）、
画布 `rgCv` に字が描かれている（明るい画素 86%）ことを確認。画面の写真でも確認。

**教訓**：板を足すときは `grep 'id="…"'` で**同じ名前が無いか必ず見る**。
`#xxx[hidden]{display:none !important;}` と合わせて、この 2 点が板の決まり。

---

## v34.4 — **読みも AI に**（分類器・guess の口・手本帳の送り口）

本人の問い「文字を登録は AI にしないのですか」→「作っておきましょう　U-net ですか？」。

**U-Net ではない。** U-Net は「絵 → 絵」（墨・字の中心）用。
**読みは分類器**＝ 64×64 の切り抜きを畳み込みで縮めて、字の数だけ点数を出す小さな CNN。

### 作ったもの（takuhon-ai/）
- **`charnet.py`** … `CharNet`（畳み込み 4 段 → 平均 → 字の数）。
  **字の一覧は重みと一緒にしまう**（あとで字が増えても、古い版が読める）。
  `guess()` が確からしさの高い順に候補を返す。
- **`chardata.py`** … 材料集め。
  ① `dataset/lines` の枠のうち **`ch` が入っているもの**を `raw.png` から切り出す
     （＝［この列を AI に登録］で既に貯まっている読み。**本物の拓本の字**）
  ② `dataset/chars/<字>/*.png`（手本帳から送られた分）
  `fit()` は **縦横の比を変えずに** 64×64 に収める（引き伸ばすと 十 と 一 が
  同じ形になる。手本帳で実際に起きていた読み違いの元）。水増しは ±6°・ずらし・明るさのみ。
- **`train_char.py`** … 字ごとに 15% を検証へ。**当たり（acc）と上位 3（top3）**で測り、
  良くなったときだけ `runs/char_current.pth` を差し替え。
  **3 枚に満たない字は学習に入れない**（`--min-per-char`）。
- **`app.py`**
  - `POST /api/takuhon/guess`（1 字 → `{"cands":[{"ch","p"}...]}`）
  - `POST /api/takuhon/chars`（手本帳の 1 字を `dataset/chars/<字>/` へ）
  - `GET /api/takuhon/chars`（字ごとの枚数）／`/status` に `char` を追加
  - `_reloadc_if_new()` で学習後の差し替えを自動で拾う

### アプリ側（chokoku-genko.html）
- 4c に **［🤖 AI に読ませる］**（`btnReadAi` → `rubReadAiGo`）。
  その列の字を 1 つずつ `/guess` に通す。**人が決めた読み（`anch.hand`）は触らない**。
  **採るかどうか**：いちばん上が 0.35 以上、または 0.2 以上で 2 番めの 2 倍以上。
  それ未満は読みに入れず、**候補だけ `it.ai` に残す**（実測：平 0.44／2 番め 0.10 で採用、
  学習していない「成」は採らずに保留 ＝ ねらい通り）。
- 5 に **［🤖 読みの学習へ送る］**（`btnTehAi` → `rubTehSendAi`）。
  手本帳の 64×64 を PNG（墨＝黒）にして `/api/takuhon/chars` へ送る。
- 切り出しは `rubCharCv()`＝**枠の 1.15 倍**。学習（`chardata._cut`）と同じ切り方。

### 確かめたこと（この環境で実際に動かした）
1. 日本語フォントで にせの列 10 本（21 種・192 字）を作り、`train_char.py --epochs 40`
   → **当たり 1.00／上位3 1.00**。
2. `uvicorn` で `app.py` を起こし、`/status` に「読みモデル 当たり 1.0・字数 21」。
3. Playwright で［🤖 AI に読ませる］→ **「平□七年」**（学習していない「成」だけ保留）、
   確かさ 0.44／0.45／0.37。［🤖 読みの学習へ送る］→ サーバーに `chars:{"年":1}`。
   `pageerror` 0 件。

### GPU 機ですること
```
cd takuhon-ai
python3 train_box.py  --epochs 80     # 枠
python3 train_char.py --epochs 60     # 読み
```
材料はどちらも［この列を AI に登録］で貯まる（読みは枠についている `ch`）。
**枠の教材は読みが無くてもよい／読みの教材は読みを入れた枠が要る**。
手本帳は［🤖 読みの学習へ送る］で穴うめに回せる。

---

## v34.5 — 「登録」の言い方を **字枠の登録** にそろえた

本人の指摘「右クリックで選んだ字を登録とあるが、枠の登録ですよね？
そうであるなら 字枠の登録と表現してほしい」。そのとおりなので直した。

- ［⬆ この列を AI に登録］→ **［⬆ この列の字枠を登録］**
- ［ぜんぶの列を登録］→ **［ぜんぶの列の字枠を登録］**
- ［⬆ えらんだ枠を登録（n 字）］→ **［⬆ えらんだ字枠を登録（n 字）］**
- 右クリックの［⬆ えらんだ ○ 字を登録］→ **［⬆ えらんだ字枠 ○ 個を登録］**
- ［🔍 登録したものを見る］→ **［🔍 登録した字枠を見る］**、板の見出しも **「字枠の登録の確かめ」**
- 知らせも「○ 列の字枠を拓本AI に登録しました／切り抜き・字枠（読みがあれば読みも）を
  送っています」に統一

中身は前から変わっていない（送るのは 切り抜き・AI の墨・**字枠**・読みが入っていれば読み）。
名前だけをそろえた。

---

## v34.6 — 登録したあとに **直せる／外せる／消せる**

本人の指示「登録した後に編集できるようにしてほしいです」。

### 画面（［🔍 登録した字枠を見る］の板）
**［いまの拓本］の札**
- ［⬆ **登録し直す（上書き）**］… すでに登録ずみなら、ボタンの名前がこれに変わる。
  名前（key）が同じなので **前の分に上書き**される。
- ［✎ **この列を直す**］… 板を閉じて **その列に移る**（`rubRegEdit`）。
  字枠を直してから登録し直せばよい、と知らせにも出す。
- ［🗑 **登録を消す**］… サーバーからこの列の登録を消す（確かめてから）。
  消したら `L.regAt` も外すので、札は「まだ登録していません」に戻る。

**［サーバーに貯まった分］の札**
- ［⛔ **学習から外す**］／［↩ **また使う**］… 消さずに外す。**あとで戻せる**。
- ［🗑 **消す**］… 戻せない。確かめてから。
- 見出しに「○ 列（うち ○ 列を学習に使います）」と出る。

### サーバー（takuhon-ai/app.py）
- `POST /api/takuhon/line/{id}/off`（`off=true/false`）…
  列に **`off` という空ファイル**を置く／外す。`boxdata.list_lines` はこれがある列を飛ばすので、
  **消さずに学習から外せる**（墨の組で前からある仕組みと同じ考え方）。
- `DELETE /api/takuhon/line/{id}` … その列を消す（戻せない）。
- `GET /api/takuhon/lines` の返りに `off`（列ごと）と `on`（学習に使う数）を足した。

### 確かめたこと
Playwright で、登録 → 札が［⬆ 登録し直す（上書き）］［✎ この列を直す］［🗑 登録を消す］に
なること、⛔ で `on` が 1 → 0 になる（`all` は 1 のまま＝消えていない）こと、
✎ で板が閉じて その列に移ること、🗑 で `all` が 0 になり `regAt` も外れることを確認。
`pageerror` 0 件。

---

## v34.7 — HTTP 404 の言い方／右クリックからも字枠を登録

本人の報告「枠を登録したら〈登録できませんでした（HTTP 404）〉が出ました」
「枠を選択したら、右クリックでも枠の登録できるようにして」。

### 404 の正体
**サーバーが古い**（`/api/takuhon/line` がまだ無い版が動いている）。
アプリ側の不具合ではないので、GPU 機で `git pull` → サーバーを起こし直せば直る。

### 直したこと
- `takuErr(r, どの口)` を 1 つ作り、拓本AI への口ぜんぶ（line／boxes／guess／chars／
  lines／off／delete）で使うようにした。
  - **404** …「拓本AI のサーバーが古いようです（○○ がありません）。
    GPU 機で git pull をして、サーバーを起こし直してください」
  - 503 … サーバーが返す理由をそのまま（モデルがまだ無い、など）
  - 413 …「絵が大きすぎて受け取ってもらえませんでした」
  - 500 番台 …「サーバーでつまずいています」
- **1 列も送れなかったときは理由だけを出す**（前は「0 列を登録しました。1 列は
  送れませんでした…」と並んで読みにくかった）。

### 右クリックからも登録
枠を右クリック →「拓本AI に登録（字枠）」の段
- ［⬆ **この字枠を登録**］（1 つだけ。`rubLineRegOne`）
- えらんでいるときは ［⬆ **えらんだ字枠 ○ 個を登録**］
- ［⬆ **この列の字枠を登録**］
- ［🔍 **登録した字枠を見る**］

**確かめたこと**：404 を返すにせサーバーに向けて登録 → 上の案内文が出ること、
右クリックの品書きに 3 つが出ること、［この字枠を登録］で実際のサーバーに
`line_…_c1_p2-2x1／1字` として 1 つだけ貯まることを確認。`pageerror` 0 件。

---

## v34.8 — 登録した字枠の絵が **さかさま**だった（重要）

本人の報告「拓本がさかさまになっています」（確かめの板の札）。

**真因**：v33.8 で足した切り抜き（`rubLineShot` / `rubCutCv` / `rubCharCv`）が
**`RUB.img`＝取り込んだ そのままの絵**から切っていた。
回転・180° 反転は `rebuildRub()` が **`RUB.canvas`** に焼き込んでいて、
`RUB.data` も 枠（`it.px`）も こちらの座標。
つまり **絵と枠が食いちがったまま**、拓本AI に送っていた
（さかさまに見えるだけでなく、**教材として間違っていた**）。

**直し**：`rubSrcCv() { return RUB.canvas || RUB.img; }` を作り、3 か所すべてを
これに向けた（列の切り抜き・字枠の切り抜き・1 字の切り抜き）。

**確かめたこと**：上半分が濃く下半分が薄い絵を作り、切り抜きの上端の明るさを測った。
回す前 48（濃い）→ 180° 回したあと 232（薄い）。**向きが付いてくる**ことを確認。

**すでに送ってしまった分について**：回した拓本から登録した列は、絵と枠がずれている。
［🔍 登録した字枠を見る］→［サーバーに貯まった分］で見て、さかさまのものは
⛔ か 🗑 で外し、**登録し直して**ください（同じ列なら上書きされます）。

**教訓**：拓本の絵を使うときは **`RUB.canvas`**（回したあと）。`RUB.img` は生の絵。
`RUB.data` は canvas から作っているので、そちらを使う計算は元から正しかった。

---

## v34.9 — ［🚫 登録しない］を足した

本人の指示「登録しない場合は ［この列の字枠を登録］を押さなければいいのでしょうが、
分かりづらいので、登録しないボタンをつけて」。

- 確かめの板（［いまの拓本］）の札に **［🚫 登録しない］**。押すと
  **［↩ やっぱり登録する］**に変わる（`L.regSkip`）。
- 印の付いた札は「**登録しません（この列は教材にしません）**」と出る。
- 見出しにも「登録ずみ 0 / 2 列（**登録しない 1 列**）」と出る。
- **［ぜんぶの列の字枠を登録］は、印の付いた列を飛ばす**。
  知らせに「1 列は［登録しない］なので飛ばしました」と出る。
- 登録してしまったときは、印は自動で外れる（もう「登録しない」ではないため）。
- 印は案件に残る（`persist`）ので、開き直しても覚えている。

**確かめたこと**：Playwright で 2 列のうち 2 列目に印を付け、ボタンが
［↩ やっぱり登録する］に変わること、見出しが「（登録しない 1 列）」になること、
［ぜんぶの列の字枠を登録］で 1 列目だけ送られ 2 列目が飛ばされることを確認。

---

## v35.0 — 見出しに ＜〇〇家　寺名　顧客名＞ を出す

本人の指示「彫刻原稿 v34.8　＜○○家　寺名　顧客名＞　←これを追加して」。

- 上の帯の版の右に `#appWho` を足し、**＜久ヶ山家　慈宏寺　庄司 太郎＞** のように出す。
  家名は「〇〇家」に整える（申し送りと同じ決まり。すでに「家」で終わっていれば足さない）。
  空の項目は詰め、ぜんぶ空なら何も出さない。
- **窓の見出し（タブ）にも**同じものを入れる（`document.title`）。
  彫刻原稿をいくつも開いたとき、タブで見分けられるようにするため。
- 情報欄を直すと、その場で変わる（`appWhoSync` を `infoFill` と入力の受け口から呼ぶ）。
- **顧客名は 本体アプリから渡していなかった**ので、`index_b.html`（v1.9.441）の
  `_chokokuGenkoHandoff` に `customer` を足した
  （納骨リストは `顧客名`、工事関連は `Customers`）。

**確かめたこと**：3 つそろったとき／家名だけのとき／ぜんぶ空のときを Playwright で確認。

---

## v35.1 — 枠の色で「AI が描いた枠」かどうかを分ける

本人の指示「［🤖 AI に枠を描かせる］を押したのですが、枠の色で AI の枠かそうでないかを
分けてほしい」。

| 色 | 意味 |
|---|---|
| **紫 #8A2BE2** | **AI が描いた枠**（`it.by === "ai"`。列に `boxBy:"ai"` があれば その列も） |
| **水色 #00B7C3** | **人が手で直した枠**（つかんで動かす・角を引くと `it.byHand` が付く） |
| 桃 #FF2D9B | 計算で切った枠（AI の墨のすき間・覚えた大きさ・手本帳） |
| 黄 #FFD400 | いまえらんでいる枠 |
| だいだい #FF8A00 | Ctrl／Shift で いくつも選んだ枠 |

- 黄・だいだいは今までどおり**出どころより優先**する（いま何をつかんでいるか
  分からなくなるため）。
- `rubBoxAiGo` が 1 字ずつに `it.by = "ai"` を付けるようにした。手で動かせば
  `byHand` が付くので、**AI の枠のうち どれを直したか**が色で分かる。
- 4b の先頭に**色の見本**を出した。

**確かめたこと**：AI・手直し・計算 の 3 種を並べた列を作り、絵の中の枠の
stroke が `#8A2BE2` / `#00B7C3` / `#8A2BE2` になること、見本が出ることを Playwright で確認。

