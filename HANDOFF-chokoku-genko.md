# 引継ぎメモ — 彫刻原稿（chokoku-genko.html）

最終更新: 2026-09-04　**chokoku-genko.html = v14.3**／**index_b.html = v1.9.417**

新しいセッションを始めたら、まずこのファイルを読んでください。
（アプリ全体の古い資料は `HANDOFF.md`。バージョン記述が v1.9.073 のまま古いので注意）

---

## いまの状態

- 作業ブランチは `claude/chokoku-genko-v3-7-9xj06c`。`main` と同じ内容
  （`claude/init-project-setup-smotz-00s2gp` はマージ後に消えています）
- 未コミットの変更なし。**すべて GitHub に反映ずみ**
- GitHub Pages に配信ずみ（`.deploy-trigger` 更新まで完了）

## 作業の進め方（毎回この手順）

1. `chokoku-genko.html` を直す
2. `var APP_VER` と `<title>` のバージョンを上げる
3. 中の `<script>` を取り出して `node --check` で構文確認
4. コミット → `git fetch origin main && git rebase origin/main`
5. `git push -u origin <branch> --force-with-lease`
   と `git push origin <branch>:main` の両方
6. `mcp__github__create_or_update_file` で `.deploy-trigger` を更新（Pages の再生成用）
7. `git fetch origin main && git merge --ff-only origin/main && git push -u origin <branch>`

`mcp__github__*` はよく切断されるので、使う直前に ToolSearch で取り直すこと。

## テスト

Playwright のテスト一式がコンテナ内の作業用ディレクトリにありました（t51〜t82、824項目）。
**コンテナが破棄されると消えます。** 新しいセッションでは作り直しになります。

やり方：`python3 -m http.server 8899` でリポジトリを配信し、
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome` を `playwright-core` から起動して
`http://127.0.0.1:8899/chokoku-genko.html` を開く。
SharePoint は `ctx.route('https://graph.microsoft.com/**')` で差し替える。

## 最近直したこと（v3.0〜v3.7）

| 版 | 内容 |
|----|------|
| v3.0 | 作字画面：ダイヤルで拡大・縮小、中ボタンで移動、右クリックメニュー |
| v3.1 | 確認資料に作字が反映されない不具合。実寸mm・横倍率・上下の合わせ方もゴムシートに統一 |
| v3.2 | 確認資料の水色の破線を、ガイドのチェックが入っているときだけ出す |
| v3.3 | 右クリックメニューが作字画面の裏に回っていた（z-index 70→90）／囲んで選ぶ |
| v3.4 | フォントの記憶が消える件。`navigator.storage.persist()` と書き込み失敗の通知 |
| v3.5 | 確認資料に直しが出ない／拓本取込を「SP取込」に／テンプレートの〔追加〕 |
| v3.6 | テンプレートで足したガイドラインが拓本解析に出ない（`centersPx` の数が合っていなかった） |
| v3.7 | 囲んだ枠を残し、四隅で拡大・縮小。画面拡大のボタンは削除 |
| v3.8 | 「構成」を 1画面に。校正文字（左）と拓本（右）を同じ縮尺で並べる |
| v3.9 | 「構成」に拓本側の線（頭／脚・センター・文字の長さ）。選んだガイドラインの分だけ出す |
| v4.0 | 「構成」で線をつかんで動かす。ゴムシートの「文字の位置」をガイドラインごとに |
| v4.1 | v4.0 のつまみが小さすぎて掴めなかった。枠の外の余白へ。ガイドラインの位置も直せる |
| v4.2 | つかんだ状態が動かすと外れる不具合（作り直した SVG を測っていた） |
| v4.3 | 拓本を重ねるを「構成」へ。ゴムシートは出力の下見と文字位置だけに |
| v4.4 | 拓本タブを削除。「構成」→「構成」。押した先で左メニューが追随 |
| v6.3 | 保存先を `Folder_Pass/工事仕様/彫刻資料` に。無ければ作る（`spEnsurePath`）。メニュー名を「テンプレート」に |
| v6.4 | **枠**（石の彫刻できる面）を追加。校正に出す。文字と拓本は枠に入れる（拓本は枠外も可） |
| v6.5 | 文字の既定を **20mm**。枠の選択をガイドラインの中へ。枠もテンプレートに入る。左メニューを枠ごとにまとめる |
| v6.6〜v7.2 | 枠の名前を枠の外へ（`FRTOP` の余白）。枠の上端は固定にし、**文字の始まり `top` は拓本の頭に合わせる**（`framesFitRub`）。枠テンプレートを「テンプレート」の中へ |
| v7.3〜v7.5 | **並び替えを `S.glSeq` で持つ**。それまでは `centers[].x` を動かしていたが、行は同じ x に置くのがふつうなので何も動かなかった |
| v7.6〜v7.7 | 確認資料の出る条件を見直し。既定は校正と同じ配置、［位置調整］でずらせる、［元に戻す］、枠名も出す |
| v7.8〜v7.9 | 枠テンプレートに中のガイドラインも入れる。**まとめて入力**（改行ごとに同じガイドラインの文字グループ①②③へ）＋［数字を漢数字に］ |
| v8.0 | 確認資料の位置調整が利かなかった（拓本の x を `demoX` から出していて文字と一緒に動いていた）→ `demoXBase` |
| v8.1 | 案件が「読み込み中」で止まる。`spGetBlob` の大きさ確認が厳しすぎた（下の「気をつけるところ」） |
| v8.2 | 作字に **［画（丸）を足す］**（`ovalContour`）。Shift でまん丸。何もない所から字を起こすときに使う |
| v8.3 | ［画（四角）を足す］をやめた（角度が付けられず使い道が無い）。左メニューの「文字編集」と行の一覧のあいだを 1行分あけた |
| v8.4 | **枠が上下に動くようになった**。拓本の頭を基準に `f.y` を逆算（それまでは枠が固定で、拓本と合わせようがなかった）。枠カードに「上端 mm」を戻し、校正のつまみは上下でも動く |
| v8.5 | トリミングを確認資料から**拓本解析**へ移した（`cropMode`）。180°反転すると切れる所が上下逆になる不具合も直した |
| v8.6 | トリミングを**校正にも**効かせた。枠の「上端」は石の面の位置なので、直すと**中の文字もいっしょに動く**（下げは不変）に戻した |
| v8.7 | 枠の「上端 mm」の欄を**廃止**。上端は下げから計算で出す（`framesSyncY`）。校正のつまみは左右だけに戻した |
| v8.8 | トリミングで切った分が**場所を取ったまま**だったのを直した（枠の上や横に空きができていた）。校正に**拓本の左右のつまみ**（緑のふうせん・`S.rubDx`）|
| v8.9 | 確認資料の拓本の置き場所を、**校正とまったく同じ決め方**にした（それまでセンターラインに合わせる別の式だった）|
| v9.0 | 拓本を左右にずらしたあと［拓本を重ねる］を押すと、拓本のガイド線と文字がずれた。`bothRubX` をずらす前に覚えていたため |
| v9.1 | **拓本解析の作り直し**。直す操作はぜんぶ拓本の画面の帯へ、パネルは「② 修正結果」の一覧だけに。実測長の欄は脚の線の下へ |
| v9.2 | 拓本の画面から**文字まわりの線**（センターライン・文字グループの上下）を消した。「② 修正結果」→「② 補正結果」|
| v9.3 | 左メニューの［行追加］に**テンプレートから追加**を、その下に［**枠追加**］（新規・コピー・テンプレートから）を足した。「テンプレート」→「テンプレート管理」|
| v9.4 | **左メニューが唯一の選び場所**に。枠の札・拓本の札を押せるようにし、メインメニューの「拓本解析」「文字編集」「枠」を消した |
| v9.5 | 拓本解析の ① に「入れる枠」のえらびを足した（ガイドラインの「入れる枠」と同じ形）|
| v9.6 | 枠のサブメニューから［拓本をこの枠に入れる］を外した（拓本解析の「入れる枠」と左メニューのドラッグで足りる）|
| v9.7 | ガイドラインのサブメニューを **行の構成 → まとめて入力 → その字の中身** の順に。グループ数の表示・⣿ のつまみ・［いまの内容を出す］を消し、「入れる枠」「行幅 mm」を 1行に |
| v9.8 | **センターラインを左右に動かせるようにした**（行の構成の「左右 mm」と、校正の丸いつまみ `cenx:`）。それまでどこにも動かす手が無かった |
| v9.9 | v9.8 の手動の左右移動をやめ、**同じガイドラインのセンターラインはすきま無く並べ直す**（`packCentersInGl`）。行の左右は枠の［1行目の位置］のふうせんで |
| v10.0 | センターラインを足すと**ガイドラインの幅を分け合う**ようにした（足しても広がらない＝枠からはみ出さない）。読み込みのときも並べ直す |
| v10.1 | **自動整列をやめ、ふうせんを戻した**。拓本の写真に目で合わせるので、左右は手で決める。文字がまだ無い線にも線とふうせんを出す |
| v10.2 | センターラインの左右の**決め方を仕様どおりに**。1-1 はふうせん無し、2本目からは 拓本があれば拓本まかせ・無ければふうせん |
| v10.3 | 拓本の上のセンターラインの線とつまみを、**文字の側と同じ上下**に（線は文字ぶんの長さ、つまみはその すぐ上）|
| v10.4 | 行の構成の「左右 mm」の欄を廃止。センターラインの左右は**ふうせんと拓本だけ**で決める |
| v10.5 | **文字グループの札の中で文字を打てる**ように（`.gtxt`）。下の「文字」欄と［数字を漢数字に］は廃止。連動の既定を「入」に |
| v10.6 | ［画（丸）を足す］が **1つ引くごとに解除**されていた（2つめが引けず「効かない」ように見えた）。道具は入れたままにした |
| v10.7 | ［画（丸）を足す］は **押した所をクリックすると、そこに丸ができる**。斜めドラッグでしか出なかったのを直した |
| v10.8 | 作字を **モード（続く）と 操作（1回だけ）** に分けた。モードの札・モードごとのポインタ・消すモードの赤い × |
| v10.9 | 上の帯に **［試し切り］** を追加。刃の効きを見るためのかたちを 1枚だけ出す（原稿の中身には触らない） |
| v11.0 | ［SVG］の出力を **赤・黒の色分けのまま** にした（`outSVG("allc")`）。ぜんぶ黒だった |
| v11.1 | 文字グループの札の中で**字が打てなかった**のを直した（行のクリックが作り直していた）。［試し切り］は帯から**設定パネル**へ移した |
| v11.2 | 作字に **自動の名前（月1・月2…）** を付けた。同じ字を何個でも共有フォルダーに残せる。前は同じ字・同じフォントだと上書きされて消えていた |
| v11.3 | 備考メモを組み直した。**その他**の欄、**訂正マーク**（納骨リストと違うところ）、**縦書き 2行**。文面の欄・置き場所の欄は廃止 |
| v11.4 | 縦書きで **かっこ・棒を 90度倒す**ようにした。申し送りは**切れない縁の内側に収める**ようにして、はみ出しの空振り警告をなくした |
| v11.5 | v11.4 の「縁の内側に収める」で申し送りが**動かせなくなった**のを直した。紙の中なら自由に置ける。縦書きへの切り替えは**下端をそのまま**にする |
| v11.6 | 申し送りの**はみ出し警告を廃止**した。見れば分かるので出さない |
| v11.7 | フォント・作字を整理。**共有フォルダーの作字の一覧**（見本つき）を出し、そこから**編集・削除**できるようにした |
| v11.8 | 作字の一覧を**別ウインドウ**に移した（`#glibWin`）。しぼりこみ欄つき。パネルは［作字の一覧をひらく］だけ |
| v11.9 | **見出しを帯**にして、ほかの字と見分けがつくようにした（全メニュー）。作字の一覧と作字の画面を**つかんで動かせる**ようにした |
| v12.0 | 校正の［寸法］に、**枠の上から最初の字まで**と**最後の字から枠の下まで**を足した。右上の「ホイールで拡大縮小」の案内は削除 |
| v12.1 | 連動を**小さいトグル**にして、横・連動・縦を 1行にした。文字グループの札の**2つめ以降の欄に入れなかった**のを直した |
| v12.2 | 札の**太わく（選んでいる印）と、下の編集がずれていた**のを直した |
| v12.3 | 文字1つぶんの右クリックメニューを並べ替え・改名。上に**決定・取消**を付け、**つかんで動かせる**ようにした |
| v12.4 | 右クリックメニューから「縦横そろえる」「既定の説明」「細さのしきい値」「グループぜんぶ調べる」を削除。**この字を赤に**は太さで自動選ぶのをやめ、**単純な赤/黒トグル**にした |
| v12.5 | ［作字一覧を表示］が、文字メニューごと消えていたのを直した。**同じ窓の中**に一覧を出すようにした |
| v12.6 | 寸法が**センターラインを足すと出なくなる**のを直した。列をまたいでもブロック間を測る |
| v12.7 | 文字メニューの候補一覧を**常に表示**に。**異体字も同じ一覧**へ。絵は半分、共有名・フォント名は出さない。［パスで編集］に改名 |
| v12.8 | 作字に**［新規登録］**（上書きせず別の名前で）を追加。校正で**拓本の場所を留める**ようにした（ガイドラインを動かすと拓本まで動いていた） |
| v12.9 | 校正で**ガイドラインの置き場所も留める**ようにした。センターラインを動かしても、親の文字・ほかのガイドライン・拓本は動かない |
| v13.0 | **拓本を重ねたままでも、拓本のセンターラインを動かせる**ようにした（合わせ先の 1本だけは動かせない） |
| v13.1 | 重ねているときの**合わせ先の線も動かせる**ようにした。拓本は動かず、**文字のほうが動く** |
| v13.2 | SVG は**下見を見てから保存**するようにした。出力に**実測した文字全長のチェック線**（青）を入れた |
| v13.3 | チェック線の字を**線の下**へ（上だと校正文字にかぶる）。SVG の下見に**ホイールの拡大縮小とドラッグ移動**を付けた |
| v13.4 | **校正のロック**。お客様に出した校正の**中身**を読み取り専用にする（位置合わせは通す）。解除は［設定］メニュー |
| v13.5 | **案件ファイルを履歴つきに**。1 つの .json に版をならべて持つ。ロックした版は書き換わらない。開くとき版を選べる |
| v13.6 | メインメニューに**［ファイル管理］**を新設（履歴が見られる）。「ロックを解除」を**［新規バージョンで制作］**に改名 |
| v13.7 | ファイル管理を 履歴／エクスポート／インポート の 3 つに整理。**ステータスは後から直せる**。上のバーに［🔒 ロック］ |
| v13.8 | ステータスを**選択式**にして履歴の行でそのまま選べるように。「案件」の語を外し、ラベルを**［✎ 修正（新Verへ）］**に |
| v13.9 | 履歴の行を**たたんだ**（見出しを押すと開く）。開き具合は `VOPEN` に覚える |
| v14.0 | 上のバーに**いまの版**を常に出す（ロック中でなくても）。「新Ver 準備中」を履歴にも出す |
| v14.1 | **作字のロック**。ロック中は共有フォルダーの作字を書き換えない。同じ名前で形がちがうときは**見くらべて選ぶ** |
| v14.2 | 見くらべ窓から［やめる］を外し、**元のフォントに戻す**を足して 3 択にした |
| v14.3 | 見くらべ窓の 3 択を作り直した。**案件から共有を書き換えることはしない**（共有に合わせるか、別名で足すか、やめるか） |

## 気をつけるところ

- **共有フォルダーの作字は、勝手に上書きしない**（v14.1）。止めどころは 2 つ。

  ① **ロック中は書かない。** `glyphLibSave()` の頭で `locked()` なら投げる（最後の砦）。
  そこまで行く前に `glyphLibPushOne()` / `peCommitLib()` / `glibLibDelete()` の
  それぞれでも止めて、［✎ 修正（新Verへ）］を案内する。

  ② **同じ名前で形がちがうときは、見くらべて選んでもらう。**
  `glyphLibPushOne()` が共有を読んだあと、同じ `glyphKey(ch, font, name)` の
  ものがあって `samePathShape()` が偽なら、`openGlyCmp()`（`#glyCmp`）を出して止める。

      元のフォントに戻す　　この字の作字をやめる（`setAdj(g, ci, "path", null)`）
      いまの形を残す　　　　いまの形を「月2」として新しく足す
      　　　　　　　　　　　（`glyphLibPushOne(gi, ci, true)` ＝ asNew）
      共有の「月1」に合わせる　共有の形を案件に取り込む。いまの形は捨てる
      　　　　　　　　　　　（`setAdj` で共有の path を入れるだけ。共有は書かない）

  **どれを選んでも、共有フォルダーの「月1」は書き換わらない**（v14.3）。
  想定しているのは「共有の字に不具合があって、別の案件で直した」場面。
  案件の側が古い形を持っているのだから、案件が共有を上書きしては元も子もない。
  共有の字を直すのは［フォント・作字］の一覧から開いて直す道（`peCommitLib`）だけ。
  `glyphLibPushOne` の `force` はどのボタンからも渡していない（残してあるだけ）。

  **3 枚の絵は「選んだあとにゴムシートに出る形」**でそろえる。
  絵と結果がずれると、とたんに分からなくなる。
  直したのか、別の形を作ったのかは中からは判らないので、かならず選ばせる。

  **［やめる］は置かない**（v14.2）。「やめる」と何も起きず、名前だけ「月1」を
  名乗ったまま残る。あとで候補一覧から「月1」を押したときに共有の別物へ
  入れ替わるので、かえって分かりにくい。かならず 3 つのどれかを選ばせる。
  いちばん左の絵は元のフォントの字なので、`shapeOne` → `glyphPath` →
  `parsePath` で作る（`glibItems()` の "orig" と同じ）。

  ※ `glyphKey` には**フォント名が入る**。テストで共有の作字を手で仕込むときは
  `font` に実際のフォント名（`FR(0).name`）を入れること。空にすると別物として
  扱われ、見くらべ窓が出ずに「月1」が 2 つできる。


- **案件ファイルは 1 案件 1 ファイル、中に版をならべる**（v13.5）。名前は
  `caseFileName()` ＝「（家名）_彫刻原稿.json」。日付は付けない
  （付けると日が変わるたびに別ファイルになって、履歴が二重になる）。

      { v:2, savedAt, fonts:[], rubSrc:"data:…", cur:3,
        versions:[ { id, at, lock:{…}|null, status, note, state:{…S} }, … ] }

  **拓本の画像は `rubSrc` に 1 つだけ**置く。版ごとに複製すると、
  1 枚数MBが版の数だけ増えてファイルが壊れるほど大きくなる。
  `docPutCurrent()` が `state` から `rub.src` を抜いて `rubSrc` に寄せ、
  `applyVersion()` が戻す。

  **ロックした版は二度と書き換えない。** `docPutCurrent()` は
  「いまの版（`S.verId`）がロック済みなら、上書きせず新しい版を足す」。
  だから ロック → 解除 → 直す → 保存 で、自動的に次の版になる。
  ロックそのもの（`lockOn`）も保存を呼ぶので、ロックした校正は必ず残る。

  古い形（S を丸ごと入れたもの）は `docUpgrade()` で読めるが、
  **書き戻しはしない**。古いファイルを開いたときは `CASEF` を空にして、
  保存は新しい名前のファイルへ回す（既存のファイルには触らない）。

  `applyVersion()` は**版の記録の `lock` を正**とする（`state` の中の写しと
  ずれていても、版の記録どおりに開く）。

  `saveProjectLocal()`（パソコンに保存）は写しなので、**`S.verId` を戻す**こと。
  戻さないと SharePoint 側の版の番号とずれる。

  **［ファイル管理］メニュー**（`panFile` / `filePanel()`）で履歴を見る。
  中身は `CASEDOC`（最後に読んだ／書いた案件ファイル）を写しているだけなので、
  保存・読み込みのたびに `CASEDOC` を入れ替えること。
  ロック中も触れるように、`#panFile` は `#panSP` と同じく素通しにしてある。
  見出しは 履歴／エクスポート／インポート の 3 つだけ（v13.7）。
  パネルを開くと `fileLoadOnce()` が勝手に読む（`_fileBusy` で二重読みを止める）。
  **ステータスとメモは後から直せる**（`verEditStatus()`）。校正提出のあとで
  不備が見つかったときのため。直すのはラベルだけで、版の中身（`state`）は触らない。
  書き戻しは `casePut()`＝版を増やさずファイルをそのまま置き換える。
  履歴の行は `<details class="vrow">`（v13.9）。見出し（`<summary>`）は
  🔒／ステータス／版番号／日時／メモ／▽ の 2 行だけ。開き具合は `VOPEN` に覚え、
  描き直しても閉じないようにする。ステータスやメモを変えたときは
  `filePanel()` を呼ばずに、見出しの `.l1 span` と `.l2 .nt` だけ書き換える
  （呼ぶと開いている行が閉じる）。`.nt` は空でも必ず置いておくこと。
  無いと、メモを消してから入れ直したときに戻せない。

  ステータスは `STATUS_LIST`（お客様校正提出／（中止）お客様校正提出／誤操作／
  その他）からの**選択式**で、履歴の行の `<select>` をその場で変えるだけで保存される
  （v13.8）。空のときのために `statusOptions()` は先頭に「（なし）」を入れること。
  入れないと、ステータスなしの版が先頭の「お客様校正提出」に見えてしまう。

  **上のバーの［🔒 ロック］**（`#btnLock` / `lockBtnFill()`）は、ロック中は
  ［✎ 修正（新Verへ）］に変わる。表示の入れ替えは `applyLock()` の中で行う。

  その**左**に、いまの版を常に出す（`#barVer` / `barVerFill()`・v14.0）。
  ロック中だけ帯を出す作りだと、ロックしていないときに何も出ず、
  切り替わったかどうか分からなかった。3 とおり。

      ✎ 版 2　2026/09/04 10:33                     ふつう（灰）
      🔒 版 1　お客様校正提出　2026/09/04 10:33      ロック中（黄）
      ✎ 新Ver 準備中　保存すると版 2 になります      修正中（青）

  3 つめは `verPending()`＝**いまの版（`S.verId`）がファイルの中でロック済みなのに、
  画面はロックしていない**状態。［✎ 修正（新Verへ）］を押した直後がこれ。
  このとき履歴のいちばん上にも青い「新しい版（まだ保存していません）」を出す。
  **版が増えるのは保存したときだけ**なので、これを出さないと
  「修正を押したのに履歴が変わらない」と見える。
  同じ理由で、`verPending()` の間はロック済みの行に「・表示中」を付けない。

  **「ロックを解除」とは呼ばない**。解除したつもりなのに新しいファイルが
  できる、と読めてしまうため、**［✎ 修正（新Verへ）］**（`lockNext()`）にした。
  やることは同じで、ロックした版は履歴に残り、次の保存で版が 1 つ増える。


- **校正のロック**（v13.4）。`S.lock = null` か `{ at, status, note }`。`locked()` で見る。
  止めるのは**中身が変わる入口だけ**で、**位置合わせは通す**（これが決め事）。

      止める　文字の打ち込み・右クリックの 1 文字メニュー・その場の編集欄・
      　　　　サブメニューの入力欄とボタン・グループの並べ替え・
      　　　　**校正と拓本の画面でつかんで動かす**・ガイドライン間 mm・
      　　　　位置を戻す・拓本の反転／回転／トリミング
      通す　　**ゴムシートと確認資料**でつかんで動かす・押して選ぶ（全画面）・
      　　　　サブメニューのスクロールとたたみ欄・表示の切り替え・寸法・
      　　　　拓本を重ねる・印刷・SVG 出力・案件の保存／読み込み

  校正（both）と拓本（rub）で動かせるのは、ガイドラインの並びや拓本の位置＝
  **校正そのもの**なので止める。ゴムシートと確認資料で動かせるのは紙の上の
  置き場所なので通す。この線引きが決め事。

  やり方は 2 つ。① `body.locked` を付けて CSS で **`#sub` の中の
  `input / select / textarea / button` だけ**を `pointer-events:none` にする
  （`#sub` そのものは触らない。触るとホイールでスクロールできなくなる）。
  `#panSP` は打ち消して素通しにする（解除ボタンがここにある）。
  校正・拓本のバーのボタン（`#bGap` `#bRubHome` `#rbFlip` `#rbRot` `#rbCrop`
  `#rbCropAll`）も止める。ゴムシートの `#btnSheetMove2` と確認資料の `#dAdj`
  `#dReset` は**ここに入れないこと**。
  ② `document` の**取り込み（capture）**で `contextmenu` / `dblclick`（範囲は
  `LOCK_ZONE`＝絵の 4 か所）、`pointerdown`（範囲は `LOCK_DRAG`＝
  `#bothwrap,#rubwrap` だけ）、`beforeinput`（`#panSP` を除く全部）、
  `dragstart` を止める。`pointerdown` は `stopPropagation` だけで
  `preventDefault` はしない — こうすると「つかんで動かす」は死ぬが、
  「押して選ぶ」（click）は校正の画面でもそのまま使える。
  ※ ロックの帯（`#lockBar`）は v14.0 で廃止した。絵の位置がずれなくなった。


- **実測チェック線は SVG（`printMode === "allc"`）にだけ入れる**（v13.2・`lenCheckMark()`）。
  拓本で実測した文字全長（`S.rub.realLen`）を、そのままの長さの青い線として
  紙の左の余白に引く。上端は `S.rub.originY`。紙に出したあと、この線に現物を当てれば
  縮尺が合っているか分かる。**プロッターへ渡す赤・黒（"red" / "blk"）には入れない**
  （線が 1本増えると、そのまま彫られてしまう）。

- **SVG は下見（`#svgPrev`）を見てから保存**（v13.2）。［SVG］で作って見せるだけ。
  実際に落とすのは［保存］を押したとき。下見の中の svg は mm 指定のままだと
  画面いっぱいになるので、`viewBox` の縦横比で枠に収まる px に直している（`svFit()`）。
  拡大縮小と移動は `SVZ {k,x,y}` を `#svZoom` の `transform` に当てるだけ（v13.3）。
  ホイールはカーソルの下の点が動かないように `x = px - (px - x) * (k1/k0)`。
  見出しの倍率ボタンとダブルクリックで、枠に収まる大きさへ戻る。

- **重ねているときは、拓本のセンターラインを 2通りに動かす**（v13.0 → v13.1）。
  見分けは `bothOv() && ci === bothOvAci`。

      合わせ先いがい　拓本の画素（centersPx）を動かす。文字は syncCenters で追う
      合わせ先　　　　拓本は動かさず、文字（S.centers[ci].x）のほうを動かす

  合わせ先のときは、拓本の場所が `ix = bx(aci) - bothAnchorPx(aci) * mp` と
  その線から逆算されているので、**文字を動かしたぶんだけ画素も同じだけずらして**
  逆算の答えを変えない。ずらす量は**画素にそろえてから mm に直す**（`dpx`）。
  ここで `syncCenters()` を呼ばないこと。2本目以降は動かさないのが目的で、
  呼ぶと 1回動かすごとに丸めのぶんだけ、じわじわずれていく。

- **校正のガイドラインの置き場所は `S.bothOff[g]` に留める**（v12.9）。
  `bothPack()` の `placeRun()` は毎回「文字のかたまりの幅」から置き場所を出していたので、
  **センターラインを 1本動かすと、親のセンターラインの文字も、ほかのガイドラインも動いた**
  （拓本に合わせる作業ができない）。いまは、はじめに決めた値を覚えてそこに留める。
  **枠の中での左右ずらし（`f.dx`）だけは、覚えた値に毎回足す**（あとから効かせたいので、
  `placeRun(list, from, fdx)` の第3引数）。ここを `at` に足し込むと、f.dx が
  覚えた値に焼き付いて効かなくなる。
  留めた値を捨てるのは、校正の［位置を戻す］（`#bRubHome`。拓本の場所もいっしょに戻す）と、
  **ガイドライン間 mm を変えたとき**（並べ方そのものを決める欄なので、捨てないと効かない）。

- **校正の拓本の場所は `S.rubX` に留める**（v12.8）。自動の置き場所は
  `pk.right`（文字のかたまりの幅）から出しているので、ガイドラインやセンターラインを
  動かすたびに拓本まで動いてしまい、**拓本に合わせる作業ができなかった**。
  いまは、はじめて描いたときの値を `S.rubX` に覚えて、以後そこに留める。
  捨てて置き直すのは校正の［拓本を右へ戻す］（`#bRubHome`）。別の拓本を読み込んだとき
  （`loadRubImageQuiet`）と［新規］でも null に戻す。**「重ねる」のときは留めない**
  （センターラインにぴったり合わせるのが目的なので）。

- **作字の共有フォルダーへの保存は 2通り**（v12.8）。`glyphLibPushOne(gi, ci, asNew)`。

      上書き保存（月1）  すでに付いている名前のものを書き換える
      新規登録           名前を外した写しを渡し、「月2」「月3」…として足す

  ボタンの出し分けは `peNameShow()`。名前が無いうちは［作字登録］だけを出し、
  名前が付いてから［上書き保存（月1）］と［新規登録］の 2つになる。
  形がまったく同じものが既にあるときは、どちらでも増えない（glyphLibPut の "same"）。

- **「この字を赤に」は単純なトグル**（v12.4・`toggleCharRed()`）。前は太さのしきい値
  （`thinMm`/`S.thinMm`）より細い輪郭だけを自動で赤に回していたが、その仕組みは廃止した。
  いまは押すたびに、その字の**輪郭ぜんぶ**を赤↔黒に切り替えるだけ。`redThinOne()` と
  `charMmPerUpem()`、しきい値の入力欄（`mThin`）、グループ一括（`mThinAll`）は削除した。

- **候補一覧は「作字＋異体字」をまとめて出す**（v12.7・`glibItems()`）。
  札は 3種類あり、`kind` で見分ける。

      "orig"  元の字（フォントのまま）      当てるもの：path を外す
      "path"  作字（この案件＋共有）        当てるもの：その path
      "vs"    異体字                        当てるもの：文字に異体字セレクタを足す
                                            （作字が乗っていると見えないので path も外す）

  当てる処理は `glibApply()` 1か所に集めてある。試し描き（ホバー）と決めるとき（クリック）で
  同じものを使うので、ここを直せば両方に効く。もとに戻すために `GLP.origPath` と
  `GLP.origCh` の 2つを覚えている（異体字は文字そのものを書き替えるため）。
  **共有フォルダーは自動では読まない**。右クリックのたびに通信すると重いので、
  ［🔄 読み込む］を押されたときだけ。絵は 26px（もとの半分）、名前だけで、
  どこにあるか・フォント名は出さない。

- **作字一覧（旧・別窓 `#glibPick`）は、いまは `#charMenu` の中の `#mLibBox`**（v12.5）。
  `openGlyphLib(gi, ci)` から cx/cy を外し、`glibRender()` は位置決めをせずに
  `#mLibBox` の中身を組むだけになった。GLP の仕組み（ホバーで試し描き→離すと戻す、
  押すと決める）はそのまま。決めたら異体字と同じく `closeCharMenu()` で窓ごと閉じる。
  `closeCharMenu()` は `GLP` が残っていれば `closeGlyphLib()` も呼ぶ（外側クリック・Escape対策）。
  外側クリックの監視は `#charMenu` 用の 1つだけに統合した（`#glibPick` 用は削除。
  残したままだと `#mLibBox` の中を押すたびに閉じてしまう）。

- **作字一覧を表示（B2）は元から同じ字だけ**。`glibCandidates(bare, gi, ci)` が
  `bareCh` で絞っている。「全部の一覧に見える」という指摘があったが、コードは
  最初から同じ字限定。紛らわしければ、開いたときの見出しに絞り込みの説明を足すとよい。

- **右クリックのメニュー（`#charMenu`）は「決定・取消」つき**（v12.3）。開いたときに
  `menuSnap = { adj: JSON.stringify(g.adj[ci]||{}), text: g.text }` を取っておき、
  **取消**はそこへ丸ごと戻す（異体字の変更もここで戻る）。フィールドの入力は今までどおり
  その場で `setAdj` → ライブ反映。**決定**はただ閉じるだけ（`persist()` のみ）。
  並びは 横/縦/連動 → 縦横そろえる → 既定の説明 → 左右移動/上下移動 → リセット →
  後ろの送り → 作字 → 細さ → 作字登録/元の字に戻す → 太さ/フォント → 異体字。
  「この文字の調整を消す」ボタンは削除（決定・取消に置き換え）。
  窓は開くたびに `WPOS.delete(el)` して位置をリセットしてから `winDrag()` を呼び直す
  （中身を毎回 `innerHTML` で作り直すため、.hd も毎回新しい要素になる）。

- **札の欄で `sel` を変えたら、太わくも手で動かす**（v12.2）。一覧を作り直せない
  （打っている欄が消える）ので、`.grow` の `on` クラスだけ付け替える。
  忘れると、太わくと下の編集がちがう文字グループを指す。

- **札の欄の blur では、すぐ `renderCols()` を呼ばない**（v12.1）。
  次に押した欄ごと作り直して消してしまうので、**2つめ以降の欄に入れなくなる**。
  `setTimeout(…,0)` で送らせ、行き先が `.gtxt` なら作り直さない。

- **連動のトグルは `.sw`**（v12.1）。`#pLink`（文字グループ）と `#mScLink`（右クリック）の 2か所。
  入っているかどうかは `on` クラス。文字は入れず、右の `.swlb` が「連動」の札。
  中身は今までどおり `S.linkWH` ひとつ。

- **ブロック間の寸法は 2周まわして測る**（v12.6）。①同じセンターライン（列）ごと、
  ②同じ 1行ガイドラインの中で列をまたいで。同じ組み合わせは `dimDone` で 1回だけ描く。
  ②だけにすると、間に別の列のブロックが挟まったとき同じ列どうしの寸法が消える。
  ①だけ（v12.5 まで）だと、センターラインを足して 1本に 1グループずつ置いたときに
  測る相手がいなくなり、寸法が 1本も出なかった。**どちらか片方にしない。**
  横に並んで上下がかさなるブロックは `dimV` が空を返すので、自然に飛ばされる。

- **校正の［寸法］が出すのは 3種類**（v12.0）。ぜんぶ `renderBoth()` の中の `dims`。

      文字ブロックどうしの縦の空き　　ブロックの中心に線。数字は右
      枠の上 → いちばん上の字　　　　枠の左はしの外に線。数字は左
      いちばん下の字 → 枠の下　　　　同上

  測るのは**墨の端**（`c.iy1`/`c.iy2`）で測る。枡で測ると見た目の空きと合わない。
  枠に入っている文字かどうかは `S.glFrame[glOf(ci)]` で見る。

- **窓を動かすのは `winDrag()`**（v11.9）。見出しの帯をつかむと `transform: translate` が付く。
  位置は `WPOS`（WeakMap）に持つだけで、保存はしない。帯の中のボタン・入力欄からは始めない。
  帯のダブルクリックでまん中へ戻る。窓を増やすときは `winDrag("#…の箱", ".…の帯")` を 1行足す。
  暗幕（`#pathEd` / `#glibWin` の background）は**うすい**。動かして下の原稿を見るためなので、濃くしない。

- **見出し `h3.sec` は帯**（v11.9）。ほかの字と同じ見た目に戻さない（見分けがつかないという指摘があった）。

- **作字の画面には 2つの入り方がある**（v11.7）。見分けは `PE.lib`。

      PE.lib が null　　案件の字を直す（これまでどおり）。［決定］で S.groups へ
      PE.lib が数字　　 共有フォルダーの作字を直す（openLibEditor）。
                       ［決定］で FDIR.lib.items[PE.lib].path へ書き戻し、そのまま SP へ保存

  `PE.gi` / `PE.ci` は共有のときは null なので、`S.groups[PE.gi]` を新しく足すときは必ず
  ぶんきさせる（いまは peTotalPct・peNameShow・peCommit・peShare の4か所）。
  `PE.base` は「開いたときの形」。空にすると `JSON.parse` で落ちるので、必ず入れる。

- **作字の一覧は別ウインドウ `#glibWin`**（v11.8）。中身は `glwRender()` が毎回組み直す。
  `data-i` は `FDIR.lib.items` の番号そのものなので、**しぼりこんでも番号はずれない**。
  消したときは、開いていれば `glwRender()` も呼ぶこと（一覧が古いままになる）。

- **共有フォルダーの作字は、いまは 1字ずつしか入れられない**（v11.7）。
  入り口は作字の画面の［この字を共有フォルダーに保存］と、字の右クリックメニューだけ
  （`glyphLibPushOne`）。まとめて入れる `glyphLibPush` と、まとめて当てはめる
  `glyphLibApply` は、分かりにくいという理由で削除した。

- **縦書きで倒す字は `V_ROT`**（v11.4）。かっこ・棒・点々のたぐいは、縦書き用の別の字が
  あるのではなく、**同じ字を 90度倒したものが正しい形**。`vRun()` が、その字の枠のまん中を
  軸に `rotate(90 …)` を掛けている。倒す字を増やすときは `V_ROT` に足す。
  （、。は倒さない。本来は右上に寄せるが、いまは動かしていない）

- **申し送りを縁の内側に押し込んではいけない**（v11.4 → v11.5 で撤回）。
  `noteGeom()` が切れない縁の中に押し込むと、**縁のぶんの余白へ動かせなくなる**
  （下に白いところが見えているのに動かない、という形で出る）。
  いまは **紙からはみ出さないところまで**しか収めない。
  縁にかかっているかどうかの知らせも出さない（v11.6・本人の指示。見れば分かるため）。
  縦書きの切り替えは、入れ物の高さが 3倍ほど変わるので **下端をそのままにして**動かす
  （`nVert` の change）。上端をそのままにすると、切り替えたとたん下へはみ出す。

- **申し送りの文面は、いつも備考メモからの自動（v11.3）**。`noteLines()` は `infoLines()` を
  返すだけになった。古い案件ファイルの `note.text` は読み込みのところで捨てている。
  置き場所（`note.x` / `note.y`）は残っていて、ゴムシートの画面でつかんで動かす。
  欄で打つ手だては無くしたので、直すなら `noteGeom()` を見る（横書き・縦書きの両方をここで出す）。

- **訂正マークは画面の中だけ**（v11.3）。`S.info.src` が納骨リストの値のひかえ。
  `infoFixed()` がちがう項目を返し、`infoFixShow()` が ✎訂正 の印と帯を出す。
  **ゴムシートには出さない**（本人の指示）。`src` が無い＝手入力だけの案件では、印も帯も出ない。

- **縦書き 2行は、右の列が 1列目**（v11.3）。割り方は `noteCols()`：
  行が 2本以上なら前半・後半で分ける（1列目＝納骨日・家名／2列目＝場所・その他）。
  縦に長すぎるときは `noteGeom()` が切れない縁に収まるまで文字を小さくする。

- **作字の名前は自動（v11.2）**。共有フォルダーの目印は `glyphKey(字, フォント, 名前)`。
  入れ口は `glyphLibPut()` 1つに集めてあるので、直すならそこだけ見る。

      すでに名前を持っている　→ その名前のものを書き換え（"upd"）
      形がまったく同じものがある → 何もしない。その名前を使う（"same"）
      どちらでもない　　　　　→ 「月1」「月2」…を付けて足す（"add"）

  名前は `path.name` にも書き戻すので、案件の側にも残る。`peCommit()` は
  **古い `path.name` を引き継ぐ**（形を直しても「月2」のまま）。ここを消すと名前が飛ぶ。
  v11.2 より前の名前なしデータは `glyphLibFixNames()` が読んだところで番号を振る。
  ［共有の作字を当てはめる］は、同じ字が何個もあるときは**いちばん若い番号**を入れる。

- **札（`.grow`）の中に入力欄を置くときは、行のクリックを止める**（v11.1）。
  `.grow` の `onclick` は `renderAll()` を呼ぶので、欄を押した瞬間に欄ごと作り直されて
  字が打てなくなる。`e.target.closest("input,button,select,textarea")` なら何もしない、で回避。
  あわせて、欄にフォーカスがある間は `row.draggable = false`（でないと字をなぞって選べない）。

- **出力の色は `printMode` の4通り**（v11.0）。

      "red"   赤だけ #FF0000     ［① 赤を出力］（プロッターのペン）
      "blk"   黒だけ #000000     ［② 黒を出力］（プロッターの刃）
      "all"   赤も黒も **ぜんぶ黒**（いまはどこからも呼んでいない。1色でよいときの残し）
      "allc"  赤は赤・黒は黒     ［SVG］（ほかのソフトで色で選び分けるため）

  `buildSVG()` の `allIn`（"all" と "allc"）で「両方入れるか」、`redG` の色で「赤にするか」を
  決めている。この2つは別の判断なので、片方だけ直すと黒1色に戻る。

- **［試し切り］は原稿とは無関係**（v10.9）。`testCutSVG()` が決まったかたちだけの SVG を作り、`#stage`
  に入れて `window.print()`、そのあと `renderStage()` でもとに戻す。`buildSVG()` も `printMode` も通らない。

      出るかたち　コの字 2mm ×10 ／ 丸 3mm ×5 ／ 四角 2・5・10mm ／ 1mm はばの線 ×3 ／ 直線・ななめ線
      紙の大きさ　いまのゴムシートのまま（プロッター側の用紙設定を作り直さないで済むように）
      線　ぜんぶ黒・fill なし・太さ PEN_MM（0.12mm）。`plotOff()` のずらしも効く
      **文字（ラベル）は入れない**。カッターだと、その字まで切ってしまうため

- **作字は「モード」と「操作」に分ける**（v10.8）。

      モード（押すと続く。もう一度押すと［選ぶ・動かす］に戻る）
          選ぶ・動かす／点を足す／点を消す／丸を置く／輪郭を消す／線で切る
          ※ 丸を置くだけは **1つ置くと自動で［選ぶ・動かす］に戻る**（置いた丸は選んだまま）
      操作（押すと 1回だけ）
          選んだ点を消す／選んだ輪郭を消す／赤・黒／重なりを1つにする／1つ戻す／元の字に戻す

  いまのモードは `peModeShow()` が 3つで見せる ――
  **札**（`#peModeTag`。`.pebody` の中に置く。`#peStage` の中に入れると
  `peRender()` の `innerHTML` で消える）、**ポインタ**（`#peStage` の `m-<tool>` クラス。
  × とハサミは SVG を data URI で貼っている）、**点の形**（消すモードは赤い ×）
- **文字を打つ所は 2つだけ**（v10.5）。行の構成の**札の中**（`.gtxt`）と、**まとめて入力**。
  下にあった「文字」の欄（`pText`）と［数字を漢数字に］（`pKanji`）は消した。
  札は掴んで動かせる（`draggable`）ので、中の入力欄には `draggable="false"` が要る。
  打っている間は `renderCols()` を呼ばない ―― 呼ぶと欄ごと作り直されて打てなくなる
  （直すのは `renderProps` `renderStage` `renderBoth` だけ。作り直しは blur のとき）
- **横と縦の連動（`linkWH`）の既定は「入」**（v10.5）。`S.linkWH` が未設定なら true。
  古い案件で false を入れてあるものは、その値のまま

- **センターラインの左右の決め方は、1本目と 2本目からで別**（v10.2。ここは何度も作り直した所）。

      センター 1-1（行のはじまり）
          枠の［1行目の位置］のふうせん（`frx:`）で決める。**自分のふうせんは持たない**
      センター 1-2 以降
          拓本があるとき … 拓本の上の線に合わせた**離れぐあいがそのまま入る**
                            （`syncCenters()`。1行ガイドラインごとに 1本目を基準にする）
          拓本が無いとき … 校正のふうせん（`cenx:<ci>`）か「左右 mm」（`.cx`）で手で合わせる

  だから `cenx:` のふうせんは、**2本目から** かつ **拓本で決まっていないとき**だけ出す。
  ぜったいに自動でそろえない（v9.9〜v10.0 の `packCentersInGl` は取りやめ）。
  `addCenterLine` は、いちばん左の線のすぐ左（重ならない所）に置くだけ。
  校正では、**文字がまだ入っていない線**にも線とふうせんを出す
  （上下は同じガイドラインの範囲、左右は同じガイドラインのずれ量を借りる。`bx`）。
- **拓本の上のセンターラインも、上下は文字の側にそろえる**（v10.3）。
  線は `colBox[ci]`（無ければ同じガイドラインの範囲 `glY`）のぶんだけ引き、
  つまみはその **すぐ上**（`ry1 − KN*1.5`）に置く。
  以前は切り抜き前の画像の上端（`iy`）を基準にしていたので、
  トリミングすると絵から離れた所につまみが出ていた
  行そのものの左右は、枠の［1行目の位置］のふうせん（`frx:`）で動かす。
  ※ v9.8 で「左右 mm」の欄と `cenx:` のつまみを付けたが、
  ふうせんと役目が重なるうえ、幅の外へ出せてしまうので取りやめた

- **左メニューの木が、選ぶ場所のすべて**（v9.4）。メインメニューに
  「拓本解析」「文字編集」「枠」のボタンは無い。`#mmGl` の中に

      枠 石塔 120×260      … `.frhd[data-f]`。押すと `selFrame(fi)`
        ガイドライン 1      … `.mmsub[data-k]`。押すと `selGl(k)`
        拓本               … `.mmsub.rubrow`。押すと `selRub()`
      枠の外               … `.frhd.out[data-f="-1"]`。押せないが、落とせる
      行追加 ＋ / 枠追加 ＋

  行も拓本も**枠の札へドラッグ**すると、その枠に入る（`frameAssign` / `rubToFrame`）。
  `frameSel` が選んでいる枠。枠のパネルは**その 1つだけ**を出す
  （以前は全部の枠のカードを縦に並べていた）
- **`setSub(null)` でサブメニューをぜんぶ閉じる**（v9.4）。枠を消したときなど、
  選んでいたものが無くなったら閉じる
- **枠のパネルから消したもの**（v9.4）：［＋ 枠を追加］（左メニューの［枠追加］へ）、
  「行をどの枠に入れるか」（各ガイドラインの「入れる枠」へ）、
  「拓本の置き場所」（拓本解析 ① の「入れる枠」へ）。
  置き場所を決める道は **拓本解析のえらび** と **左メニューのドラッグ** の 2つで、
  どちらも `rubToFrame(fi)` を通り、そのなかで `rubPanel()` も呼ぶので表示は一致する
  （v9.5 で枠側にもレ点を置いたが、同じことが 3か所になるので v9.6 で外した）

- **左メニューの［行追加］［枠追加］は同じ吹き出し（`#addMenu`）を使い回す**（v9.3）。
  中身を作るのは `openAddMenu()`（行）と `openFrameMenu()`（枠）。
  枠を足す道は `addFrameNew()` `copyFrame(i)` `frtUse(i, true)` の 3つで、
  枠パネルの［＋ 枠を追加］も `addFrameNew()` を呼ぶ（作りが 2か所に分かれないように）

- **拓本解析は「パネル＝結果」「拓本の画面＝操作」に分けてある**（v9.1）。

      パネル（#panRub）  ① 拓本データ … 取り込み・削除・ファイル名
                         ② 補正結果   … [✎ 補正する] と、反転・角度・トリミング・
                                         解像度・実測長・歪み・縮尺の一覧（表示のみ）
      拓本の画面（帯）   [↻ 180° 反転] ／ [◎ 回して合わせる]
                         ／ [▣ トリミング] ／ [全体に戻す] ／ [終了]

  拓本の画面は**拓本を直すためだけ**の画面（v9.2）。`rubRender()` の `showG` は
  `false` 固定で、センターラインも文字グループの上下線も出さない
  （`cen:` `grp:` のつまみもここには無い。文字との見比べは校正の画面で）。
  ガイドのレ点は帯から外し、`#rShowGuides` は隠したまま残してある

  帯は `.forRub`（`view === "rub"` のときだけ出る）。状態の色付けは `rubBar()`。
  ふうせん（`#rubOv`）はもう使わない（`rubOverlay()` は隠すだけ）
- **実測した文字全長の欄（`#rLen`）は `#rubwrap` の直下にひとつだけ置く**（v9.1）。
  `#rubstage` の中に入れると、絵を描き直すたびに作り直されて**打っている途中で消える**。
  `rubRender()` は場所（脚の線の下）と値を直すだけ。つなぐのも 1回だけ（`_bound`）
- **モードを抜けても拓本の画面に居残る**（v9.1）。校正へ戻るのは［終了］（`endFix`）だけ。
  `endRot` `endCrop` は画面を切り替えない

- **`bothRubX` は「ずらしたあと」の左はしを入れる**（v9.0）。これは拓本の画素 0 が
  校正のどこに来るかで、**ガイド線を引くのにも、つまみをつかむのにも**使う。
  `S.rubDx` を足す前に覚えていたので、ずらした分だけ食い違っていた。
- **［拓本を重ねる］（`ov`）のときは `S.rubDx` を足さない**（v9.0）。
  重ねるのはセンターラインにぴったり合わせるためのものなので、
  そこへ手のずらしを足すと合わなくなる。解除すればずらしは戻る

- **確認資料の拓本の置き場所は、校正と同じ式で出す**（v8.9）。

      枠の中 … その枠の中で、見えている範囲を真ん中に
      枠の外 … 文字のかたまりの右（`pkD.right + max(bothGap(),10)`）へ
      そのあと S.rubDx（校正のずらし）＋ S.demoRx（確認資料だけのずらし）を足す

  v7.7 で「確認資料は校正と同じ並びに」と直したとき、**文字側だけ** `bothPack` に
  そろえて、拓本の x は昔の式（`rubAnchor()` でセンターラインに合わせる）のままだった。
  そのため校正で決めた拓本と文字の位置関係が確認資料に出ていなかった

- **拓本の置き場所・広さは「見えている範囲」で計る**（v8.8）。
  校正は `rvL/rvT/rvW/rvH`、確認資料は `cx1..cy2`。`RUB.W`・`RUB.H` で計ると、
  **トリミングで切り落とした分まで場所を取り**、枠の上や横に白い空きができる。
  絵そのものを置く `ix`・`iy` は切る前の左上のままなので、
  `ix` を出すときは切った分（`cb.x1*mp`）を引いておくこと
- **校正の拓本は左右に動かせる**（v8.8。`S.rubDx`）。右はしから上へ伸びた線の
  **緑のふうせん**（`data-k="rubx:0"`）をつかむ。枠の `frx` と同じ作り。
  校正の見え方だけで、ゴムシートの出力・確認資料（あちらは `S.demoRx`）には効かない

- **トリミングは「拓本解析」で決める**（v8.5）。`cropMode` が真のあいだ、
  拓本の画面（`view === "rub"`）に赤い四角と 4辺のつまみを出す。
  終わると校正へ戻る（回転の `rotMode` と同じ作り）。
  範囲 `S.rub.crop` は**画像の画素**で持ち、`rubFlip180()` が裏返すので、
  ほかの目印（頭・脚・帯・センターライン）と同じ扱いになる
- **トリミングは校正と確認資料の両方に効く**（校正は v8.6 から。`bclip` / `dclip`）
- **切り抜き（`clip-path`）は、回す `<g>` の外側に付ける**（v8.5）。
  内側（`<image>` 自身）に付けると、切り抜きの四角も一緒に回ってしまい、
  **180°反転したときに、見えている赤枠と実際に切れる所が上下逆**になる。
  これが「反転してからトリミングすると動作がおかしい」の正体だった

- **枠の縦で、人が決めるのは「下げ」だけ**（v8.7）。上端 `f.y` は入力欄を持たず、
  `framesSyncY()` が計算で出す。**枠の上端の欄を足したくなったら、まずこれを読むこと。**

      拓本があるとき … 行の上端 ＝ 拓本の頭。   だから f.y ＝ 拓本の頭 − f.top
      拓本が無いとき … 行の上端 ＝ 行そのもの。 だから f.y ＝ 行の上端 − f.top

  `framesSyncY()` は `renderAll` `renderBoth` `renderDemo` `renderFrames` の頭で毎回通る
  （`framesFitRub()` は同じものの別名。呼んでいる所が多いので残してある）。
  下げを直したときは、拓本があれば**枠が動き**（文字は頭のまま）、
  無ければ**文字が動く**（枠はそのまま）。
  ※ ここは 3回作り直している。v7.2〜v8.3 は枠が固定で、拓本と永久に合わなかった。
  v8.4〜v8.5 は上端を直すと下げが入れ替わり、枠と文字の関係が変わってしまった。
  v8.6 は上端が中身ごと動いたが、そもそも上端は下げから出るので**欄が要らなかった**
- **校正の枠のつまみ（`frx:`）は左右と上下の両方**（v8.4）。
  左右＝1行目の位置（`f.dx`）、上下＝枠そのもの（`frameSetY`）

- **［画（丸）を足す］は「クリックした所に丸ができる」**（v10.7）。
  押した所を中心に、決まった大きさ（upem の 15%）の丸を **その場で作る**。
  そのまま引くと、押した所を中心に大きさが変わる（Shift でまん丸）。
  それまでは **8px 以上斜めにドラッグしないと何も出ず**、
  「ボタンは黒くなるのに何も起きない」と見えていた。
- **形を足す道具は、引いても入れたまま**（v10.6）。1つ引くごとに［選ぶ・動かす］へ
  戻していたので、続けて 2つめが引けず「押しても効かない」ように見えていた。
  終わりは、同じボタンをもう一度押す（`.pt` のボタンは押すたびに入・切が変わる）。
- **作字で形をひとつ足す道具は 丸だけ**（v8.2 で追加、v8.3 で四角をやめた）。
  `peDrawTool()` が真のあいだは、点や輪郭の上を押しても台紙側で受けて、新しい形を引ける。
  丸は `ovalContour()`。節点は 左・上・右・下 の 4つで、ハンドルの長さは半径の
  **0.5523 倍**（3次ベジエでいちばん円に近くなる長さ）。
  向きは四角と同じ**時計回り**にそろえる ―― `unionContours` は回る向きで
  穴かどうかを決めるので、逆にすると穴になってしまう

- **`spGetBlob` の大きさ確認は「足りないとき」だけ弾く**（v8.1）。
  `expect`（SharePoint が持っているファイルの大きさ）と**ぴったり同じか**を見ていたので、
  受け取った中身のほうが大きいと「途中で切れました」になり、
  **5回とも取り直して**いつまでも「読み込み中」に見えていた。
  途中の通信で圧縮されて届くと、開いたあとの大きさは置いてあるものと合わないことがある。
  案件ファイル（JSON）はよく圧縮されるので、ここに引っかかりやすい。
  いまは `b.size < expect` のときだけ取り直す
- **自動保存は「この端末のブラウザ」だけ**。`persist()` が
  `localStorage["chokoku.state"]`（拓本の絵は IndexedDB）へ毎回書く。
  **SharePoint の案件ファイルは［案件を保存］を押したときだけ**書き込む

- **画面のタブは ゴムシート／構成／確認資料 の 3つ**。拓本の画面（`view === "rub"`）は
  消していないが、タブには出さない（`#viewRub` に `hidden`）。
  **拓本をつかんで回すときだけ**［マウスで回す］が一時的に開き、［終了］で構成へ戻る。
  前回 `rub` で終わっていても、開くときは構成にする（`restore()`）
- **文字編集の並びは 行の構成 → 文字グループ一覧 → その字の中身** の順（v4.6）。
  行の構成（`#clist`）は**左のメニューで選んでいるガイドライン 1本だけ**を出す
  （`curGlKey()` で絞る）。ほかの行は左のメニューで選び直す。
  「センターライン未設定」の箱だけは、出している行に関係なく全部のグループを見て決める
- **古いテンプレート・案件はセンターラインに `gl` を持っていないことがある**。
  そのままだと `glOf` が 0 を返し、**ぜんぶが「1行ガイドライン 1」にまとまる**（v4.7 で修正）。
  `fillGl()` で並び順の番号を入れる。`tplApply` `tplAdd` `migrate` の 3か所で通す
- **消したもの**（v4.7）：文字グループの一覧カード（`#glist`。行の構成の中に同じものがある）、
  ［＋ ガイドライン追加］のボタン（左メニューの［行追加］→ 新規 に集約。
  はたらきは `addGuideline()`）、「いま 横…×縦… 字送り…」の説明文
- **構成の 頭・脚は `calTop`/`calBot`（拓本の画素の行）を決める**（v5.6／v5.7）。
  つまみは `cal:top` / `cal:bot`。つかんでいる間は `bothImgHold` で
  **拓本の絵の置き場所（iy・mp）を止める**ので、印だけが動く。
  **離したあとも同じ置き場所のままにする**（v5.7）。そのために
  `realLen = mp × (calBot − calTop)`、`originY = iy + calTop × mp` を入れ直す。
  こうしないと、離したとたんに較正がかかって絵が跳ね、
  合わせ直す→また跳ぶ、で**終わらなくなる**（v5.6 の不具合）。
  絵の大きさが変わるのは、**実測長の欄に数字を打ち込んだときだけ**。
  文字の行（`groups[].topY/bottomY`）はどちらでも動かない。
  ※ v4.0〜v5.5 は 頭＝`originY`、脚＝`realLen` を直接動かしていた。
  これだと拓本ぜんぶが動いてしまい、新規の較正に使えなかった
- **上のバーの［SVG］** は、**ぜんぶ黒の SVG を 1枚**落とす（`outSVG("all")`。v6.2）。
  プロッターへ渡すのは印刷（赤・黒）のほうなので、SVG は色で分けない。
  `printMode === "all"` は赤の組も黒のペン線で書く
- **細い所をペン（赤）に回す**（v6.2。案B）。`redThinOne(gi, ci, mm)`。
  輪郭ごとに `measureGlyph([c]).ink`（画線幅の中央値）を測り、
  `ink × その字の mm/upem` がしきい値（`S.thinMm`、既定 0.8mm）に足りないものを
  `path.red` に入れる。作字でない字は、そこではじめて作字にする。
  細い所が無ければ**作字にしない**（あとで字を差し替えられなくなるため）。
  mm/upem は縦横の**細いほう**で見る（安全側）。
  1文字修正のメニューの［この字を赤に］／［この文字グループぜんぶ調べる］
- **新しい行は拓本の 頭 のラインからはじめる**（v6.2）。`snapToRubTop(list)`。
  テンプレート呼び出し・テンプレート追加・行追加→新規 の 3か所。
  **いちばん上をそろえて、まとめて動かす**ので、行どうしの上下の関係は変わらない。
  拓本が無い・較正していないときは何もしない（`rubTopMm()` が null）
- **「文字の位置」は上のバーの［✥ 文字の位置］だけ**（v6.2 でゴムシートの
  サブメニューから削除。同じものが 2か所にあると迷う）。`#btnSheetMove` は無い
- **縦横の連動**（v5.8）。`S.linkWH`。入れておくと、横を変えると縦も、
  縦を変えると横も、同じ割合で変わる。文字グループの大きさ（`pW`/`pH`）と
  1文字修正（`mScW`/`mScH`）の両方で同じ覚え書きを見る。
  比は**打ち始めたところで覚える**（`pSeed()` / `mSeed()`）。
  打っている途中の値で取り直すと、少しずつ形がくずれる
- **1文字ごとの上下ずれ `adj[i].dy`**（v5.8。下が＋）。左右の `dx` と同じ扱いで、
  `cellTop` に足す。**ゴムシート・出力・構成・確認資料の 4か所**と、
  作字で拓本を下敷きにする `peRubFit()` に入れること。
  `hasAdj()` にも足さないと、一文字ごとの調整の一覧に出てこない
  （ついでに `peRubFit()` が `dx` を見ていなかったのも直した）
- **画面のタブは ゴムシート／校正／確認資料**（v6.0 で「構成」→「校正」に改名）。
  中の名前（`view === "both"`、`renderBoth`、`bothPack` など）はそのまま
- **校正の［寸法］**（v5.8〜v6.1）。`S.bothDim`。**縦の空きだけ**を出す。
  文字ブロック（＝文字グループ）の**墨の端**で測る
  （枡で測ると見た目の空きと合わない。`cells[].ix1…iy2`）。
  ※ 横の間は出さない（v6.1 で削除）。ガイドラインの位置も間隔も数値で
  決めているので、画面で測らなくても分かるため。
  横をやろうとして 2回しくじっている。v5.8 は詰めた並びの上に実寸の数字だけを
  書いた（矢印 6mm に「実 30.3」）。v5.9 は `bothPack()` を詰めないようにした
  （**ガイドラインのシート位置は重なっていることがあるので、行が重なって
  編集できなくなる**）。v6.0 は下に帯を作ったが、そもそも要らなかった
- **まとめて入力**（v7.8／v7.9。`bulkApply()` / `bulkFill()` / `bulkGis()`）。
  文字編集の上のテキスト欄に 1行ずつ書いて、**いま選んでいる 1行ガイドラインの中の
  文字グループ ①②③…** へ上から入れる（v7.9）。**ガイドラインは増やさない**。
  足りない文字グループは足し（`addGroupTo`）、多いぶんは文字を空にするだけ。
  ［数字を漢数字に］（`kanjifyDigits`）も欄の中で使える。
  ※ v7.8 は 1行＝1ガイドラインに入れていた
- **新しい文字グループの大きさは 20mm**（v6.5。`NEW_MM`。`mkGroup()` が `hMm` に入れる）。
  入れないと「上端〜下端にちょうど収まる大きさ」になり、行ごとにばらつく。
  **古い案件は `hMm` を持っているか 0 なので、見え方は変わらない**
- **確認資料の並びは校正と同じ**（v7.7）。`renderDemo()` も `bothPack()` の位置を使う
  （枠・行・拓本の位置関係をそろえる。拓本は枠に入れていればその枠の真ん中、
  枠を使っていなければ、これまでどおりセンターラインに合わせる）。
  ずらしは〔位置調整〕のレ点が入っているときだけ（`#dAdj`。既定は切）。
  **拓本の置き場所に `demoDx`（文字側のずらし）を混ぜないこと**（v8.0）。
  混ぜると文字と拓本が一緒に動き、いくらずらしても重なり方が変わらない
  ＝「位置調整が利かない」ように見える。拓本は `demoXBase()`（ずらし無し）＋ `demoRx`。
  拓本そのものも、位置調整のときだけつかんで動かせる（`.rubhit` / `data-rubdrag`。
  文字の面より先に置いて、文字を優先させる）。
  〔元に戻す〕（`#dReset`）で `demoDx`/`demoRx` を消して、校正と同じ並びへ。
  枠の名前も出す
- **確認資料を出す条件**（v7.6。`renderDemo()`）。
  枠が無ければ、これまでどおり**拓本があるときだけ**。
  枠があれば**枠と文字を出す**（拓本が無くてもよい）。
  枠と拓本がそろっているときは、**拓本を「枠の中」に置いているときだけ**拓本も出す
  （`S.rubFrame >= 0`。枠の外の拓本は、校正で横に並べている見本なので出さない）。
  確認資料の枠は、その枠に入れた行のまわりに置く（枠はシートの左右の位置を持たないため）
- **校正での行の並びは `S.glSeq`**（v7.5。右から左＝縦書きの読み順。`glSeq()`）。
  `bothPack()` も左メニューも、これで並べる。**シートの x は見ない／動かさない**。
  ※ v7.4 までは並べ替えで `S.centers[].x` を動かし、x の順で並べていた。
  ところが行はどれもシートの真ん中に置くのがふつうで、**x が同じだと
  「間隔を保ったまま置き直す」計算が成り立たず、何も動かなかった**
  （端の行だけ動いて見えるのはそのため）。ゴムシートに載せる位置は
  ✥ 文字の位置（`glOff`）で決めるものなので、並べ替えとは切り離した
- **枠 ＝ 石の彫刻できる面**（v6.4／v6.5）。`S.frames[] = {name,w,h,y}`、
  `S.glFrame[ガイドライン番号] = 枠の番号`、`S.rubFrame`（拓本を入れる枠。null＝枠の外）。
  **校正の画面にだけ出す**。ゴムシート・出力・確認資料には出さない
  （＝ 石の面であって、紙やゴムシートの枠ではないため）。
  並べ方は `bothPack()` の中。枠ごとに、その枠の行を 6mm 詰めで並べ、枠の幅の中で
  中央に置く。枠に入れていない行は、これまでどおり右へ続ける。
  中身が枠より広いときは、枠の幅ぶんを赤い線で重ねて知らせる。
  枠がひとつも無ければ `frames` が空なので、並べ方は前と同じになる。
  **枠だけ作った段階でも校正に出す**（v7.0）。文字も拓本も無いときの
  「文字を入れるか…」の案内は、枠もひとつも無いときだけ出す。
  **行を別の枠の行の所へ落とすと、その枠に入る**（v7.4。`moveGlTo()`。校正でも
  左メニューでも同じ）。枠が違うまま左右だけ入れ替えても、校正は枠ごとに並べるので
  見た目が変わらず「動いていない」ように見えるため。
  枠を選ぶところは 3か所（すべて同じ `S.glFrame` を見る）：左メニューの「枠」、
  文字編集の行の構成の見出し（`clFr_<gl>`）、そして左メニューのガイドライン一覧は
  **枠ごとにまとめて出す**（`renderMenuGl()` の `.frhd`）。
  上下は **`f.y`（枠の上端。v7.2 から固定・欄なし）と `f.top`（文字のはじまり。
  上端からの下げ。負なら枠より上）の 2つ**。
  行をそろえる先は `frameTextTop() = y + top`。`y` を動かすと**枠も行もいっしょに**動き、
  `top` を動かすと**行だけ**が枠の中で下がる。
  ※ v6.6 は `y` ひとつだったので、文字がいつも枠の上端にべったり付き、
  数字を変えても枠と行が同時に動くため「効いていない」ように見えた。
  行の上端は **枠（`y`＋`top`）と 拓本の頭（`rubTopMm()`）の 2つに合わせる**ことになる。
  枠のカードに、いまの行の上端と拓本の頭の差を出し、
  ［拓本の頭に合わせる］（`.frfit`）でも直せる（v6.9）。
  **拓本があるときは `framesFitRub()` が自動でそろえる**（v7.2）――
  取り込んだとき・頭を動かしたとき・実測長を変えたとき・枠を足したとき。
  枠の上端は動かさず、`top` だけで合わせる（`top` が負＝面から上にはみ出す、が見える）。
  枠の名前は**枠の外（上）**に出す（中だと校正の文字とかぶる。つまみ用の余白 `FRTOP` に載る）。
  左メニューの一覧は**行が 1本でも、枠だけでも出す**（v7.2）。
  並びは**シートの左右の位置に合わせ、いちばん上が右の行**（v7.3。縦書きなので
  右が 1行目）。コピーの行も位置の順（v7.4。札で分かるため）。※ v7.2 までは gl の番号順に出していたので、
  並べ替えても一覧が変わらず「動いていない」ように見えた（中身は動いていた）。
  **1行目の寄せ（中央／右から／左から）と 左右のずらし mm**（`f.ax` / `f.dx`）で、
  枠の中のどこから並べるかを決める。`dx` は校正の画面でも動かせる ――
  枠の中の行の**右はしの線を枠の外（上）まで伸ばし、その先の丸いつまみ**
  （`data-k="frx:<枠の番号>"`）をつかんで左右へ。つまみの分の余白は `FRTOP`。
  枠の名前は**枠の中**に出す（外に出すと画面のふちで切れる）。
  **枠テンプレート**（v6.7。置き場所は v7.0 で「テンプレート」のメニューの中へ）は、
  枠と、**その枠に入れてある行（センターライン・文字グループ）も**取っておく（v7.8）。
  中身は `data:{centers,groups,frames,glFrame}` ＝ふつうのテンプレートと同じ形にして、
  呼び出しは `tplApply()` / `tplAdd()` に通す（同じ道を使う）。
  そのあと `setSub("tpl")` でテンプレートの画面に戻すこと（tplApply は文字編集へ切り替える）。
  共有フォルダーの `枠テンプレート.json`（`FRT` / `FDIR.frtId`）。作りは
  テンプレート（`TPL`）と同じで、置き場所のファイルだけ別。〔置換〕〔追加〕〔×〕。
  枠は案件のテンプレートにも入る（`data.frames` / `data.glFrame`）。
  ただし **`tplApply()` は、テンプレートが枠を持っているときだけ入れかえる**（v7.1）。
  枠を先に作ってから文字だけのテンプレートを呼ぶ使い方で、枠が消えてしまうため。
  〔置換〕は入れかえ、〔追加〕は後ろに足して割り当ての番号をずらす
- **本体から受け取ったフォルダーは、無ければ作る**（v6.3。`spEnsurePath()`）。
  上から順に GET → 404 なら POST で作る（409 は開き直す）。403 はそのまま知らせる。
  工事関連から開くと `Folder_Pass/工事仕様/彫刻資料` を指してくるので、これが要る
- **説明文は `<details class="help">` で畳む**（v6.1）。ゴムシートのサブメニューは
  入力欄より説明のほうが長く、読みづらかった。増やすときも畳むこと
- **作字の輪郭ごとに「刃で切る（黒）／ペンで書く（赤）」を選べる**（v5.5）。
  `adj[i].path.red = [輪郭の番号…]`。作字の画面で輪郭を選び［赤にする］。
  `charPathSplit()` が黒と赤に振り分ける。**ゴムシートと出力だけ**分け、
  **構成と確認資料は全部まとめて黒**（`charPathD` のまま）。
  `path` ごと共有フォルダーの作字一覧に入るので、フォントに紐づいて残る。
  輪郭を消したら `peRedDrop()` で番号をずらす。
  ［重なりを1つにする］は番号が変わるので赤を外す
- **構成で拓本のガイドラインを動かしても、`syncCenters()` は呼ばない**（v5.4）。
  拓本の線は「拓本の見本の行」を指しているだけ。呼ぶと `S.centers[].x` が
  拓本から決め直されて、構成した文字の行まで動いてしまう。
  文字の行を動かすのは、名前の帯をつかんだ並べ替え（`reorderGl()`）だけ
- **`copyGuideline()` は「いまある行ぜんぶの外」に置く**（v5.4）。
  「コピー元の隣」に置いていたので、同じ行を 2回コピーすると**同じ場所に重なり**、
  並べ替えても動かないように見えていた
- **共有フォルダーから落とすものは、すべて大きさ（`expect`）を渡す**（v5.4）。
  渡さないと `spGetBlob` が「途中で切れた」を見つけられず、
  欠けた中身をそのまま読もうとして止まる（作字の一覧で起きていた）。
  作字（`FDIR.libSize`）・テンプレート（`FDIR.tplSize`）・案件・拓本・フォントの 5か所
- **確認資料の JPEG は、既定で工事フォルダー**（`spNeedFolder()` → `spPutIn()`）。
  つながっていないときと、しくじったときは、このパソコンに落とす。
  ボタンの右の ⤓（`dSaveLocal`）はいつでもこのパソコン
- **ファイル名は保存先で分ける**。工事フォルダーは `demoJPEGName()`＝
  「宇田川-確認資料-20260831-2330.jpg」のように漢字のまま。
  このパソコンは `demoJPEGNameLocal()`＝`safeName()` の英数字。
  **Chromium は `a.download` の漢字の名前を落として「download」にしてしまう**ため
- **［全体に戻す］［位置を戻す］のボタンは無い**（v5.3 で削除）。
  切り取りを全体に戻すのは、**トリミングのレ点を外す**（外している間は切らずに出す）
- **1行ガイドラインは、文字グループが 1つも無いと画面に出てこない**。
  左のメニュー（`glKeys()`）も行の構成（`curGlKey()` で絞る）も、
  文字グループから作っているため。だから `addGuideline()` は
  **空の文字グループを 1つ一緒に作る**（v5.2 で修正。それまで「新規」は
  何も起きないように見えていた）
- **コピーの行も、行の構成にふつうに出す**（v5.2）。それまでは
  「◯ のコピー」の札だけ出して打ち切っていたので、消すことも、
  文字グループやセンターラインを足すこともできなかった。
  コピーで紐づいた文字グループも `vis` に入れて並べる（カードに「コピー」と出る）
- **ゴムシートの「出す行」のレ点は `S.sheet.glHide`**（`glShown()` / `renderSheetGl()`）。
  外した行は `buildSVG` で字・線・つかむ面のどれも作らないので、
  **下見にも出力（赤・黒）にも出ない**。拓本・構成・確認資料には効かない。
  ガイドラインが 2本以上あるときだけツールバーに出る
- **一文字ごとの左右ずれは `adj[i].dx`**（mm。右が＋）。字を描く 3か所
  （`buildSVG` `renderDemo` `renderBoth`）で `cx + g.dx + AD.dx` として足す。
  つかむ面も `cells` の位置から出すので、ずらした字はそこを押せば選べる。
  **出力（赤・黒）にも効く**
- **起動時、前回の続きを戻すあいだ ％ を出す**（`BOOTP` / `bootProg()` / `bootStep()`）。
  フォント 1件ごとに 1つ、拓本は「読み込み」「下ごしらえ」の 2つとして数える。
  `await tick()` を挟まないと画面が描き変わらず、％ が見えないので注意
- **`loadRubImageQuiet()` は最後に `redraw()` を呼ぶ**。
  `rubRender()` だけを呼んでいたため、起動して構成の画面に戻ったとき
  拓本が出ず、タブを行き来しないと現れなかった（v4.9 で修正）
- **行を足すときは左へ**（縦書きなので次の行は左）。`addGuideline()` `tplAdd()`
  `copyGuideline()` の 3つとも、紙からはみ出すときだけ右に置く
- **ガイドラインの左右の並びはドラッグで入れ替えられる**（`reorderGl()` / `reorderGlTo()`）。
  左メニューの行（HTML のドラッグ＆ドロップ）と、構成の名前の帯（`glmove:` のつまみ）。
  いまの間隔をそのままに、左から置き直す。**中の並びは変えない**。
  なお、そのあと構成でガイドラインの縦の帯をつかむと `syncCenters()` が走り、
  左右の位置は拓本の位置から決め直される（元からの作り）
- **［− 削除］は 1行ガイドラインの見出しの右**（`delGuideline()`）。
  いちばん下ではなく、**いま出している行**を消す。文字グループの ✕ ともども確認を出す
- **消したもの**（v4.6）：行の見出し（`renderGlHead` ＝ 左右の位置 mm ／この行をコピー）と、
  センターラインの「中心 X mm」。**数字で X を入れる欄はもう無い**。
  左右の位置は 構成でガイドラインをつかんで動かす（`cen:` のつまみ）か、
  ゴムシートの［文字の位置］で決める
- **［1行ガイドラインと文字グループ］（旧 拓本解析の ④）は文字編集の中**。
  `#clist` の id はそのままなので `renderCols()` の中身は変えていない
- **左メニューの［行追加］**（`#mmAdd` ／ `openAddMenu()`）。押すと
  新規（`btnAddC` と同じ）か、どの行のコピーか（`copyGuideline()`）を選べる。
  `#mmAdd` も `.mmsub` なので、ガイドラインの並びを拾うループは
  `.mmsub[data-k]` で絞ること（絞らないと onclick を上書きされる）
- **構成で押したものに、左のメニューが追随する**。
  拓本の絵（`data-rub`）→［拓本解析］、ガイドラインの列（`data-gl`）と文字（`data-gi`）
  →［文字編集］＋そのガイドライン。当たり面は 拓本 → ガイドライン列 → 文字 の順に
  重ねてあるので、細かいほうが優先される

- **「構成」の校正文字は、ゴムシート上の位置では並ばない**。1行ガイドラインごとに
  左から詰め直す（`bothPack()`）。隣り合うガイドラインの**行幅の線どうし**のあいだが
  既定 6mm（`S.bothGap`、ツールバーで変更）。ただし文字が行幅からはみ出す列は
  文字の幅で詰める（隣と重ならないように）。
  高さ（mm）はシートのまま動かさないので、拓本と行の高さを見比べられる。
  出力（プロッター）には一切効かない。**表示だけ**
- **「構成」の線は、選んでいるガイドラインの分しか出ない**（`bothGl()`）。
  ほかのガイドラインの線は隠す。列を押すとそのガイドラインに切り替わる（`data-gl`）。
  **頭／脚**（実測した文字全長を決める線）だけは拓本ぜんぶに効くので出したまま
- **「構成」の ■ をつかむと線が動く**（`bindBothDrag()`）。
  `grp:<gi>:top|bot` は行の上端・下端（`topY`/`bottomY` mm）。拓本解析の色つき線と同じもの。
  `orig` は頭＝`S.rub.originY`（拓本ぜんぶが上下に動く）、`len` は脚＝`S.rub.realLen`
  （頭からの実測した文字全長。拓本が伸び縮み）、`cen:<ci>` はガイドラインの
  左右の位置＝`S.rub.centersPx`（拓本解析でつかむ線と同じ）。
  **較正そのもの（`calTop`/`calBot`）は動かさない**ので、`syncOriginY()` で
  行が勝手にずれることがない。ただし拓本解析で頭／脚を動かすと `syncOriginY()` が走り、
  手で決めた `originY` は上書きされる
- **つまみの大きさは「画面で見た px」で決める**（`KN = 16 / (MMPX * sc)`）。
  mm で決めると、細長い原稿は縮尺が小さく数 px になって掴めない（v4.0 の不具合）。
  左右に `GUT` の余白をとり、拓本の画面と同じで線の外につまみを出す
- **`bothMapper()` は、つかんだときに一度だけ位置を測る**（`rubMapper()` と同じ作り）。
  動かすたびに測り直すと、`renderBoth()` で作り直したあとの「外れた古い SVG」を
  測ってしまい、大きさ 0 → 計算できず、2回目以降の移動が全部無視される（v4.0/v4.1 の不具合）
- **つかんでいる間は `bothBusy` で表示範囲と縮尺を止める**（`bothBox` に覚える）。
  止めないと、動かすたびに絵の大きさが変わってつまみがポインタから逃げる。
  拓本の左はし（`bothRubX`）と `mmPerPx()` も、つかんだときの値で通す

## テストの注意

**ドラッグのテストは「動いた量」まで見ること。** v4.0/v4.1 は、最初の 1 回ぶんだけ
動いて残りが無視される不具合だったが、「値が変わったか」しか見ていなかったので
素通りした。`scratchpad/t14.mjs` のように、
画面の移動 px ÷（`bothBox.sc * MMPX`）＝ 期待 mm と突き合わせる。
縮尺は描き直すたびに変わるので、比べる直前に測り直すこと。

- **ゴムシートに残っているのは、出力の下見（赤／黒／塗って表示）と
  「文字の位置」だけ**。拓本を重ねるは「構成」へ移した（`vRub` は使わない
  隠し入力として残してあるだけ。`buildSVG` の重ねる分岐はもう通らない）。
  カットできない縁・グループの重なりの知らせ（`#overflow`）は消した
- **「構成」の拓本を重ねる**（`S.bothOv` ／ ツールバーの［拓本を重ねる］）。
  選んでいるガイドラインのセンターラインに、拓本の上の同じ線（`centersPx`）が
  重なるように拓本を動かす（`bothAnchorPx()`）。拓本は opacity 0.6、
  文字は fill-opacity 0.45 で透かす。ほかのガイドラインを押すと合わせ直す。
  重ねている間は、左右のつまみ（`cen:`）は出さない（横位置は合っているので）
- **ゴムシートの「文字の位置」は 1行ガイドラインごと**（`S.sheet.glOff[gl] = {x,y}`）。
  つかんだ字のガイドラインだけが動く。**ゴムシートと出力（赤・黒）にだけ効き、
  拓本・並べて・確認資料には効かない**。`glOffOf()` は buildSVG の中だけで使う。
  古い案件の `S.sheet.ox/oy`（全体のずらし）はそのまま残し、その上に足す形。
  「中央」で両方 0 に戻る

- **`S.rub.centersPx` と `S.centers` は数をそろえる**。拓本解析は centersPx をたどって
  線を描くので、ずれると線が出ない。`padCentersPx()` が面倒を見る
- **出力（プロッター）の線幅**は、縮小して置くパスだと縮尺のぶん細くなる。
  `penAttr()` で打ち消している。赤 `#FF0000`／黒 `#000000`、太さ 0.12mm、塗りなし
- **プロッターの位置ズレ**は「ゴムシート」パネルの位置補正で直す（端末ごとに localStorage）
- **共有フォルダーのファイル名**は濁点の書き方が違うことがある。`sameName()` で比べる
- **テンプレートと作字ライブラリ**は SharePoint の `/業務アプリ/アプリ使用フォント` に置く

## 残っている宿題

- `data_kugayama.js`（264名の氏名）が **git の履歴に残っている**。HEAD からは消したが
  履歴の削除は未実施（本人の明示的な承諾が要る）
- このリポジトリは **公開**。`HANDOFF.md` に運用アカウントのアドレスと
  SharePoint の各種 ID が書かれている
- さくらインターネットへの移設（手動アップロードの方針）。
  移設したら `loadMapDataFromScript` のURLを書き換え、Entra ID にリダイレクトURIを追加
- `gaichu-portal/index.php`(v1.2.2)、`upload.php`(v18)、`submit.php`(v3) のアップロード
- 工事関連データタブの「彫刻記入用紙返却／彫刻校正確定／彫刻納品」ゾーン

## 触ってはいけないもの

- `docker compose down -v`（Dify のデータが消える）
- `tailscale funnel`（インターネット全体に公開される）
- LLMサーバー・管理PCの Tailscale ログアウト
